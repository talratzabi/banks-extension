# HANDOFF — מסירה בין סוכנים

**קרא את הרשומה העליונה לפני שאתה מתחיל. הוסף רשומה חדשה כשאתה מסיים.**

git שומר *מה* השתנה, לא *למה נעצרנו* ומה כבר הופרך. הקובץ הזה סוגר את הפער.

## כללי כתיבה

- **הוספה בלבד.** רשומה חדשה נכנסת מעל הקודמות. אל תערוך ואל תמחק רשומות של אחרים —
  כך שני סוכנים לעולם לא מתנגשים באותן שורות.
- כותרת: `## תאריך שעה · שם הסוכן · נושא`
- קצר. חמש שורות טובות שוות יותר מדף.
- **מה שהופרך חשוב כמו מה שעבד** — זה מה שמונע מהבא לחזור על אותו ניסיון.

---

## 2026-08-25 · Claude · השומר מ-1.9.0 חסם רישום מחדש — 1.9.6

**⚠⚠ רגרסיה שהכנסתי ב-1.9.0, והיא הסבירה את „זיהוי הישויות לא השיב
תוך 60 שניות" שחזר כמה פעמים היום.**

הסנכרון של טל נעצר שוב על אותה הודעה, ו-`discountPhases` לא נכתב כלל —
כלומר **הדף לא ענה**, לא שהוא היה איטי.

**השורש:** השומר שהוספתי היה דגל בוליאני פשוט:
`if(window.__discountSyncLoaded)return;`
כשמרעננים את התוסף, ה-content script הישן **מת** — אבל **ה-`window` של
העולם המבודד שורד** עם הדגל דלוק. ההזרקה החדשה רואה אותו, יוצאת מיד,
**ואינה רושמת מאזין כלל.** התוצאה: הרקע ממתין 60 שניות לתשובה שאין לה
מאזין. **תיקנתי מאזינים כפולים ויצרתי מצב של אפס מאזינים.**

**מה השתנה:** במקום דגל — **בדיקת חיות.** הסקריפט משאיר פונקציה
שנוגעת ב-`chrome.runtime.id` **מתוך הסגור שלו**; כשההקשר שלו מת הגישה
זורקת, הבדיקה מחזירה `false`, והסקריפט החדש נרשם. שני המצבים נפתרים
יחד: הזרקה חוזרת בהקשר חי → מאזין אחד; הזרקה אחרי רענון → רישום מחדש.

**⚠ והבדיקה תפסה טעות בתיקון עצמו:** הגרסה הראשונה שכתבתי קראה
`chrome.runtime.id` **מן הגלובלי הנוכחי** ולא מן ההפניה של ההקשר שלה,
ולכן דיווחה „חי" גם כשההקשר מת — **8/8 נכשלו.** התיקון: ההפניה
ל-`chrome.runtime` נתפסת בסגור בזמן הטעינה.
**בלי מקרה הבדיקה הזה הייתי מוסר בדיוק את אותו באג בלבוש חדש.**

**איך אומת:** שמונת ה-content scripts, **שני תרחישים כל אחד — 16/16**:
| תרחיש | לפני | אחרי |
|---|---|---|
| שלוש הזרקות בהקשר חי | 1 מאזין | ✅ 1 |
| הזרקה אחרי מוות ההקשר (⟳) | ❌ **0 חדשים** | ✅ **נרשם מחדש** |

**⚠ מה שלא נמדד:** האם זו הסיבה **היחידה** ל„לא השיב תוך 60 שניות".
היא הופיעה גם לפני 1.9.0 (ב-12:12 וב-14:17), כשהשומר עוד לא היה קיים
בדיסקונט. **כלומר יש סיבה נוספת שטרם אותרה.**

**מה עדיין פתוח:**
1. **13.7 שניות לפני לחיצת הסינון** — המארקים נוספו ב-1.9.5 אך טרם רצו.
2. תנאי היציאה החדש מהלולאה (1.9.5) טרם נמדד חי.
3. מסגרת „ינון" טרם נקראה מחדש.

**מה טל צריך לבדוק:** ⟳ ל-1.9.6 **ורענון לשונית דיסקונט**. אחרי
הרענון הזה השומר החדש יעבוד בשני הכיוונים, ובריצה אחת יופיעו סוף-סוף
כל שלוש המדידות הפתוחות.


## 2026-08-25 · Claude · תנאי היציאה היה פרוקסי ולא היעד — 1.9.5

**המדידה מ-1.9.4 פירקה את 27 השניות סופית:**
```
     15 מ״ש   טווח: earliestRow #1        ← 2 מ״ש
     16 מ״ש   טווח: earliestRow #2        ← 1 מ״ש
 13,755 מ״ש   טווח: השדות הוזנו והכפתור נלחץ   ← +13.7 שניות
 41,801 מ״ש   טווח: לולאת ההמתנה נגמרה (75 שורות) ← +28.0 שניות
 41,886 מ״ש   סיום הקריאה                  ← +0.1
```
**`rowDates` ירד ל-2 מילישניות** — תיקון 1.9.3 עבד במלואו.

**⚠ והשורש שנחשף: הלולאה מיצתה את כל 25 הסבבים בעוד הטבלה כבר הציגה
75 שורות.** תנאי היציאה היה **יציבות ספירה** (`n===last` פעמיים
ברציפות) — פרוקסי. בטבלה שמתרנדרת הספירה מתנודדת, ולכן התנאי לעולם
אינו מתקיים והלולאה רצה עד הסוף.

**מה שבאמת רצינו לדעת: האם הטבלה מגיעה לתאריך המבוקש.**
עד היום זו הייתה שאלה יקרה — `earliestRow` הריץ `innerText` על כל שורה.
**⚠ כלומר תיקון הביצועים מ-1.9.3 הוא מה שאיפשר את תיקון הנכונות כאן,
לא רק זירז אותו.** עכשיו הבדיקה עולה 2 מ״ש, ולכן היא יכולה להיות תנאי
היציאה בכל סבב.
יציבות הספירה **נשארה כנפילה-לאחור** — לחשבון שאין בו תנועה עד התאריך
המבוקש, היעד לעולם לא יושג.

**הלקח:** *כשתנאי יציאה הוא פרוקסי ליעד ולא היעד עצמו — הוא ייכשל
בדיוק במקרה הנפוץ.* שווה לבדוק אם היעד עצמו נמדד בזול.

**איך אומת:** ארבעה קבצים נטענים ורצים; **הלולאה חולצה מן הקובץ והורצה**
בארבעה תרחישים — **8/8**:
| תרחיש | סבבים |
|---|---|
| הטבלה כבר מכסה את התאריך (**המקרה בפועל**) | **1** ← היה 25 |
| ספירה יציבה, היעד לא מושג | 3 (נפילה-לאחור) |
| ספירה מתנודדת, היעד לא מושג | 25 |
| טבלה ריקה | 25 |
**⚠ הרווח בזמן לא נמדד חי** — הראיה תגיע מ-`discountPhases` הבא.

**מה עדיין פתוח:** **13.7 השניות שלפני הלחיצה.** הקוד שם הוא
`from.focus(); nativeSet(from,want); wait(300); to.focus();
nativeSet(to,today); wait(300)` — **600 מ״ש בלבד של המתנות מפורשות.**
החשד: `nativeSet` מפעיל `input`+`change`, ובדף Angular עם טבלה גדולה
כל אירוע מפעיל מחזור רינדור מלא. **לא תוקן — לא נמדד.** נוספו שני
מארקים („שדה מתאריך הוזן", „שדה עד תאריך הוזן") שיפרידו בין השניים.

**מה טל צריך לבדוק:** ⟳ ל-1.9.5 ורענון לשונית דיסקונט. ישות 1 אמורה
לרדת מ-32 שניות לכ-15. ובאותה רשומה יופיעו שני המארקים החדשים, ואדע
אם 13.7 השניות הן בשדה הראשון, בשני, או מפוזרות.


## 2026-08-25 · Claude · 1.9.3 עבד — 209ש׳ → 32ש׳ · מדידה בתוך הטווח — 1.9.4

**✅ תיקון `rowDates` אומת חי, וזו הקפיצה הגדולה של היום:**
| | לפני | אחרי |
|---|---|---|
| ישות 024844714 מקצה לקצה | 209 שניות | **32 שניות** |
| `applyCollectSince` | 115.7 שניות | **27.2 שניות** |
| ניסיון ראשון | ❌ פג ב-75ש׳ | ✅ **מצליח** — `{n:1, ms:27228, ok:true}` |
| „פותח תנועות" → סיום | 3 ניסיונות | **ניסיון אחד** |

**⚠ אבל טל צודק: „אין נתונים להורדה, צריך לסיים מהר."**
27 שניות עדיין יושבות ב-`applyCollectSince`, ולולאת ההמתנה שבתוכו
**חסומה ל-15 שניות** (25 × 600 מ״ש). כלומר **כ-12 שניות אינן מוסברות.**

**⚠⚠ ולא ניחשתי.** בסבב הזה כבר טעיתי פעמיים באותו אזור: קיצרתי תקציב
על סמך מדגם מוטה (1.9.2), ותיקנתי שלוש שכבות `innerText` בזו אחר זו
בלי לחפש את הרביעית. **לכן: מדידה בלבד.**
נוספו **שישה מארקים בתוך `applyCollectSince`**:
```
טווח: פקדים נמצאו · earliestRow #1 · earliestRow #2 ·
השדות הוזנו והכפתור נלחץ · לולאת ההמתנה נגמרה (N שורות) · הדיווח נכתב
```
ההפרש בין „earliestRow #1" ל-„#2" מבודד את עלות `rowDates` **אחרי**
המעבר ל-textContent; ההפרש עד „לולאת ההמתנה נגמרה" מראה כמה סבבים
באמת רצו; ו-`N` יאמר אם הטבלה התייצבה או שהלולאה מיצתה 25 סבבים.
**שלוש האפשרויות נפרדות לחלוטין ברשומה אחת.**

**מצב מסגרות האשראי (לא השתנה — ינון טרם נקרא מחדש):**
| ישות | מסגרת | יתרה |
|---|---|---|
| טל רצבי | 50,000 | 26,513 ✔ |
| אביסידריס יובל | **null** ✅ | 154.92 |
| מטאדור שער | 40,000 | 93,188 ✔ |
| **ינון מושב** | 285,292.66 ❌ | 285,292.66 |

**איך אומת:** ארבעה קבצים נטענים ורצים (מאזין אחד גם אחרי הזרקה כפולה);
המארקים קיימים, **בסדר הביצוע הנכון**, הראשון לפני הלחיצה והלחיצה לפני
הדיווח. **⚠ מדידת זמן לא נוספה כאן — רק נקודות מדידה.**
(הבדיקה דיווחה „5 מארקים ציפינו" מול 6 בפועל — **שגיאה בציפייה שלי
בבדיקה**, לא בקוד.)

**מה טל צריך לבדוק:** ⟳ ל-1.9.4 ורענון לשונית דיסקונט, וסנכרון אחד.
**אין צורך לחכות לסוף** — מספיקה ישות אחת. הרשומה `discountPhases`
תראה לאן הולכות 27 השניות, ואז אפשר יהיה לתקן במכה אחת במקום לנחש.


## 2026-08-25 · Claude · 115.7 שניות ב-rowDates — השכבה הרביעית — 1.9.3

**הגשש `discountSyncAttempts` נתן את המספרים המדויקים:**
```
attempts: [{n:1, ms:75003,  ok:false, err:"לא השיב תוך 75 שניות"},
           {n:2, ms:129626, ok:true}]
```
**⚠⚠ ובכך הוא הפריך את התיקון שלי מ-1.9.2 והוכיח שהחמרתי.**
קיצרתי את הניסיון הראשון ל-75 שניות על סמך „המקסימום הנמדד הוא
46 שניות" — **והמדידה ההיא הייתה מוטה**: סיננתי רשומות `discountPhases`
בתנאי ששלל בדיוק את הריצות הארוכות. **המקסימום האמיתי הוא 129.6
שניות**, ולכן הקיצור רק הבטיח בזבוז של 75 שניות לפני הניסיון שמצליח.
**לקח: כשמסננים מדגם כדי למצוא מקסימום, לבדוק מה הסינון זרק.**

**ואז `discountPhases` הראה איפה 129 השניות:**
```
      9 מ״ש   המתנה לטעינת הדף
115,688 מ״ש   אחרי החלת הטווח     ← +115.7 שניות
115,689 מ״ש   אחרי גלאי הזהות     (+0.0)
115,724 מ״ש   סיום הקריאה         (+0.0)
```
**כל הקריאה בפועל היא 35 מילישניות. 115.7 שניות הן ב-`applyCollectSince`.**

**השורש — `innerText`, בשכבה הרביעית:**
```js
rowDates=()=>{for(const r of document.querySelectorAll('tr,[role="row"],.rc-table-row'))
                for(const m of text(r).matchAll(/dd\/mm\/yyyy/g))...}
```
`text(r)` הוא `innerText` **על כל שורה בטבלה**, וכל אחת כופה חישוב
פריסה. `applyCollectSince` קורא ל-`earliestRow()` **ארבע פעמים**.
עבר ל-`tc()` (textContent), ובוטלה קריאה כפולה מיותרת באותו ביטוי.

**⚠ הלקח, והוא העיקר ברשומה:** זו הפעם הרביעית שאותו באג צץ במקום חדש
(`bodyText` · `valueAfter` · `txRowCount` · ועכשיו `rowDates`).
בכל פעם תיקנתי את המופע שנמדד והנחתי שסיימתי. **מעכשיו: אחרי מציאת
`innerText` בנתיב חם — סריקה שיטתית של כל אחיו.** ביצעתי אחת, והיא
מצאה עוד מועמדים (בעיקר סורק ההלוואות שסורק `li,[role=row]` עם
`innerText`). **הם לא תוקנו — לא נמדדו, ולא אתקן בלי מדידה.**

**`SYNC_BUDGET` הוחזר ל-`[120,150,150]`** — 120 מכסה את 115.7 גם אם
תיקון `rowDates` לא יספיק, ועדיין חוסך 30 שניות מול הקודם.

**איך אומת:** ארבעה קבצים נטענים ורצים; `rowDates` **חולץ מן הקובץ
והורץ מול טבלה של 1,500 שורות** — **9/9**: קורא את כל 1,500 התאריכים,
`earliestRow` מחזיר את המוקדם, אין `innerText` בפונקציה, והתקציב 120.
**⚠ הרווח בזמן לא נמדד** — headless אינו מחשב פריסה. **הראיה תגיע רק
מ-`discountPhases` בריצה החיה הבאה.**

**מה טל צריך לבדוק:** ⟳ ל-1.9.3 ורענון לשונית דיסקונט.
**המספר להסתכל עליו: „אחרי החלת הטווח" ב-`discountPhases`.** אם הוא
יורד מ-115,688 מ״ש לשניות בודדות — זה נפתר, וישות 1 תרד מ-209 שניות
לפחות מ-20. ואז גם ינון ייקרא מחדש.


## 2026-08-25 · Claude · הזמן היה ב-timeout של ניסיון 1 — 1.9.2

**✅ הגשש מ-1.9.1 הכריע, ופסל את החשוד שרדפתי אחריו שלושה סבבים:**
```
discountWaitProbe  ישות 024844714: [{r:1, ms:2014, prepMs:1, stateMs:6, rows:34}]
                   ישות 514220276: [{r:1, ms:2023, prepMs:1, stateMs:6, rows:33}]
                   ישות 514649565: [{r:1, ms:2021, prepMs:1, stateMs:5, rows:32}]
```
**לולאת ההמתנה היא 2 שניות בכל הישויות** — סבב אחד, `prepare` מילישנייה
אחת, `DISCOUNT_STATE` שש. **155 השניות שייחסתי ללולאה ב-1.8.2 וב-1.8.3
נעלמו עם שומר ההזרקה של 1.9.0** — הן היו המאזינים הכפולים, שגרמו לכל
`DISCOUNT_STATE` להמתין 15 שניות לתשובה שנבלעה.

**⚠ ואיפה הזמן באמת:** ישות 514220276 — זמן קיר 155 שניות, לולאה
2 שניות, ו-`discountPhases` מראה **`extract` = 26 מילישניות.**
כלומר **ניסיון 1 של `DISCOUNT_SYNC_SELECTED` מיצה את מלוא 150 השניות,
וניסיון 2 הצליח מיד.** התקציב היה 150 שניות **בשלושת הניסיונות**.

**מה השתנה:** `SYNC_BUDGET=[75,150,150]` שניות.
⚠ **לא קיצרתי אגרסיבית במכוון.** מדדתי את `total` של כל
`discountPhases` שהושלם ב-19 ריצות: אחרי תיקוני 1.8.x הטווח הוא
**26 מ״ש עד ~46 שניות** (ישות 024844714, הגדולה), והערכים 259ש׳/142ש׳
הם היסטוריים מלפני תיקון ה-`innerText`. **קיצור מתחת ל-46 שניות היה
הופך ישות גדולה לכישלון קבוע.** 75 הן פי 1.6 מהמקסימום הנמדד.
בנוסף `discountSyncAttempts` רושם לכל ניסיון `{n, ms, ok, err}` — עד
עכשיו שרד רק `lastErr`, ולא היה אפשר לדעת אם ניסיון פג בזמן או השיב שגיאה.

**⚠⚠ הבדיקה תפסה פער בגשש שכתבתי באותו סבב:** הכתיבה ישבה **אחרי**
ה-`break`, ולכן **הניסיון המוצלח — המקרה הנפוץ ביותר — לא נרשם מעולם.**
זה בדיוק הלקח מ-1.8.0, וחזרתי עליו. **גשש שאינו שורד את המסלול הנפוץ
אינו גשש.** תוקן: הכתיבה לפני ה-break.

**מצב מסגרות האשראי (נמדד):**
| ישות | מסגרת | יתרה | |
|---|---|---|---|
| טל רצבי | 50,000 | 26,513 | ✔ |
| אביסידריס יובל | **null** | 154.92 | ✅ תוקן |
| מטאדור שער | 40,000 | 93,188 | ✔ |
| **ינון מושב** | 285,292.66 | 285,292.66 | ❌ **טרם נקרא מחדש** |

**איך אומת:** ארבעה קבצים נטענים ורצים (מאזין אחד גם אחרי הזרקה כפולה);
לולאת הניסיונות **חולצה מן הקובץ והורצה** — **11/11**: הצלחה ראשונה
נרשמת · ניסיון שפג בזמן ואז הצלחה · שלושה כישלונות · והתקציבים
75/150/150 עם אימות שהראשון גדול מהמקסימום הנמדד.

**מה טל צריך לבדוק:** ⟳ ל-1.9.2 ורענון לשונית דיסקונט. ישות שנתקעת
תעלה 75 שניות במקום 150. **ובריצה מלאה — ינון סוף-סוף ייקרא מחדש**
ומסגרתו אמורה להתאפס. `discountSyncAttempts` יראה אם ניסיון 1 עדיין נתקע.


## 2026-08-25 · Claude · מדידה בלולאת ההמתנה, במקום ניחוש רביעי — 1.9.1

**✅ ישות הפנטום נעלמה — אומת חי:** „נמצאו ואומתו **4** חשבונות", לא 5.

**❌ אבל 155 השניות חזרו, בפעם השלישית:**
```
14:40:06  פותח תנועות (024844714)
14:42:41  discountIdentityPass   ← 155 שניות
discountPhases: "המתנה לטעינת הדף" = 23 מ״ש
```
**`extract` לוקח 23 מילישניות. 155 השניות הן שוב בלולאת ההמתנה שברקע** —
למרות `txRowCount` והיציאה המוקדמת של 1.8.3.

**⚠⚠ ניחשתי על הלולאה הזו פעמיים (1.8.2, 1.8.3), ובשתיהן טעיתי ביעד.
אין לי נתון אחד מתוכה:** לא כמה סבבים רצו, לא כמה עלה כל סבב, ולא אם
`DISCOUNT_STATE` בכלל השיב. **לכן בסבב הזה לא תיקנתי — מדדתי.**

`discountWaitProbe` רושם לכל סבב:
`{r, ms, prepMs, stateMs, rows, prepErr, stErr}` ונכתב **תוך כדי**
(הלקח מ-1.8.0 — גשש שנשמר רק בסוף אובד בכישלון).
זה יפריד סופית בין שלוש האפשרויות שאני עדיין לא יכול להכריע ביניהן:
1. `prepareDiscountContent` יקר (עד 5 ניסיונות הזרקה עם השהיות),
2. `DISCOUNT_STATE` חורג מ-15 שניות ומחזיר `null`,
3. `rows` נשאר 0 ולכן היציאה המוקדמת מחכה לשלושה סבבים.
**`prepMs` מול `stateMs` מול `rows` מכריעים מיד.**

**איך אומת:** ארבעה קבצים נטענים ורצים (ו-content scripts רושמים מאזין
**אחד** גם אחרי הזרקה כפולה — השומר מ-1.9.0 מחזיק); הלולאה **חולצה מן
הקובץ והורצה** — **10/10**: יש שורות → סבב אחד · 0 שורות → שלושה
סבבים · אין תשובה → 12 סבבים · והגשש רושם שגיאה, זמנים וישות.
**התנהגות הלולאה לא השתנתה — רק נוספה מדידה.**

**מה טל צריך לבדוק:** ⟳ ל-1.9.1 ורענון לשונית דיסקונט, וסנכרון אחד.
**אין צורך לחכות שיסתיים** — הגשש נכתב תוך כדי, ואפשר לקרוא אותו אחרי
ישות אחת.


## 2026-08-25 · Claude · הזרקה חוזרת הכפילה מאזינים — שומר בכל הקבצים — 1.9.0

**טל: „A listener indicated an asynchronous response by returning true,
but the message channel closed before a response was received."**

**⚠ הופרך תחילה:** חשדתי במטפל שנכשל בלי לענות — **ולא זה**. כל מטפלי
`DISCOUNT_DISCOVER`/`DISCOUNT_SYNC_SELECTED` עוטפים ב-`.catch(e=>reply(...))`.

**השורש, והוא אסימטריה שהייתה בקוד מאז ומתמיד:**
```
leumi-content.js      → window.__leumiSyncLoaded   ✔ יש שומר
discount-content.js   → אין
poalim / isracard / mizrahi / fibi / cal / max → אין
```
`prepareDiscountContent` מזריק את הקובץ שוב ושוב, **וללא שומר כל הזרקה
מוסיפה `onMessage` נוסף באותו דף**. אחרי שלוש הזרקות יש שלושה מאזינים;
כולם מחזירים `true` על אותה הודעה, **רק אחד עונה**, והשאר סוגרים ערוץ
בלי תשובה — **וזו בדיוק ההודעה של Chrome.**
**זה גם מסביר למה לאומי לא סבל מזה ודיסקונט כן.**

**מה השתנה:** שומר הזרקה בכל שמונה הקבצים שחסרו, בתבנית של לאומי
(`if(window.__xSyncLoaded)return;window.__xSyncLoaded=true;`).
**התקדים כבר היה בקובץ — הוא פשוט לא הוחל רוחבית.** זה הדפוס שחזר גם
ב-1.4.0 („השומר בדיסקונט לא הוחל על פועלים").

**איך אומת:** כל תשעת ה-content scripts **הוזרקו שלוש פעמים** מול סטאב
`chrome`, ונספרו המאזינים — **9/9 רושמים בדיוק אחד.**
לפני התיקון: `fibi`, `cal`, `max` ו-`discount` רשמו **שלושה**.
זו מדידה ישירה של הבאג ושל תיקונו, לא בדיקת מבנה.

**⚠ מה שעדיין לא ידוע:** האם זו הייתה הסיבה **היחידה** להודעה. הודעה
כזו נוצרת גם כשהדף מנווט או כשהתוסף נטען מחדש באמצע הודעה תלויה, ושני
המצבים קרו היום. **הסינון הזה מסיר סיבה ודאית אחת — לא בהכרח את כולן.**

**מה טל צריך לבדוק:** ⟳ ל-1.9.0 **ורענון לשונית דיסקונט** (הרענון חשוב
במיוחד הפעם — הוא מנקה את המאזינים הכפולים שכבר יושבים בדף).
בריצה הזו נכנסים יחד: מהירות (1.8.3) · „ינון" (1.8.4) · ישות פנטום
(1.8.5) · והשומר (1.9.0).


## 2026-08-25 · Claude · „החשבון החמישי" היה שורת תנועה — 1.8.5

**טל: „מאיפה החשבון החמישי?"** נמדד מן האחסון, ותשובה מלאה:
```
discountEntityReport: [{"entity":"000213991","resolved":false,"passes":2,
  "owner":"העברה ממשה ושרונה מיכאל חשבון 12-544-0005- שכירות צרכניה"}]
```
**זו שורת תנועה, לא ישות.** התיאור נקרא כשם בעלים, והאסמכתא בת 9
הספרות (`000213991`) נקראה כמזהה ישות. מכאן „4 מתוך 5 חשבונות זוהו",
וניסיונות חוזרים לעבור לישות שאינה קיימת.

**השורש ב-`menuEntitiesLoose()`** — הנפילה-לאחור כשתפריט הישויות אינו
נמצא. היא סורקת **את כל הדף** ומקבלת כל
`button/[role=button]/[role=option]/[role=menuitem]/li/a/div[tabindex]`
שהטקסט שלו קצר מ-90 תווים ומכיל `\d{9}`.
**שורת תנועה עם אסמכתא עונה על ההגדרה במדויק.**

**⚠ למה הסינון מבני ולא דירוגי:** ההערה בקוד עצמו מתעדת ש-ב-21.08
דירוג שהעדיף `a/button` על `li` **הפיל את הזיהוי כולו** —
`discoveredAccounts` התרוקן ל-0. לכן המבחן היחיד שנוסף הוא:
`!el.closest('[role="row"],div.rc-strip-row,table tr')`.
**אפשרות בבורר לעולם אינה יושבת בתוך שורת תנועה**, ולכן המבחן אינו
יכול לפסול אפשרות אמיתית — בשונה מדירוג.

**איך אומת:** ארבעה קבצים נטענים ורצים; `menuEntitiesLoose` **חולץ מן
הקובץ והורץ מול DOM אמיתי** — **10/10**:
- בורר עם ארבע ישויות → כל הארבע נקראות
- שלוש צורות של שורת תנועה (`role=row` · `table tr` · `rc-strip-row`)
  עם אסמכתאות בנות 9 ספרות → **כולן נדחות**, ורק הישויות האמיתיות נשארות
- בורר בתוך `[role=menu]` → לא נפגע

**⚠ מה שלא ידוע ולא נחקר:** **מדוע** הזיהוי רץ בכלל כשדף התנועות על
המסך. זה התנאי שאיפשר את התקלה. הסינון חוסם את התסמין; אם הזיהוי לא
אמור לרוץ שם — יש כאן עוד תקלה, והיא לא נבדקה.

**מה טל צריך לבדוק:** ⟳ ל-1.8.5 ורענון לשונית דיסקונט. אמור להופיע
**4 חשבונות ולא 5**. ובאותה ריצה נכנסים לראשונה גם 1.8.3 (מהירות)
וגם 1.8.4 (מסגרת „ינון").


## 2026-08-25 · Claude · מסגרת שזהה ליתרה היא היתרה — 1.8.4

**✅ „אביסידריס יובל" תוקן ואומת חי:** `creditLimit: null` (היה 154.92).
**❌ „ינון מושב" נשאר שגוי: 285,292.66 — וזו בדיוק היתרה.**

**הגשש נתן את התשובה המלאה:**
```
tried: [ {t:"",  why:"אח נדחה — לא סכום נקי"},
         {t:"תנועות עתידיות (סכום משוער)₪1,900.00", why:"אח נדחה"},
         {t:"₪285,292.66", v:285292.66, why:"אח שכולו סכום"} ]  → value 285292.66
```
בחשבון **ללא** מסגרת, התווית „מסגרת אשראי" יושבת ליד סכום היתרה,
והסריקה תופסת אותו כי הוא אכן „אלמנט שכולו סכום".

**⚠ אין דרך להבחין ביניהם לפי מבנה — רק לפי הערך.** ובשני המקרים
שדווחו מן השטח ההתאמה הייתה **לאגורה**: יובל 154.92 = יתרה 154.92,
ינון 285,292.66 = יתרה 285,292.66. לכן: מסגרת שזהה ליתרה נדחית.
ההסתברות שמסגרת אמיתית תהיה זהה ליתרה עד האגורה זניחה, והנפילה בטוחה —
ריק במקום מספר שגוי, והסיבה נרשמת ב-`discountCreditProbe`.

**🔎 על המהירות — המדידה מפרקת את זה:** `discountPhases` של ינון
(ריצה על **1.8.2**, לפני שהתיקונים של 1.8.3 נטענו):
```
21 מ״ש    המתנה לטעינת הדף
2,082     אחרי החלת הטווח
2,151     סיום הקריאה        ← extract כולו: 2.15 שניות
27,751    (מעבר שני)
```
**`extract` עצמו לוקח כשתי שניות.** הישות לקחה 183 שניות — כלומר
**~155 שניות היו שוב בלולאת ההמתנה שברקע**, וזה בדיוק מה ש-1.8.3 מתקן
(`txRowCount` + יציאה מוקדמת מחשבון ריק). **הריצה שנמדדה קדמה לו.**

**⚠ באג בגשש עצמו, שתוקן:** `selectEntity` נקרא גם **מחוץ** ל-`extract`
(מעבר ישות ברקע), ואז `phaseT0` היה 0 ו-`ms` יצא **חותמת זמן מוחלטת**
(1.787e12) במקום משך. **גשש שמדווח מספר חסר פשר גרוע מגשש שותק.**

**איך אומת:** ארבעה קבצים נטענים ורצים; **13/13** —
**שני המקרים מן השטח נדחים** (ינון 285,292.66 · יובל 154.92),
**וכל ארבע המסגרות האמיתיות שורדות** (50,000 · 40,000 · 75,000 · 30,000,
כולל יתרה שלילית וכולל „אין יתרה ידועה → לא נדחה"),
ושלוש רגרסיות קודמות עוברות.

**מה עדיין פתוח:**
1. **1.8.3 טרם נמדד חי** — הריצה האחרונה הייתה על 1.8.2.
2. **הדלתא בלאומי טרם רצה** (`leumiDelta` ריק).
3. `applyCollectSince` לקח 25 שניות במעבר השני של ינון — **לא נחקר.**

**מה טל צריך לבדוק:** ⟳ ל-1.8.4 **ורענון לשונית דיסקונט**. הפעם גם
המהירות (1.8.3) וגם „ינון" (1.8.4) נכנסים יחד לראשונה.


## 2026-08-25 · Claude · innerText בעלים — השכבה השלישית והאחרונה — 1.8.3

**✅ 1.8.2 עבד, ונמדד חי:** „פותח תנועות" 14:01:34 → `extract` 14:01:39.
**154 שניות → 5.** וישות 1 הושלמה ב-**37 שניות** (במקום 259 ו-timeout),
**והסנכרון עבר לישות 2 בפעם הראשונה מאז שהתחלנו.**

**⚠ אבל ישות 2 (514220276, יתרה 154.92) לקחה 155 שניות.** למה: לחשבון
הזה **אין תנועות בטווח**, ולולאת ההמתנה ממתינה ל-`rows>0` — מצב שלעולם
לא יגיע — ולכן מיצתה שוב את כל 12 הסבבים.

**השכבה השלישית של אותו באג, ובמקום היקר ביותר בקובץ:**
```js
leavesOf = el => [...el.querySelectorAll('*')]
  .filter(x=>!x.children.length && text(x)).map(text)
```
`text()` הוא `innerText`, והוא נקרא על **כל עלה של כל שורה מועמדת** —
**ופעמיים**, גם ב-`filter` וגם ב-`map`. `txCandidates` מריץ זאת על שלוש
קבוצות. בדף עם אלפי עלים זו הפעולה היקרה ביותר בפרויקט.

**⚠⚠ ומה שלא עשיתי, במכוון:** לא החלפתי את `text()` בתוך `leavesOf`.
`transactions()` בונה מן העלים האלה את **התנועות עצמן**, וטקסט מוסתר
היה נכנס לתיאורים ולסכומים. **תיקון ביצועים לא ישבש נתונים.**
במקום זאת: `txRowCount()` — מסלול **ספירה** זול על `textContent`,
שמשרת רק את הסקר (12 קריאות). `txCandidates()` המדויק נשאר לקריאה
עצמה, שרצה **פעם אחת**.
אומת שהשניים מחזירים **בדיוק אותו מספר** (1200 = 1200).

**תיקון שני:** הלולאה יוצאת מוקדם כשהדף **ענה** שלוש פעמים ברציפות עם
0 שורות — הוא טעון ופשוט ריק. ⚠ אין סיכון לקיצור מוקדם: `extract`
ממתין לטעינה בשנית (עד 30 שניות), ולכן שורות שיופיעו מאוחר ייקראו.
⚠ וכשאין תשובה כלל (`st===null`) הלולאה **עדיין ממצה 12** — שם ההמתנה
מוצדקת. שלושת המצבים נבדקו.

**⚠ הדפוס, בפעם הרביעית היום:** *חישוב יקר בשביל ערך שלא נקרא, או
בפירוט שלא נדרש.* `valueAfter` בלולאה שלא יכלה להצליח · `text(document.body)`
פי חמישה · `currentBalance()` פי 12 · ועכשיו פירוק מלא של עלים כשהיה
צריך רק מספר. **הכלל: לפני שמייעלים חישוב — לבדוק מי קורא את התוצאה
ובאיזה פירוט.**

**איך אומת:** ארבעה קבצים נטענים ורצים; **12/12** — הספירה הזולה שווה
למדויקת, `DISCOUNT_STATE` מחזיר אותה, דף ריק מחזיר `0` ולא `null`,
ולולאת הרקע **חולצה מן הקובץ והורצה**: 0 שורות → 3 סבבים · יש שורות →
סבב אחד · אין תשובה → 12 סבבים.
**⚠ יחס הזמן לא נמדד** — headless אינו מחשב פריסה, ושני המסלולים יצאו
0.0 מ״ש. הראיה היא מן הדף החי בלבד.

**מה עדיין פתוח:**
1. **מסגרות האשראי של ישויות 2-4 טרם נקראו** — זה עדיין הפריט שטל מחכה לו.
2. **הדלתא בלאומי טרם רצה** (`leumiDelta` ריק).

**מה טל צריך לבדוק:** ⟳ ל-1.8.3 **ורענון לשונית דיסקונט**. ישות ריקה
אמורה לקחת ~6 שניות במקום 155. אם כל ארבע הישויות עוברות —
**„אביסידריס יובל" (154.92) ו„ינון" (1,900) אמורים להשתנות.**


## 2026-08-25 · Claude · 154 שניות בלולאת המתנה שחישבה יתרה לחינם — 1.8.2

**✅ 1.8.1 עבד, ובגדול — נמדד חי אחרי שטל רענן גם את לשונית הבנק:**
```
discountPhases:       "המתנה לטעינת הדף" = 8 מ״ש        (היה עד 30 שניות)
discountIdentityPass: via="מספר תואם"  seen="2556371"  minePresent=true
```
**גלאי הזהות עבר 3 מ״ש אחרי תחילת `extract` — מ-155 שניות.**
ובונוס: `activeAccount()` מחזיר עכשיו את המספר **הנכון** (2556371 ולא
`0690300`), כי `textContent` רואה גם טקסט שאינו מרונדר. **זה מייתר את
העקיפה מ-1.6.1** — היא נשארת כרשת ביטחון.

**⚠ ובכל זאת טל: „עדיין איטי." והמדידה הראתה שהזמן פשוט עבר שלב:**
„פותח תנועות" ב-13:37:26, `extract` התחיל ב-13:40:00 — **154 שניות
לפני ש-`extract` בכלל נקרא**, בעוד `extract` עצמו לוקח 8 מ״ש.
**הזמן היה ברקע, לא בקריאה.**

**השורש, בלולאת ההמתנה שברקע:**
```js
for(let w=0;w<12;w++){await delay(2000);await prepareDiscountContent(tab.id);
  let st=null;try{st=await withTimeout(sendMessage(DISCOUNT_STATE),15000,'מצב')}catch(e){}
  if(st?.rows>0)break}
```
הלולאה קוראת מן התשובה **רק את `rows`** — אבל המטפל ב-`DISCOUNT_STATE`
חישב גם `currentBalance()`, שנופל ל-`latestRowBalance(transactions())`,
כלומר **פרסור מלא של כל התנועות**, בכל אחת מ-12 הקריאות, **בשביל ערך
שאיש אינו קורא**. וכשהחישוב חורג מתקרת 15 השניות — `st` הוא `null`,
`st?.rows>0` שקר, **והלולאה ממצה את כל 12 הסבבים.**
12 × (2 + עד 15) = עד 204 שניות. נמדד 154.

**מה השתנה:** `balance` מחושב רק לפי `withBalance:true`.
⚠ **אומת שאיש אינו קורא אותו:** `st.balance` ו-`st?.balance` — **אפס
מופעים** ב-`background.js`. (מופע שנראה תחילה כהתאמה היה `last.balance`
בתוך `gapOf` — התאמה חלקית של `st` מתוך „last".) השדה נשאר זמין לפי
בקשה מפורשת, כדי שלא לשבור קורא עתידי בשקט.

**⚠ הדפוס שחוזר בפרויקט הזה, וזו הפעם השלישית היום:** *חישוב יקר
שמתבצע בשביל ערך שלא נקרא.* קודם `valueAfter('יתרת עו"ש')` בלולאה שלא
יכלה להצליח; אחר כך `text(document.body)` פי חמישה בכל סבב; ועכשיו
`currentBalance()` פי 12. **לפני שמייעלים חישוב — לבדוק אם מישהו קורא
את התוצאה.**

**איך אומת:** ארבעה קבצים נטענים ורצים; המטפל **נקרא בפועל** דרך
ה-listener — **8/8**: משיב, `balance===null` כברירת מחדל, `rows` מוחזר,
ו-`withBalance:true` עדיין מחשב.
**⚠ מדידת הזמן בבדיקה חסרת ערך** — ה-DOM המסונתז לא תאם את
`txCandidates()` (`rows=0`), ושני המסלולים יצאו 0.0 מ״ש. **הראיה לעלות
היא 154 השניות מן הדף החי בלבד.**

**מה עדיין לא ידוע:** האם 12 הסבבים ייעצרו מוקדם עכשיו. אם `rows>0`
יחזור בסבב הראשון, השלב הזה יירד ל-~2 שניות.

**מה טל צריך לבדוק:** ⟳ ל-1.8.2 **ורענון לשונית דיסקונט** (שניהם —
זה מה שהכריע ב-1.8.1). אם הסנכרון מגיע לישויות 2-4, **מסגרות האשראי
ייקראו מחדש** — „אביסידריס יובל" 154.92 ו„ינון" 1,900 אמורים להשתנות.


## 2026-08-25 · Claude · TDZ מהחלפה גורפת · והגשש הוכיח ש-1.8.0 עובד — 1.8.1

**⚠⚠ באג שהכנסתי ב-1.8.0, ושתי הבדיקות שלי לא יכלו לתפוס:**
```
"ישות 024844714 נכשלה אחרי 3 ניסיונות:
 Cannot access 'bodyText' before initialization"
```
ההחלפה הגורפת `text(document.body)` → `bodyText()` פגעה גם ב**משתנה
מקומי** בתוך `assertEntityMatches` שנקרא במקרה `bodyText`, וייצרה
`const bodyText=bodyText()` — **הפניה עצמית ו-TDZ בזמן ריצה.**
תוקן: המקומי הוא `pageText`.

**⚠ הלקח על שיטת הבדיקה, והוא העיקר ברשומה הזו:**
- `parse` אינו תופס TDZ.
- **גם הרצת הקובץ אינה תופסת** — היא מריצה רק את הרמה העליונה,
  והשגיאה חיה **בתוך גוף פונקציה** שלא נקראה.
- ⚠ **וגם החלפה גורפת של מחרוזת היא כלי מסוכן:** היא אינה מבחינה בין
  גלובלי למקומי, ולא בין קוד להערה. באותו סבב היא גם הפכה `
` בתוך
  הערה לשורה חדשה ושברה את הקובץ.
**מעכשיו הרתמה קוראת לפונקציות עצמן.** `assertEntityMatches` ו-
`creditLimitValue` מוזרקים החוצה מתוך ה-IIFE (הקובץ עטוף `(()=>{…})()`)
ונקראים מול DOM אמיתי, עם תקרת זמן לכל קריאה. **13/13**, כולל:
מסלול „מספר תואם", חסימת ישות אחרת, ושבעה מקרי מסגרת אשראי.

**✅ והבשורה: הגשש שתיקנתי ב-1.8.0 הוכיח שהתיקון עצמו עובד.**
```
discountPhases: {"phases":[{"ms":6,"name":"המתנה לטעינת הדף"}],"total":6}
```
**6 מילישניות.** אותה לולאה עצמה לקחה עד 30 שניות כשהיא קראה `innerText`.
זו ההוכחה הראשונה מן הדף החי ש**המעבר ל-`textContent` הוא הסדר גודל
הנכון** — ובלי התיקון של „גשש שנכתב תוך כדי" (1.8.0) היא הייתה אובדת
עם ה-crash, בדיוק כמו ש-ה-timeout מחק את המדידה בסבב הקודם.

**מה עדיין לא ידוע:** כמה זמן לוקח **גלאי הזהות** עכשיו. הריצה קרסה
לפניו. זה החלק שהיה 155ש׳, והוא זה שקובע אם הסנכרון נכנס ל-150ש׳.

**מה טל צריך לבדוק:** ⟳ ל-1.8.1 וסנכרון דיסקונט. `discountPhases`
יראה עכשיו את כל השלבים תוך כדי. אם הסנכרון מגיע לישויות 2-4 —
**מסגרות האשראי ייקראו מחדש**, וזו הבדיקה שטל מחכה לה
(„אביסידריס יובל" 154.92 ו„ינון" 1,900 אמורים להשתנות).


## 2026-08-25 · Claude · innerText על כל הדף — זה היה כל הסיפור — 1.8.0

**טל: „מאוד איטי, לא סביר שזה ייקח כל כך הרבה זמן שכל התנועות כבר נמצאות."**
**צדק, וההודעה באחסון סוגרת את זה:**
```
"שגיאה: ישות 024844714 נכשלה אחרי 3 ניסיונות:
 קריאת התנועות לא השיב תוך 150 שניות"
```
**הקריאה נכשלת ב-timeout ומנסה שלוש פעמים.** גלאי הזהות לבדו סיים
ב-155 שניות — כלומר **הוא חורג מן התקציב לפני שהקריאה בכלל מתחילה.**

**השורש: `text()` בנוי על `innerText`, ו-`innerText` כופה חישוב פריסה
מלא של הדף.** ובדף דיסקונט עם מאות שורות:
- `bodyText`/`text(document.body)` נקרא **~32 פעמים** — הגלאי קורא אותו
  פעם בסבב, `activeAccount()` קורא אותו **שוב** בכל סבב, כפול 8 סבבים,
  כפול שניים עם מסלול התיקון.
- `valueAfter` הריץ `text(el)` על **כל** `button/p/div/span` בסינון,
  **וגם בתוך ה-`sort`** — כלומר פעמיים לכל השוואה.

**מה השתנה:** כל סריקת-רוחב עברה ל-`textContent`, שאינו כופה פריסה.
`tc()` לאלמנט ו-`bodyText()` לגוף, **שניהם מנרמלים רווחים** כמו קודם.
⚠ **`text()` עצמו לא שונה** — הוא משמש לקריאת תוויות וערכים, שם ההבדל
בין טקסט גלוי למוסתר כן משנה. אומת: אפס `innerText` בסריקות רוחב.

**⚠ 1.7.3 עזר אך לא פתר** — 259ש׳ → 155ש׳ (הפחתת מספר הסריקות). **1.8.0
מוזיל כל סריקה**, וזה הסדר גודל שחסר. הלקח: הפחתת *מספר* הקריאות היא
טיפול בסימפטום כשה*מחיר* של כל קריאה הוא הבעיה.

**⚠⚠ שתי טעויות שלי בסבב הזה, ושתיהן שוות רישום:**
1. **שברתי את `discount-content.js`.** `
` בתוך הערה הפך לשורה חדשה
   אמיתית, ושארית ההערה נותרה כקוד. **`parse=FAIL` תפס את זה מיד** —
   בדיוק בגלל שעברתי לבדוק הרצה ולא רק לקרוא. **בלי הבדיקה הזו הייתי
   מוסר לטל קובץ שבור.**
2. **הגשש `discountPhases` נשמר רק בסוף `extract`** — והריצה שנכשלה
   ב-timeout מעולם לא הגיעה לשם, ולכן **המדידה שהייתי צריך בדיוק אז
   אבדה.** תוקן: כל `phase()` נכתב מיד.
   **גשש ששורד רק בהצלחה אינו גשש.**

**⚠ מה שלא הצלחתי למדוד:** ניסיתי לכמת `innerText` מול `textContent`
ב-Chrome headless וקיבלתי 0.0 מ״ש לשניהם, גם עם `--window-size` ואילוץ
`offsetHeight`. **headless עם `--dump-dom` אינו מחשב פריסה, ולכן אי אפשר
למדוד שם עלות פריסה.** הראיה היחידה היא מן הדף החי: 155 שניות
ו-timeout. **הנתון „פי כמה" אינו ידוע ואין להמציא אותו.**

**איך אומת:** ארבעה קבצים עוברים parse, ושני ה-content scripts **רצים
ורושמים listener**. `creditLimitValue` הורץ מן הקובץ מול DOM אמיתי —
**9/9**, כולל מקרה חדש שנוסף במיוחד בגלל השינוי: **תווית מפוצלת בין
שורות** (`textContent` אינו מנרמל רווחים מעצמו — לכן `tc()`).
**⚠ הרווח בזמן עצמו לא נמדד. רק ריצה חיה תכריע.**

**מה טל צריך לבדוק:** ⟳ ל-1.8.0 וסנכרון דיסקונט. אם הזמן ירד מתחת
ל-150ש׳ לישות, הסנכרון יגיע לכל ארבע הישויות ו**מסגרות האשראי ייקראו
מחדש** — לבדוק ש„אביסידריס יובל" (154.92) ו„ינון" (1,900) מתוקנים.
`discountPhases` נכתב עכשיו **תוך כדי**, ולכן גם אם ייכשל — תהיה מדידה.


## 2026-08-25 · Claude · 99.98% מזמן דיסקונט לפני שהקריאה מתחילה — 1.7.3

**⚠⚠ המדידה החשובה של הסבב, מ-`discountPhases` שנוסף ב-1.7.2:**
```
ישות 024844714:  „אחרי גלאי הזהות"  = 259,100 מ״ש
                 „סיום הקריאה"       = 259,139 מ״ש   (total 259,139)
```
**כל הקריאה בפועל — הטווח, מסגרת האשראי, התנועות, ההלוואות — לוקחת
39 מילישניות. 259 השניות כולן לפני כן.** ארבע ישויות × 259ש׳ ≈ 17 דקות.

**⚠ וזו גם התשובה לטל („עדיין לא ראיתי שמסגרות האשראי טופלו"):**
הערכים השמורים כרגע —
| ישות | מסגרת | יתרה |
|---|---|---|
| טל רצבי | 50,000 | 26,513 ✔ |
| **אביסידריס יובל** | **154.92** | **154.92** ← המסגרת זהה ליתרה בדיוק |
| מטאדור שער | 40,000 | 93,188 ✔ |
| **ינון מושב** | **1,900** | 286,668 |
**אלה ערכים מלפני 1.5.3.** התיקון עובד — `discountCreditProbe` נותן
`{value:50000, why:"מיד אחרי התווית"}` לישות 1 — אבל **הסנכרון לא מגיע
לישויות 2-4**. הבאג של מסגרות האשראי והאיטיות הם **אותה תקלה אחת**.

**מה שנמצא ב-259 השניות — שוב `innerText` על כל הדף:**
1. `for(let i=0;i<120;i++){if(txCandidates().length||valueAfter('יתרת עו"ש')!=null)break;...}`
   `valueAfter` סורק **כל button/p/div/span** וקורא `innerText` על כל
   אחד — חישוב פריסה מלא — עד **120 פעם**.
   **⚠ וההערה בקוד עצמו כבר אמרה שזה חסר תוחלת:** „בדף התנועות התווית
   היא „עובר ושב", ו„יתרת עו״ש" קיימת רק בדף הבית." כלומר בדף התנועות
   הקריאה מחזירה `null` **תמיד** — 120 סריקות יקרות שאינן יכולות להצליח.
   `txCandidates()` הוא מה שבאמת עוצר את הלולאה, והוא זול.
   **עכשיו: הזול בכל סבב, היקר אחת ל-8 (15 קריאות במקום 120).**
2. `onPage` קרא `text(document.body)` **מחדש לכל מועמד** — פעם ל-`mine`
   ועוד אחת לכל ישות אחרת, בכל אחד מ-8 הסבבים, ופעמיים עם מסלול התיקון.
   **עכשיו הגוף נקרא פעם אחת לסבב** (אומת מן הקובץ: 1).

**⚠ שתי טעויות מדידה שלי בסבב הזה, ושתיהן שוות רישום:**
- מדדתי עלות `innerText` ב-Chrome headless וקיבלתי 0 מ״ש. **headless עם
  `--dump-dom` אינו מחשב פריסה, ולכן `innerText` זול שם.** מדידת ביצועי
  פריסה בסביבה הזו חסרת ערך — ההוכחה היא `discountPhases` מהדף החי.
- רתמת הבדיקה חתכה `for(let i=0;i<8;i++)` הראשון בקובץ, שהוא של
  `entityOptions` ולא של הגלאי. **חילוץ לפי מחרוזת חייב להיות מתוחם
  לפונקציה.**

**מה עדיין פתוח:**
1. **אין דלתא בדיסקונט** — ועכשיו ברור שזה גם לא העיקר: הקריאה עצמה
   39 מ״ש. **דלתא בדיסקונט מיותרת כמעט לחלוטין.**
2. **הדלתא בלאומי טרם רצה** (`leumiDelta` ריק).
3. `activeAccount()` עדיין מחזיר `0690300` — נעקף ב-1.6.1, לא תוקן.

**מה טל צריך לבדוק:** ⟳ ל-1.7.3 וסנכרון דיסקונט. אם 259 השניות ירדו,
הסנכרון יגיע לכל ארבע הישויות ו**מסגרות האשראי ייקראו מחדש** — אז
לבדוק ש„אביסידריס יובל" ו„ינון" מציגים ריק ולא 154.92 / 1,900.
`discountPhases` יראה את הפירוק החדש.


## 2026-08-25 · Claude · הדלתא היא ללאומי בלבד · מעבר ישות בדיסקונט — 1.7.2

**טל: „תבדוק שזה מחפש רק דלתא, זה צריך להיות מאוד מהיר."**
**התשובה: זה לא דלתא — כי זה דיסקונט.** `leumiDelta` **מעולם לא נכתב**;
הדלתא של 1.7.0 היא **ללאומי בלבד**. `discount-content.js` מזכיר
`lastSync` אפס פעמים ומחיל `01/01→25/08` בכל ריצה, לכל אחת מ-4 הישויות.

**⚠ אבל המדידה מראה שדלתא לבדה לא הייתה פותרת את זה.** פירוק ישות אחת
מ-`statusBySource` (חותמות אמיתיות, 12:18–12:23):
| שלב | משך |
|---|---|
| פותח תנועות | 5ש׳ |
| **מעבר ישות + גלאי זהות** | **154ש׳** |
| טווח + מסגרת אשראי | 108ש׳ |
**החלק הגדול אינו קריאת הטווח.** דלתא בדיסקונט הייתה חוסכת חלק מ-108,
ומשאירה את 154 על כנן.

**⚠ טעות שלי תוך כדי, ששווה רישום:** קראתי „3.5 דקות בלי כתיבה" והכרזתי
תקיעה — בעוד הרשומות באחסון ב-UTC והשעון המקומי +3. **הוא כן התקדם.**
להשוות אזור זמן לפני שמכריזים על תקיעה.

**מה נמצא ב-154 השניות, ואותו שורש כמו כל היום:**
```js
if(entityNow()===id && account && account!==previous) return true;
```
`previous` מגיע מ-`activeAccount()` — **אותו קורא שנמדד כמחזיר `0690300`,
מספר החשבון של הנעבר בשורת תנועה**. כשהמספר הזר אינו משתנה התנאי לעולם
אינו מתקיים, **והלולאה שורפת את מלוא 3 השניות גם כשהמעבר הצליח מיד**,
ואז נופלת לשורה האחרונה שמחזירה true בדיוק על סמך `entityNow()`.

**מה השתנה:** בורר הישות הוא האות האמין (נקבע ב-1.6.1). שינוי מספר חשבון
נשאר האות החזק ומחזיר מיד; בהיעדרו די בבורר שמצביע על הישות **שלוש
בדיקות ברציפות (750 מ״ש)** במקום 3 שניות מלאות. **פי 4 לכל מעבר.**

**⚠ 154ש׳ ÷ 3ש׳ ≈ 51 קריאות — ואין לי הסבר לכך.** לכן **לא ניחשתי את
השאר** אלא הוספתי `discountPhases`: חותמות לכל שלב בתוך `extract`
ו-`selectEntity`. הריצה הבאה תגיד לאן הולכות השניות במקום שאשער.

**איך אומת:** ארבעה קבצים עוברים parse, ושני ה-content scripts **הורצו
באמת** ורושמים listener (הלקח מ-1.7.1). `switched` **חולץ מן הקובץ והורץ
מול שעון מדומה** בארבעה תרחישים — **4/4**:
| תרחיש | קודם | עכשיו |
|---|---|---|
| הבורר עבר, מספר החשבון זר וקבוע (**הבאג**) | 3000 מ״ש | **750 מ״ש** |
| מספר החשבון השתנה | 3000 מ״ש | **250 מ״ש** |
| המעבר נכשל | 3000 מ״ש | 3000 מ״ש ✔ |
| הבורר מהבהב | — | 3000 מ״ש, **לא מאשר מוקדם** ✔ |

**מה עדיין פתוח:**
1. **אין דלתא בדיסקונט.** לא נבנתה במכוון — קודם למדוד לאן הולכות 154ש׳.
2. **הדלתא בלאומי טרם רצה ולו פעם אחת.** `leumiDelta` ריק.
3. „זיהוי הישויות לא השיב תוך 60 שניות" (12:12) — חזר פעם אחת ואז עבד.

**מה טל צריך לבדוק:** ⟳ ל-1.7.2. **סנכרון לאומי פעמיים** — השני הוא
מבחן הדלתא. וסנכרון דיסקונט אחד, ואז לקרוא `discountPhases`.


## 2026-08-25 · Claude · כל ⟳ מחק את רשימת החשבונות — 1.7.1

**טל: „לא מופיע חשבונות הבנק לסנכרון."**
`discoveredAccounts` היה `[]`, והסטטוס האחרון היה **דיסקונט**:
„זיהוי הישויות לא השיב תוך 60 שניות".

**⚠ הפרכה ראשונה, ובדיקה שהייתה חסרה לי:** חשדתי בקוד שכתבתי היום.
עד עכשיו בדקתי רק `parse` — **וזה אינו תופס שגיאת ריצה בטעינת הקובץ**,
ששגיאה כזו מונעת רישום ה-listener וזה בדיוק „לא השיב". לכן **הרצתי את
ארבעת ה-content scripts באמת** מול סטאב `chrome`: כולם רצים ורושמים
listener אחד. הקוד לא היה האשם.
**מעכשיו: בדיקת content script היא הרצה, לא parse.**

**השורש האמיתי:** `chrome.runtime.onInstalled` יורה עם `reason:'update'`
**בכל ⟳ של תוסף unpacked**, והמאזין כתב `freshStart` שכולל
`discoveredAccounts:[]`. כלומר **כל רענון מחק את רשימת הבחירה** —
וזיהוי מחדש דורש סשן חי בבנק. עם שמונה גרסאות ביום אחד זה נעשה בלתי נסבל,
**ואני זה שגרם לזה** בקצב העדכונים.

**מה השתנה:** `freshStart` פוצל.
- `freshFlags` (דגלי pending, `leumiAttempts`, `pendingSources`) —
  **מנוקים תמיד.** הם קיימים מסיבה טובה: תהליך חלקי שנקטע, ו-`pendingSources`
  תקוע הוא מה שגרם לחטיפת לשוניות ב-18.08. **לא נגעתי בהם.**
- `discoveredAccounts` — נמחק ב**התקנה** וב**הפעלה מחדש של הדפדפן**
  (`onStartup`), שם באמת אין סשן חי מאחורי הרשימה. **ברענון תוסף
  הדפדפן ממשיך לרוץ ולשונית הבנק עדיין מחוברת**, ולכן הרשימה תקפה.

**✅ הנתונים עצמם מעולם לא נמחקו** — נבדק באחסון: `leumi-921-348300`
עם `lastSync 08:21`, 196 תנועות, 129 שיקים, יתרה 18,487.92. רק רשימת
הבחירה נעלמה. **כלומר גם בסיס הדלתא של 1.7.0 שלם.**

**איך אומת:** ארבעה קבצים עוברים parse; **גוף המאזין `onInstalled` חולץ
מן הקובץ והורץ** בשלושה מצבים — **3/3**: `update` שומר את הרשימה,
`install` ו-`chrome_update` מוחקים, **והדגלים מנוקים בכל שלושתם.**

**מה עדיין פתוח:** „דיסקונט עסקי: זיהוי הישויות לא השיב תוך 60 שניות".
הקוד תקין ורושם listener, ולכן החשד הוא **סביבתי** — הלשונית לא הייתה
דף דיסקונט מחובר, או ה-content script לא הוזרק. **לא נחקר לעומק**;
אם זה חוזר, לבדוק את כתובת הלשונית בזמן הזיהוי.

**מה טל צריך לבדוק:** ⟳ ל-1.7.1 (⚠ **הרענון הזה עדיין מוחק** — התיקון
מתחיל לפעול מהרענון **הבא**). זיהוי לאומי, ואז סנכרון. אחרי זה ⟳ נוסף
ולוודא ש**הרשימה שרדה**. ואז סנכרון שני — **הוא המבחן של הדלתא**.


## 2026-08-25 · Claude · סנכרון דלתא — עיגון על ההסכמה האחרונה — 1.7.0

**בקשת טל:** „האם בזמן סנכרון הוא מזהה שיש תקופה ומוריד רק את השינויים?"
**התשובה שנמדדה הייתה לא, וחד-משמעית:** `lastSync` נכתב בשבעה מקומות
ב-`background.js` ו**אף אחד מהם לא קורא אותו כדי לקבוע טווח**; במחרוזת
`lastSync` **אין אף מופע ב-`leumi-content.js`**. השימוש היחיד הוא
`sourceFreshToday` — חסימת סנכרון אוטומטי חוזר, לא צמצום טווח.
כלומר כל ריצה קראה מ-01.01 מחדש: תשעה מקטעים ו-129 שיקים, מהם 99 שמורים.

**⚠ ההנחיה השנייה של טל שינתה את התכנון, והיא הייתה נכונה:**
„עדיף שתחפש את היתרה האחרונה בבנק התואמת ומשם תריץ עדכון."
בניתי תחילה עיגון על **השורה הישנה ביותר** — **וזה היה שגוי**: שורה
שתוקנה רטרואקטיבית נכנסה **פעמיים**, הגרסה הישנה מן השמור והחדשה מן
הבנק, כי המפתח כולל את היתרה המצטברת ולכן הן שונות. **הבדיקה תפסה זאת
כמדויק — כפילות אחת.**

**⚠⚠ וגם „ההסכמה האחרונה" לבדה לא הספיקה, וזה התיקון השני באותו סבב:**
שורה שתוקנה **לפני** נקודת ההסכמה עדיין הוכפלה. הכלל שעובד:
**בתוך החלון שנקרא, הבנק הוא המקור היחיד** — נשמר רק מה שישן מן השורה
הראשונה שהבנק החזיר. נקודת ההסכמה נשארת **מבחן התקינות**: היא מוכיחה
שהחלון חופף למה שיש לנו ואין חור באמצע.

**המנגנון:**
- טווח הדלתא: `lastSync` פחות **30 יום חפיפה** (ולא `lastSync` המדויק —
  תנועה בלאומי משנה תאריך רטרואקטיבית, וזה נמדד).
- אין הסכמה בחלון → **מרחיב אחורה גיאומטרית** (60 · 180 · ואז ישר
  ל-`collectSince`). ⚠ **הרחבה זוחלת הייתה איטית מקריאה מלאה** —
  חמש הליכות — וזה מפספס את כל מטרת הדלתא. ארבע במקרה הגרוע, אחת ברגיל.
- אין נתונים שמורים → קריאה מלאה, בדיוק כמו קודם.
- `leumiDelta` באחסון רושם `mode`, `attempts`, `anchorAt`,
  `oldestFromBank`, `droppedFromSaved`, `keptFromSaved`.

**איך אומת:** ארבעה קבצים עוברים parse; **`collectRangeRows`, `walkFrom`
ו-`mergeRows` חולצו מן הקובץ והורצו** מול בנק מדומה ומול מצב שמור,
בארבעה תרחישים — **4/4**:
| תרחיש | בקשות לבנק | שורות | כפילויות |
|---|---|---|---|
| דלתא רגילה | **2** | 237 | 0 |
| שורה שונתה רטרואקטיבית | **2** | 237 | 0 |
| אין הסכמה כלל → מרחיב עד מלא | 15 | 237 | 0 |
| אין שמור → קריאה מלאה | 9 | 237 | 0 |
**כל ארבעתם מתכנסים ל-237 שורות ולאותה יתרה אחרונה — הדלתא שקולה
לקריאה מלאה, ב-2 בקשות במקום 9.** זו הראיה, לא מהירות.
**⚠ המשתפים מדומים. לא רץ מול לאומי חי.**

**מה עדיין לא ידוע:** ההתנהגות מול שינוי רטרואקטיבי **אמיתי** בלאומי —
התרחיש נבנה ידנית. `leumiDelta.attempts` יראה בריצה הראשונה אם החלון
של 30 יום מספיק, או שהוא מרחיב לעתים קרובות מדי.

**מה טל צריך לבדוק:** ⟳ ל-1.7.0. **הריצה הראשונה תהיה מלאה** (אין עדיין
נתונים שנשמרו במבנה הזה); **השנייה היא המבחן** — היא אמורה להיות קצרה
בהרבה. לוודא שמספר התנועות **לא ירד** (196), ושהסטטוס נשאר
„מסונכרן ומאומת" ולא „מסונכרן חלקית". ואז `leumiDelta`.


## 2026-08-25 · Claude · „0690300" היה חשבון הנעבר בשורת תנועה — 1.6.1

**✅ שיקי לאומי נסגרו — אומת חי.** `leumiChequeWindows {applies:1, misses:[]}`
ו-`leumiChequeReport {asked:30, saved:30, notFound:0, failed:0, already:99}`.
**כל 129 השיקים שמורים.** הפער של ינואר-מרץ (30 דילוגים ב-1.5.2) נסגר,
וגם ה-keepalive של 1.6.0 החזיק — הריצה הושלמה.

**🔎 דיסקונט: „ממתין לחיבור" חזר, וטל צדק שציפה שזה טופל.** 1.5.4 טיפל
בסימפטום אחד; **הגשש שנוסף שם הוא שגילה שהשורש אחר לגמרי:**
```
minePresent:false · othersPresent:[] · label:"024844714" (=want) ✔
near: "…העברה לטל רצבי בנק 12 ל 12-645-0000690300 ₪ -250,000.00…"
tenDigits: ["1550032252","1550032765","1550033567","0000690300","1550017714"]
```
**`0690300` הוא מספר החשבון של הנעבר בשורת תנועה**, ו-`1550…` הן
אסמכתאות. `activeAccount()` גורף רצפים בני 10 ספרות מטקסט הגוף ולקח את
הראשון. **ומספר החשבון שלנו, `2556371`, אינו על המסך הזה כלל** — מסך
התנועות פשוט אינו מציג אותו.

**⚠⚠ זו לא הייתה רק חסימה — זו הייתה שחיתות נתונים בהמתנה.**
`extract` מחזיר `{...activeAccount()}`, ולכן ברגע שהגלאי היה נפתח
**החשבון היה נשמר כ-0690300**. הגלאי, בלי כוונה, מנע נזק גדול יותר
מזה שהוא נועד למנוע.

**שני תיקונים:**
1. **`knownAccountFor(id)`** — מספר החשבון נלקח מ-`discoveredAccounts`,
   שהזיהוי כבר קרא פעם אחת, ולא נגרף שוב מן הדף בכל סנכרון.
   נפילה-לאחור ל-`activeAccount()` לדיסקונט פרטי ולישות שאינה ברשימה.
2. **בורר הישות הוא ראיה חיובית**, ולא רק בדיקת שלילה: אם הבורר מצביע
   על הישות המבוקשת **ואף חשבון של ישות אחרת אינו בדף** — זו הישות.
   ⚠ **החלשה מכוונת, ואלה גבולותיה:** בכשל שנמדד ב-21.08
   (`label:"570012930"` מול `want:"024844714"`) הבורר הצביע על הישות
   הלא נכונה, ושם `labelConfirms` שקר והחסימה נשארת.
   `discountIdentityPass` רושם **דרך איזה מסלול** התקבל האישור —
   „בורר הישות בלבד" הוא מצב חלש יותר מ„מספר תואם".

**⚠ הכלל שחוזר, בפעם הרביעית בפרויקט:** *regex גורף על טקסט הגוף אינו
קריאה — הוא ניחוש.* אותו שורש בדיוק כמו מסגרת האשראי (מספר חשבון
שנקרא כ-514,220,276) ו-`valueAfter`. **מה שכבר נמדד פעם אחת — לקרוא
מן האחסון, לא לגרוף שוב מן הדף.**

**איך אומת:** ארבעה קבצים עוברים parse; היגיון `okNumber` **חולץ מן
הקובץ כלשונו והורץ מול DOM אמיתי** בחמישה מקרים — **5/5**: המקרה שנמדד
עובר, **וכל שלוש הרגרסיות עדיין חוסמות** (בורר על ישות אחרת · חשבון של
ישות אחרת בדף · בורר ריק). המסלול הישן (`seen===mine`) לא נפגע.
**⚠ DOM מסונתז. לא רץ מול דיסקונט חי.**

**מה טל צריך לבדוק:** ⟳ ל-1.6.1, סנכרון דיסקונט. ואז **לוודא בדשבורד
שמספר החשבון הוא 2556371 ולא 0690300** — זה החלק החשוב יותר.
לקרוא `discountIdentityPass` (איזה מסלול אישר) ו-`discountCreditProbe`.


## 2026-08-25 · Claude · ה-service worker מת באמצע הסנכרון — keepalive — 1.6.0

**טל: „זה נראה תקוע." זה לא נראה — זה היה.**
**המדידה שהכריעה, והיא היעדר:** `000003.log` לא נכתב **9.5 דקות** בזמן
שהסטטוס אומר „קורא תנועות, ניסיון 1 מתוך 3" — ו-`applyLeumiRange` כותב
רשומה בכל מקטע. **ואפילו ה-timeout לא ירה**, וזה מה שפסל את כל שאר
ההסברים: `withTimeout` בנוי על `setTimeout`, והוא חי בתוך אותו worker.

**השורש: אין keepalive בכל הפרויקט** (`chrome.alarms` — אפס מופעים).
ב-MV3 ה-service worker נהרג אחרי ~30 שניות **ללא קריאת API**, וכל עוד
הרקע רק ממתין ל-`sendMessage` הוא אינו קורא לשום API. **עד 1.5.2
הקריאה הייתה בקשה אחת לחשבון ונכנסה בתקציב; ההליכה של 1.5.2 חרגה —
וזה מה שחשף חור שהיה שם מאז ומתמיד.**

**⚠ הלקח: שינוי שמאריך פעולה אינו רק „איטי יותר".** הוא חוצה סף שלא
ידעת עליו. ההליכה עצמה נכונה ואומתה (196 תנועות, הפער נסגר) — היא רק
לא שרדה את סביבת הריצה.

**שלושה תיקונים, וכל אחד נחוץ בפני עצמו:**
1. **`keepAlive`** — קריאת API זולה (`getPlatformInfo`) כל 20 שניות כל עוד
   יש פעולה ארוכה באוויר, מחווט לתוך `withTimeout` שהוא נקודת החנק
   היחידה. ⚠ **נספר בעומק ולא בדגל**: שתי פעולות ארוכות במקביל, והראשונה
   שנגמרת הייתה מכבה את ה-keepalive של השנייה.
2. **מקטע ריק עלה 63 שניות מבוזבזות** — 18 ב-`applyLeumiRange` (התנאי
   `n>0` מנע ספירת יציבות כשהתוצאה 0 שורות, והלולאה מיצתה 30 סיבובים)
   ועוד 45 ב-`openCurrentAccount`. עכשיו ~12 שניות: יציבות נספרת גם
   לאפס (סף 6, גבוה יותר, כי „עדיין לא רונדר" נראה כמו „ריק"), והמתנה
   חסומה במקום `openCurrentAccount`.
3. **התקציב נגזר ממספר החשבונות** — `leumiSyncBudget`. הקבוע 420 שניות
   נקבע כשהקריאה הייתה בקשה אחת; טל בחר **6 חשבונות**, ואין תקרה קבועה
   שמכסה את זה. תקרה מוחלטת (20 דקות) נשמרת בכוונה — סנכרון שממתין
   לנצח גרוע מכישלון מוצהר.

**✅ מה שכן עבד באותה ריצה, ואומת מן האחסון:**
- **מסגרת האשראי בדיסקונט תוקנה.** `discountCreditProbe`:
  `{value:50000, why:"מיד אחרי התווית", negated:false}` — הערך הנכון,
  והמסלול הנכון.
- **גלאי הזהות בדיסקונט נפתח.** אחרי `discountIdentityBlock` ב-07:07,
  הריצה ב-07:09 עברה (`discountRangeApplied`) והמשיכה לישות 2 מתוך 4.

**איך אומת:** ארבעה קבצים עוברים parse; `leumiSyncBudget` ו-`keepAlive`
**חולצו מן הקובץ והורצו**: התקציב נותן 420ש׳ לחשבון אחד (**ללא רגרסיה
למקרה הישן**), 1200ש׳ לשישה, ותקרה מוחלטת; ו-keepAlive עבר חמישה מבחני
עומק — טיימר יחיד לשתי פעולות, לא כבה כשהראשונה נגמרה, כבה כששתיהן
נגמרו, ולא יורד מתחת לאפס.
**⚠ לוגיקה בלבד. ה-worker לא נבחן חי תחת עומס אמיתי — וזה הדבר היחיד
שבאמת יכריע.**

**מה טל צריך לבדוק:** ⚠ **הסנכרון הנוכחי מת — ה-worker כבר לא קיים.**
⟳ ל-1.6.0 ולהריץ מחדש. אם הוא שוב שותק, **המדידה היא אותה מדידה**:
האם `000003.log` נכתב. `leumiChequeWindows` עדיין לא נבדק כלל.


## 2026-08-25 · Claude · חלון השיקים נרשם לפי הבקשה ולא לפי הדף — 1.5.4

**⚠⚠ באג שהוכנס בסבב הזה עצמו (1.5.2), ונתפס רק במדידה.**
טל: „את השיקים מתחילת השנה עד אמצע מרץ הוא לא צילם."
```
leumiChequeReport: asked 94 · saved 64 · already 35 · failed 0 · notFound 30
```
**30 דילוגים, אפס כשלים** — כולם מינואר עד מרץ, כלומר הישנים ביותר.

**השורש, ב-`ensureWindowFor` שכתבתי ב-1.5.2:** `curFrom=from` — `from`
הוא מה ש**ביקשנו**, לא מה שהדף הציג. כשההחלה נכשלת בשקט (הפאנל סגור,
השדות לא נמצאו) הדף ממשיך להציג מקטע מאוחר, אבל הקוד מצהיר „החלון
מתחיל בינואר". מאותו רגע **כל שיק שנפל בין ינואר לתאריך המוצג נחשב
„כבר בחלון" ולא גרם לטעינה מחדש** — ודולג בשקט.
**התיקון:** `curFrom=earliestShown()` — החלון נרשם לפי **מה שנצפה בדף**.
נוסף `leumiChequeWindows` (`applies` + `misses`), כי „0 החלות" ו„12 החלות
ללא החטאות" נראים זהה בדוח השיקים.

**⚠ הכלל, והוא חוזר בפעם השלישית בפרויקט:** *„מה שביקשנו" אינו „מה
שקרה".* בדיוק זה הכריע גם ב-`wrote` בטווח של לאומי, וגם ב-`valueAfter`
של דיסקונט. **מצב נרשם מן התצפית, לעולם לא מן הבקשה.**

**🔧 דיסקונט — החסימה נמדדה, וההשערה הראשונה שלי הופרכה:**
```
discountIdentityBlock {want:"024844714", label:"024844714",
                       expected:"2556371", seen:"0690300"}
```
**הבורר היה על הישות הנכונה** (`label === want`). שיערתי „לישות יש כמה
חשבונות" — **הופרך מן האחסון**: לכל אחת מארבע הישויות יש **חשבון אחד
בדיוק** (024844714→2556371 · 514220276→3920651 · 514649565→5817365 ·
570012930→9832685), ו-**`0690300` אינו אף אחד מהם.**
כלומר `activeAccount()` חטף מספר זר מן הדף — **אותה משפחת באג בדיוק
כמו מסגרת האשראי**: regex גורף על טקסט הגוף.

**מה השתנה:** ראיה שנייה, **בלי להחליש את הגלאי** — אם מספר החשבון שלנו
מופיע בדף ו**אף חשבון של ישות אחרת אינו מופיע**, זו הישות הנכונה גם אם
הקורא המספרי טעה. שתי ישויות בדף = דו-משמעות = חסימה, כמו קודם.
`discountIdentityBlock` רושם עכשיו `minePresent`, `othersPresent`,
`tenDigits` ו-`near` — כדי שלא נצטרך לנחש מה היה `0690300`.

**איך אומת:** ארבעה קבצים עוברים parse; ההיגיון של `okNumber` **חולץ מן
הקובץ כלשונו והורץ מול DOM אמיתי** בחמישה מקרים — **5/5**: המקרה שדווח
עובר, חשבון של ישות אחרת בדף עדיין חוסם, שתי ישויות בדף חוסמות,
המסלול הישן (`seen===mine`) ממשיך לעבוד, והיעדר החשבון שלנו חוסם.
**⚠ DOM מסונתז. לא רץ מול דיסקונט חי.**

**מה עדיין לא ידוע:** מה **הוא** `0690300`. הגשש יאמר בריצה הבאה.
אם הוא דווקא כן חשבון אמיתי שהזיהוי פספס — הכיוון משתנה.

**מה טל צריך לבדוק:** ⟳ ל-1.5.4. סנכרון לאומי — לבדוק ש**שיקי ינואר-מרץ
נשמרים** (`leumiChequeWindows.misses` צריך להיות ריק). סנכרון דיסקונט —
לבדוק אם הוא עובר את הגלאי; אם לא, לקרוא `discountIdentityBlock`
המורחב, ובעיקר `tenDigits` ו-`near`.


## 2026-08-25 · Claude · ההליכה בלאומי עבדה · מסגרת אשראי בדיסקונט — 1.5.3

**✅ לאומי — אומת חי, וזה סוגר פריט שהיה פתוח מאז 21.08.**
`leumiWindows` מן האחסון, ריצה של 06:30:
```
01.01.26 → 03.02.26   read 29  added 29
03.02.26 → 10.03.26   read 28  added 27
10.03.26 → 14.04.26   read 28  added 27
14.04.26 → 10.05.26   read 28  added 27
10.05.26 → 17.06.26   read 28  added 27
17.06.26 → 13.07.26   read 28  added 26
13.07.26 → 09.08.26   read 28  added 26
09.08.26 → 20.08.26   read  8  added  7
20.08.26 → 20.08.26   read  2  added  0   → stop:"אין התקדמות"
total: 196
```
**196 תנועות במקום 29, ו-`leumiGap: null` — הפער נסגר** (היה 16,406.79).
זו הראיה החזקה: היתרה מתיישבת עם התנועות. השיקים עלו ל-129 מבוקשים.

**⚠ גודל המקטע יצא ~28-29 שורות בעקביות — תקרת שורות, לא תקרת זמן.**
כל קבוע שנבחר בסבבים הקודמים („חודשיים", „55 יום") היה שגוי. **ההחלטה
לא לקודד את הגודל היא מה שהציל את זה** — ההליכה מתכווננת לפי
`latestShown()` בפועל ואדישה לגבול.

**🔧 דיסקונט — מסגרת אשראי (טל: „בחשבונות ללא מסגרת הוא רושם סתם מספרים").**
**השורש ב-`valueAfter`, והוא מבני:** כשאין סכום באלמנט של התווית הוא סורק
את האחים **ואת כל ילדי ההורה** ומחזיר את המספר הראשון שנקרה; `money`
תופס כל ספרה במחרוזת. לכן מספר חשבון בן עשר ספרות חזר כ„מסגרת אשראי
של 514,220,276 ₪".

**⚠ `valueAfter` לא שונה בכוונה** — היתרה וההתחייבויות עוברות דרכו
ועובדות. מסגרת האשראי בלבד יצאה למסלול ייעודי `creditLimitValue`:
שלילה מפורשת („לא קיימת/אין/ללא") → null מיידי · הסכום חייב לבוא
**מיד** אחרי התווית או באלמנט **שכל הטקסט שלו סכום** · ורשת ביטחון
`plausibleLimit` (מתחת ל-1e8) נגד מספרי חשבון ומזהי ישות.

**⚠ „באותו אלמנט" היה לא בטוח בעצמו, והבדיקה תפסה את זה.** רשימת
המועמדים כוללת את ה-div ההורה, ושם הטקסט שאחרי התווית חופשי —
„מסגרת אשראי **חשבון 514220276 עו״ש**" עדיין החזיר מספר אשפה אחרי
התיקון הראשון. הכלל „מיד אחרי התווית" הוא מה שסגר אותו.
**בלי הרצת המקרים זה היה נכנס כתיקון „מאומת".**

**איך אומת:** ארבעה קבצים עוברים parse; **`creditLimitValue` חולץ מן
הקובץ והורץ מול DOM אמיתי ב-Chrome** בשמונה מקרים — **8/8**:
ארבע צורות של מסגרת קיימת (אח נקי · אותו אלמנט · בן-דוד · שלילית)
חוזרות נכון, ושתי צורות האשפה שדווחו (מספר חשבון · תאריך) חוזרות null.
**⚠ DOM מסונתז. לא רץ מול דיסקונט חי.**

**סיכון שנשאר, ונרשם במפורש:** אם בדף האמיתי הסכום יושב בצאצא עמוק
ולא באח/בן-דוד, המסגרת תחזור `null` במקום מספר שגוי. זו נפילה בטוחה
יותר, אבל היא נפילה. `discountCreditProbe` באחסון יראה בדיוק איזה
מסלול נבחר ומה נדחה — **לקרוא אותו אחרי הסנכרון הבא של דיסקונט.**

**מה טל צריך לבדוק:** ⚠ **לא לרענן את התוסף עד שקריאת השיקים בלאומי
תסתיים** — רענון באמצע הורג את הריצה. אחר כך ⟳, סנכרון דיסקונט,
ולבדוק שחשבונות ללא מסגרת מציגים ריק ולא מספר. אקרא
`discountCreditProbe`.


## 2026-08-25 · Claude · הטווח תקין — הבנק מחזיר מקטע אחד. הליכה קדימה — 1.5.2

**הזיהוי חזר לעבוד (1.5.1 אומת חי — טל: „רעננתי ויש חשבונות").**
ואז ריצת סנכרון מלאה נתנה את המדידה שסוגרת שלוש השערות בבת אחת.

**הטווח נבדק ונמצא תקין — זו כבר לא הבעיה:**
```
wrote:        {from:"01.01.26", to:"25.08.26"}   ← בדיוק מה שביקשנו
placeholder:  "dd.mm.yy"                          ← הפורמט נכון, אין yy/yyyy
rangeText:    "01.01.26 - 25.08.26"               ← הדף מציג את הטווח
controls:     „1 סננים פעילים"                    ← הסינון פעיל
filterRadioList: כל הרדיו ב-checked:false          ← אין preset שדורס
earliestAfter: 2026-01-01                         ← הקצה התחתון כובד במדויק
latestAfter:   2026-02-02                         ← ⚠ הקצה העליון לא
rows: 29
```

**⚠⚠ `gridProbe` הפריך סופית את השערת הגלילה, אחרי שלושה סבבים:**
`rowsInDom:29` · `sentinels:[]` · `siblingAfterTail:null` ·
`rowTransform:"none"` · `rowHasIndex:false` · `declared:null` ·
ו**אף אב אינו נגלל** — `table-scroll` הוא `overflow:auto` אבל
`scrollHeight === clientHeight` (18681 = 18681).
**אין וירטואליזציה, אין זקיף, ואין לאן לגלול.** 1.2.3 ו-1.2.5 היו
נידונים מראש, וההחזרה של הגשש הייתה שווה את עצמה.

**שלוש הנחות שנקברו, ורשומות בקוד כדי שלא יקומו:**
1. „הרשת מווירטואלית / צריך לגלול" — הופרך ע"י `gridProbe`.
2. „לאומי חוסם לחודשיים" (1.5.0, שלי) — הופרך ע"י טל.
3. „התאריכים לא נבררים נכון" — נבדק ונשלל ע"י `wrote`/`rangeText`.

**מה שנשאר, וזה מה שנמדד:** הבנק מקבל את הטווח במלואו אבל **מחזיר
מקטע אחד** שמתחיל בקצה המוקדם שלו. לכן אין „לטעון עוד שורות" —
**צריך לבקש שוב, החל מהמקום שבו המקטע נגמר.**

**מה השתנה:** `collectRangeRows` הוא עכשיו **הליכה מתקדמת**: מבקש
`(from, היום)`, קורא את המקטע, ומקדם את `from` ל-`latestShown()` של
מה שחזר בפועל. **גודל המקטע לא מקודד בשום מקום** — הוא לא נמדד, וכל
קבוע היה ניחוש. עוצר ב„הגיע להיום", „מקטע ריק", „אין התקדמות", או
תקרת 24 מקטעים. מסלול השיקים עבר לאותה שיטה (מבקש טווח שמתחיל יום
לפני תאריך השיק) ומוין **מהישן לחדש**, בכיוון שבו הבנק מגיש.
בנוסף: לולאת ההמתנה ב-`applyLeumiRange` בדקה „זז אחורה בלבד" והליכה
קדימה לא נתפסה — כל שינוי נחשב עכשיו, וזה חוסך 18 שניות למקטע.

**איך אומת:** שלושה קבצים עוברים parse; **`collectRangeRows` חולץ מן
הקובץ והורץ** מול בנק מדומה שמחזיר מקטע אחד לבקשה:
- 29 שורות למקטע → **9 בקשות, 237 שורות**, עצירה „הגיע להיום"
- מקטע גדול → **בקשה אחת, 237 שורות** — *אותה תוצאה בדיוק*
- בלי `collectSince` → בקשה אחת בלי טווח, התנהגות ישנה ללא שינוי
**ההתכנסות של שני הראשונים לאותן 237 שורות היא הראיה שההליכה שקולה
לקריאה אחת מלאה.**
**⚠ המשתפים היו מדומים. הלולאה האמיתית לא רצה מול לאומי חי.**

**מה עדיין לא ידוע:** מה קובע את גודל המקטע (~חודש? ~29 שורות?).
**זה בכוונה לא נמדד ולא מקודד** — ההליכה אדישה לו. `leumiWindows`
באחסון (`steps` + `stop`) יראה את הגודל האמיתי בריצה הבאה.

**מה טל צריך לבדוק:** ⟳ ואז סנכרון לאומי. הריצה תהיה ארוכה יותר —
תשע בקשות במקום אחת, וזה צפוי. אחר כך: `leumiWindows` (כמה מקטעים,
ולמה נעצר), `leumiGap` (האם הפער נסגר), ו-`leumiChequeReport`.


## 2026-08-25 · Claude · ה-throw ברח מן ה-if — הזיהוי בלאומי היה מת יומיים — 1.5.1

**טל: „דף הבנק על חשבונות הבנק אבל אין אפשרות לסנכרן לפי חשבון."**
האחסון הכריע, ולא בכיוון שציפיתי — **זו לא הייתה בעיית טווח כלל.**

**השורש, ב-`runDiscoverLeumi`:** לפני `f7aed41` (1.2.8, 23.08) השורה הייתה
`if(!r?.ok)throw Error(...)` — **משפט אחד**. הריפקטור שהוציא את האבחון
מהודעת המשתמש עטף את כתיבת `leumiDebug` בבלוק — **וה-throw נשאר מחוץ
לתנאי.** מאז, **כל זיהוי לאומי נכשל גם כשהצליח**, וכל הקוד שמתחת
(בניית `found`, שמירת `discoveredAccounts`, „נמצאו N חשבונות")
**היה קוד מת שלא רץ מעולם.**

**שתי החתימות באחסון שהכריעו, ושתיהן „היעדר" ולא „נוכחות":**
1. הודעת הכשל הייתה **בדיוק מחרוזת ברירת המחדל** — כלומר `r.error` היה
   `undefined`, כלומר **לא הייתה שגיאה**.
2. **לא נכתב `leumiDebug` כלל.** האחרון במיקום 1,371,163 מול הכשל של
   היום ב-1,470,638 — כלומר מ-23.08. והבלוק שכותב אותו הוא **היחיד**
   שנשאר בתוך התנאי. היעדרו הוא ההוכחה ש-`r.ok` היה **true**.

**למה זה נראה כמו „אין חשבונות":** `startLeumi` מוחק את
`discoveredAccounts` של לאומי בלחיצה (בכוונה — רשימה בלי סשן פעיל
אסורה). הזיהוי הצליח, נזרק, ולא נשמר דבר. עד 23.08 זה הוסתר כי הסנכרון
נפל אחורה ל„מסנכרן את 1 החשבונות שכבר אושרו" — **הרשימה הישנה כיסתה
על כך.** ברגע שנמחקה, התסמין הופיע.

**⚠ הלקח, וכללי:** כשעוטפים גוף של `if` חסר-סוגריים בבלוק — **לספור מה
נכנס פנימה.** parse עובר, אין אזהרה, והבאג שקט לחלוטין. הערה בת חמש
שורות שנדחפה בין ה-`if` לגוף שלו הסתירה אותו לגמרי מקריאה רגילה.

**⚠⚠ הופרך באותו סבב — ההנחה שלי מ-1.5.0.** טל: „אין מגבלה בבנק לצפיה
בנתונים עד **שנתיים אחורה וברצף**. צריך לברור נכון את תאריכי הצפיה."
כלומר „חודשיים" **אינו** גבול הבנק, וחלונות של 55 יום היו סיבוך מיותר
פי חמישה. `LEUMI_WINDOW_DAYS` הועבר ל-**730**, ולכן כל טווח עד שנתיים
מחזיר **חלון יחיד** — התנהגות זהה לקריאה בודדת. המנגנון נשאר רק לחתוך
בקשות ארוכות משנתיים (`LEUMI_MAX_BACK_MS`), שאותן הבנק ממילא לא מגיש.

**המדידה שנוספה כדי להכריע את „לברור נכון את התאריכים":**
`leumiRangeApplied` רושם עכשיו `wrote:{from,to}` — **מה שבאמת יושב
בשדות אחרי ההחלה, ולא מה שביקשנו לכתוב** — וגם `placeholder`
(יגלה אם השדה מצפה ל-`yyyy` בעוד `ilShort` כותב `yy`) ו-`latestAfter`
(האם הקצה העליון של הטווח כובד). בלי אלה „הטווח נחתך" ו„הטווח הוחל אבל
נקראו מעט שורות" נראים זהה.

**איך אומת:** שלושה קבצים עוברים parse ב-Chrome headless; `leumiWindows`
חולץ מן הקובץ והורץ — שלושת המקרים מחזירים **חלון יחיד**, ובקשה של
5 שנים נחתכת ל-2024-08-25. מיקום ה-`throw` אומת ב**התאמת סוגריים על
הקובץ**: הוא בתוך `if(!r?.ok){…}`, ומיד אחרי הבלוק יושב `found=r.accounts…`
כלומר הקוד המת חזר להיות נגיש.
**⚠ בדיקת מבנה ולוגיקה. אף שורה מזה לא רצה מול לאומי חי.**

**מה עדיין לא ידוע:** האם 29 השורות ייפתרו מאליהן עכשיו. **אין לי הסבר
מאומת להן**, וההסבר שהצעתי אתמול (חלונות) הופרך. `wrote`/`latestAfter`
ו-`leumiGridProbe` הם מה שיכריע בריצה הבאה.

**מה טל צריך לבדוק:** ⟳ ב-`chrome://extensions`, ואז **זיהוי חשבונות
בלאומי** — הרשימה אמורה להופיע. אם כן: לבחור חשבון, לסנכרן, ולומר לי.
אקרא `leumiRangeApplied` (בעיקר `wrote` ו-`latestAfter`),
`leumiWindows` ו-`leumiGridProbe`.


## 2026-08-25 · Claude · לאומי חוסם לחודשיים — הטווח נקרא בחלונות — 1.5.0

**טל דיווח, וזה הפיל השערה שעמדה שלושה סבבים:** „בלאומי הטווח תמיד מוגבל
לחודשיים." כלומר **„29 שורות" מעולם לא היו וירטואליזציה או גלילה חסרה —
הטווח פשוט נחתך.** הראיה הייתה בנתונים כל הזמן: ב-23.08 ביקשנו
01.01→23.08 (כמעט 8 חודשים) וקיבלנו `01.01..02.02` — **חלון אחד.**
שלושת ניסיונות הגלילה (1.2.3, 1.2.5 ועוד אחד) חיפשו במקום הלא נכון.

**⚠ נמדד לפני הכול, ומשנה את קריאת ההיסטוריה:** האחסון נעצר ב-23.08 08:55,
ו-`manifest.json` נכתב 10:56. כלומר **1.4.0 (חטיפת הלשוניות) ו-1.3.2
(השיקים) מעולם לא רצו.** שניהם עדיין „לא נבדק", לא „עובד".

**מה השתנה:**
1. `applyLeumiRange(from,to)` מקבל חלון מפורש; בלי ארגומנטים ההתנהגות זהה לקודם.
2. `leumiWindows()` — פורס את התקופה לחלונות של **55 יום** (ולא 60: לא ידוע
   אם הגבול נספר בימים או בחודשי לוח), עם **חפיפה של יום** על התפר.
   **מהחדש לישן במכוון** — רק בכיוון הזה `applyLeumiRange` יוצא מוקדם
   מלולאת ההמתנה („התאריך המוקדם ביותר זז אחורה"). סדר הפוך היה מוסיף
   18 שניות לכל חלון.
3. `collectRangeRows()` — קורא חלון אחר חלון וצובר, מנוכה כפילויות לפי
   **כל התאים כולל היתרה המצטברת** (שתי תנועות זהות באותו יום נבדלות בה).
4. **גם צילומי השיקים עוברים חלונות.** בלי זה שיק מינואר לא היה נמצא
   בחלון של אוגוסט — כשל 1.3.2 בלבוש חדש.
5. `gridProbe()` הוחזר מ-`7e83fdf` (קריאה בלבד) — עכשיו כדי **להפריך**:
   אם גם אחרי החלונות ייקרא מספר קבוע, הוא יגיד אם יש וירטואליזציה בכלל.
6. תקרת ההודעה 180ש׳ → **420ש׳**. חמישה חלונות אינם נכנסים ב-180.

**⚠ שני מלכודות שנסגרו תוך כדי, ושתיהן היו מפילות את הסנכרון:**
- **חלון בן 55 יום ללא תנועות הוא מצב חוקי.** `openCurrentAccount` זורק
  אחרי 45 שניות כשאין שורות — חודשיים שקטים אחד היו מפילים את כל הריצה.
  עכשיו נתפס ונרשם כ-`empty:true`.
- `Date.parse` קורא `03.02.2026` כ-2 במרץ. לכן `ilToMs` מפרסר מפורשות.

**איך אומת:** חמישה קבצים עוברים parse ב-Chrome headless (אין node);
`leumiWindows` ו-`ilToMs` **חולצו מן הקובץ עצמו והורצו** על שלושה מקרים:
המקרה של טל (01.01→25.08) נותן **5 חלונות, כל אחד ≤60 יום, ללא פערים,
כיסוי מלא**; טווח קצר מחלון נותן חלון אחד; שלוש שנים עוצרות ב-24 חלונות.
**⚠ בדיקת לוגיקה, לא ריצה חיה מול לאומי. אף חלון לא הוחל על דף אמיתי.**

**מה עדיין לא עובד / לא ידוע:**
- **האם 55 יום אכן מתחת לגבול** — אם לאומי סופר חודשי לוח, ייתכן שצריך פחות.
  `leumiWindows` באחסון יראה זאת: `read` גדול עם `added:0` = החלון לא הוחל.
- מעל ~3.5 שנים החלונות נעצרים ב-24 (שומר לולאה) ומשמיטים את הישן ביותר.
  ניתן לזהות בהשוואת `since` ל-`from` של החלון האחרון ברשומה.

**הצעד הבא:** ריצת לאומי חיה אחת. היא מכריעה **ארבעה** דברים בבת אחת.

**מה טל צריך לבדוק:**
1. ⟳ ב-`chrome://extensions` — Chrome לא נטען מחדש מאז 23.08 בבוקר.
2. לגלוש דקה בפועלים **בלי** לבקש סנכרון — התוסף לא אמור לגעת בלשונית (1.4.0).
3. סנכרון לאומי אחד. הריצה ארוכה יותר מקודם — **זה צפוי**, חמישה חלונות.
4. אחר כך: `leumiWindows` (כמה נקרא בכל חלון), `leumiChequeReport`
   (`saved` מול `notFound`), ו-`leumiGridProbe`.


## 2026-08-23 · Claude · ⚠ התוסף חטף לשוניות שלא ביקשו ממנו — 1.4.0

**התקלה החמורה ביותר שנמצאה בסבב הזה.** טל: „אני מנסה לעבוד על בנק
הפועלים, לא קשור לתוסף, והתוסף חוטף לי את החשבון."

**השורש — שתי שורות שהגיבו בלי שום תנאי:**
```js
if(/^[A-Z_]*AUTHENTICATED$/.test(m?.type)&&sender.tab?.id) returnToDashboard(sender.tab.id);
if(m?.type==='AUTHENTICATED'&&sender.tab?.id){ … queueDiscover(sender.tab.id,source); … }
```
ו-`poalim-content.js` שולח `AUTHENTICATED` **בכל שינוי נתיב, כל 750ms**.
כלומר: המשתמש גולש בחשבון הבנק שלו → הרקע **מנווט את הלשונית שלו**
לדף הבית, ו**מריץ זיהוי** שפותח את בורר החשבונות ומחליף חשבון.

**⚠ אי-העקביות שאיפשרה את זה:** המסלול המקביל,
`handleAuthenticatedNavigation`, **כן** בודק `pendingSources` לפני
`queueDiscover`. מסלול ההודעה פשוט לא בדק. שני מסלולים לאותה מטרה,
ורק אחד מוגן.

**מה השתנה:** `userAskedFor(source)` — התוסף נוגע בלשונית **רק** כאשר
יש כוונה מפורשת: `running` (סנכרון שהמשתמש יזם), `pendingSources`, או
דגל ה-pending של אותו בנק. שני המטפלים עברו דרכו.

**הכלל, ויש לו תקדים:** ב-21.08 נרשם בדיסקונט „השומר מדווח בלבד,
ולעולם אינו מנווט", אחרי שהתוסף שיבש הקלדה בדף כניסה. **אותו לקח,
בנק אחר, ולא הוחל רוחבית.** מעכשיו: **נוכחות בדף בנק אינה בקשה.**

**איך אומת:** `background.js` עובר parse; שתי השורות הבלתי-מותנות אינן
בקוד; ו-`userAskedFor` **חולץ מן הקובץ** והורץ על שישה מצבים:
- **פועלים במצב הנוכחי של טל (`pendingSources` ריק, אין סנכרון) → `false`.
  התוסף לא ייגע.** ✔
- פועלים כשהמשתמש ביקש זיהוי → `true`
- בזמן סנכרון פעיל → `true`
- לאומי עם דגל דלוק → `true`; עם דגל כבוי → `false`
- מקור לא מוכר → `false`

**מיידי למשתמש שנתקל בזה:** לכבות את מתג התוסף ב-`chrome://extensions`
(**לא להסיר** — הסרה מוחקת את האחסון), לעבוד, ולהדליק בסוף.


## 2026-08-23 · Claude · השיקים חיפשו בחלון הלא נכון — 1.3.2

**טל: „אין צילומי שיקים." נמדד, וההודעה הסתירה את הכשל:**
```
leumiChequeReport = {asked:15, saved:0, failed:0, already:0, why:""}
syncStatus        = „… 15 הפקדות שיקים"   ← סופר תנועות שיק, לא תמונות
```
**15 התבקשו, 0 נשמרו, ואפס נרשמו ככשל.**

**השורש:** `chequeImages()` טוענת את הרשת **מחדש**
(`selectAccount → openCurrentAccount → loadAllRows`) — ו**לא החילה את
הטווח שוב**. לכן היא חיפשה אסמכתאות מינואר בתוך חלון ברירת המחדל
(„40 התנועות האחרונות"), לא מצאה אף שורה תואמת, ו-`if(!hit)continue`
דילג על כל 15 **בשקט**.

**⚠ זו תופעת לוואי של תיקון הטווח עצמו.** כל עוד החלון היה ממילא
יולי-אוגוסט, האסמכתאות היו שם והשיקים נשמרו (23, ואז 16). ברגע
שהתחלנו לכפות ינואר — התנועות נקראו מינואר, אבל השיקים חיפשו באוגוסט.
**כלל: כל שלב שטוען את הרשת מחדש חייב להחיל את הטווח מחדש.**

**מה השתנה:**
1. `applyLeumiRange()` מוחל גם בתוך `chequeImages`, בסדר
   `openCurrentAccount → applyLeumiRange → openCurrentAccount → loadAllRows`.
2. **הדילוג השקט הפך לנספר.** `notFound` נאסף בצד הדף, מוחזר בתשובה,
   ונרשם ל-`leumiChequeReport` כ-`notFound` + `notFoundRefs`.
   „asked:15, saved:0, failed:0" לא יוכל יותר להיראות כהצלחה.

**איך אומת:** שני הקבצים עוברים parse; הסדר בתוך `chequeImages` נבדק מן
הקובץ (129 → 673 → 724 → 1547), והשרשרת `notFound` נבדקה מקצה לקצה —
נאסף, מוחזר, נקלט ברקע, ונכנס לרשומה.
**⚠ בדיקת מבנה, לא ריצה חיה.**

**מצב לאומי אחרי הסבב:**
| | |
|---|---|
| בחירת חשבון · טווח מדויק · סיומת /NN | ✅ אומתו חי |
| גלאי הפער | ✅ מדווח נאמנה |
| צילומי שיקים | 🔧 תוקן, טרם נבדק חי |
| **~29 שורות (עמוד אחד)** | ❌ **הפריט האחרון, ועדיין פתוח** |


## 2026-08-23 · Claude · 1.3.1 עבד — נשאר פריט אחד בלבד

**ריצה חיה מלאה, 05:51–05:54.** התוצאה מן האחסון:
```
הסתיים חלקית: 1 חשבונות, 28 תנועות, 1 הלוואות, 15 הפקדות שיקים
תנועות:  28  ·  01.01.2026 .. 02.02.2026
leumiRangeApplied: „טווח מדויק הופעל"  06.07 → 01.01.2026
leumiLoadRows:     {rows: 29}
leumiGap:          {diff: 21716.79, until: "02.02.2026"}
leumiAccountMatch: **לא נכתבה רשומה חדשה**
```

**מה שנסגר, ואומת חי:**
| יכולת | ראיה |
|---|---|
| בחירת החשבון | אין רשומת כשל חדשה ב-`leumiAccountMatch` |
| **הטווח המדויק** | התנועות מתחילות ב-**01.01.2026** — בדיוק `collectSince` |
| סיומת `/NN` | `suffix:"39"` נקרא נכון בדף |
| גלאי הפער | „מסונכרן חלקית … פער 21,716.79 ₪" — אומר את האמת |
| שיקים | 15 צילומים |

**מה שנשאר — פריט אחד, מבודד לחלוטין:** `loadAllRows` קורא **29 שורות**,
עמוד אחד. אותו מספר בכל ריצה מאז אתמול (28/29), ללא תלות בטווח. **זו
כל התקלה שנותרה בלאומי.**

**⚠ לפני ניסיון שלישי — לקרוא את 1.2.3 ו-1.2.5.** שני ניסיונות עיוורים
נכשלו, והשני אף החמיר (13 שורות, 70 שניות). נאסר: **אירועי `scroll`
מסונתזים, וגלילה של כל אב נגלל** — שניהם נמדדו כמזיקים.

**המדידה החסרה, והכלי כבר כתוב:** `gridProbe()` מ-1.2.6 (commit
`7e83fdf`) — קריאה בלבד, רושם `aria-rowcount` מול שורות ב-DOM, סימני
וירטואליזציה, זקיפי IntersectionObserver, „מוצגות X מתוך Y", והאבות
הנגללים. הוא נבדק ועובד; הוא הוסר רק בהחזרה ל-1.2.5. **ריצה אחת איתו
תכריע בין רשת וירטואלית, זקיף, ועימוד בכפתור.**

**היפותזה חדשה שנוצרה מן הנתון הזה:** 29 שורות בדיוק, ללא תלות בטווח
של שמונה חודשים, מרמז ש**אין גלילה אינסופית כלל** אלא עימוד — ולכן כל
אסטרטגיית גלילה נכשלה. `gridProbe` יאשר או יפריך.


## 2026-08-23 · Claude · החשבון לא היה בדף — הסינון הרס את הלשוניות — 1.3.1

**`leumiAccountMatch` הכריע, וההשערה שלי מ-1.3.0 הופרכה.**
```
want:    "921-348300"
chooser: "טל רצבי … 921-88154/39"     chooserKey: 921-88154
tabs:    5   — וכל הרשומות ב-seen הן 921-88154, או רעש
```
**החשבון המבוקש לא היה בדף כלל.** והסלש דווקא נקרא נכון: `suffix:"39"` —
כלומר הנפילה-לאחור מ-1.3.0 עובדת, היא פשוט לא הייתה הבעיה.

**השורש:** `applyLeumiRange` רץ בתחילת `syncSelected`, לפני
`selectAccount`. הוא מחיל סינון, הדף מתרנדר, ו**לשוניות החשבון נהרסות
לפני שמחפשים בהן** — נשאר רק החשבון הפעיל.
**זה בדיוק התיקון שנעשה ב-1.2.7 והוחזר ב„חזרה ל-1.2.5" לבקשת טל.**
הזהרתי על כך ברשומת ההחזרה, וזה אכן קרה. **החזרה של גרסה מחזירה גם את
הבאגים שלה — לרשום זאת מפורשות בכל החזרה עתידית.**

**ראיה שנייה באותה רשומה:** `"תאריך23.08.2026-01.01.2026"` נקרא כחשבון
`026-01`. זהו **טווח התאריכים שהקוד עצמו כתב לדף** — כלומר הסינון גם
הרס לשוניות אמיתיות וגם ייצר לשונית מדומה.

**מה השתנה:**
1. `applyLeumiRange` הוחזר אל תוך `extract`, בסדר
   `openCurrentAccount → applyLeumiRange → openCurrentAccount → loadAllRows`.
2. `parseAccount` המחמיר: `(?<!\d)(\d{3})-(\d{4,9})(?:\/(\d{1,3}))?(?!\d)`
   — `(?<!\d)` פוסל התחלה בתוך מספר ארוך, ו-`{4,9}` דורש אורך סביר
   (הקיימים: 88154, 348300).

**איך אומת:** parse תקין; הסדר בתוך `extract` נבדק מן הקובץ;
`parseAccount` הורץ על **הטקסטים שנמדדו בפועל בדף**:
- `"טל רצבי 9 2 1 8 8 1 5 4 / 3 9 921-88154/39"` → `921-88154 suffix="39"` ✔
- `"תאריך23.08.2026 - 01.01.2026"` → **null** (היה `026-01`) ✔
- `"01.01.26 - 23.08.26 סכום…"` → **null** ✔
- ארבע צורות חשבון אמיתיות — כולן עובדות, כולל `921 348300/77`.

**⚠ מה שעדיין לא ידוע:** האם `options()` פותח את בורר החשבונות כשהדף
תקין. אם אחרי התיקון הזה החשבון עדיין לא יימצא, `leumiAccountMatch`
יראה אילו חשבונות **כן** מופיעים — וזו תהיה תשובה אחרת לגמרי.


## 2026-08-23 · Claude · הסניף והחשבון נדבקו זה לזה — 1.3.0

### שאלה ראשונה של טל: „אתה חוסם את ההתחברות בלאומי?" — **לא.**

זו הייתה שאלה נכונה, כי בדיסקונט ב-21.08 התשובה הייתה **כן**. בלאומי
נבדק ונמצא ההפך:
- `leumiSession()` **מחריג במפורש** לשוניות על `/login/` — יש שם הערה
  מפורשת: „לשונית על /login/ אינה סשן".
- אין `tabs.update` על לשונית לאומי בזמן כניסה. הניווט היחיד ל-
  `LEUMI_LOGIN` הוא **פתיחת חלון popup חדש**, ורק כשהמשתמש לוחץ על
  אריח הבנק.
- `leumi-content.js` תואם רק `hb2.bankleumi.co.il/*`, ובטעינה הוא רק
  רושם מאזין הודעות — אין פעולה יזומה, ויש שומר `__leumiSyncLoaded`.

### התקלה: „יש התעלמות מהסלש ושתי הספרות"

החשבון הוא **348300/77**, והסנכרון נפל על „החשבון 921-348300 לא נמצא".

**המנגנון, מאומת בבדיקה על צורות טקסט:**
```
"921-348300/77"             → key=921-348300 suffix=77   ✔
"921 - 348300 / 77"         → key=921-348300 suffix=77   ✔
"921 348300 77"             → normalized="92134830077"   ✗ null
"עו״ש שקלים 921 348300/77"  → normalized="…921348300/77" ✗ null
```
`normalized()` מוחק רווחים **בין ספרות** (`(?<=\d)\s+(?=\d)`). הוא נועד
לאחות מספר שפוצל בין אלמנטים — **אבל הוא גם מוחק את הגבול בין הסניף
לחשבון.** בלי מקף אין התאמה, `parseAccount` מחזיר `null`, ו-
`selectAccount` זורק „לא נמצא".

**מה השתנה — נפילה לאחור שמרנית ב-`parseAccount`:** רצה **רק** כשהפירוק
המחמיר נכשל, על הטקסט **הגולמי** (שבו הרווח עוד קיים), ומחייבת מפריד
מפורש, חשבון בן 5–9 ספרות, וסיומת של **בדיוק שתי ספרות**.
⚠ השמרנות מכוונת: הרחבה גורפת כאן כבר הפילה את הזיהוי פעם אחת
(„עשרות חשבונות מדומים", ראה ההערה מעל `menuEntitiesLoose`).

**בנוסף — `leumiAccountMatch`:** בכשל התאמה נרשם עכשיו לאחסון מה **כן**
נמצא בדף — טקסט גולמי, אחרי `normalized`, והמפתח שנגזר — לכל לשונית
ואפשרות. הודעת המשתמש נשארת משפט אחד.

**איך אומת:** `leumi-content.js` עובר parse; `parseAccount` **חולץ מן
הקובץ** והורץ על שלוש קבוצות:
- ארבע צורות שעבדו קודם — **כולן ממשיכות לעבוד** (אין רגרסיה);
- שלוש שנשברו — **כולן נפתרות** ל-`921-348300`, עם `suffix="77"` היכן
  שהסלש קיים;
- חמישה מקרי רעש (סכום, תאריך, מספר בן 12 ספרות, אסמכתא, 4 ספרות) —
  **כולם `null`.** אפס תפיסות שווא.

**⚠ עדיין השערה מה קורה בדף החי.** אם הכשל יחזור, `leumiAccountMatch`
יגיד בדיוק מה נראה שם.


## 2026-08-23 · Claude · המשטח השלישי: אריח הבנק — 1.2.9

**שלושה משטחים מציגים סטטוס, ותיקנתי אותם אחד-אחד במקום למפות אותם
מראש.** זה עלה שלושה סבבים מיותרים:
| משטח | מקור | תוקן ב- |
|---|---|---|
| פופ-אפ | `syncStatus` | 1.2.7 |
| באנר בדשבורד | `syncStatus` | 1.2.6 |
| **אריח הבנק** | **`statusBySource[id]`** ← ערוץ שלישי | **1.2.9** |

`bankLine()` קורא מ-`statusBySource`, לא מ-`syncStatus` — ולכן שני
הקיצורים הקודמים פסחו עליו לגמרי, ושם ישבה אותה הודעה בת 1,079 תווים.

**נמדד באריח אמיתי בתוך `dashboard.html`:**
```
הודעה מלאה:    רוחב הדף 6,255px  (בחלון 900px)
הודעה מקוצרת:  רוחב הדף   885px
```

**מה השתנה:** `shortLine()` ל-110 תווים, מוחל **בנקודת התצוגה**
(`esc(shortLine(bankLine(b)))`) ולא בכתיבה — כדי שגם הודעות שכבר שמורות
באחסון יוצגו קצר, בלי להמתין לסנכרון הבא. ובנוסף CSS על
`.bank-button small` כהגנה שנייה.

**הכלל, מנוסח סופית:** מלל שמקורו חיצוני חייב קיצור בכל **נקודת תצוגה**,
ולא פעם אחת ב„המקום שנראה נכון". לפני תיקון כזה — **למפות את כל
נקודות התצוגה תחילה** (`grep` על מפתחות הסטטוס), ולא ללכת אחרי הצילום.

### פתוח — סיומת החשבון בלאומי

טל: „בלאומי יש 348300/77, הסלש בסוף עם 2 ספרות, שכבר טיפלנו בזה."
נמדד באחסון: `branch=921 accountNumber=348300 **accountSuffix=''**`.
- `parseAccount` **כן** תומך: `/(\d{3})-(\d+)(?:\/(\d+))?/` לוכד את
  הסיומת ל-`accountSuffix`, ו-`fullAccount()` בדשבורד מרכיב
  `348300/77`. כלומר התשתית קיימת ולא נשברה.
- אבל הערך השמור **ריק**, כלומר הטקסט שנקרא מן הדף באותו רגע לא כלל
  את `/77`.
- ובנוסף: הודעת השגיאה מציגה את **המפתח הפנימי** (`921-348300`), שבו
  הסיומת מושמטת בכוונה. למשתמש זה נראה כמספר חשבון שגוי.
**לא תוקן** — דורש מדידה חיה של הטקסט שממנו נקרא החשבון.


## 2026-08-23 · Claude · אבחון המפתח הודבק להודעת המשתמש — 1.2.8

**טל: „למה יש את ההודעות האלה כל פעם שאנחנו עובדים על בנק."** צודק, וזה
מקור כל שרשרת התקלות של אמש.

**מה שנמצא:** `dbgText()` בונה אבחון עשיר — כתובת מלאה, מספר טבלאות,
תוכן השורה הראשונה, פתיח הדף — והוא הודבק **לתוך הודעת השגיאה** בשלושה
מקומות. ההודעה נשמרת ל-`syncStatus` ומוצגת על אריח הבנק ובפופ-אפ.
התוצאה שנמדדה: **1,136 תווים** בכרטיס לאומי.

**והכפילות הייתה מיותרת לחלוטין:** אותו אבחון כבר נשמר ל-`leumiDebug`
בשורה שקדמה ל-`throw`. כלומר המשתמש נשא על המסך עותק שני של משהו
שהמפתח קורא ממקום אחר ממילא.

**מה השתנה:** `dbgText` יצא משלוש הודעות השגיאה ונכנס אל תוך הרשומה
השמורה (`leumiDebug.text`), כך שהוא **נשאר זמין למפתח ואינו הופך לקוד
מת**. הודעת המשתמש מקוצרת ל-120 תווים מן השגיאה עצמה.
```
לפני:  1,136 תווים
אחרי:     80 תווים  → „קריאת תנועות לאומי נכשלה לאחר 3 ניסיונות:
                        החשבון 921-348300 לא נמצא בחיבור לאומי"
```

**⚠ זה השורש שמאחורי שלוש התקלות של אמש:** „אין תוסף", „הדשבורד לא
נפתח" ו„התוסף לא תקין" — כולן נבעו ממחרוזת האבחון הזו. תיקנתי את
הסימפטום בשני משטחים (פופ-אפ, דשבורד) לפני שהגעתי למקור.

**הכלל שנרשם:** אבחון מפתח נכתב ל**מפתח ייעודי באחסון**, לעולם לא
לשורת סטטוס. למשתמש — משפט אחד קריא. שני קהלים, שני ערוצים.

**איך אומת:** `background.js` עובר parse; **0** מופעים של `dbgText`
בהודעות שגיאה, **3** בתוך `leumiDebug`, ו-4 סה"כ (הפונקציה בשימוש ולא
מתה). ההודעה החדשה בפופ-אפ נותנת רוחב מסמך 800px.


## 2026-08-22 · Claude · זה היה הפופ-אפ, לא הדשבורד — 1.2.7

**התקלה שעלתה לארבעה סבבים, והשורש שלה בשתי שורות.**

`popup.js`: `status.textContent=x.syncStatus` — בלי קיצור.
`styles.css`: `.popup{width:290px}` ו-`.hint{color:#6d788b}` — **בלי שום
שבירת מילה**.

`syncStatus` הגיע ל-**1,052 תווים**: הודעת כשל לאומי הנושאת כתובת עם
פרמטר `?q=` דחוס, בלי רווח אחד. מחרוזת כזו אינה נשברת לשורות, ולכן היא
מתחה את מסמך הפופ-אפ ל-**9,003 פיקסלים** ודחפה את כפתור „פתיחת
הדשבורד" הרחק מחוץ למסך. **מכאן „הדשבורד לא נפתח", ומכאן גם
„אין תוסף?"** — הצילום שנראה כדף ריק עם מחרוזת אחת היה **הפופ-אפ**.

**⚠ ואיפה טעיתי ארבע פעמים:** תיקנתי את `dashboard.js` שוב ושוב, בהנחה
שמדובר בדשבורד. **המדידה שסתרה אותי ולא הקשבתי לה מספיק מהר:** הדשבורד
לא גלש באף רוחב שנבדק (1200/760 ללא גלישה; 520 גולש גם בלי המחרוזת),
והרצה מלאה שלו עם נתונים באותו מבנה נתנה 263 אלמנטים ו-12 כרטיסי בנק,
בלי חריגה. **היה לי ממצא שלילי ברור, ולא הסקתי ממנו שאני בקובץ הלא נכון.**

**מה השתנה:**
1. `popup.js` — קיצור ל-160 תווים (הפופ-אפ הוא כשליש מרוחב הדשבורד).
2. `styles.css` — `.popup{box-sizing:border-box;max-width:320px;overflow-x:hidden}`
   ו-`.popup .hint{overflow-wrap:anywhere;word-break:break-word;max-height:8em;
   overflow-y:auto}`. **הגנה שנייה, בלתי תלויה בקיצור** — גם אם מישהו
   יסיר את הקיצור, הכפתור יישאר במקומו.

**איך אומת — שחזור מלא של התקלה:** `popup.html` האמיתי בתוך iframe,
עם מחרוזת של 1,023 תווים באותו מבנה:
```
בלי התיקון:  רוחב מסמך 9,003px
עם התיקון:   רוחב מסמך   800px
```
**זהו ההפרש שהסתיר את הכפתור.**

**לקח:** כשמדידה מפריכה השערה — לחפש **קובץ אחר**, לא ניסוח אחר של
אותה השערה. ארבעה סבבים אבדו על תיקון חוזר בקובץ שהמדידה כבר ניקתה.

**לקח שני:** כל מלל שמקורו חיצוני (כתובת, הודעת שגיאה של הדפדפן) חייב
קיצור **וגם** שבירה ב-CSS לפני שהוא מוצג — בכל משטח, לא רק באחד.


## 2026-08-22 · Claude · הדשבורד נראה ריק — ההשערה שלי לא אושרה — 1.2.6

**⚠ רשומה זו מתעדת מה שלא הצלחתי לאבחן.** קראו אותה לפני שתנחשו שוב.

**מה שטל רואה:** דף כמעט ריק, מחרוזת base64 ארוכה אחת, ופס גלילה אופקי.

**ההשערה שלי:** `syncStatus` באורך ~1,050 תווים (הודעת כשל לאומי עם
פרמטר `?q=` דחוס) מותח את הדף. **המדידה לא אישרה אותה:**
```
1200px  בלי תיקון 1185 · עם תיקון 1185   → אין גלישה
 760px  בלי תיקון  745 · עם תיקון  745   → אין גלישה
 520px  בלי תיקון  663 · עם תיקון  663   → גולש **בשני המצבים**
```
כלומר הגלישה ב-520px נובעת מ-min-width כלשהו בפריסה, **לא מן המחרוזת**,
והמחרוזת אינה מותחת את הדף באף רוחב שנבדק.

**ומה שכן נמדד — הדשבורד בריא:** הרצה של `dashboard.js` בתוך
`dashboard.html` אמיתי, עם נתונים **סינתטיים באותו מבנה** (5 חשבונות,
518 תנועות, אותה הודעת סטטוס ארוכה, `viewSince` פעיל) נתנה: **263
אלמנטים, 12 כרטיסי בנק, 1,546 תווי טקסט, אפס חריגות ואפס דחיות**.
הדף מרונדר מלא. 16/16 הקבצים עוברים parse, וכל קובץ שהמניפסט מפנה
אליו קיים.

**מה נשאר בקוד:** הקיצור ל-320 תווים ושבירת המילה ב-CSS. **הם היגיינה
נכונה — מלל חיצוני בלתי חסום באורכו לא אמור להגיע לתצוגה כמות שהוא —
אבל אין ראיה שהם פותרים את מה שטל רואה.** אל תרשמו אותם כתיקון לתקלה
הזו.

**⚠ מה שחסם את האבחון:** ניסיתי להריץ את הדשבורד מול **האחסון האמיתי**
של טל, ולשם כך התחלתי לכתוב את תוכן ה-LevelDB לקובץ בתוך
`extension/`. **מסנן ההרשאות חסם, ובצדק** — זה היה מכניס מספרי חשבון,
יתרות ותנועות אמיתיים לתוך ריפו שנדחף ל-origin. **לעולם אל תכתבו דמפ
של אחסון התוסף לתוך עץ העבודה.** אם צריך נתונים אמיתיים לבדיקה —
scratchpad מחוץ לריפו, ובאישור מפורש.

**מה באמת יכריע, ולא נוסה:** הקונסול של עמוד הדשבורד אצל טל
(F12 → Console). זה הדבר היחיד שאינו נראה מן הצד שלי, ואחריו אין צורך
לנחש.


## 2026-08-22 · Claude · חזרה ל-1.2.5 לבקשת טל

**טל: „לא עובד בכלל, תחזיר לגרסה 1.2.5."** בוצע:
`git checkout 0eab5cb -- extension/`.

**מה הוסר:** 1.2.6 (`gridProbe`), 1.2.7 (העברת `applyLeumiRange` אל
תוך `extract`), 1.2.8 (קיצור ושבירת שורה בשורת הסטטוס).

**⚠ מה שחוזר יחד איתן — לידיעת הבא:**
1. `applyLeumiRange` שוב יושב בתחילת `syncSelected`, **לפני
   `selectAccount` ולפני שהרשת נטענת**. זה המיקום שנמדד ככושל ב-20:03
   („החשבון 921-348300 לא נמצא", `rows:2`). ב-1.2.2 אותו מיקום דווקא
   הצליח — כלומר זה **מרוץ**, לא כשל ודאי.
2. שורת הסטטוס שוב יכולה להימתח לרוחב: הודעת הכשל של לאומי מגיעה
   ל-1,052 תווים בגלל פרמטר `?q=` הדחוס בכתובת, ומחרוזת בלי רווחים
   אינה נשברת. **אם המסך ייראה שוב כמעט ריק עם מחרוזת אחת ארוכה —
   זו הסיבה, והתיקון יושב ב-4c6db37.**

**HANDOFF לא נוגע.** הרשומות של 1.2.6–1.2.8 נשארות: הן מתעדות ממצאים
אמיתיים (מיפוי הרדיו, מיקום `applyLeumiRange`, מקור הפריסה השבורה)
שיחסכו לבא סבבים שלמים. הקובץ הוא הוספה-בלבד.

**מה שנשמר ועובד, ואינו קשור לחזרה:** מיפוי בורר התקופה
(40/7/30.2/3/365 עם התוויות), מנגנון הטווח המדויק דרך שדות
`dd.mm.yy`, גלאי הפער `leumiGap`, והנפילה-לאחור מ-1.2.4.

**אומת אחרי החזרה:** גרסה 1.2.5, **16/16 קבצים עוברים parse**,
ו-`dashboard.js` נטען נקי בהרצה מול שלד ה-DOM האמיתי.

**המלצה לבא:** לפני נגיעה נוספת בלאומי — לקרוא את הרשומות של
1.2.0–1.2.8 מלמטה למעלה. הן מתעדות שישה כשלים, מהם **שלושה בכלי
הבדיקה ולא בקוד**.


## 2026-08-22 · Claude · „התוסף לא תקין" — הפריסה, לא התוסף — 1.2.8

**מה שטל ראה:** מסך כמעט ריק ובו מחרוזת אחת ארוכה וגלילה אופקית.

**מה שנמדד:** `manifest.json` חוקי, כל 16 קובצי ה-JS קיימים ועוברים
parse, וחמשת החשבונות שמורים. **התוסף היה תקין לגמרי.**

**מה שנשבר:** הודעת הכשל של לאומי כוללת את כתובת הלשונית, ובלאומי
הכתובת נושאת פרמטר `?q=` דחוס. ההודעה הגיעה ל-**1,052 תווים**, רובם
מחרוזת אחת בלי רווחים. הדשבורד מרנדר אותה כ-`<small class="sync-detail">`,
ומחרוזת בלי רווחים **אינה נשברת לשורות** — היא מתחה את הדף לרוחב
והסתירה את כל השאר.

**שתי הגנות, כי אחת אינה מספיקה:**
1. **קיצור לתצוגה** ל-320 תווים. האבחון המלא ממילא נשמר במפתחות
   הייעודיים (`leumiDebug`, `leumiDiscoverError`) — שם קוראים אותו.
2. **CSS**: `overflow-wrap:anywhere; word-break:break-word` על
   `.sync-detail`, עם `max-height` וגלילה אנכית — כך ש**שום** מחרוזת
   עתידית לא תוכל לעשות זאת שוב, גם אם מישהו יסיר את הקיצור.

**איך אומת:** שלושת הקבצים עוברים parse. ההודעה האמיתית באורך 1,052
תווים הוזרקה לאלמנט ברוחב 600px: לפני — 585px גלישה **הודות ל-CSS**;
אחרי הקיצור — 321 תווים ו-600px. **שתי ההגנות עובדות בנפרד.**

**⚠ הכשל שמתחתיו לא נפתר:** `syncStatus` עדיין נושא את השגיאה מלפני
1.2.7 („החשבון 921-348300 לא נמצא"), ו-`leumiGridProbe` עדיין `null`.
תיקון המיקום מ-1.2.7 טרם נוסה בריצה.

**לקח:** כל מלל שמגיע ממקור חיצוני (כתובת, הודעת שגיאה של הדפדפן) ולא
נשלט באורכו — חייב קיצור **וגם** שבירה ב-CSS לפני שהוא מוצג.


## 2026-08-22 · Claude · applyLeumiRange רץ לפני שהדף היה מוכן — 1.2.7

**הסנכרון נפל, והגשש מ-1.2.6 לא הספיק לרוץ.** `leumiGridProbe` הוא
`null` כי `extract` זרק לפניו.

**מה שנמדד:**
```
leumiRangeApplied = {reason:"טווח מדויק ללא שינוי מספיק", rows:2,
                     earliestBefore:"2026-02-24", earliestAfter:"2026-02-24"}
syncStatus = שגיאה: החשבון 921-348300 לא נמצא בחיבור לאומי
אבחון מן ההודעה: "טבלאות 0, שורות 3, מהן עם תאריך 2; לשוניות חשבון 5"
```

**השורש — טעות מיקום שלי.** הצבתי את `applyLeumiRange()` בתחילת
`syncSelected`, כלומר **לפני `selectAccount` ולפני שהרשת נטענה**.
שתי תקלות בבת אחת:
1. הסינון הוחל על דף עם **שתי שורות** — לכן „ללא שינוי מספיק".
2. הרינדור מחדש שגרר **הרס את לשוניות החשבון**, ו-`selectAccount`
   שרץ אחריו לא מצא את 921-348300 → הסנכרון נפל.

**התיקון:** הקריאה הועברה אל תוך `extract`, לסדר
`openCurrentAccount() → applyLeumiRange() → openCurrentAccount() → loadAllRows()`.
כלומר: אחרי שהחשבון נבחר והרשת קיימת, ולפני קריאת השורות; והמתנה
נוספת לרשת אחרי הסינון, כי היא נטענת מחדש.

**⚠ הלקח הכללי:** בלאומי, כל פעולה שמרנדרת מחדש חייבת לרוץ **אחרי**
בחירת החשבון. ניווט או סינון שקודמים לה מוחקים את הבחירה — אותו דפוס
שנרשם בדיסקונט ב-20.08 („ניווט שקדם לבחירה נמחק על ידה"). **זה חוזר,
וזה כלל ולא מקרה.**

**איך אומת:** הקובץ עובר parse; הסדר בתוך `extract` נבדק מן הקובץ
עצמו — `openCurrentAccount` (60) → `applyLeumiRange` (612) →
`loadAllRows` (663), ויש `openCurrentAccount` נוסף בין הסינון לקריאה.
`syncSelected` נקי מן הקריאה. **⚠ בדיקת סדר, לא ריצה חיה.**

**הצעד הבא:** רענון ל-1.2.7. אם הסנכרון יעבור, `leumiGridProbe` יגיע
סוף סוף — וזה מה שדרוש כדי לפתור את „~29 שורות".


## 2026-08-22 · Claude · גשש מבנה הרשת, לפני ניסיון שלישי — 1.2.6

**למה גשש ולא תיקון:** „נקראות ~29 שורות בלבד" עמד בפני **שני ניסיונות
תיקון עיוורים**, ושניהם נכשלו — האחרון אף החמיר (13 שורות, 70 שניות,
ראה 1.2.5). ניסיון שלישי באותה שיטה יבזבז זמן. לכן קודם מודדים **מה
בכלל מפעיל טעינת עמוד נוסף**.

**`gridProbe()` — קריאה בלבד, אפס לחיצות ואפס גלילה.** רושם:
- **וירטואליזציה:** `aria-rowcount` מול שורות ב-DOM, `position`/`transform`
  של השורה, `data-index`/`aria-rowindex`. אם הרשת מווירטואלית — שורות
  **מתחלפות** במקום להצטבר, וכל אסטרטגיית „לגלול עד הסוף" שגויה מיסודה.
- **זקיף IntersectionObserver:** אלמנטים ששמם מרמז
  (`sentinel|observer|loader|spinner|infinite|load-more`), עם הגובה שלהם.
- **ספירה מוצהרת:** `aria-rowcount`, `data-total`, ומלל „מוצגות X מתוך Y".
- **האח שאחרי השורה האחרונה** — שם יושב בדרך כלל הזקיף או „טען עוד".
- **שרשרת האבות** עם `scrollHeight/clientHeight/scrollTop/overflowY`
  ומי מהם באמת נגלל.

**מה זה יכריע:** אם `aria-rowcount` הוא 217 ובדף יש 29 — הרשת
מווירטואלית וצריך גישה אחרת לגמרי. אם יש זקיף — צריך להביא **אותו**
לתוך הנוף, לא לגלול לתחתית. אם יש „מוצגות X מתוך Y" — יש לנו יעד
מדיד לעצירה, במקום „יציבות".

**איך אומת:** שני הקבצים עוברים parse. `gridProbe` **חולץ מן הקובץ**
והורץ מול רשת וירטואלית לדוגמה — זיהה `aria-rowcount=217` מול 2 שורות
ב-DOM, `position:absolute` ו-`data-index`, זקיף
`infinite-scroll-sentinel` בגובה 1px, ואת המלל „מוצגות 29 מתוך 217".
לא זורק על דף ריק.
**⚠ „האב הנגלל" יצא ריק בבדיקה — בסביבה המוסתרת אין פריסה ולכן
`scrollHeight==clientHeight`. בדף החי הוא יתמלא.** זו אותה מגבלה
שנרשמה ב-1.2.3, וכאן היא לא מפריעה: הגשש רק מדווח.

**הצעד הבא:** רענון ל-1.2.6, סנכרון אחד, ואז לקרוא `leumiGridProbe`.
**רק אחריו לכתוב תיקון.**

**עדיין פתוח:** צילומי שיקים; פעימת סטטוס ב-`extract` בדיסקונט;
`selectedAccountKeys` של דיסקונט צריך אישור מחדש דרך אריח הבנק.


## 2026-08-22 · Claude · loadAllRows הוחזר — התיקון שלי החמיר — 1.2.5

**נמדד בשטח, וזו הפעם הראשונה ש-`leumiLoadRows` דיבר:**
```
leumiLoadRows = {loops:5, rows:15, timedOut:true, moreClicks:0}
תוצאה סופית: 13 תנועות, 02.08–20.08   (לפני כן: 29 תנועות, 01.01–03.02)
```
חמישה סיבובים בתוך 70 שניות — כלומר **~14 שניות לסיבוב**, מול ~0.6
שניות מתוכננות. כמעט בוודאות סערת רינדור שגררו אירועי ה-scroll
המסונתזים והגלילה של כל אב נגלל. **התוצאה: איטי פי עשרים, ופחות שורות.**

**הוחזר במלואו.** המימוש המקורי חזר כלשונו; רק `leumiLoadRows` נשאר,
כי הוא זה שגילה את זה.

**לקח לפרוטוקול: אל תשלחו אירועי scroll מסונתזים לרשת של לאומי, ואל
תגללו כל אב נגלל.** שניהם נמדדו כמזיקים.

**ומה שנשאר נכון מן הסבב:** ב-19:44 `leumiRangeApplied` שוב דיווח
„טווח מדויק הופעל" עם `earliestAfter 2026-01-01`. **מנגנון הטווח
עובד.** מה שלא עובד הוא כמות השורות שנקראות.

### מצב לאומי, מסודר

| מה | מצב |
|---|---|
| בקשת טווח מדויק (dd.mm.yy + „סינון") | ✅ עובד, נמדד פעמיים |
| כמות השורות (~29, גודל עמוד) | ❌ **פתוח**, ושני ניסיונות תיקון נכשלו |
| גלאי הפער (`leumiGap`) | ✅ מדויק בשני הכיוונים |
| נפילה לאחור כשהזיהוי נכשל | ✅ נוסף ב-1.2.4, טרם נצפה חי |

**⚠ לפני ניסיון שלישי ב-`loadAllRows`: צריך למדוד מה באמת מפעיל טעינת
עמוד נוסף באתר** — IntersectionObserver? כפתור? גלילת חלון? — ולא
לנחש עוד מנגנון. **שני ניסיונות עיוורים כבר עלו זמן והחמירו.**

### „נעלמו חשבונות לסנכרון" — הבהרה

`accounts` מכיל את **כל חמשת החשבונות** (4 דיסקונט + לאומי), שלמים.
מה שריק הוא `discoveredAccounts` (רשימת הבחירה) — הזיהוי נכשל ולא מילא
אותה, ו-`syncSelected` מנקה אותה בסוף כל סנכרון מוצלח.
בנוסף `selectedAccountKeys` מחזיק רק את `leumi|921-348300` — ארבעת
מפתחות דיסקונט נשרו מוקדם יותר היום, לפני התיקון ב-1.1.2.
**שום נתון לא אבד; רק רשימת הבחירה התרוקנה.**

**עדיין פתוח:** צילומי שיקים; פעימת סטטוס ב-`extract` בדיסקונט.


## 2026-08-22 · Claude · זיהוי שנכשל חסם סנכרון שלא היה זקוק לו — 1.2.4

**מה שנמדד** (וזו בדיוק הרשומה ש-1.1.4 נועדה לייצר):
```
leumiDiscoverError = {at:"19:38:42",
  message:"הניווט בתפריט נכשל: A listener indicated an asynchronous response
           by returning true, but the message channel closed...",
  url:"https://hb2.bankleumi.co.il/.../nis-transactions/"}
leumiAttempts 1 · pendingLeumi false · discoveredAccounts 0
leumiLoadRows **null** ← 1.2.3 מעולם לא רץ, כי הסנכרון לא התחיל
```
ניווט הרג את ה-content script באמצע הודעה. חולף ותלוי-תזמון.

**התובנה: הפעולה הזו כלל לא הייתה זקוקה לזיהוי.**
`selectedAccountKeys` כבר החזיק `leumi|921-348300`, והחשבון כבר שמור עם
29 תנועות. הזיהוי נועד למצוא חשבונות **חדשים** — מי שכבר נבחר ואושר
אינו צריך אותו. ובכל זאת כשל בזיהוי הותיר את המשתמש בלי כלום.

**⚠ ושני המסלולים היו חסומים בו-זמנית:** ⟳ על הכרטיס → זיהוי שברירי;
סנכרון כללי → שער הרעננות (`sourceFreshToday`) מדלג כי לאומי סונכרן
היום. אין דרך שלישית.

**מה השתנה:** כשל בזיהוי **נופל לאחור לסנכרון של מה שכבר אושר**.
נופלים לאחור **רק** על מפתחות שהמשתמש כבר אישר, ורק כשקיים חשבון שמור
מאותו מקור, ורק כשאין סנכרון אחר רץ. **לא בוחרים חשבונות בשמו של
המשתמש ולא נוגעים בבורר.** אם אין מפתחות שמורים — הכשל נשאר כשל.

**איך אומת:** שלושת הקבצים עוברים parse. תנאי הנפילה-לאחור **חולץ מן
הקובץ** והורץ על חמישה מצבים: יש מפתח + חשבון שמור + לא רץ → נופל
לאחור; אין מפתחות → לא; יש מפתח אבל אין חשבון שמור → לא; סנכרון אחר
רץ → לא; מפתחות של בנק אחר → לא נחשבים.
**⚠ בדיקת לוגיקה. הנפילה-לאחור החיה עוד לא נצפתה.**

**⚠ `loadAllRows` מ-1.2.3 עדיין לא נבדק בשטח** — `leumiLoadRows` הוא
`null`, כי הסנכרון לא הגיע לשם. זה נשאר הפריט הפתוח המרכזי בלאומי.

**עדיין פתוח:** צילומי שיקים (דורש מדידת IndexedDB); פעימת סטטוס
במהלך `extract` בדיסקונט.


## 2026-08-22 · Claude · הטווח עובד; נקרא עמוד אחד — 1.2.3

**הצלחה חלקית שנמדדה:** `leumiRangeApplied = {reason:"טווח מדויק הופעל",
from:"01.01.26", to:"22.08.26", earliestBefore:"2026-07-06",
earliestAfter:"2026-01-01"}`. **הטווח המדויק עובד.** מילוי שני שדות
ה-dd.mm.yy ולחיצה על „סינון" מזיזים את הרשת ל-01.01.2026 בדיוק.

**מה שנשאר:** נקראו **29 שורות** (01.01–03.02) מתוך כמעט שמונה חודשים,
והפער מול היתרה 16,406.79. שלוש ריצות עצרו על 28/29 — **גודל עמוד**.

**שחזור שהכריע:** DOM עם מכולה נגללת שטוענת עמוד 1200ms אחרי scroll —
`loadAllRows` הישן הביא **29 מתוך 116**. הבאג אמיתי ומשוחזר.

**מה השתנה:** גלילה של **כל** האבות שבהם `scrollHeight>clientHeight`
(ולא רק `[role="table"|"grid"]`); הרמה, **המתנה של 120ms**, ואז חזרה
לתחתית — כי שתי השמות סינכרוניות ל-`scrollTop` מתמזגות ולא יורות אירוע;
`dispatchEvent(new Event('scroll'))` כגיבוי; חלון יציבות של 5 שניות
במקום 2; תקציב זמן של 70 שניות; וזיהוי כפתור „הצג עוד" בשיטת הטקסט
הקצר. הכול נרשם ל-**`leumiLoadRows`**.

### ⚠⚠ אזהרה מתודית — קראו לפני שסומכים על בדיקות כאן

בסבב הזה נכשלתי שלוש פעמים ברצף, וכל כשל היה **בכלי הבדיקה**, לא בקוד:

1. **פאץ' שלא הוחל ולא נבדק.** `patch_load.py` החזיר `FAIL loadAllRows: 0`
   — המחרוזת לא התאימה כי **בקובץ יש סופי-שורה מעורבים** (`
` בודד
   בתוך `loadAllRows`). הוא רץ ברקע ולא קראתי את הפלט. **הפתרון:
   החלפה לפי מיקום (`find` של תחילת וסוף הפונקציה), לא לפי טקסט רב-שורתי.**
2. **בדיקה שלא בדקה כלום.** `String.replace` שאינו מוצא התאמה מחזיר את
   המקור **בשקט** — ולכן „הקוד החדש" בבדיקה היה הקוד הישן, ושתי
   התוצאות יצאו זהות. **הפתרון: לוודא שההחלפה תפסה ולזרוק אם לא.**
3. **הסביבה אינה יכולה לבדוק גלילה.** חלונית הדפדפן מוסתרת, ונמדד
   `אירועי scroll: 0` גם עם jitter. **אין טעם לבדוק כאן גלילה או
   פריסה** — רק תחביר ולוגיקה טהורה. ההכרעה על `loadAllRows` חייבת
   לבוא מריצה חיה דרך `leumiLoadRows`.

**מה אומת כאן:** ארבעת הקבצים עוברים parse, וכל ארבעת רכיבי התיקון
קיימים בקובץ. **הגלילה עצמה לא נבדקה ואינה ניתנת לבדיקה בסביבה הזו.**

**פתוח — צילומי שיקים:** טל דיווח „אין צילומי שקים". באחסון: 16 תנועות
שיק, **כולן עם אסמכתא**, `chequeCount:16`, ו-`chequeError:null`.
המספר ירד מ-23 ל-16 כי חלון התנועות עבר מיולי-אוגוסט לינואר-פברואר —
**אלה שיקים אחרים לגמרי.** אם התמונות נשמרו יושב ב-IndexedDB
(`cheque-store.js`), שאינו נראה מיומן ה-LevelDB. **דורש מדידה נפרדת.**

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` בדיסקונט — 2:47 שקט.


## 2026-08-22 · Claude · „365" הוא השנה שעברה, לא 365 יום — 1.2.2

**מה שטל דיווח:** „אין תנועות בכלל עכשיו."

**מה שקרה באמת — והנתונים לא אבדו:**
```
leumiRangeApplied = {reason:"הופעל", applyText:"סינון", value:"365",
                     earliestBefore:"2026-07-06", earliestAfter:"2025-01-01"}
28 תנועות נשמרו — **כולן מ-01.01.2025 עד 23.01.2025**
viewSince = 2026-01-01  →  הדשבורד הסתיר את כולן  →  „אין תנועות"
```

**הלקח:** „שנה קלנדרית אחרונה" פירושה **השנה הקודמת (2025)**, לא „365
יום אחורה". `01.01.2025` הוא בדיוק גבול שנה קלנדרית — הראיה. עבור
`collectSince=2026-01-01` זו קפיצה **לכיוון הלא נכון**, והיא החליפה
קריאה טובה (שהגיעה ליתרה הנוכחית) בקריאה של ינואר 2025.
**⚠ הנחתי שהמספר הוא ימים. המדידה הפריכה. אל תניחו סמנטיקה ממספר.**

**ומכאן מסקנה רחבה יותר: אין preset שמכסה „מתחילת השנה הנוכחית".**
7 = שבוע, 30.2 = מתחילת החודש, 3 = מתחילת הרבעון, 365 = השנה שעברה.
לכן **הטווח המדויק הוא המסלול היחיד הנכון** עבור הגדרה כמו של טל.

**מה השתנה:**
1. **מסלול ברירת מחדל חדש — טווח מדויק.** ממלאים את שני שדות ה-`dd.mm.yy`
   שנמדדו ב-`leumiDateMenu` (`collectSince` → היום) ולוחצים „סינון".
   שדה מבוקר של React אינו מגיב להשמה ישירה, ולכן `nativeSet` עם הסטר
   הנייטיב ואז `input`+`change` — בדיוק כמו בדיסקונט.
2. **„365" הוסר לצמיתות** מרשימת המועמדים.
3. **אין יותר „נפילה לרחב ביותר".** כשאף preset אינו מכסה, מוחזר `null`
   ומדווח — במקום להחליף חלון טוב בחלון קצר יותר. **קריאה גרועה יותר
   לא תדרוס קריאה טובה בגלל „משהו עדיף מכלום".**

**באג שנתפס בבדיקה, לפני שהגיע לטל:** קריטריון ההצלחה היה
`earliest <= collectSince`. לחשבון אין בהכרח תנועה ב-1 בינואר, ולכן
ריצה מוצלחת שהחזירה 05.01 סומנה ככשל. עכשיו ההצלחה נמדדת ב**תזוזה
אחורה** (`earliestAfter < earliestBefore`), או בהגעה לגבול.

**איך אומת:** הקובץ עובר parse. תשע הפונקציות **חולצו מן הקובץ עצמו**
והורצו מול DOM עם שדות `dd.mm.yy`, „סינון" ו„נקה":
- `01/01/2026 → null` (אין preset מתאים), `01/08/2026 → 30.2`.
- טווח מדויק: השדות מולאו `01.01.26`/`22.08.26`, והמוקדמת זזה
  `2026-07-06 → 2026-01-05` → **„טווח מדויק הופעל"**.
- תנועה ראשונה ב-01/01 בדיוק → גם „הופעל".
- החלה שלא הועילה → „ללא שינוי מספיק". **הכשל מובחן.**
- **בורר החשבונות לא נלחץ באף תרחיש.**

**⚠ DOM מלאכותי. ההחלה החיה עוד לא נבדקה.**

**⚠ מה שעדיין לא ידוע:** ב-1.2.1 נקראו רק 28 שורות מתוך טווח של שנה
וחצי — כלומר גם כש-`loadAllRows` מקבל חלון רחב, הוא מביא **עמוד אחד
בלבד**. ייתכן שחלון היציבות שלו (5 דגימות × 400ms = 2 שניות) קצר מדי
לטווח גדול. **לא נגעתי בו** — קודם נראה מה הטווח המדויק מחזיר.

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` בדיסקונט — 2:47 שקט.


## 2026-08-22 · Claude · בחירת הרדיו אינה מחילה — צריך „סינון" — 1.2.1

**מה שטל דיווח:** „היתרה לא נכונה ואין סנכרון מינואר". שתי התלונות הן
תוצאה אחת של אותו שורש.

**מה שנמדד ב-1.2.0:**
```
leumiRangeApplied = {reason:"הופעל", value:"365", rowsBefore:29, rowsAfter:29}
```
`target.checked` אכן הפך ל-true — **הבחירה עבדה** — אבל `rowsBefore`
שווה ל-`rowsAfter`. **הרשת לא נטענה מחדש**, ולכן נקרא שוב אותו חלון ישן
(06.07–02.08, 28 שורות). מכאן „אין מינואר", ומכאן גם „היתרה לא נכונה":
התנועות נעצרות ב-50,176.99 בעוד היתרה 18,487.92, פער 31,689.07.

**החסר: כפתור ההחלה.** לאומי דורש לחיצה מפורשת אחרי בחירת התקופה.
`leumiRangeProbe` כבר צילם אותו בסבב קודם — „סינון" הופיע ברשימת
ה-controls שלוש פעמים, ולא שמנו לב.

**איך נמנעה חזרה על מלכודת המכולה:** מחפשים אלמנט שה**טקסט העצמי שלו
קצר** (≤10 תווים) ושווה למילה עצמה. מכולה מכילה את כל מלל הפאנל ולכן
נפסלת על אורך — זו בדיוק השגיאה שהכשילה את 1.1.5. „נקה"/„איפוס"/
„ביטול" מוחרגים במפורש: לחיצה עליהם הייתה מבטלת את הבחירה.

**סימן ההצלחה השתנה:** `rowsBefore/After` הוא סימן חסר תועלת — הוא נשאר
29 גם כשהסינון לא הוחל. עכשיו נמדד **התאריך המוקדם ביותר המוצג**
(`earliestShown`), והדוח מבחין בין `הופעל` (המוקדמת זזה אחורה) לבין
`הופעל ללא שינוי נראה` — כך שכשל לא ייראה יותר כהצלחה.

**איך אומת:** הקובץ עובר parse. `applyLeumiRange`, `applyButtonIn`,
`earliestShown` ו-`leumiRangeOption` **חולצו מן הקובץ עצמו** והורצו מול
DOM שמשחזר את המבנה — פאנל רדיו, כפתור „סינון", כפתור „נקה", ומכולה
שהמלל שלה כולל את כל הפאנל:
- החלה שעובדת → `reason:'הופעל'`, המוקדמת זזה **2026-08-02 → 2026-01-15**.
- החלה שלא משנה כלום → `הופעל ללא שינוי נראה`. **הכשל מובחן.**
- נבחר הכפתור הקצר („סינון") ולא המכולה הארוכה.
- „נקה"/„ביטול" אינם נבחרים, ו„נקה" לא נלחץ באף תרחיש.
- **בורר החשבונות לא נלחץ בשום מקרה.**

**⚠ DOM מלאכותי. ההחלה החיה עוד לא נבדקה.**

**הצעד הבא:** רענון ל-1.2.1 וסנכרון לאומי. אם `leumiRangeApplied.reason`
יהיה `הופעל` עם `earliestAfter` בינואר — נגמר. אם יהיה `כפתור ההחלה לא
נמצא`, הדוח יכלול את רשימת הכפתורים הקצרים שנראו בדף, ומשם נדע. אם
יהיה `הופעל ללא שינוי נראה` — הכפתור נלחץ אך לא הועיל, וזה כיוון אחר.

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` בדיסקונט — 2:47 שקט.


## 2026-08-22 · Claude · לאומי מבקש טווח לפי collectSince — 1.2.0

### המיפוי המלא הגיע, והמימוש נבנה עליו

`leumiRadios` מהריצה של 18:57 — `input[name="filterRadioList"]`:
```
40   → „40 תנועות אחרונות"      ← ברירת המחדל. **אינה תאריכית כלל.**
7    → „7 ימים אחרונים"
30.2 → „מתחילת החודש"
3    → „מתחילת הרבעון"
365  → „שנה קלנדרית אחרונה"
```
והשרשרת הראתה שהפקד הלחיץ הוא ה-`label` שעוטף את הרדיו
(`inline-block cursor-pointer`), בתוך `div[role="radiogroup"]`.
**זה כל מה שהיה חסר.**

**`applyLeumiRange()`** בוחר את **האפשרות הקטנה ביותר שמכסה** את
`collectSince` ולוחץ על ה-`label` שלה, לפני קריאת השורות. אם אף אפשרות
אינה מגיעה אחורה מספיק — נבחרת הרחבה ונרשם `capped:true`. „40" לעולם
אינה נבחרת, כי אין לה משמעות תאריכית.

**⚠ למה זה לא ניחוש:** הבחירה לפי `value` של רדיו אמיתי ולפי `name`
שנמדד, והלחיצה על ה-`label` שלו — לא על סלקטור מומצא, ולא על מכולה
שבמקרה מכילה את המלל. **זו הייתה השגיאה ב-1.1.5, ולפניה בדיסקונט
ב-20.08.** הפעם השרשרת נמדדה לפני שנכתבה שורת קוד אחת.

**⚠ גבולות:** נוגעים רק בתוך ה-radiogroup של המסננים; **לא בבורר
החשבונות**; הכול בתוך `try` — כשל משאיר את חלון ברירת המחדל בדיוק
כפי שהיה; וכל מסלול, כולל יציאה מוקדמת, נרשם ל-`leumiRangeApplied`.
מוחל **לפני** קריאת השורות — הפוך מזה, והקריאה מקדימה את הבקשה.

### ובדרך: גלאי הפער מ-1.1.2 הוכיח את עצמו לשני הכיוונים

ריצת 18:57 החזירה **19 תנועות, 28.07–20.08, `leumiGap: null`,
„מסונכרן ואומת"** — יתרת התנועה האחרונה 18,487.92 שווה בדיוק ליתרת
החשבון. הריצות שלפניה החזירו 28 תנועות עם פער 31,689.07 ו„חלקית".
**אותו קוד סימן נכון גם חוסר וגם שלמות.** (החלון עצמו זז בין הריצות —
לאומי זוכר את בחירת המסנן בסשן, כפי שנמדד גם בדיסקונט.)

### איך אומת

`leumi-content.js` עובר parse. `leumiRangeOption` ו-`applyLeumiRange`
**חולצו מן הקובץ עצמו** והורצו:
- בחירה, נכון ל-22.08.2026: `18/08→7`, `01/08→30.2`, `01/07→3`,
  **`01/01/2026→365`** (מה שמוגדר אצל טל), `01/01/2020→365` עם
  `capped:true`. **„40" לא נבחר באף תרחיש.**
- הפעלה מול DOM שמשחזר את המבנה: פאנל פתוח → נבחר 365; **פאנל סגור →
  נפתח ואז נבחר**; כבר 365 → „כבר בטווח" בלי לגעת; בלי `collectSince`
  → יציאה מסודרת.
- **בכל ארבעת התרחישים בורר החשבונות לא נלחץ.** זו הבדיקה שחוזרת
  בכל סבב מאז 20.08.

**⚠ DOM מלאכותי. הערכים והתוויות נמדדו באתר החי; ההפעלה עצמה עוד לא.**

### הצעד הבא

רענון ל-1.2.0 וסנכרון לאומי. הציפייה: `leumiRangeApplied.reason='הופעל'`
עם `value:'365'`, ותנועות מינואר 2026. אם הרדיו לא ייבחר —
`leumiRangeApplied` יגיד `הרדיו לא נבחר` עם מספר השורות, ומשם נדע אם
צריך לפתוח את הפאנל אחרת.

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` בדיסקונט — 2:47 שקט.


## 2026-08-22 · Claude · אין לוח שנה — בורר התקופה הוא קבוצת רדיו — 1.1.6

### התגלית שמשנה את כיוון המימוש

`leumiDateMenu` מהריצה של 18:51 החזיר:
```
radio name="filterRadioList"  values: 40, 7, 30.2, 3, 365   (class "peer sr-only")
input[type=text] × 2          placeholder "dd.mm.yy"
calendarCells: 0
```
**אין לוח שנה.** בורר התקופה של לאומי הוא **קבוצת רדיו רגילה** עם ערכים
מוכנים, לצד שני שדות טקסט לטווח מותאם. זה הרבה יותר קל ממה שחששנו
כשראינו „מתאריך"/„עד תאריך" ככפתורים — הם תוויות מעל שדות אמיתיים.
`peer sr-only` הוא הדפוס המוכר של Tailwind: קלט מוסתר עם label נראה לצדו.

### ומה שנכשל, ולמה

כל השדות חזרו `shown:false`, ו-`inputsBefore: 18` שווה למספר שאחרי —
כלומר **הלחיצה שלי לא פתחה כלום.** הסיבה נמדדת מן המלל של הכפתור
שהגשש בחר: „תאריך40 תנועות אחרונותטווח תאריכיםמתאריךעד תאריך7 ימים
אחרונים…" — **זו מכולה שמכילה את כל הפאנל המוסתר, לא הפקד שפותח אותו.**

זה **בדיוק** הלקח שכבר נרשם כאן על דיסקונט ב-20.08: „האלמנט הפנימי
ביותר שמכיל את המזהה הוא טקסט, לא הפקד הלחיץ." חזרנו על אותה שגיאה
בבנק אחר. **הזיהוי-לפי-מלל מוצא את המכולה, לא את הפקד.**

### מה נוסף — `radioProbe()`, קריאה בלבד ובלי שום לחיצה

המסקנה המעשית מן הכשל: **אין צורך לפתוח כלום כדי למדוד.** האלמנטים כבר
ב-DOM, רק מוסתרים, ו-`textContent` קריא גם כשהם מוסתרים (בשונה מ-
`innerText`). לכן הגשש החדש קורא ישירות:
- לכל רדיו: `value`, `checked`, ו**תווית** (דרך `label[for]`, `closest('label')`,
  אח, או הורה קצר) — כדי לדעת מה `365` ומה `30.2` באמת אומרים.
- לשני שדות ה-`dd.mm.yy`: התווית שלהם, כדי לדעת מי „מתאריך" ומי „עד תאריך".
- שרשרת ההורים מן הרדיו כלפי מעלה, עם `hidden`/`role`/`aria-expanded` —
  כדי שהסבב הבא ידע **במה ללחוץ** במקום לנחש. אותה שיטה שעבדה בדיסקונט.

**⚠ אפס לחיצות.** `dateMenuProbe` נשאר לצדו, אבל התשובה תגיע מ-`radioProbe`.

### איך אומת

שני הקבצים עוברים parse. `radioProbe` **חולץ מן הקובץ עצמו** והורץ מול
DOM שמשחזר את המבנה שנמדד — פאנל ב-`display:none`, רדיו `peer sr-only`
עטופים ב-`label`:
- קרא נכון את חמשת הערכים ואת תוויותיהם **למרות שהפאנל מוסתר**,
  וסימן את `40` כברירת המחדל.
- זיהה את שני שדות ה-`dd.mm.yy` והצמיד לכל אחד „מתאריך"/„עד תאריך".
- מיפה את שרשרת ההורים וסימן אילו מהם מוסתרים.
- לא זורק על דף ריק.

**⚠ DOM מלאכותי. הערכים 40/7/30.2/3/365 אמיתיים ונמדדו; המיפוי שלהם
לתוויות הוא מה שהריצה הבאה תביא.**

### הצעד הבא — ואז אפשר לממש

רענון ל-1.1.6 וסנכרון לאומי אחד. `leumiRadios` יגיד מה כל ערך אומר.
אז המימוש ברור: **לבחור את הרדיו הקטן ביותר שמכסה את `collectSince`**
(או למלא את שני שדות ה-`dd.mm.yy` לטווח ישן יותר), ולהחיל. בלי לוח שנה,
בלי לנחש סלקטורים, ובלי להתקרב לבורר החשבונות.

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` בדיסקונט — 2:47 שקט.


## 2026-08-22 · Claude · לאומי סופר שורות, לא תאריכים — 1.1.5

### התשובה לשאלה „האם הוא מסנכרן לפי התקופה המבוקשת" — לא. וידוע למה.

`leumiRangeProbe` מ-1.1.3 הביא את התשובה בריצה אחת:
```
button  "תאריך · 40 תנועות אחרונות · טווח תאריכים · מתאריך · עד תאריך"
button  "מתאריך"     button "עד תאריך"
inputs  [ fromAmount, toAmount ]        ← סכומים בלבד. **אין שום שדה תאריך.**
url     .../nis-accounts/nis-transactions/
```
**ברירת המחדל של לאומי היא „40 תנועות אחרונות" — חלון לפי מספר שורות, לא
לפי תאריך.** זה מסביר סופית למה שלוש ריצות עצמאיות החזירו חלון זהה בדיוק
(28 תנועות, 06.07–02.08), ולמה `collectSince` אינו משפיע: אין למה להשפיע.
**השערת „loadAllRows עוצר מוקדם" מופרכת סופית — אל תחזרו אליה.**

**וההבדל הקריטי מדיסקונט:** שם היה `input#fromDate` שאפשר לכתוב לתוכו
ולהקליק. כאן „מתאריך"/„עד תאריך" הם **כפתורים**, ואין אף שדה תאריך ב-DOM.
זה כנראה מה שהפיל את הניסיון ב-20.08: מי שניגש לזה הניח שדות כמו בדיסקונט.

### מה נוסף — `dateMenuProbe()`, וכאן הגשש כבר לוחץ

פותח את כפתור התאריך (מזוהה **לפי המלל** „40 תנועות אחרונות"/„טווח
תאריכים", לא לפי סלקטור מנוחש), קורא את האפשרויות שנחשפו, בודק אם
**נוצרו שדות תאריך** אחרי הפתיחה, סופר תאי לוח שנה — וסוגר בחזרה
ב-Escape ובלחיצה על הרקע.

**⚠ הגבולות, אחרי 20.08:** נוגע רק בכפתור התאריך; **לא נוגע בבורר
החשבונות**; רץ רק אחרי שהתנועות כבר נקראו, ולכן אינו יכול לשבש את
הקריאה; כולו בתוך `try` — כשל בגשש לא יפיל סנכרון.

### תיקון שהשלמתי לעצמי

ב-1.1.2 תיקנתי את `status` של הרשומה כך שיאמר „מסונכרן חלקית" —
ו**שכחתי את שורת הסטטוס העליונה**, שהמשיכה לומר „הסתיים ואומת".
נמדד בריצה של 18:38: הבאנר אמר „הסתיים ואומת: 28 תנועות" בעוד הרשומה
אמרה „מסונכרן חלקית · פער 31,689.07 ₪". **הכותרת היא מה שהמשתמש רואה
קודם.** עכשיו: `הסתיים חלקית (N מתוך M חסרים תנועות)`.

### איך אומת

שני הקבצים עוברים parse. `dateMenuProbe` ובניית `leumiStatus` **חולצו
מן הקבצים עצמם** והורצו מול DOM שכולל גם כפתור תאריך וגם **בורר
חשבונות מדומה**:
- הכפתור זוהה נכון, האפשרויות נקראו, ושדות שנוצרו רק אחרי הפתיחה
  נתפסו עם `shown=true` — כלומר הגשש יגלה אם „מתאריך" הופך לשדה.
- **בורר החשבונות לא נלחץ.** זו הבדיקה החשובה כאן.
- Escape נשלח, והפאנל נסגר בסוף.
- בלי כפתור תאריך בדף — מחזיר `why` ולא זורק.
- הכותרת: „ואומת" כשהכול שלם, „הסתיים חלקית (1 מתוך 2…)" במעורב.

**⚠ DOM מלאכותי, לא אתר לאומי.**

### הצעד הבא

רענון ל-1.1.5 וסנכרון לאומי אחד. `leumiDateMenu` יגיד אם „טווח תאריכים"
פותח שדות אמיתיים (אז המימוש דומה לדיסקונט) או לוח שנה (אז צריך ניווט
בתאים). **רק אז לכתוב את הרחבת הטווח.**

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` בדיסקונט — 2:47 שקט.


## 2026-08-22 · Claude · „אין חשבונות לבחירה" — הכשל נמחק לפני שנראה — 1.1.4

**מה שטל דיווח:** קודם „הסנכרון דולג", ואז „אין חשבונות לבחירה".

**„דולג" — לא באג.** `background.js:253`:
`force?requested:requested.filter(s=>!sourceFreshToday(s,saved.accounts))`.
לאומי סונכרן היום ב-18:07, ולכן הסנכרון הכללי דילג עליו בכוונה. המסלול
שאינו מדלג הוא `refreshBank`/אריח הבנק → `startLeumi` → אישור בחירה →
`SYNC_SELECTED`, ובו אין שער רעננות.

**„אין חשבונות" — כאן היה באג, וחמור באופיו.** נמדד באחסון:
```
leumiAttempts      1        ← הזיהוי רץ
pendingLeumi       false    ← והסתיים
discoveredAccounts 0        ← בלי כלום
leumiDebug         null
leumiOptionProbe   null
syncStatus         "זוהתה כניסה ללאומי, אך הסנכרון האוטומטי כבוי"
```
`leumiAttempts` נשאר 1 בעוד **כל** המסלולים המסתיימים מאפסים אותו (787,
793, 804) — חוץ מן ה-`catch` של `startLeumi`. כלומר הזיהוי **נכשל**,
ה-`catch` כתב את הסיבה ל-`syncStatus`, ואז גלאי הכניסה רץ אחריו ו**דרס
אותה** בהודעה השגרתית „הסנכרון האוטומטי כבוי". **הסיבה האמיתית אבדה,
ולמשתמש נשאר מסך ריק בלי שום רמז.**

**שני תיקונים:**
1. `leumiDiscoverError` — הכשל נרשם למפתח ייעודי (הודעה, כתובת הלשונית,
   חותמת זמן) שאיש אינו כותב עליו. בדיוק כמו `discountDiscoverError`.
2. `noteAutoSyncOff(label)` — **הודעה שגרתית אינה מוחקת שגיאה.** ההודעה
   הופיעה בשני מסלולי כניסה (`autoSyncFromLogin` ו-`maybeAutoRun`) ולכן
   אוחדה לעוזר אחד שבודק את הסטטוס הקיים לפני שהוא כותב.

**זו אותה משפחה של תקלות שכבר נרשמה כאן פעמיים** — „יציאה שקטה היא באג
בפני עצמו" (18.08) ו„כשל שנכתב רק ל-reply נעלם" (18.08). הפעם הכשל כן
נכתב, אבל **נדרס**. שווה כלל: מי שכותב `syncStatus` שגרתי חייב לשאול
קודם אם יושבת שם שגיאה.

**איך אומת:** `background.js` עובר parse. `noteAutoSyncOff` **חולץ מן
הקובץ עצמו** והורץ מול ארבעה מצבי סטטוס: ריק → נכתב; „הסתיים בהצלחה"
→ נדרס כרגיל; „נכשל" ו„שגיאה" → **נשמרו**. בנוסף נספר בקובץ: 2 קריאות
ל-`noteAutoSyncOff`, ו-`leumiDiscoverError` נכתב. שלוש הכתיבות הישירות
שנותרו הן העוזר עצמו ושני ענפים שרצים רק כשהאוטומטי דלוק.
**⚠ בדיקת לוגיקה, לא ריצה חיה.**

**מה זה לא פותר:** *למה* הזיהוי נכשל עדיין לא ידוע — ההודעה אבדה.
הריצה הבאה תרשום אותה.

**הצעד הבא:** טל מרענן ל-1.1.4, לוחץ על ⟳ שליד כרטיס לאומי (לא על
הסנכרון הכללי — הוא ידלג), ומאשר את החשבון. אם הזיהוי ייכשל שוב —
`leumiDiscoverError` יגיד למה. אם יצליח — `leumiRangeProbe` מ-1.1.3
יגיד אילו פקדי תקופה יש בדף, וזה מה שחסר כדי לממש את הרחבת הטווח.

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` בדיסקונט — 2:47 שקט.


## 2026-08-22 · Claude · חלון לאומי חוזר זהה — גשש תקופה, קריאה בלבד — 1.1.3

**המדידה שמכריעה בין שתי ההשערות.** טל רענן ל-1.1.2 וסנכרן שוב את לאומי
(18:07:05Z). התוצאה **זהה לחלוטין** לריצה של 17:42:

```
28 תנועות · 06.07.2026 .. 02.08.2026 · פילוח 07/2026:22, 08/2026:6
שרשרת יתרות: 0 שברים ב-27 מעברים
leumiGap = {"921-348300": {diff:-31689.07, rows:28, until:"02.08.2026"}}
```

**שתי ריצות עצמאיות, אותו חלון עד התנועה האחרונה.** חזרתיות מדויקת
כזו **שוללת** את ההשערה ש-`loadAllRows` עוצר מוקדם — מרוץ על גלילה
היה מחזיר מספרים שונים. נשארת ההשערה שהאתר מגיש **חלון קבוע**.
⚠ מה שעדיין לא ידוע: **מי קובע אותו.** החלון מסתיים 20 יום לפני היום,
וזה לא מתיישב עם „30 הימים האחרונים". חשוד מוביל, לפי התקדים שנמדד
בדיסקונט: **האתר זוכר טווח מסשן קודם.**

**1.1.2 עובד כמתוכנן** — הסטטוס השמור אומר עכשיו את האמת:
`מסונכרן חלקית — התנועות עד 02.08.2026, פער ‎-31,689.07 ₪ עד היתרה`.

**מה נוסף כאן — `rangeProbe()` ב-`leumi-content.js`, קריאה בלבד:**
שדות שנראים כתאריך (סוג/ערך/מזהה), פקדים שהמלל שלהם מרמז על תקופה
(„תקופה", „מתאריך", „חיפוש מתקדם"…), `<select>` עם כל האפשרויות שלו,
מלל שנראה כהצהרת טווח בדף, ותאריכי השורה הראשונה והאחרונה. נשלח בתשובת
`LEUMI_SYNC_SELECTED` ונשמר ב-`leumiRangeProbe`.

**⚠ הגשש אינו נוגע בבורר החשבונות ואינו משנה שום מסלול.** אחרי ניסיון
20.08 שהפיל את הבורר (רשומת Codex), כל מה שנכנס לאזור הזה חייב להיות
תצפית בלבד — וזה מה שזה. **לא מנחשים סלקטורים; קודם מודדים.**

**איך אומת:** שני הקבצים עוברים parse. `rangeProbe` **חולץ מן הקובץ
עצמו** והורץ מול DOM בסגנון לאומי — תפס `fromDatePicker`/`toDatePicker`
עם ערכיהם, „בחירת תקופה" ו„חיפוש מתקדם", `select` עם „3 חודשים אחרונים",
ומלל הטווח. **סינן** שדה חיפוש וכפתור הדפסה, ולא זרק על דף ריק.
**⚠ DOM מלאכותי, לא אתר לאומי.**

**הצעד הבא:** טל מרענן ל-1.1.3 ומסנכרן לאומי פעם אחת. `leumiRangeProbe`
יגיד אילו פקדי תקופה קיימים בדף — ואז אפשר לממש הרחבת טווח **ממדידה**,
מבודדת אחרי אישור הבחירה, כפי ש-Codex דרש.

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` בדיסקונט — 2:47 שקט.


## 2026-08-22 · Claude · „מאומת" בלאומי היה הבטחה שהקוד לא קיים — 1.1.2

### לאומי חובר ורץ — ומבחן הרגרסיה עבר

טל חיבר לאומי וסנכרן **חשבון אחד** (`leumi|921-348300`). התוצאה, מן האחסון:
28 תנועות, הלוואה אחת, **23 צילומי שיקים**. הריצה 18 דקות.

**ארבע ישויות דיסקונט שרדו ללא שינוי** — 74/174/224/33, ותנועה ראשונה
זהה בכל אחת. 505 → 533 תנועות, אף חשבון לא נעלם ואף אחד לא הצטמק.
זהו האימות החזק ביותר עד כה לשני תיקונים, כי הוא **חוצה-בנקים**:
- **המיזוג (1.0.22)** — סנכרון לאומי לא נגע בדיסקונט.
- **אי-המחיקה (1.1.0)** — תנועת יובל מ-**31/12/2025** שרדה למרות
  `collectSince='2026-01-01'`. הקוד הישן היה מוחק אותה בדיוק כאן,
  כי `applyCollectSince` רץ על כל הרשימה בכל סנכרון.

### מה שטל תפס, וצדק

„הסנכרון בלאומי לא נראה לי תקין." נמדד:
```
status            "מסונכרן ומאומת"
תנועות            28, מ-06.07.2026 עד 02.08.2026   (היום 22.08)
שרשרת היתרות      0 שברים ב-27 מעברים — הקריאה נכונה
יתרת תנועה אחרונה 50,176.99
יתרת החשבון       18,487.92
פער               31,689.07 ש״ח, ו-20 יום שלא נקראו
```
**הקריאה נכונה אך חסרה** — ונשמרה תחת התווית „ומאומת".

**מה האימות באמת בדק:**
`r.accounts?.length===keys.length && every(a=>a.balance!=null && Array.isArray(a.transactions))`
— מספר חשבונות, קיום יתרה, ומערך. **מעולם לא נבדק שהתנועות מגיעות עד
היתרה.** קריאה חלקית שנראית שלמה היא המסוכנת מכולן.

**מה השתנה:** `gapOf(account)` משווה את יתרת התנועה האחרונה ליתרת החשבון.
יש פער → `מסונכרן חלקית — התנועות עד <תאריך>, פער X ₪ עד היתרה`, והפרטים
נרשמים ל-`leumiGap`. אין פער → „מסונכרן ומאומת" כמקודם.

**⚠ הנתונים לא נזרקים.** 28 התנועות נכונות ושימושיות. זה שונה מתקלת
הזהות בדיסקונט, שבה שמירה הייתה מייחסת כסף לחשבון אחר ולכן עדיף היה
להיכשל. כאן רק מפסיקים לשקר.

### תקלה שנייה — אישור בנק אחד הפיל את הבחירה של בנק אחר

בתיבת הבחירה של לאומי הופיעו גם **ארבע ישויות דיסקונט** — שריד מזיהוי
דיסקונט שנכשל ב-19:53 ולכן לא נוקה (`syncSelected` מנקה רק בהצלחה).
הן לא סומנו, וההסרה שהוספתי ב-1.0.25 הפילה אותן: `selectedAccountKeys`
ירד ל-`["leumi|921-348300"]` בלבד, והסנכרון האוטומטי הפסיק לרענן דיסקונט.
**הנתונים לא נפגעו — רק הבחירה.**

**מה השתנה:** אישור של בנק אחד אינו אמירה על בנק אחר. ההסרה מוגבלת
עכשיו למקורות שמופיעים ב-`keys` של אותו סבב אישור.

### איך אומת

שני הקבצים עוברים parse. `gapOf`, `money` ובניית `inBox` **חולצו מן
הקבצים עצמם** והורצו:
- על **הנתונים האמיתיים** → `{diff:-31689.07, until:"02.08.2026"}`,
  והסטטוס „מסונכרן חלקית — התנועות עד 02.08.2026, פער ‎-31,689.07 ₪".
- קריאה שלמה עדיין עוברת כ„מאומת"; חשבון בלי תנועות/בלי יתרה מחזיר
  `null`; סטייה של אגורה נסבלת.
- אישור לאומי בלבד → **כל ארבע ישויות דיסקונט שורדות** בבחירה;
  אישור דיסקונט חלקי עדיין מסיר את הלא-מסומן, כפי שנועד.

**⚠ בדיקות לוגיקה, לא ריצה חיה.**

### הפתוח הגדול בלאומי — טווח המשיכה

`leumi-content.js` (197 שורות) **אינו נוגע בתאריכים בכלל** — אין
`collectSince`, אין `fromDate`, אין בורר. הוא קורא בגלילה אינסופית
(`loadAllRows`) את מה שהאתר מציג כברירת מחדל. לכן החלון 06.07–02.08.
**⚠ הכיוון לא נחקר עד הסוף:** חסרים גם 20 הימים **האחרונים**, וזה מרמז
שהבעיה אינה רק „לא ביקשנו טווח" אלא גם ש-`loadAllRows` עוצר מוקדם —
`stable>=5` על 400ms הוא שתי שניות של שקט בלבד. **צריך מדידה חיה**:
`leumiGap` יגיד עכשיו בכל ריצה כמה חסר, וזו נקודת הפתיחה.
נמדד גם: קריאת התנועות דרשה את **כל שלושת הניסיונות** (180 שניות כל
אחד) — שתי פסילות ואז הצלחה, בלי שום רישום ב-`leumiDebug`.

**⚠ תזכורת מ-Codex (20.08):** ניסיון קודם לחבר טווח ללאומי הפיל את בורר
החשבונות. „לא לגעת שוב בבורר; המימוש הבא חייב להיות מבודד אחרי אישור
הבחירה."

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` בדיסקונט — 2:47 שקט.


## 2026-08-22 · Claude · הבורר לא נפתח כי שריד תפריט סגור ענה במקומו — 1.1.1

### קודם: חמישה תיקונים אומתו **בריצה חיה**, מן האחסון

הריצה של 08:35Z סנכרנה את יובל (514220276) **לבדו**, וזה מה שהיא הוכיחה:

| התיקון | הראיה |
|---|---|
| **המיזוג (1.0.22)** | שלוש הישויות האחרות שלמות — 74/174/224, `lastSync` עדיין 21.08 20:37 |
| **השם (1.0.23)** | `514220276` = „אביסידריס יובל". אתמול היה „טל רצבי" |
| **הזיכרון (1.0.25)** | `discountKnownNumbers` נכתב לראשונה: `{514220276: 008-3920651}` |
| **אי-המחיקה (1.1.0)** | ליובל תנועה מ-**31/12/2025** בעוד `collectSince='2026-01-01'` — הקוד הישן היה מוחק אותה |
| **ההגירה (1.1.0)** | `viewSinceInit:true`, `viewSince='2026-01-01'` |

**המיזוג הוא העיקר** — המבחן שנשאר תלוי משלוש רשומות, ועבר.

### הכשל שנשאר, ותוקן כאן

אותה ישות, אותו יום, שתי תוצאות הפוכות:
```
08:35:35Z  discountSelectWorked = {entity:"514220276", path:"a.dropdown-item"}       ✔
16:53:29Z  discountSelectFailed = {entity:"514220276", candidates:["li.accountComboLinks"]}  ✘
```
בכשל נמצא **מועמד יחיד** — ולכן „נסה את כל המועמדים" מ-1.0.26 לא היה מה לנסות.
(השדה `candidates` עצמו מוכיח ש-1.0.26 רץ.)

**השורש — היציאה המוקדמת ב-`entityOptions`:**
```js
const already=menuEntities(); if(already.length) return already;   // ← יוצא כאן
realClick(trigger);                                                 // ← לא הגיע
```
`menuEntities` מחפש בין השאר `[role="menu"] li`, ותפס את `li.accountComboLinks`
**בזמן שהתפריט סגור**. היציאה המוקדמת החזירה אותו, הבורר מעולם לא נפתח,
ו-`a.dropdown-item` לא נוצר כלל.

**הכלל החדש, מדיד:** יש כמה ישויות בחשבון, ולכן רשימה עם **מזהה אחד בלבד**
אינה רשימת הישויות אלא שריד של תפריט סגור — במקרה כזה לוחצים וממשיכים.
אם אף רשימה אינה נראית מלאה, מוחזרת העשירה שבהן.

**⚠ אין כאן סינון של li** — זה מה שהוחזר ב-21.08 והפיל את הזיהוי
(`discoveredAccounts=0`). כאן רק **בוחרים בין רשימות שלמות**, ולכן חשבון
עם ישות אחת בלבד ממשיך לעבוד. זה נבדק במפורש.

**איך אומת — בדיקה שמפלה בין הישן לחדש:** DOM עם בורר שלחיצה עליו יוצרת
`a.dropdown-item` לכל ישות, ועם שריד `li.accountComboLinks` בתוך `role=menu`.
`entityOptions` **חולץ מן הקובץ עצמו**:
- ישן + שריד → **מועמד אחד, בלי `a.dropdown-item`** — משחזר את כשל 16:53
- חדש + שריד → 5 מועמדים, כולל `a.dropdown-item` ✔
- בלי שריד → שניהם 4, אין רגרסיה
- **ישות אחת בלבד** → החדש מחזיר אותה כרגיל, עם ועם בלי שריד

**⚠ DOM מלאכותי, לא אתר דיסקונט.**

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` — 2:47 דקות שקט.

**הצעד הבא לפי טל: לאומי.**


## 2026-08-22 · Claude · משיכה ותצוגה הופרדו — סנכרון מפסיק למחוק — 1.1.0

**החלטת מוצר של טל, לא באג:** „בוא נשנה שיהיו שני כפתורים — כפתור
הסנכרון וכפתור הצגת הנתונים."

**מה שהיה:** הגדרה אחת (`collectSince`) עשתה שתי עבודות שאין ביניהן קשר —
(א) **טווח משיכה**: מאיזה חודש לבקש מהבנק. לגיטימי, חוסך זמן.
(ב) **מחיקה**: `applyCollectSince` ב-`syncSelected` מחק תנועות שכבר
נשמרו — ומכל החשבונות ברשימה, לא רק ממה שסונכרן. נמדד: סנכרון יובל
לבדו עם גבול מרץ מוחק **70 תנועות משלוש ישויות שלא נגעו בהן**.

**המדידה שהכריעה:** ל-`manifest.json` יש **`unlimitedStorage`**, ו-503
תנועות שוקלות **80KB** (163 בייט לתנועה). שיקול המקום — הנימוק היחיד
שיכול היה להצדיק מחיקה — פשוט אינו קיים.

**מה השתנה:**
- `collectSince` — **טווח משיכה בלבד.** נשאר בתוקף ב-`storeCardMonth`
  (אילו חודשי כרטיס לפתוח), בהורדת השיקים בלאומי, ובטווח שנשלח לאתר
  דיסקונט. שלושתם לא נגענו בהם — הם היו נכונים מלכתחילה.
- `viewSince` — **חדש, סינון תצוגה בלבד.** `visibleTx(account)` מוחל
  בשלוש נקודות הקריאה בדשבורד: תצוגת התנועות, מודאל החשבון, והחיפוש.
- **`applyCollectSince` הוסרה.** היא הייתה הצרכן היחיד שמחק נתונים
  שמורים. `keepSince`/`txDateMs` נשארו — הן משמשות את גבול השיקים.
- שני פקדים נפרדים בפאנל, עם משפט מצב לכל אחד.

**שתי החלטות שטל קיבל במפורש:** (1) נתונים ישנים **לעולם אינם נמחקים** —
גם לא בכפתור נפרד. (2) הטווחים **עצמאיים לגמרי** — אפשר למשוך שנה
ולהציג חודש.

**הגירה חד-פעמית:** `viewSinceInit`. משתמש קיים מקבל `viewSince =
collectSince` הישן, כך שהמסך נראה בדיוק כמו קודם; מכאן השניים נפרדים.
בלעדיה משתמש קיים היה רואה פתאום חודשים ישנים שחשב שסילק.

**⚠ מרוץ שנמנע:** בתחילה `viewSince` נטען בשני מקומות במקביל —
ב-`load()` ובטוען העצמאי של `collectSince`. הכתיבה המאוחרת יכלה להחזיר
אותו לריק אחרי ההגירה. **`load()` הוא עכשיו הבעלים היחיד**, והטוען
העצמאי נוגע רק ב-`collectSince`.

**מה לא נכלל בכוונה:** דפי הכרטיסים (`renderAllCards`, `CARD_MONTH_DATA`)
לא סוננו — יש להם בורר חודש משלהם, ומסנן תאריך שני שם היה כפילות
מבלבלת. מאחר שכבר לא מוחקים, תנועות הכרטיס פשוט נשארות שלמות.

**איך אומת:** שלושת הקבצים עוברים parse. `visibleTx`, `sinceControls`,
המשפטים ושורת ההגירה **חולצו מן הקבצים עצמם** והורצו:
- ברקע: **0 קריאות** ל-`applyCollectSince`, `marked` נבנה בלי סינון,
  ו-`collectSinceMs` עדיין משמש את שני מסלולי המשיכה.
- `visibleTx`: חלון מרץ מסתיר ינואר-פברואר, **תאריך פגום נשאר מוצג**
  (בדיוק ככלל „תאריך שלא ניתן לפענוח נשמר" שברקע), והמערך המקורי
  אינו משתנה.
- UI: ארבעה בוררים בעלי מזהים נפרדים, ערכים עצמאיים (מרץ למשיכה,
  פברואר לתצוגה).
- הגירה: משתמש קיים → `{viewSince:'2026-03-01', viewSinceInit:true}`;
  מי שכבר הוגר → **לא נכתב דבר**; משתמש חדש → חלון פתוח.

**⚠ בדיקות לוגיקה ו-DOM מלאכותי, לא ריצה חיה בדפדפן.**

**עדיין פתוח:** (א) מרדף הזיהוי לישות שמעולם לא סונכרנה (1.0.24).
(ב) אין פעימת סטטוס במהלך `extract` — 2:47 דקות שקט. (ג) **המיזוג
מ-1.0.22 עדיין לא נבחן בריצה חיה.**


## 2026-08-22 · Claude · הבורר נבחר לפי סדר ה-DOM, ולכן הגריל — 1.0.26

**מה שטל דיווח:** „עשיתי סנכרון ממרץ ליובל" → `דיסקונט: המעבר נכשל —
דיסקונט לא עבר לישות 514220276 — נוסו 1 רמות לחיצה
(0:li.commonDropdown__menuItem)`.

**השורש — שלוש רשומות אבחון שהצטלבו:**
```
discountSelectWorked = {entity:"570012930", path:"a.dropdown-item"}          ✔
discountSelectFailed = {entity:"514220276", tried:["0:li.commonDropdown__menuItem"]}
כשל קודם             = {entity:"514220276", tried:["0:li.accountComboLinks"]}
```
בדף יש **כמה אלמנטים שונים הנושאים את אותו מזהה ישות**, ורק
`a.dropdown-item` באמת מחליף ישות. `selectEntity` עשה
`options.find(b=>entityId(text(b))===id)` — כלומר לקח את **הראשון בסדר
ה-DOM**. הסדר משתנה בין ריצות, ולכן אותה ישות פעם עוברת ופעם לא. **זו
לא תקלת ישות אלא הגרלה.** ומכיוון שההורה של מועמד כושל מכיל יותר ממזהה
אחד, `idsIn(el)>1` שבר את השרשרת מיד — נוסתה רמה אחת, ושאר המועמדים
מעולם לא נבדקו.

**מה השתנה:** `selectEntity` עובר על **כל** המועמדים התואמים, לא על
הראשון. `rankOption` מקדים a/button ואז role=option/menuitem ואז li,
כך שהמועמד שנמדד כמצליח מנוסה ראשון ואין קנס זמן. `discountSelectFailed`
רושם עכשיו גם את רשימת המועמדים.

**⚠ זה אינו הדירוג שהוחזר ב-21.08.** שם `entityOptions` **סינן** li
והפיל את הזיהוי כולו (`discoveredAccounts=0`), כי `discover()` נשען על
אותה רשימה. כאן **לא מסננים דבר** — הרשימה נשארת שלמה, רק סדר הניסיון
משתנה, והשינוי מקומי ל-`selectEntity` ואינו נוגע ב-`discover()`.

**איך אומת — בדיקה שמפלה בין הישן לחדש:** נבנה DOM עם שלושה אלמנטים
הנושאים את 514220276 (`li.commonDropdown__menuItem`,
`li.accountComboLinks`, `a.dropdown-item`), כשרק לחיצה על השלישי מחליפה
מצב. `selectEntity` **חולץ מן הקובץ עצמו** והורץ מולו:
- הקוד הישן נכשל ושחזר **מילה במילה את ההודעה שטל קיבל**
  (`0:li.commonDropdown__menuItem`)
- הקוד החדש עבר ל-3920651
- בסדר DOM הפוך שניהם עוברים — כלומר הוכח שהישן תלוי סדר
- כשאין מועמד תקין החדש עדיין נכשל, עם הודעה עשירה יותר

**⚠ בדיקה על DOM מלאכותי, לא על אתר דיסקונט.**

**מה שנמדד ולא נשבר:** הסנכרון הכושל **לא כתב דבר** — `syncSelected`
זרק לפני הכתיבה, ולכן גבול מרץ (`collectSince:"2026-03-01"`) עוד לא גזם
כלום. `accounts` עדיין 74/31/174/224 עם נתוני ינואר.
`discountKnownNumbers` עדיין `null` — לא היה סנכרון מוצלח מאז 1.0.25.

**עדיין פתוח:** (א) אין פעימת סטטוס במהלך `extract` — 2:47 דקות שקט.
(ב) **המיזוג מ-1.0.22 עדיין לא נבחן בריצה חיה.**


## 2026-08-22 · Claude · זיכרון מספרי חשבון ששורד מחיקה — 1.0.25

**נולד משאלה של טל, לא מבאג:** „מה יקרה אם אמחק את החשבונות ואסנכרן
מחודש פברואר?" הבדיקה חשפה ששתי הפעולות שונות לגמרי, ואחת מהן מלכודת.

**המלכודת:** ההזרעה של 1.0.24 קוראת את `accounts`. מחיקת חשבון בדשבורד
מסלקת אותו משם (`dashboard.js`, מטפל `.remove`), ולכן מחיקת הכל מרוקנת
את המקור היחיד למספר של ינון. ואז: אין ממה להזריע → הזיהוי חוזר למרוץ
השבור → `3 מתוך 4` → **והתיבה של ינון `disabled`** (`ready=!!(a.branch&&
a.accountNumber)`), כלומר אי אפשר אפילו לסמן אותו לסנכרון. **ההזרעה
נשענה על נתונים שהמשתמש רשאי למחוק.**

**מה השתנה:** `discountKnownNumbers` — מפה `entityId → {branch,
accountNumber, at}` באחסון, שהמחיקה אינה נוגעת בה.
- **נכתבת** ב-`syncDiscountBusiness` אחרי לולאת הישויות ו**לפני** בדיקת
  השלמות (`out.length!==keys.length`) — כדי שריצה חלקית תפקיד את מה
  שהספיקה לאמת. נכתבת רק ממה שעבר `assertEntityMatches`, כלומר מספר
  שאומת מול הדף.
- **נקראת** ב-`runDiscoverDiscount` יחד עם `accounts`. הסדר מכוון:
  הזיכרון נטען ראשון ו-`accounts` דורס אותו, כי הוא הטרי מבין השניים.
- **מתיישנת מתקנת את עצמה:** כל סנכרון מוצלח דורס, ואם מספר בבנק באמת
  השתנה — `assertEntityMatches` יעצור את הסנכרון במקום לשמור חשבון אחר.

**איך אומת:** שלושת הקבצים עוברים parse. **שני הקטעים — ההפקדה וההזרעה —
חולצו מן הקובץ עצמו** והורצו מול אחסון מדומה בתרחיש המלא: סנכרון 4
ישויות → הפקדה → **מחיקת כל החשבונות** → זיהוי מחדש. תוצאה: הזיכרון שרד
עם 4 רשומות, 3 ישויות הוזרעו, ינון נפתר ל-9832685, וישות שמעולם לא
סונכרנה נשארה ריקה (הזיהוי האמיתי עדיין רץ עבורה). נבדק גם שרשומה
מתיישנת מפסידה לטרייה. **⚠ בדיקת לוגיקה, לא ריצה חיה.**

### מה שנמדד אגב כך — גבול האיסוף

`applyCollectSince` רץ על **כל** הרשימה, לא רק על מה שסונכרן. כלומר
שינוי הגבול + סנכרון של **ישות אחת** גוזם את החודשים שלפניו **מכל**
החשבונות השמורים. נמדד מן האחסון: הזזת הגבול מ-01/01 ל-01/02 מפילה
**76 תנועות מתוך 503 (15%)** — 7/6/23/40 בהתאמה לארבע הישויות. הפיך:
דיסקונט הגיש מ-01/01, ולכן החזרת הגבול וסנכרון מחדש מחזירים אותן.

**⚠ מלכודת שם במחיקה:** דיאלוג המחיקה מציג `account.nickname`, ובאחסון
**עדיין** רשום „טל רצבי" על ישות 514220276 — תיקון השם ב-1.0.23 חל על
קוד, לא על נתונים שכבר נשמרו. מחיקה לפי שם תמחק את הלא-נכון עד
שהסנכרון הבא יתקן את הנתונים.

**עדיין פתוח:** (א) מרדף הזיהוי לישות חדשה שמעולם לא סונכרנה — הלולאה
מוותרת לפני שהמעבר נוחת (ראה 1.0.24). (ב) אין פעימת סטטוס במהלך
`extract` — נמדד 2:47 דקות שקט. (ג) **המיזוג מ-1.0.22 עדיין לא נבחן
בריצה חיה.**


## 2026-08-22 · Claude · הזיהוי ויתר רגע לפני שהמעבר נחת — 1.0.24

**⚠ תיקון לרשומה של 1.0.23: הצהרתי שם ש„ינון נסגר". זה היה שגוי.**
ינון *סונכרן* (224 תנועות), ומזה הסקתי שהוא *זוהה*. אלה שני מנגנונים
נפרדים: הסנכרון עבד כי `selectedAccountKeys` כבר החזיק את המפתח שלו, בלי
תלות בזיהוי. **אל תסיקו הצלחת זיהוי מהצלחת סנכרון.**

**מה שקרה באמת — משני מפתחות אבחון שהקוד כותב לעצמו:**
```
discountEntityReport[570012930] = {passes:2, seenEntities:["514649565"],
                                   resolved:false, selectError:""}
discountSelectWorked            = {entity:"570012930", path:"a.dropdown-item",
                                   at:"2026-08-21T21:00:42Z"}
```
בשני המעברים ובכל 12 הדגימות `DISCOUNT_STATE` החזיר את הישות הקודמת —
**ואז המעבר הצליח, אחרי שהלולאה כבר ויתרה.** מעבר ישות טוען מחדש את הדף
והורג את ה-content script; חלון הדגימה קצר מדי, ואיש אינו קורא את המצב
שוב אחרי הוויתור.

**`selectError` תמיד ריק ואינו ראיה לכלום:** המטפל ב-`DISCOUNT_SELECT_ENTITY`
עונה `ok` **לפני** הבחירה (המעבר הורג את הסקריפט), ולכן שגיאת מעבר לעולם
לא חוזרת לרקע. היא נוחתת ב-`discountSelectFailed` ובסטטוס בלבד.

**מה השתנה — הזרעה במקום מרדף:** מספר החשבון של ינון כבר ידוע ואומת —
015-9832685, שנשמר בסנכרון שעבר `assertEntityMatches` מול הדף. אין סיבה
לרדוף אחרי הדף כדי לגלות מחדש מה שאומת ונשמר. `runDiscoverDiscount` טוען
עכשיו את `accounts` ומזריע `branch`+`accountNumber` לכל ישות שסונכרנה
בעבר; `if(a.branch&&a.accountNumber)continue` שכבר קיים בלולאה גורם לה
לדלג על הניווט כולו. אותו דפוס כמו `isracardActiveSince`.

**מוזרעים מספרים בלבד, לא שמות** — השמות נקראים מתוויות הבורר בקריאה
אחת ולכן אמינים, ושם שמור עלול להיות הישן והשגוי (תקלת השם ב-1.0.23).

**הסטטוס אומר את האמת:** `נמצאו ואומתו N חשבונות (K ממספרי חשבון שנשמרו
בסנכרון קודם)` — כדי שלא ייראה כאילו כולם נקראו מן הדף עכשיו.

**איך אומת:** שלושת הקבצים עוברים parse; קטע ההזרעה **חולץ מן הקובץ
עצמו** והורץ מול הנתונים האמיתיים שנמדדו מן האחסון. ינון נפתר ל-015-9832685,
ישות שמעולם לא סונכרנה נשארת ריקה (כלומר הזיהוי האמיתי עדיין רץ עבורה),
והשמות לא נדרסו. **⚠ בדיקת לוגיקה, לא ריצה חיה.**

**עדיין פתוח — המרדף עצמו לא תוקן.** ישות חדשה שמעולם לא סונכרנה עדיין
תלויה באותה לולאה שמוותרת מוקדם מדי. הכיוון המתבקש, מן המדידה: לקרוא
`DISCOUNT_STATE` פעם נוספת אחרי שהלולאה ויתרה, או להאריך את חלון הדגימה
מעבר ל-12 שניות. **לא נעשה — דורש מדידה חיה כדי לדעת כמה זמן המעבר באמת
לוקח.**

**עדיין פתוח:** אין פעימת סטטוס במהלך `extract` — נמדד 2:47 דקות שקט.

**⚠ המיזוג מ-1.0.22 עדיין לא נבחן** — ראה הרשומה שלו.


## 2026-08-21 · Claude · השם של הישות נדרס בשם קודמתה — 1.0.23

**ריצה חיה של 4 ישויות נמדדה מן האחסון עצמו** (`Local Extension Settings/
lfaejhhipmndfikdeabindkoiemjlljl/000003.log`, מפוענח כיומן LevelDB — הכלי
שנחסם בסבב הקודם עובד). הסנכרון הצליח: `הסתיים בהצלחה: סונכרנו 4 חשבונות`,
7 דקות, וכל ארבע הישויות נשמרו.

**ינון (570012930) סונכרן לראשונה** — 224 תנועות, חשבון 015-9832685, אחרי
שהזיהוי החזיר `נמצאו ואומתו 4 חשבונות`. נדרשו לו שלושה ניסיונות מעבר.
**הפריט הפתוח הזה נסגר.**

**הבאג שהריצה חשפה:** ישות `514220276` נשמרה בשם **„טל רצבי"** — השם של
`024844714`, הישות שקדמה לה בלולאה. מספר החשבון (008-3920651), היתרה
(154.92) ו-31 התנועות **נכונים** — רק התווית שיקרה.

**השורש:** `owner` נקרא ב**שורה הראשונה** של `extract` בדיסקונט —
לפני לולאת ההמתנה לטעינה ולפני `assertEntityMatches(id)`. בישות השנייה
ואילך הבורר עדיין הציג את הקודמת. **שומר הזהות מ-1.0.17 אימת את מספר
החשבון בלבד, והשם עקף אותו** — כלומר ההגנה ההיא היתה חלקית, וזה הפער.

**מה השתנה:** `const owner=…` הוזז אל צמוד ל-`const account=activeAccount()`
— אחרי `assertEntityMatches`, ובאותו רגע שבו נקראים היתרה, מספר החשבון
והתנועות. כך כל נתוני הישות מגיעים ממצב דף אחד ומאומת.

**נמדד לפני התיקון — התווית היא תצוגה בלבד, לא זיהוי:** נסרקו כל 19
המופעים של `nickname`/`owner` בקוד; כולם השמה או תצוגה. מפתחות הזהות הם
`accountSyncKey` (source|branch-account), `transactionSyncKey` (תאריך,
פעולה, פרטים, אסמכתא, סכומים), `chequeId` (selectionKey|reference)
ו-`wasRequested` — **אף אחד לא נוגע בשם.** כלומר שום תנועה לא נותבה
לחשבון שגוי. **⚠ החריג היחיד:** דיאלוג המחיקה ב-`dashboard.js` מציג את
השם, ולכן שם שגוי עלול להטעות **את המשתמש** למחוק ידנית את הלא-נכון.

**⚠ המיזוג מ-1.0.22 עדיין לא נבחן.** ארבע הישויות נשלחו ב**קריאה אחת**
ל-`syncSelected`, ולכן `untouched` יצא ריק והקוד התנהג כמו לפני התיקון.
**המבחן האמיתי:** עכשיו, כשארבע ישויות שמורות, לסנכרן **ישות אחת בלבד**
ולוודא שהשלוש האחרות נשארות.

**נמדד בדרך אגב — פריט „שקט בסנכרון":** 23:46:37 „פותח תנועות (570012930)"
→ 23:49:24 „קורא הלוואות". **2:47 דקות בלי אות חיים.** עדיין פתוח.

**⚠ `discount-content.js` הוא קובץ בבעלות יחידה.** העריכה נקודתית: הזזת
הצהרה אחת בתוך `extract`.

**כלי שנוסף (בתיקיית העבודה הזמנית, לא בריפו):** מפענח יומן ה-LevelDB של
האחסון. הוא מאפשר לקרוא `syncStatus`, `accounts` ו-`selectedAccountKeys`
האמיתיים בזמן ריצה, בלי לשאול את המשתמש מה הוא רואה. שווה לשחזר אותו.


## 2026-08-21 · Claude · סנכרון ישות אחת מחק את כל השאר — 1.0.22

**התקלה שנסגרה כאן היא זו שגרמה לאובדן נתונים אמיתי**, ולא לתצוגה שגויה:
סנכרון ישות אחת בדיסקונט עסקי מחק מן האחסון את כל שאר הישויות. זה מה שדווח
ב-1.0.17 כ„מחק את הסנכרון של טל".

**השורש — נמדד בקוד, לא שוער:** `syncSelected` סינן את החשבונות השמורים לפי
**מקור** ולא לפי מה שנתבקש:
```js
saved.accounts.filter(a=>!syncedSources.includes(a.source||'business'))
```
`syncedSources` הוא רשימת **מקורות** (`discount-business`), ולכן סנכרון ישות
אחת סילק את **כל** חשבונות דיסקונט עסקי ודחף בחזרה רק את מה שנקרא עכשיו.
המפתח הנכון קיים כבר: לדיסקונט עסקי `selectionKey` הוא
`discount-business|<entityId>` — כלומר מפתח לכל ישות, ומפתח אחד נושא כמה חשבונות.

**מה השתנה — שלושה מקומות, אותה תקלה:**

1. `background.js` · `syncSelected` — הסרה לפי **מה שנתבקש בריצה** (`selectionKeys`)
   במקום לפי מקור. `wasRequested` בודק `selectionKey`, ולחשבונות ישנים שנשמרו
   לפני שהיה `selectionKey` יש שתי נפילות לאחור: `entityId` (דיסקונט) וסניף-חשבון
   (פועלים, לאומי, מזרחי).
2. `background.js` · `markNewTransactions` רץ עכשיו **רק על מה שנקרא בריצה**.
   בלי זה, החשבונות ששרדו היו עוברים סימון מחדש, מוצאים כל תנועה זהה לעצמה
   ומאבדים את סימון „תנועה חדשה". `newCount` נספר על הריצה בלבד — כפי שהמסר מעיד.
3. `background.js` + `dashboard.js` — **אותה תקלה ב-`selectedAccountKeys`.**
   הן ב-`syncSelected` והן ב-`#confirmSelection` נמחקו מפתחות של ישויות שלא
   סונכרנו, ולכן הן נשרו גם מן הסנכרון האוטומטי. עכשיו הבחירה נשמרת, וביטול
   בחירה נשאר היכן שהוא שייך — בדשבורד.

**איך אומת — ובמה זה מוגבל:** הקוד נבדק תחבירית (`new Function` על שני הקבצים
המלאים), והביטוי `wasRequested` **חולץ מן הקובץ עצמו** והורץ מול שש רשומות
חשבון: שתי ישויות דיסקונט, פועלים, ושתי רשומות ישנות בלי `selectionKey`.
סנכרון ישות 513 מותיר את 570 ואת השאר; סנכרון 570 מותיר את 513; שתי הנפילות
לאחור תופסות. **⚠ זו בדיקת לוגיקה, לא ריצה חיה מול הבנק** — עדיין לא נמדד
סנכרון אמיתי של שתי ישויות בזו אחר זו.

**מה טל צריך לאמת אצלו:** לסנכרן ישות אחת בלבד ולוודא שהישות השנייה **נשארת**
בדשבורד עם התנועות שלה, ושסימוני „תנועה חדשה" שלה לא נמחקו.

**⚠ `background.js` ו-`dashboard.js` הם קבצים בבעלות יחידה ו-Codex עבד בהם.**
העריכות נקודתיות: ב-`background.js` בתוך `syncSelected` בלבד, וב-`dashboard.js`
שורה אחת בתוך המטפל של `#confirmSelection`.

**עדיין פתוח — ללא שינוי:** (א) ינון (570012930) לא מזוהה; מספר החשבון 9832685
קיים בקוד רק כהערה ב-`discount-content.js:251`, והמפה נבנית אך ורק מ-
`discoveredAccounts`. (ב) אין פעימת סטטוס במהלך `extract` — שלוש דקות שקט לכל ישות.


## 2026-08-21 · Claude · סדר התנועות בתוך היום היה הפוך — 1.0.21

**טל אישר שהסנכרון עובד** („יפה עובד") — טווח מינואר, שתי ישויות מסונכרנות.
נותרו שתי הערות שלו, ואחת מהן תוקנה כאן.

**„למה הסדר ביתרה לא נכון" — נמדד מהמסך, והחשבון מוכיח:** בחשבון 009-2556371,
ב-17/08 הוצגה העברה 250,000 (יתרה **-267,664.63**) **מעל** הקמת הלוואה 300,000
(יתרה **32,335.37**). אבל ‎-267,664.63 + 300,000 = 32,335.37 **בדיוק** — כלומר
ההעברה קדמה להלוואה, והתצוגה הפכה ביניהן.

**השורש:** `sort((a,b)=>dateKey(b.date)-dateKey(a.date))` ממיין **לפי תאריך בלבד**.
מיון ב-JS יציב, ולכן תנועות מאותו יום שמרו על סדר המקור — והמקור מהאתר הוא
**עולה בזמן**. התוצאה: בין ימים חדש→ישן, ובתוך יום ישן→חדש. **חוסר עקביות, לא באג בנתונים.**

**מה השתנה:** שובר שוויון לפי אינדקס המקור בסדר הפוך —
`dateKey(b)-dateKey(a) || b.i-a.i`. אומת על הרצף האמיתי מהמסך: התוצאה
26,513.54 → 27,624.38 → 30,985.37 → 32,335.37 → ‎-267,664.63, ושרשרת היתרות
מתיישבת לחלוטין.

**⚠ `dashboard.js` הוא קובץ בבעלות יחידה ו-Codex פעיל בו.** העריכה נקודתית,
שורה אחת בתוך `openAccountTransactions`.

**עדיין פתוח — ינון (570012930):** לא מזוהה בזיהוי, **אבל מספר החשבון שלו כבר ידוע
מהכשל של 1.0.17: 9832685.** הכיוון הבא: להזין אותו כידוע מראש, או לתקן את הזיהוי
עצמו — שדורש מדידה חיה של הבורר פתוח מול סגור.

**עוד פתוח:** אין פעימת סטטוס במהלך `extract` — שלוש דקות של שקט לכל ישות, וזה
מה שגרם לטל ללחוץ שוב באמצע.


## 2026-08-21 · Claude · הסנכרון התחיל על הישות הלא נכונה — מתקן במקום ליפול, 1.0.20

**הכשל שנרשם:** „ישות 024844714 נכשלה: מוצג חשבון 9832685 במקום 2556371
(הבורר מציג 570012930)".

**המדידה שסוגרת את הסיפור:**
```
discountIdentityBlock = {want:"024844714", expected:"2556371",
                         seen:"9832685", label:"570012930"}
```
ביקשנו את **טל**; הדף היה על **ינון**, והציג את חשבון **9832685**.
**כלומר הסנכרון מתחיל על הישות שבה הדף במקרה נמצא, בלי לוודא מעבר.**
זו בדיוק ההצלבה שטל דיווח עליה קודם (שם של ינון עם חשבון של יובל) —
והפעם היא **נחסמה במקום להישמר**. שומר הזהות מ-1.0.17 עשה את עבודתו.

**בונוס מהמדידה:** מספר החשבון של ינון הוא **9832685** — הישות שהזיהוי מעולם
לא הצליח לקרוא. הוא הופיע דווקא בכשל.

**מה השתנה:** `assertEntityMatches` אינו נכשל בניסיון הראשון. כשהוא מזהה ישות
שגויה הוא **מבצע את המעבר בעצמו** דרך `selectEntity` — המנגנון שכבר אומת חי
(`a.dropdown-item`, שלוש הצלחות רשומות) — ואז מאמת שוב. רק אם גם זה נכשל,
נרשמת חסימה ונזרקת שגיאה. **תיקון, ורק אם אי אפשר — עצירה.**

**לא נבדק:** הריצה החיה של התיקון העצמי.


## 2026-08-21 · Claude · השומר שיבש את הכניסה לדיסקונט — הניווט הוסר, 1.0.19

**דיווח טל:** „לא מצליח להתחבר לדיסקונט. האם אתה משבש את הכניסה לחשבון?"
**התשובה: כן. אני.**

**מה עשיתי לא נכון ב-1.0.16:** שומר הסשן בדק את כתובת הלשונית, וברגע שראה `/login/`
**ניווט את הלשונית לדף הכניסה** — כלומר בדיוק בזמן שטל הקליד בדף הכניסה, הדף נטען
מחדש והמלל נמחק. וכיוון שהזיהוי מופעל בכניסה לדיסקונט, זה יכול לחזור כל שתי שניות.
**זו לולאת ניווט — בדיוק מה שהכללים אוסרים במפורש**, ואני יצרתי אותה בעצמי.

**מה השתנה:** `chrome.tabs.update` הוסר לחלוטין. **השומר מדווח בלבד ולעולם אינו
מנווט.** אם הלשונית על `/login/` — הוא עוצר את ההמתנה ואומר „התחבר בלשונית ולחץ שוב".
אם היא יצאה מהמארח — „הדפדפן יצא מהסשן". הכתובת לא נגעת בשום מקרה.

**⚠ הכלל שנשבר, לזיכרון:** „אין להסיר שומרי לולאה… בלעדיהם נוצרה לולאת ניווט
שהשביתה דפדפן". לא הסרתי שומר — **הוספתי מנווט**, וזה אותו נזק בדיוק.
**קוד אבחון לא מנווט. אף פעם.**

**גם:** חלון הדיבוג נסגר, כדי שלא תישאר שאלה אם סשן מקביל מפריע להתחברות.

**מצב:** טווח התאריכים עובד (אומת). זיהוי הניתוק מדווח בלבד. הזיהוי החלקי
(1–3 מתוך 4) עדיין פתוח, וממתין למדידה חיה ולא להיסק.


## 2026-08-21 · Claude · הסנכרון חרג מתקציב הזמן — קוצץ והורחב, 1.0.18

**הכשל:** „ישות 024844714 נכשלה אחרי 3 ניסיונות: קריאת התנועות לא השיב תוך 90 שניות".

**מה נמדד, ומה **לא** קרה:** `discountIdentityBlock` **לא נכתב** — כלומר שומר
הזהות של 1.0.17 **לא חסם**, וזו אינה הסיבה. `discountRangeApplied` כן נכתב
(15:54:41, `reason:"הופעל"`, `from:"01/04/2026"`, `earliest:"2025-12-30"`),
כלומר הרחבת הטווח עבדה גם בריצה הזו.

**השורש — תקציב זמן, לא לוגיקה.** הוספתי ל-`extract()` שתי המתנות חדשות בלי
לבדוק את התקציב הכולל: `assertEntityMatches` עד 15 שניות, ו-`applyCollectSince`
עד 30. יחד עם המתנת הטעינה הקיימת (עד 30) והקריאה עצמה — חצינו את תקרת
90 השניות של הרקע, והסנכרון נפל על טיימאאוט **אחרי** שכל העבודה נעשתה.

**מה השתנה:**
- `assertEntityMatches` — 15 שניות → **8**.
- `applyCollectSince` — 30 → **15**, והלחיצה החוזרת מ-i=16 ל-i=10.
  (נמדד חי: הטבלה מתמלאת תוך פחות משנייה, 30 היו נדיבות בלי סיבה.)
- `background.js` — תקציב „קריאת התנועות" **90 → 150 שניות**. שינוי מספר יחיד;
  הרחבת טווח היא עבודה אמיתית שלא הייתה קיימת כשהמספר נקבע.

**⚠ הלקח:** כל המתנה שנוספת לתוך `extract` אוכלת מתקציב הרקע. **מי שמוסיף
המתנה חייב לבדוק את הסכום**, אחרת התיקון מצליח והסנכרון נכשל.

**מצב הזיהוי כרגע — שלוש מתוך ארבע מזוהות:** 024844714 (009-2556371),
514220276 (008-3920651), 514649565 (012-5817365). ינון עדיין לא.


## 2026-08-21 · Claude · שומר זהות: לא נשמר חשבון של ישות אחרת — 1.0.17

**⚠⚠ הדיווח החמור ביותר עד כה, מטל:** „הוא סנכרן את החשבון של יובל, נתן לו שם של
ינון, ומחק את הסנכרון של טל." מהמסך: בעלים **„ע\"מ ינון-מושב עובדים"** עם
**סניף 008 · חשבון 3920651** — וזה **מספר החשבון של אביסידריס יובל**.

**השורש:** ב-`extract(id)` השם נקרא מתווית הבורר (`entityButton()`) והמספר מטקסט
הדף (`activeAccount()`), **בלי לוודא שהדף באמת התחלף.** התווית מתעדכנת לפני
הנתונים, ולכן נשמרה הצלבה: שם של ישות אחת, כסף של אחרת.
**תווית שהתחלפה אינה הוכחה שהנתונים התחלפו.**

**מה השתנה:** `assertEntityMatches(id)` רץ לפני כל קריאה. הוא בונה מפת
ישות→מספר חשבון מ-`discoveredAccounts`, ודורש: תווית הבורר תואמת לישות המבוקשת,
**וגם** מספר החשבון המוצג שווה לזה הידוע לישות — ואם אינו ידוע, שהוא לפחות
**אינו שייך לישות אחרת**. עד 15 שניות של המתנה, ואז שגיאה מפורשת עם מה נראה במקום.
החסימה נרשמת ל-`discountIdentityBlock`. **עדיף להיכשל מלשמור שקר.**

**עדיין פתוח, ולא נגעתי בו:** סנכרון של ישות אחת **מחליף** את כל חשבונות
`discount-business` השמורים, ולכן טל נמחק. זה במסלול השמירה המשותף
ב-`background.js` — קובץ שבו Codex עבד היום. **דורש תיאום לפני שינוי.**
עד אז: לסמן את כל החשבונות הרצויים באותו סנכרון, לא אחד-אחד.

**לא נבדק:** ריצה חיה של השומר. הלוגיקה נבדקה תחבירית ובנפרד (חילוץ מזהה מ-key).


## 2026-08-21 · Claude · ניתוק סשן בדיסקונט מזוהה מיד ופותח כניסה — 1.0.16

**השאלה של טל:** „אם הסשן התנתק למה הוא לא מקפיץ את דף ההתחברות, ככה הוא סתם ממתין."

**נמדד, ואומת חי בחלון הדיבוג:** כשהסשן פג דיסקונט מעביר את הלשונית ל-
`https://www.discountbank.co.il/messages/exit-page/?param=395&pernum=…` — **מארח אחר.**
`content_scripts` של דיסקונט מוגדר רק ל-`https://start.telebank.co.il/*`, ולכן
`discount-content.js` **אינו רץ שם כלל**. הרקע שולח `DISCOUNT_DISCOVER` לדף שאין בו
מאזין, ממתין 60 שניות, ואז אומר „זיהוי הישויות לא השיב". ההודעה „ההתחברות פגה"
קיימת — אבל בתוך הסקריפט שאינו רץ, ולכן לא יכולה להיאמר כשצריך אותה.

**מה השתנה (`background.js`, `runDiscoverDiscount`):** שומר סשן שרץ במקביל להמתנה
ובודק את כתובת הלשונית כל שתי שניות. ברגע שהיא עוזבת את `start.telebank.co.il`
או נוחתת על `/login/` — ההמתנה נעצרת, הלשונית מוחזרת לדף הכניסה העסקי, וההודעה
היא „ההתחברות לדיסקונט פגה — דף הכניסה נפתח מחדש". **60 שניות עיוורות → שתי שניות.**
הניווט אינו חטיפת לשונית: היא כבר יצאה מהסשן.

**אומת:** התנאי עצמו נבדק חי על שלוש כתובות — `exit-page`→עוצר, `/login/`→עוצר,
`#/OSH_LENTRIES_ALTAMIRA`→ממשיך להמתין. תחביר `background.js` נבדק מהדיסק.
**לא נבדק:** ניתוק אמיתי מקצה לקצה בזמן זיהוי.

**⚠ `background.js` הוא קובץ בבעלות יחידה ו-Codex עבד בו במקביל** (69abefe ואחריו
Revert). העריכה כאן נקודתית ומוגבלת ל-`runDiscoverDiscount`.

**למה שאר הישויות לא מזוהות — התשובה, מהקוד ומהאחסון:** הלולאה נותנת לכל ישות
**שני מעברים בלבד**, וכל מעבר טוען מחדש את הדף והורג את ה-content script.
`discountEntityReport` מראה `seenEntities:["024844714"]` — כלומר אחרי הבחירה הדף
עדיין הציג את הישות הבסיסית. `discountSelectWorked` מול `discountSelectFailed`
מראים שההצלחה תלויה בשאלה אם הבורר **באמת נפתח**: פתוח → `a.dropdown-item` קיים
ונלחץ בהצלחה; סגור → נבחרת `li.accountComboLinks` שאינה לחיצה. **התיקון הנכון הוא
לאמת פתיחה של הבורר לפני בחירת האפשרויות** — ולא לסנן מועמדים, כפי שנוסה ונכשל ב-1.0.14.


## 2026-08-21 · Claude · 1.0.14 הפיל את הזיהוי — הוחזר ב-1.0.15

**הרגרסיה:** אחרי 1.0.14 `discoveredAccounts` התרוקן ל-**0 ישויות**, ו-
`discountDiscoverError` רשם „זיהוי הישויות לא השיב תוך 60 שניות"
(15:43:04Z, `#/OSH_LENTRIES_ALTAMIRA`). לפני כן היו ארבע ישויות, אחת מהן מלאה.

**הגורם:** הסינון שהוספתי ב-`menuEntitiesLoose` — העדפת `a`/`button` על `li`/`div`.
הוא נשען על מדידה נכונה (`a.dropdown-item` הצליח, `li.accountComboLinks` נכשל),
**אבל הוסק ממנה כלל שגוי:** אותם עוגנים אינם קיימים בשלב הזיהוי, ולכן הרשימה
התרוקנה והזיהוי נתקע עד הטיימאאוט.

**⚠ הלקח:** מדידה של **שלב הבחירה** אינה תקפה לשלב **הזיהוי**. שני השלבים רואים
DOM שונה — הבורר פתוח מול סגור.

**מה השתנה:** הסינון הוסר; `menuEntitiesLoose` חזרה בדיוק ל-1.0.13. הטיפוס
בשרשרת ההורים ב-`selectEntity` — המנגנון שכן אומת — נשאר.

**מה שנשמר מ-1.0.13:** טווח התאריכים עובד ואומת חי (35→75 שורות, המוקדמת
מ-19.5.26 ל-30.12.25).

**פתוח:** רק ישות אחת מזוהה בריצה. עקיפה: לחיצה חוזרת על דיסקונט משלימה עוד.


## 2026-08-21 · Claude · הטווח עובד ואומת חי. נותר זיהוי הישויות — 1.0.14

**אומת מהאחסון, אחרי 1.0.13:**
```
discountRangeApplied = {reason:"הופעל", from:"01/01/2026", to:"21/08/2026",
  toValue:"21/08/2026", beforeRows:35, rows:75,
  earliestBefore:"2026-05-19", earliest:"2025-12-30", retried:false}
```
**35 שורות → 75. המוקדמת זזה מ-19.5.2026 ל-30.12.2025.** טל אישר: „נותן מינואר".
הבאג שנפתח ב-18.08 („תחילת איסוף נתונים אינה מרחיבה את הטווח") **סגור**.

**מה שנותר פתוח, וזה באג נפרד:** רק ישות אחת מזוהה עם מספר חשבון. השלוש האחרות
נשארות „מזהה מספר חשבון…". `discountEntityReport` מראה `passes:2`, `resolved:false`,
`seenEntities:["024844714"]` — הבחירה לא תפסה באותה ריצה.

**מדוע, לפי מדידה קודמת:** `discountSelectWorked` רשם `a.dropdown-item` (הצליח ברמה 0),
ו-`discountSelectFailed` רשם `li.accountComboLinks` (נכשל). כלומר המועמד נבחר לפי
סדר ה-DOM, ולפעמים נופל על העטיפה הלא-לחיצה.

**מה השתנה ב-1.0.14:** `menuEntitiesLoose` מעדיפה עוגנים/כפתורים
(`a`, `button`, `role=menuitem|option|button`) על פני `li`/`div`, ונופלת לשאר רק
אם אין כאלה. הטיפוס בשרשרת נשאר כרשת ביטחון.

**לא נבדק:** האם 1.0.14 מזהה את כל ארבע הישויות בריצה אחת.
**עקיפה שעובדת בינתיים:** לחיצה חוזרת על כפתור דיסקונט משלימה ישויות נוספות
(נצפה „3 מתוך 4" אחרי שתי ריצות).


## 2026-08-20 22:15 · Claude · דיסקונט: toDate לא נכתב — זה היה השורש. 1.0.13

**⚠⚠ מה שהופרך, ואסור לחזור אליו:** „דיסקונט מגביל שאילתה ל-3 חודשים" — **שגוי.**
נמדד חי דרך CDP: בקשה **01/01/2026 עד 20/08/2026** החזירה **73 שורות בכל שמונת
החודשים** — 1:7 · 2:7 · 3:10 · 4:7 · 5:14 · 6:10 · 7:11 · 8:6. שאילתה אחת מספיקה,
ואין צורך בסריקה במקטעים.

**השורש האמיתי:** `applyCollectSince` כתבה **רק `fromDate`** ולא נגעה ב-`toDate`.
האתר **זוכר את הטווח הקודם בסשן**, ולכן „מ-01/01/2026" נחתך ב-31/03/2026 שנשאר
משם — וזו בדיוק השורה שנשמרה באחסון: „ד.נ לתקופה 31/03/26-01/01/26", 230 תנועות
מינואר עד מרץ בלבד. הטווח *כן* נשלח; הוא פשוט נחסם מהצד השני.

**מה השתנה:** `to.value` נקבע להיום (`dd/mm/yyyy`) יחד עם `from`, ותנאי הדילוג
דורש עכשיו גם `to.value===today`. הדיווח כולל `to` ו-`toValue`.

**עוד ממצא מדוד:** האתר משמר את טווח החיפוש גם אחרי ניווט מחדש — הדף נפתח על
01/04–30/06 מריצה קודמת. לכן אין להסתמך על „הערך בשדה נראה נכון".

**מצב הנתונים של טל:** 230 תנועות ינואר–מרץ שמורות, לא אבד דבר. אחרי 1.0.13
הסנכרון אמור להשלים אפריל–אוגוסט.

**לא נבדק:** ריצת `applyCollectSince` המלאה מתוך הסנכרון עם שני השדות. המנגנון
(שני השדות + לחיצה) אומת חי; העטיפה לא.


## 2026-08-20 21:55 · Claude · דיסקונט: הטווח נשלח, אבל הטבלה נקראה ריקה — 1.0.11

**המדידה שסגרה את השאלה הקודמת:** `collectSince` **לא היה קיים** באחסון של המחשב הזה
(אפס מופעים) — הוא נקבע במחשב האחר, והנתונים אינם עוברים. אחרי שטל קבע ינואר 2026:
`collectSince:"2026-01-01"` נשמר, ו-`applyCollectSince` **רצה בפועל**:
`discountRangeApplied={from:"01/01/2026",at:18:47:26Z,rows:0}` וסטטוס „דיסקונט: טווח מ-01/01/2026".

**הכשל, מדויק:** `rows:0`. כלומר הטווח נשלח לאתר, אבל **הטבלה נקראה ריקה** —
היא מתרוקנת בזמן הבקשה, וההמתנה („המוקדמת השתנתה") נגמרה לפני שהתמלאה.
טל ראה יולי-אוגוסט בלבד. **זו בעיית תזמון, לא בעיית סלקטור** — הסלקטורים אומתו
חי ב-1.0.10 (69→149 שורות, מ-31.12.25).

**מה השתנה:** ההמתנה ב-`applyCollectSince` דורשת עכשיו טבלה **לא ריקה ויציבה**
בשתי דגימות רצופות (עד 30 שניות), ואם אחרי ~10 שניות היא עדיין ריקה — לחיצה שנייה
אחת על החיפוש. `discountRangeApplied` מדווח עכשיו גם `rows`, `beforeRows`,
`earliest`, `earliestBefore` ו-`retried`, כדי שהריצה הבאה תכריע בלי ניחוש.

**גם נסגר בסבב הזה:** מעבר בין ישויות עובד מקצה לקצה, כולל בסנכרון עצמו —
שלוש הצלחות ב-`a.dropdown-item`, והזיהוי עלה מ-„1 מתוך 4" ל-„3 מתוך 4"
(008-3920651, 012-5817365 נקראו). נותרה ינון-מושב עובדים.

**סשן הדיבוג בדיסקונט נותק** (`exit-page`), ולכן המדידה החוזרת של עקומת הטעינה
לא בוצעה. אם 1.0.11 ייכשל שוב — `discountRangeApplied` יגיד אם הטבלה נשארה ריקה
או שהתמלאה חלקית, וזה מפריד בין „להמתין עוד" לבין „לגלול/לבקש עמודים".


## 2026-08-20 21:40 · Claude · דיסקונט: „תחילת איסוף נתונים" נשלחת לאתר — 1.0.10

**התקלה:** טל קבע 1/1/26 וקיבל נתונים מ-21/5. `collectSince` **סינן** את מה שהאתר
החזיר ומעולם לא **ביקש** טווח — האתר החזיר את חלון ברירת המחדל שלו.

**המדידה — חיה, דרך CDP, בלי מגע של טל מלבד ההתחברות.** על
`#/OSH_LENTRIES_ALTAMIRA` נמצאו:
- `input#fromDate` — רכיב `db-datepicker`, `placeholder="dd/mm/yyyy"`, ערך **01/05/2026**
- `input#toDate` — 20/08/2026
- `button.advanced-search-btn.general-btn.no-print`

**הניסוי שהוכיח את המסלול:** הזרקת `01/01/2026` + `input/change/blur` + לחיצה על
כפתור החיפוש → הטבלה עברה מ-**69 שורות (המוקדמת 20.5.26)** ל-**149 שורות
(המוקדמת 31.12.25)** תוך **0.7 שניות**. זה גם מאשר את דיווח טל: 20-21/5 היה הגבול.

**⚠ הלקח שיחסוך סבב לבנק הבא:** `el.value=...` ישיר **אינו עובד** מול Angular —
`ngModel` לא מתעדכן והחיפוש רץ על הערך הישן. חייבים את ה-setter המקורי של
`HTMLInputElement.prototype`.

**מה השתנה (`discount-content.js` בלבד — background/dashboard לא נגעתי):**
`applyCollectSince()` נקראת מתוך `extract()` אחרי המתנת הטעינה ולפני קריאת השורות,
ורק כשלא `isPrivate`. קוראת `collectSince` מהאחסון בעצמה, ולכן **אין שינוי חוזה
ואין נגיעה בבורר החשבונות** — האזהרה של Codex אחרי רגרסיית לאומי. דף בלי הפקדים
יוצא בשקט וממשיך בסינון הקיים. התוצאה נרשמת ל-`discountRangeApplied`.

**אומת:** מסלול הפקדים — חי על האתר. תחביר — CDP + `new Function` מהקובץ בדיסק.
**לא נבדק:** הריצה מתוך הסנכרון עצמו (extract → applyCollectSince) בזרימה אמיתית.

**הצעד הבא:** ⟳ ל-1.0.10 וסנכרון דיסקונט עסקי. אקרא `discountRangeApplied` ואת
התאריך המוקדם ביותר בתנועות מהאחסון — בלי לבקש דיווח.


## 2026-08-20 21:15 · Claude · דיסקונט: הלחיצה על הישות פוגעת בטקסט, לא בפקד — 1.0.9

**התקלה שטל דיווח:** „הבורר בדיסקונט לא תקין." בדשבורד מופיעות ארבע ישויות, אבל
שלוש מהן תקועות ב„מזהה מספר חשבון…" ואי אפשר לסמן אותן.

**מה נמדד — מהאחסון, בלי לבקש מטל דבר.** `discountEntityReport` לשלוש הישויות:
`passes:2` · `resolved:false` · `seenEntities:["024844714"]` · `selectError:""` ·
`lastState` נשאר 009-2556371 („טל רצבי"). כלומר **הלחיצה מדווחת הצלחה והאתר פשוט
לא מחליף ישות.** בנוסף: `syncStatus` הראה זיהוי שרץ עד **„4 מתוך 4"** — כלומר
הספירה של הישויות תקינה, ובכך נסגרת שאלת „1 מתוך 4" שהייתה פתוחה מ-1.0.4.

**השורש:** `menuEntitiesLoose` בוחר את האלמנט **הפנימי ביותר** שמכיל מזהה בן 9 ספרות.
לזיהוי זה מספיק (צריך רק טקסט), אבל לבחירה זה טקסט ולא הפקד הלחיץ. `realClick` תקין —
הוא שולח רצף pointer/mouse מלא — **היעד היה שגוי, לא האירועים.**

**מה השתנה ב-`discount-content.js` (הקובץ היחיד שנגעתי בו):**
1. `selectEntity` מטפס בשרשרת ההורים עד 5 רמות, לוחץ ומאמת אחרי כל רמה (3 שניות),
   ועוצר בהורה שמכיל יותר ממזהה אחד — כי זו הרשימה כולה ולא האפשרות.
2. הרמה שעבדה נרשמת ל-`discountSelectWorked`, והכישלון ל-`discountSelectFailed`
   עם כל הרמות שנוסו — **הסבב הבא ילך ישר לרמה הנכונה.**
3. `selectError` הפסיק להיבלע: `.catch(()=>{})` הוחלף ב-`note(...)`.
4. ההמתנה ל-`role=menu` ירדה מ-10 שניות ל-2 — נמדד 0 בכל ריצה מאז 18.08,
   כלומר 40 שניות מבוזבזות על ארבע ישויות.

**אומת:** תחביר של ארבעת קובצי ה-JS — **דרך CDP מהסוכן עצמו**, בלי מגע של טל
(`http.server` + `new Function` על הקובץ מהדיסק). זו ההרצה הראשונה של תשתית העיניים.

**לא נבדק:** האם המעבר בין ישויות באמת קורה באתר. זה דורש ריצה חיה מול דיסקונט.
אם ייכשל שוב — `discountSelectFailed` יגיד בדיוק אילו רמות נוסו, ואין צורך לשאול.

**הצעד הבא:** טל טוען מחדש (1.0.9) ומריץ זיהוי דיסקונט; אני קורא את
`discountSelectWorked`/`discountSelectFailed` מהאחסון ומדווח בלי לבקש דיווח.


## 2026-08-20 20:30 · Claude · עיניים לסוכן: CDP על פרופיל Chrome ייעודי

**התקלה:** כל סבב דרש את טל כעיניים — הוא הריץ, צילם, ודיווח. הוא ביקש לבנות
תשתית שתחסוך את זה.

**מה נמדד (Chrome 151, הכול חי, לא סברה):**
- `websocket-client 1.9.0` הותקן בהצלחה — יש רשת ל-pip, ו-CDP אפשרי מפייתון.
- Chrome ‎136+ **מסרב** לפתוח פורט דיבוג על `User Data` הראשי → חייבים פרופיל נפרד.
- **`--load-extension` מבוטל בשקט כשקיים `--remote-debugging-port`.** הרצה בלי הפורט:
  תהליך תוסף קיים. עם הפורט: אפס תוספים ב-`Preferences`. `--enable-unsafe-extension-debugging`
  לא שינה. **אל תנסה שוב.**
- תוסף מ-`--load-extension` **אינו נשמר** ב-`Preferences` — זמני להרצה בלבד.

**מה השתנה:** `tools/cdp.py` (targets · open · nav · eval · dom · console · storage)
ו-`tools/chrome-debug.cmd`. `storage` קורא `chrome.storage.local` מתוך ה-service worker —
מחליף את טריק ה-WAL ב-`000003.log` בקריאה חיה. `tools/README-debug.md` מתעד גבולות:
אין סיסמאות, ו-`dom` דורש ניקוי לפני קומיט.

**אומת חי:** פתיחת לשונית ל-`example.com` וקריאת `title`/`h1`/`links` דרך CDP — עבד
מקצה לקצה, בלי מגע של טל.

**מה עדיין לא עובד / לא נבדק:** התוסף **טרם נטען** בפרופיל הדיבוג, ולכן `console`
ו-`storage` מול ה-service worker **לא נבדקו**. גם `fixtures/` והמגרש (harness) טרם נבנו.

**הצעד הבא:** טל טוען פעם אחת „טען פריט לא ארוז" בפרופיל הדיבוג → אז לאמת `storage`
ו-`console`, ואז לצלם fixture של דיסקונט `#/OSH_LENTRIES_ALTAMIRA` ולחזור לבאג
`setDateRange` — עם עיניים.

**מה טל צריך לבדוק:** שהחלון החדש („banks-debug-profile") נפרד ואינו נוגע בפרופיל
הראשי, ולטעון בו את התוסף פעם אחת.


## 2026-08-20 20:11 · Codex · לאומי הוחזר ל־1.0.4 אחרי רגרסיית טווח התאריך — 1.0.8

**דיווח:** אחרי ניסיון לחבר את „תחילת איסוף נתונים” ללאומי, בורר החשבונות הפסיק
להציג את החשבונות והמסלול נראה כאילו הוא מסנכרן בלי בחירה. השינוי גם גרם לטווח
`2026-06-01` להיכנס כחשבון מזויף.

**הכרעה:** כל שינויי הסבב ב־`background.js`, `dashboard.js` ו־`leumi-content.js`
הוחזרו מילולית ל־1.0.4. נשאר רק מספר גרסה **1.0.8**, כדי לזהות את ההחזרה ב־Chrome.
טל אימת חי: **„החשבונות חזרו”.** לא נמחקו נתונים שמורים.

**מה פתוח:** „תחילת איסוף נתונים” עדיין מסננת בלבד ואינה מרחיבה את טווח הבקשה
בלאומי או בדיסקונט. לא לגעת שוב בבורר כדי לפתור זאת; המימוש הבא חייב להיות מבודד
אחרי אישור הבחירה, עם בדיקה חיה שלא השתנתה רשימת החשבונות ושלא מתחיל סנכרון ללא אישור.

## 2026-08-18 23:30 · Claude · פתוח: „תחילת איסוף נתונים" אינה מחזירה 3 חודשים בדיסקונט

**דיווח מטל, לרישום מפורש:** „לא עובד השינוי תאריך בדיסקונט עסקים — הסנכרון הצליח,
והחזיר נתונים של 3 חודשים." כלומר הזיהוי והסנכרון עצמם עבדו הפעם, אבל הגבול
שנקבע ב„תחילת איסוף נתונים" **לא הרחיב את הטווח**: דיסקונט החזיר את חלון ברירת
המחדל שלו (~3 חודשים) והגבול רק סינן.

**זה תואם למה שנרשם ב-20:10 (0.98.0) ומאשר אותו במדידה שנייה:** `collectSince`
**מסנן** את מה שהאתר החזיר, ו**אינו מבקש** טווח תאריכים מהאתר. אין בקוד שום מקום
שמפעיל בורר תאריכים באף בנק. **אין כאן באג חדש — יש פיצ'ר שלא מומש.**

**מה חסר, בדיוק:**
1. חוזה משותף — `setDateRange(from)` אופציונלי לכל מודול בנק; הסנכרון קורא לו עם
   `collectSince` לפני קריאת התנועות. בנק שלא מימש → נופל לסינון הקיים, בלי רגרסיה.
2. מימוש ראשון לדיסקונט עסקי: להפעיל את בורר טווח התאריכים בדף
   `#/OSH_LENTRIES_ALTAMIRA`. **אין לנחש סלקטור.** הגשש כבר יודע לצלם פקדי תאריך
   (`dateControls` ב-0.99.0: inputs/selects/triggers/labels + path), ו„מדוד לשונית
   פעילה" תוקן ב-0.99.1. **`bankProbe` עדיין אפס כתיבות — המדידה הזו לא בוצעה.**
3. אחריו: אותו מימוש לכל בנק בנפרד. **מחיר חד-פעמי לבנק, לא לכל תאריך** —
   שינוי הגבול עצמו (ינואר → פברואר) הוא בחירה בבורר, בלי קוד.

**גם פתוח, מאותו סבב:** `discountEntityReport` — האם אחרי 1.0.4 מספרי החשבון
נטענים לכל ארבע הישויות, או רק לפעילה („1 מתוך 4" הקודם).
וכן: `selectedAccountKeys` נצפה כמערך ריק באחסון, ולא נחקר מי כותב אותו.

**מצב הקוד בסוף הסבב:** `main`, גרסה **1.0.4** על הדיסק, עץ עבודה נקי, הכול נדחף
ל-`origin`. ⚠ **טל ממשיך ממחשב אחר:** הקוד עובר ב-git, **הנתונים לא** —
`chrome.storage.local` ו-IndexedDB מקומיים לכל מחשב ולכל מזהה תוסף (§7 ב-AGENTS).
במחשב החדש: `git pull`, טעינת `extension/` כ-unpacked, ואז **סנכרון מאפס** —
ולכן גם „תחילת איסוף נתונים" צריך להיקבע שם מחדש.

## 2026-08-18 23:10 · Claude · הבורר נפתח, האפשרויות אינן ב-role=menu — 1.0.4

**דיווח:** „טענתי, לא עובד, לא נפתחת רשימת החשבונות לסנכרון. חזרנו אחורה. מחקתי את
החשבון ומנסה לסנכרן מחדש, גם זה לא עובד."

**המכשור מ-1.0.3 עבד, וזו הפעם הראשונה שיש מדידה של הכשל עצמו:**
```
discountDiscoverError = {at:"2026-08-18T17:11:37Z",
  message:"לא זוהו ישויות בחיבור דיסקונט עסקי",
  url:"…/apollo/business2/#/OSH_LENTRIES_ALTAMIRA"}
```
וגם פעימת החיים („ממתין לתשובת הדף (5/10/15 שנ')") וההודעה „זיהוי כבר מתבצע"
הופיעו — כלומר 1.0.3 באמת רץ.

**⚠ וה-`discountProbe` של אותה ריצה מכריע את השורש, בלי השערות:**

| שדה | ערך |
|---|---|
| `trigger` | „024844714 טל רצבי" — **הבורר נמצא** |
| `expanded` | `"true"` — **הבורר נפתח** |
| `menus` · `listboxes` · `menuItems` | **0 · 0 · []** |
| `nineDigit` | 024844714 · 514220276 · 514649565 · 570012930 · 000271119 |
| `head` | מכיל את כל חמש הישויות בטקסט הדף |

כלומר **דיסקונט מרנדר את רשימת הישויות בלי `role="menu"` ובלי `role="listbox"`**,
ו-`menuEntities()` — שמחפש רק בתוכם — החזיר ריק. הבורר עבד, הרשימה הייתה על המסך,
והקוד לא ראה אותה. זה גם מסביר את „1 מתוך 4": הישות הפעילה נקראה מהכותרת, וכל
השאר לא זוהו כלל.

**השתנה (1.0.4):** `menuEntitiesLoose()` — נפילה לאחור **אחרי** שהמסלול המקורי
נכשל: כל אלמנט לחיץ שמכיל מזהה בן 9 ספרות, שאינו הבורר עצמו ואינו עוטף מועמד אחר
(הפנימי ביותר בלבד), עד 90 תווים. המסלול המקורי נשאר ראשון ולכן שום אתר שכן מרנדר
`role=menu` אינו נפגע.

**אומת בהרצת `entityOptions`/`menuEntitiesLoose` מהקובץ עצמו**, על DOM שמשחזר את
המדידה (אפשרויות מחוץ ל-`role=menu`): **4 ישויות** — 024844714 · 514220276 ·
514649565 · 570012930 — בדיוק הארבע שטל אמר שיש. הבורר עצמו לא נכפל, והמזהה
בן 10 הספרות (האתר הפרטי) לא נכנס.

**לא נבדק:** ריצה חיה. **הצעד הבא:** ⟳ ל-1.0.4, לחיצה אחת על „דיסקונט עסקי" —
אמורות להופיע 4 ישויות בבורר החשבונות. אם כן, `discountEntityReport` מ-1.0.1
ייכתב סוף סוף, ומשם נדע אם מספרי החשבון נטענים לכל ישות.

## 2026-08-18 22:45 · Claude · הזיהוי בדיסקונט נכשל בשקט — כשל שאין לו עקבות, 1.0.3

**דיווח:** „טענתי, לחצתי, תבדוק — לא עובד."

**נמדד (`000003.log`, 1.74MB, נכתב דקה לפני הקריאה):**
- `discountEntityReport` — **אפס כתיבות**, שוב.
- הסטטוס האחרון: „דיסקונט עסקי: מזהה ישויות וחשבונות" ו**אחריו כלום**.
- אף אחת מההודעות החדשות של 1.0.2 („זיהוי כבר מתבצע" / „נסה שוב בעוד N שניות")
  לא הופיעה — כלומר הלחיצה **לא** נבלמה בשומרים; היא נכנסה ומעולם לא חזרה.

**⚠ השורש הוא חוסר עקבות, לא רק כשל.** `startDiscountBusiness` קורא ל-
`discoverDiscountBusiness`, וזה ל-`runDiscoverDiscount`, ששולח `DISCOUNT_DISCOVER`
עם המתנה של **120 שניות בלי אף כתיבת סטטוס**. אם הדף לא עונה, הכשל חוזר רק
ב-`reply` — כלומר **טוסט חולף בדשבורד** — ולא נכתב לשום מקום. לכן „תקוע" ו„נכשל"
נראו זהים לחלוטין, וגם אני לא יכולתי למדוד מה קרה.

**השתנה (1.0.3) — הכל לטובת עקבות:**
- כל כשל בזיהוי נכתב ל-`syncStatus` **ול-`discountDiscoverError`** באחסון, כולל
  **כתובת הלשונית** שבה נעשה הניסיון ו-timestamp. זו המדידה שתכריע אם הלשונית
  בכלל הייתה בדף הנכון.
- פעימת חיים כל 5 שניות: „ממתין לתשובת הדף (N שנ' מתוך 60)".
- התקרה 120 → **60 שניות**.

**נבדק:** תחביר `background.js` מהדיסק, `manifest` 1.0.3, אין תו backspace.
**לא נבדק:** ריצה חיה.

**הצעד הבא:** ⟳, לחיצה אחת על „דיסקונט עסקי", ולראות מה כתוב בסטטוס. משם:
- „ממתין לתשובת הדף (…)" ואחריו כשל → **הדף אינו מגיב** (content script או דף לא נכון),
  וכתובת הלשונית ב-`discountDiscoverError` תגיד מי מהם.
- כשל אחר → ההודעה עצמה כבר על המסך.

## 2026-08-18 22:20 · Claude · שתי יציאות שקטות, ומסר שספר את כל מה ששמור — 1.0.2

**דיווח:** „לא עושה כלום דיסקונט עסקים, מאיפה מצא 10 חשבונות? יש רק 4 ואני ביקשתי
לסנכרן אחד."

**נמדד באחסון, ושתי השאלות נענות בנפרד:**

**(1) „לא עושה כלום" — `discountEntityReport` אפס כתיבות.** המכשור מ-1.0.1 לא רשם
דבר, כלומר הזיהוי **לא הגיע ללולאת הישויות בכלל**. ב-`discoverDiscountBusiness`
היו שתי יציאות **שקטות לחלוטין**: `if(discountBusy)return` ו-`if(cooldown)return`.
לחיצה בזמן שהזיהוי הקודם עוד תלוי — ו-`DISCOUNT_DISCOVER` ממתין **עד 120 שניות** —
נבלעה בלי סימן על המסך. בלוג נראה בדיוק זה: „מזהה ישויות וחשבונות" חוזר שוב ושוב,
„עוצר את הסנכרון" בין לבין, ואף פעם לא צעד הבא.

**⚠ ולחיצה על „עצור" לא שיחררה את זה** — היא לא קטעה את ההמתנה הארוכה, ולכן
`discountBusy` נשאר דלוק וכל לחיצה נוספת נבלעה גם היא. זו הייתה לולאה סגורה
של „לא קורה כלום".

**(2) „10 חשבונות" — המסר ספר את כל מה ששמור.** `baseStatus` השתמש ב-`marked.length`,
שהוא **כל** מערך החשבונות בכל הבנקים. באחסון יש בדיוק 10 רשומות (2 פועלים · לאומי ·
יהב · פרטי · 2 בינלאומי · מזרחי · 2 דיסקונט), ולכן „הסתיים בהצלחה: 10 חשבונות"
נראה כאילו סונכרנו עשרה. **טל סנכרן אחד, והמסר שיקר לו.**

**השתנה (1.0.2):**
- שתי היציאות השקטות מדווחות: „זיהוי כבר מתבצע — המתן או לחץ עצור" ו„זיהוי רץ
  לפני רגע — נסה שוב בעוד N שניות".
- `raceAbort(promise)` — עוטף המתנה ארוכה ובודק את דגל העצירה כל 400ms.
  `DISCOUNT_DISCOVER` עטוף בו, ולכן „עצור" משחרר את `discountBusy` תוך פחות משנייה.
- המסר מפריד: „הסתיים בהצלחה: **סונכרנו 1 חשבון** · סך הכל שמורים 10".

**אומת בהרצת `raceAbort` מהקובץ עצמו:** בלי עצירה — הבטחה של 1200ms הסתיימה
כרגיל (1201ms); עם עצירה — נדחתה תוך **415ms** בהודעה „הסנכרון נעצר לבקשתך".
תחביר תקין, `manifest` 1.0.2.

**⚠ עדיין לא ידוע:** למה שלוש ישויות מארבע אינן מקבלות מספר חשבון. המכשור קיים
ומחכה לריצה שתגיע אליו — עכשיו, כשהיציאות השקטות מדברות, אפשר לדעת אם הריצה בכלל
מתחילה. **הצעד הבא:** לחיצה **אחת** על „דיסקונט עסקי", להמתין שהסטטוס יזוז,
ואז לקרוא `discountEntityReport`.

**⚠ נצפה ולא נחקר:** `selectedAccountKeys` באחסון הוא **מערך ריק**. אם הוא ריק בזמן
„סנכרן", אין מה לסנכרן. לא ידוע מי כתב אותו — הדשבורד („נקה הכול") או מסלול אחר.

## 2026-08-18 21:55 · Claude · דיסקונט: רק ישות אחת מארבע מזוהה — 1.0.1

**דיווח:** „את ההוראות בשלושה צעדים שיהיה מוסתר בדיפולט, דיסקונט עסקים לא עובד הסנכרון."

**ההוראות:** `<details class="panel guide" open>` ב-`dashboard.html` — הוסר `open`.
מקופל כברירת מחדל, נפתח בלחיצה.

**דיסקונט — נמדד באחסון, והסנכרון עצמו דווקא תקין:**
„קורא ישות **1 מתוך 1**" → „הסתיים בהצלחה: 10 חשבונות · 2 תנועות חדשות",
ולפני זה „דיסקונט עסקי: **1 מתוך 4 חשבונות זוהו** — אביסידריס יובל בע\"מ…".
כלומר **שלוש ישויות מארבע לא מקבלות מספר חשבון, ולכן אינן נבחרות ואינן מסונכרנות.**
מה שנראה כמו „הסנכרון לא עובד" הוא בעצם סנכרון שמכסה רבע מהחשבונות.

**⚠ למה השורש לא היה ידוע: שני כשלים שקטים באותה לולאה.** `DISCOUNT_SELECT_ENTITY`
עטוף ב-`catch(e){}` ריק, ולולאת ההמתנה מסתיימת בלי לספר מה ראתה. אין ראיה אם
המעבר לישות נכשל, או שהוא הצליח והדף לא הציג מספר חשבון.

**השתנה (1.0.1) — מכשור, לא ניחוש:** נאסף `discountEntityReport` — לכל ישות:
`selectError` (השגיאה שנבלעה), `passes`, `lastState` (ה-`DISCOUNT_STATE` האחרון
שנראה: entity/branch/accountNumber), `seenEntities` (לאילו ישויות הדף באמת עבר),
ו-`resolved`. **הריצה הבאה תגיד בעצמה מה נשבר**, בלי שטל ילחץ על שום דבר.

**⚠ באג שגררתי ב-1.0.0 ותוקן כאן:** דגל העצירה נוקה רק ב-`beginProgress` ובסיום
`syncSelected`. אחרי לחיצה על „עצור", מסלול בלי גלגל — כמו זיהוי דיסקונט — נעצר
מיד בפעם הבאה, לנצח. **עכשיו כל בקשה חדשה של המשתמש** (`START_*`, `SYNC_SELECTED`,
`LOAD_CARD_*`) מנקה את הדגל. נמדד בהרצת השורה מהקובץ: `START_DISCOUNT_BUSINESS`
ו-`SYNC_SELECTED` מנקים · `ABORT_SYNC` ו-`DISCOUNT_AUTHENTICATED` **אינם** מנקים.
נוסף גם `abortIfRequested` בפתח `runDiscoverDiscount`, כדי שעצירה תיתפס גם שם.

**נבדק:** תחביר `background.js` מהדיסק, `manifest` 1.0.1, ההוראות מקופלות, ואין
תו backspace בקובץ. **לא נבדק:** ריצה חיה.

**הצעד הבא:** טל טוען 1.0.1 ולוחץ „דיסקונט עסקי" פעם אחת. אני קורא את
`discountEntityReport` מה-LevelDB — הוא יכריע בין „המעבר לישות נכשל" לבין
„המעבר הצליח והמספר לא מוצג", וזה קובע איזה תיקון נכון.

## 2026-08-18 21:30 · Claude · „עצור סנכרון" ליד הגלגל — 1.0.0

**בקשה:** „תעשה ליד הגלגל כפתור עצור סנכרון."

**נוסף:** כפתור `#stopSync` בתוך אריח הגלגל, שולח `ABORT_SYNC` לרקע.

**איך העצירה עובדת — וזה העיקר: אין הרג באמצע כתיבה.** הדגל `abortFlag` נבדק
**לפני הצעד הבא**, בארבע נקודות שכל הלולאות עוברות בהן:
1. `syncStep` — כלומר כל צעד של סנכרון בנק וכל כרטיס בישראכרט.
2. לולאת חודשי ישראכרט (לפני כל כרטיס×חודש).
3. אצוות צילומי השיקים בלאומי (לפני כל אצווה).
4. לולאת הישויות בדיסקונט.
לכן **מה שנשמר עד רגע העצירה נשאר שמור** — חודש שהושלם, שיק שצולם, חשבון שנקרא.

- `beginProgress` מאפס את הדגל, ולכן ריצה חדשה אינה יורשת עצירה קודמת.
- הסיום כותב „הסנכרון נעצר לבקשתך — מה שנקרא עד כאן נשמר" ולא „שגיאה: …".
- הכפתור בדשבורד עובר ל„עוצר…" ומודיע שהעצירה תתפוס בסוף הצעד הנוכחי.

**אומת בהרצת `abortIfRequested` מהקובץ עצמו:** לפני הדגל אינו זורק, אחרי הדגל
זורק `הסנכרון נעצר לבקשתך`. ארבע נקודות העצירה נמצאו בקובץ, האיפוס ב-`beginProgress`
קיים, והכפתור שולח `ABORT_SYNC`. תחביר שני הקבצים תקין, `manifest` 1.0.0.

**⚠ מה שהעצירה אינה עושה, במפורש:** היא אינה קוטעת פעולה שכבר בדרך —
`readIsracardCardMonth` באמצע, או אצווה של שיקים שהדף כבר מעבד, יסתיימו (עד
כמה עשרות שניות). היא גם אינה סוגרת לשוניות שנפתחו; זה נשאר ל-`restoreSyncTabs`.

**לא נבדק:** ריצה חיה. הבדיקה: להתחיל טעינת שנה ולעצור באמצע — הסטטוס אמור לומר
„נעצר לבקשתך", והחודשים שהושלמו אמורים להישאר בהיסטוריה.

## 2026-08-18 21:05 · Claude · המדידה נתקעה בלי טיימאאוט ובלי סימן — 0.99.1

**דיווח:** „מדדתי תבדוק" ומיד אחריו „זה תקוע".

**נמדד:** `bankProbe` **אינו קיים באחסון כלל** (אפס כתיבות) — כלומר
`probeActiveTab` מעולם לא הגיע לסוף. שני חסרים הפכו את זה ל„תקוע" ולא ל„נכשל":
`chrome.scripting.executeScript` ו-`chrome.tabs.sendMessage` **בלי `withTimeout`**,
והכפתור בדשבורד ממתין ל-`sendMessage` בלי גבול זמן — ולכן נשאר „מודד…" לנצח.

**⚠ וחשד שני, מבני:** `lastAccessed` אינו קיים בכל גרסאות Chrome. כשהוא חסר,
המיון שהוספתי ב-0.99.0 שרירותי, והמדידה יכולה ליפול על לשונית זרה לגמרי
(ואז ההזרקה נכשלת או תלויה).

**השתנה (0.99.1):**
- בחירת הלשונית לפי **מארחי הבנקים מ-host_permissions** (`BANK_HOST`) בעדיפות
  ראשונה, ורק בתוכם לפי `lastAccessed`/`active`. לשונית שאינה בנק → שגיאה מפורשת
  עם שם המארח, במקום ניסיון מדידה.
- `withTimeout` על ההזרקה (15 שנ') ועל המדידה (20 שנ').
- `syncStatus` נכתב **לפני** ההזרקה: „מודד את start.telebank.co.il…" — כך גם
  תקיעה מלמדת מה נבחר.
- בדשבורד: `Promise.race` עם 30 שניות, והכפתור חוזר לעצמו עם הודעת שגיאה.

**אומת בהרצת `BANK_HOST` מהקובץ:** telebank ✓ · bankleumi ✓ · isracard ✓ ·
mail.google.com ✗. תחביר שני הקבצים תקין, `manifest` 0.99.1, ואין תו backspace
בקובץ (הבדיקה שנוספה אחרי התקלה של 20:40).

**⚠ לא נבדק חי, וזה עדיין הפתוח:** למה ההזרקה לא חזרה. אם גם 0.99.1 נתקע, ההודעה
„מודד את X…" תגיד על איזו לשונית, והשגיאה תגיד אם זו ההזרקה או המדידה — ובשלב הזה
המדידה הבאה היא `chrome://extensions` → „שגיאות" של התוסף.

**שאלת זמנים שנשאלה במקביל („כמה זמן צריך להיות סנכרון של בנק"), נמדד מהאחסון:**
פועלים עסקי, 4 צעדים, 9 חשבונות — `syncProgress.startedAt` 12:58:22Z מול
`lastSync` 12:59:00.472Z = **38 שניות**. זה הרף. מה שמנפח: לולאות פר-פריט —
32 שיקים (עד 8 שנ' לשיק), 96 צעדי כרטיס×חודש (~5.6 שנ' שהיות קבועות לכל אחד),
וישויות דיסקונט (עד ~1.5 דק' לישות אחרי 0.96.0).

## 2026-08-18 20:40 · Claude · „מדוד לשונית פעילה" מדד את הדשבורד, והגשש לא ראה תאריכים — 0.99.0

**הקשר:** טל שאל „אם נרצה פברואר, עוד פעם נעבור בנק בנק?" התשובה: התאריך גלובלי
ומשתנה בלחיצה; מה שפר-בנק הוא **היכולת לבקש טווח** — פעם אחת לבנק, לא לכל תאריך.
כדי לממש אותה בדיסקונט צריך מדידה, וטל אמר „אני בתוך דיסקונט".

**⚠ שני חסמים שנמצאו לפני שהמדידה בכלל התאפשרה:**

1. **`probeActiveTab` שאל `{active:true,currentWindow:true}`** — הלחיצה מתבצעת
   בחלון הדשבורד, ולכן „הלשונית הפעילה" **היא הדשבורד**, וההודעה שהתקבלה תמיד
   הייתה „הלשונית הפעילה אינה דף בנק (chrome-extension://…)". הכפתור לא היה יכול
   לעבוד אף פעם למטרה הזו. **תוקן:** הלשונית ה-https הפעילה שנצפתה לאחרונה
   (`{active:true}` בכל החלונות, מיון לפי `lastAccessed`).
2. **הגשש לא צילם פקדי תאריך בכלל.** `pickers()` מסנן לפי „חשבון/בחירה", ולכן בורר
   התאריכים נעלם מהמדידה. **נוסף `dateControls()`** — שדות `input` (כולל
   `type=date` וכל id/name/placeholder/aria שנראה כמו תקופה), כל ה-`select` עם עד
   14 אפשרויות, כפתורים ותוויות של תקופה, וה-`path` של כל אחד. קריאה בלבד.

**⚠ תקלה שנוצרה ותוקנה באותו סבב, ושווה זכירה:** כתיבת `\b` לקובץ דרך
`python -c` בתוך מעטפת **הפכה ל-תו backspace אמיתי (0x08)** בקובץ, והביטוי
`RANGE` לא התאים לשום דבר באנגלית. נמדד בקודי התווים, לא בעין: `[…,124,8,40,…]`.
**מסקנה מעשית: קוד עם escape כמו `\b` או `\d` — לכתוב דרך קובץ סקריפט
(Write) ולא דרך `python -c` במעטפת**, ולאמת בקודי תווים.

**אומת בהרצת `dateControls` מהקובץ עצמו** על דף בדיקה עם `fromDate`(text),
`toDate`(date), `select#period` וכפתור „טווח תאריכים": מחזיר את שני השדות, הבורר,
הכפתור והתווית „מתאריך" — ואינו קוצר רעש (`cheque-store.js` נפסל אחרי שהמילים
האנגליות קיבלו גבולות מילה, ו-camelCase מפוצל לפני ההתאמה).

**הצעד הבא:** טל טוען 0.99.0, נשאר על דף התנועות של דיסקונט עסקי, ולוחץ „מדוד
לשונית פעילה" בדשבורד. המדידה נשמרת ל-`bankProbe`, ומשם ייכתב `setDateRange`
לדיסקונט — עם החוזה: בנק שמימש מוריד מהתאריך, בנק שלא נופל לסינון הקיים.

## 2026-08-18 20:10 · Claude · יתרת דיסקונט עסקי — השורה הראשונה של היום ניצחה, 0.98.0

**דיווח:** „לא מוריד לפי הבקשה גורפת והיתרה לא נכונה. דיסקונט עסקים."

**שני דברים נפרדים, ורק אחד מהם באג.**

**(1) היתרה — באג ודאי, ונמצא במדידה.** `currentBalance()` בדיסקונט נופל לשורות
התנועות וממיין `sort((a,b)=>stamp(b.date)-stamp(a.date))[0]`. **המיון לפי תאריך
בלבד, ו-sort יציב** — ולכן בתאריך שיש בו כמה תנועות ניצחה **הראשונה**.
אצל טל, 17/08/2026:

| שורה | יתרה מצטברת |
|---|---|
| העברה 250,000- | **267,664.63-** ← זו נשמרה |
| הקמת הלוואה 300,000+ | 32,335.37 |
| ע. אשראי ובטחונות 1,350- | **30,985.37** ← זו הנכונה |

והיתרה שנשמרה באחסון הייתה בדיוק `-267664.63`. **החפיפה מדויקת.**

**השתנה (0.98.0):** נוסף `latestRowBalance(rows)` — התאריך המאוחר ביותר, ובתוכו
**המופע האחרון לפי סדר הטבלה** (הטבלה עולה מהישן לחדש; כך גם נשמר מערך התנועות).

**אומת בהרצת הפונקציה מהקובץ עצמו על התנועות האמיתיות מהאחסון:** מחזירה
`30985.37` במקום `-267664.63`; תאריך יחיד, קלט לא ממוין ורשימה ריקה — כולם תקינים.

**(2) „לא מוריד לפי הבקשה גורפת" — אינו באג אלא גבול של מה שמימשתי, ואני מתקן
את מה שאמרתי ב-0.95.0.** `collectSince` **מסנן** את מה שהאתר החזיר; הוא **אינו
מרחיב** את חלון השליפה. נמדד: `collectSince="2026-01-01"`, והתנועות שנשמרו
מדיסקונט הן 05/2026 (5) · 06/2026 (27) · 07/2026 (63) · 08/2026 (29) — כלומר
**האתר עצמו החזיר רק מחודש מאי**. אין בקוד שום מקום שמבקש טווח תאריכים.

**הצעד הבא, וזו מדידה ולא כתיבה:** כדי למשוך אחורה עד ינואר צריך להפעיל את בורר
טווח התאריכים בדף התנועות של דיסקונט. **אל תנחש סלקטור** — טל ילחץ „מדוד לשונית
פעילה" בדשבורד כשהוא עומד על דף התנועות, זה נשמר ב-`discountProbe`, ומשם בונים
את הבחירה. אותו הדבר יידרש בנפרד לכל בנק.

## 2026-08-18 19:45 · Claude · תחילת האיסוף בעברית, ומשפט שאומר ממתי — 0.97.0

**דיווח:** „לא אהבתי את האיסוף נתונים, תעשה שיהיה ברור ממתי אחרי שבוחרים, ותעשה בעברית."

**מה היה לא בסדר:** `<input type="date">` מוצג בפורמט של **לוקאל הדפדפן** — לרוב
`mm/dd/yyyy` ולוח שנה באנגלית — ואחרי הבחירה שום דבר במסך לא אמר ממתי בעצם ייאסף.

**השתנה (0.97.0):**
- שני בוררים בעברית: חודש (ינואר…דצמבר, עם „— ללא הגבלה —") ושנה (חמש שנים אחורה).
  בורר השנה מנוטרל כל עוד לא נבחר חודש.
- **משפט מצב מתחת לבוררים, שמתעדכן מיד בבחירה:**
  „הנתונים ייאספו מ-1 בינואר 2026 ואילך. תנועות, שיקים וחודשי כרטיס שקודמים
  לתאריך זה לא ייאספו." · ובלי גבול: „לא נקבע גבול: נאסף כל מה שהבנק מציע."
- הערך נשמר כ-`YYYY-MM-01`, ולכן **אכיפת ה-background לא השתנתה כלל** —
  הגרעיניות האמיתית של האיסוף היא חודש ולא יום.

**אומת בהרצת `collectSinceSentence` ו-`collectSinceControls` מהקובץ עצמו:**
`2026-01-01` → „מ-1 בינואר 2026 ואילך" · `2025-11-01` → „מ-1 בנובמבר 2025" ·
ריק → „לא נקבע גבול" · הבורר מסמן „ינואר" כנבחר · בורר השנה `disabled` ללא גבול.

## 2026-08-18 19:25 · Claude · דיסקונט „לא מסיים" — הזיהוי המשיך אחרי שהסנכרון נגמר, 0.96.0

**דיווח:** „דיסקונט לא מסיים את הסנכרון? מצא 6 תנועות חדשות ולא מפסיק."

**נמדד באחסון — סדר הסטטוסים הוא כל התשובה:**
```
דיסקונט עסקי: מזהה מספר חשבון 1 מתוך 4
דיסקונט עסקי: מזהה מספר חשבון 2 מתוך 4
… קורא ישות · פותח תנועות · קורא מסגרת אשראי …
הסתיים בהצלחה: 10 חשבונות · 6 תנועות חדשות     ← הסנכרון נגמר כאן
דיסקונט עסקי: מזהה מספר חשבון 3 מתוך 4          ← וזה נמשך אחריו
דיסקונט עסקי: מזהה מספר חשבון 4 מתוך 4          ← ונתקע
```
**הסנכרון כן הסתיים.** מה שלא הסתיים הוא `discoverDiscountBusiness` שרץ במקביל
והמשיך לכתוב `syncStatus` אחריו — ולכן המסך נראה כאילו הסנכרון לא נגמר.
`pendingDiscountBusiness` נשאר **true** ולא נסגר, ולכן גם כל כניסה הבאה לדיסקונט
הייתה מפעילה זיהוי מחדש.

**החשבון שמסביר את התקיעה:** לכל ישות בלי מספר חשבון — 2 מעברים × 20 סבבים ×
(שנייה + טיימאאוט 10 שניות) = **עד ~7 דקות לישות אחת**, ובארבע ישויות זה רבע שעה.
ובסוף, ישות אחת שלא נטענה **זרקה את כל הריצה** והשאירה את הדגל דלוק.

**השתנה (0.96.0):**
- **סנכרון שהמשתמש סיים סוגר את דגל הזיהוי** (`pendingDiscountBusiness/Private`)
  — אין טעם לזהות חשבונות שכבר סונכרנו.
- **הלולאה בודקת את הדגל בכל ישות ויוצאת באמצע**, כך שהסגירה תופסת גם בריצה חיה.
- תקציב ההמתנה לישות: 20 סבבים → **12**, וטיימאאוט 10 → **6 שניות**.
- **סיום כן במקום זריקה:** מה שזוהה מוצג ונשמר, הדגל נסגר, והחסר נאמר במפורש
  („3 מתוך 4 זוהו — X טרם נטען. בחר מה שיש, או לחץ שוב"). רק **אפס** זיהויים
  נחשב כישלון.

**נבדק:** תחביר `background.js` מהדיסק + `manifest.json` כ-0.96.0.
**לא נבדק:** ריצה חיה. ⚠ **לא נחקר:** *למה* מספר החשבון של ישות מסוימת אינו
נטען. הגבולות החדשים מונעים תקיעה, אך אינם מרפאים את הישות שלא עונה — המדידה
הבאה היא `DISCOUNT_STATE` על אותה ישות בזמן שהיא נתקעת.

## 2026-08-18 19:00 · Claude · „תחילת איסוף נתונים" — הגדרה גלובלית אחת, 0.95.0

**בקשה:** „להוסיף בכניסה הגדרה כוללת תחילת איסוף נתונים לכל הבנקים בלבד. אם הבורר
יסומן ב-1/1/26 יש לאסוף מנקודה זו."

**נוסף (0.95.0):** מפתח אחד — `collectSince` (`YYYY-MM-DD`) ב-`chrome.storage.local`,
ושדה תאריך בראש פאנל „הבנקים וכרטיסי האשראי שלי". **גלובלי, לא פר-בנק**, כפי שנתבקש.

**איפה הוא באמת נאכף — ארבע נקודות, לא אחת:**
1. `applyCollectSince` על מערך החשבונות ב-`syncSelected`, לפני השמירה — מסנן
   `transactions` של החשבון וגם `cards[].transactions`. מכסה פועלים, לאומי,
   דיסקונט ומזרחי במסלול אחד.
2. קציר השיקים — שיק שתאריכו קודם לגבול אינו נכנס ל-`wanted`, ולכן גם לא נסרק.
3. `loadIsracardYear` — חודש שקודם לגבול **אינו נפתח בכלל**; אם כל 12 קודמים,
   הריצה נגמרת מיד. זה גם חוסך זמן, ונוגע ישירות לתלונת האיטיות מהיום.
4. `lastTwelveMonths` בדשבורד — הבורר אינו מציע חודשים שלא ייאספו.

**⚠ תאריך שלא ניתן לפענוח נשמר, ולא נמחק.** עדיף רשומה מסופקת מאשר מחיקה שקטה.

**אומת בהרצת הפונקציות מהקובץ עצמו** (`fetch` + `no-store`, לא לוגיקה מקבילה):
`txDateMs('31.07.2025')`→2025-07-31 · `'10/08/26'`→2026-08-10 ·
`keepSince('02.08.2026',1.1.26)`→true · `keepSince('15.11.2025',…)`→false ·
תאריך פגום→true · בלי גבול→true.
`lastTwelveMonths` עם גבול 2026-01-01 מחזיר בדיוק 012026…082026 (שמונה חודשים).

**⚠ לא מכוסה, ובמפורש:** ישראכרט, כאל ו-MAX שומרים `accounts` במסלולי שמירה
נפרדים משלהם (`runIsracard`, `runCal`, `runMax`), ו-`applyCollectSince` **אינו**
רץ שם. עסקאות כרטיס בודדות מחודש קודם לגבול עדיין עלולות להישמר דרך המסלולים
האלה. **זה הצעד הבא אם טל ירצה כיסוי מלא** — עדיף בנקודה אחת משותפת לשמירת
accounts ולא בעוד שלוש עריכות מקבילות.

**לא נבדק:** ריצה חיה עם גבול מוגדר.

## 2026-08-18 18:35 · Claude · הלוואת לאומי בלי „תשלומים שנותרו" — 0.94.0

**דיווח:** „גם ההלוואה בלאומי היא ללא תשלומים שנותרו."

**נמדד באחסון, בהשוואה בין שני הבנקים:**
- פועלים: `"installments":"8/71"` — ומכאן העמודה מחושבת.
- לאומי: `{balance, endDate:"20.08.2032", interest, nextPayment, nextPaymentDate:"20.08.2026", originalPrincipal, startDate:"31.07.2025", type}` — **אין `installments` בכלל**, וגם לא `remainingInstallments`/`totalInstallments`.

לכן `remaining()` בדשבורד נפל ל-`—`. **זו אינה תקלת רינדור אלא שדה שלאומי לא מוסר.**

**השתנה (0.94.0):** נוספה נפילה לאחור בשתי המופעים של `remaining()` (יש שניים —
בטבלת ההלוואות ובתצוגת החשבון; שניהם עודכנו): כשאין מספר תשלומים אך יש תאריך
התחלה, תאריך סיום ותאריך תשלום הבא — המספר נגזר מהפרשי החודשים. **מסומן ב-`~`**
כדי שלא ייקרא כנתון שהבנק מסר.

**אומת בהרצת הפונקציה מהקובץ עצמו** (`fetch` + `no-store`, ולא לוגיקה מקבילה):
- לאומי → `~73/86` (31.07.25 עד 20.08.32 = 86 תשלומים; מ-20.08.26 נותרו 73).
- פועלים „8/71" → `63/71` — **לא נשבר**.
- רשומה ריקה או עם תאריך סיום בלבד → `—`.

**⚠ הנחה מפורשת:** תשלום חודשי אחיד. הלוואת גרייס, בלון או תשלום דו-חודשי יראו
מספר שגוי. אם יופיע `~` שנראה לא נכון — זו הסיבה, והפתרון האמיתי הוא לחלץ את
מספר התשלומים מדף ההלוואות של לאומי, מה שדורש מדידת DOM חיה.

## 2026-08-18 18:20 · Claude · 14 צילומי שיקים אבדו באצווה שנתקעה — 0.93.0

**דיווח:** „לאומי סינכרן, חלק מהשיקים הם לינק ולא צילום… זה עבד, היה לינק,
טיפלנו בזה ועכשיו זה חזר."

**נמדד — `leumiChequeReport` באחסון, שנכתב בסוף הקציר:**
`{total:32, noReference:0, already:0, asked:32, saved:18, failed:14,
why:"צילומי שיקים בלאומי לא השיב תוך 300 שניות"}`

**⚠ זה מפריך את ההסבר המתבקש.** ההיסטוריה (0.58.3) קבעה ששיק „נפתח כהפניה" כי
**אין לו אסמכתא** ולכן מעולם לא נסרק. הפעם **`noReference:0`** — לכל 32 השיקים יש
אסמכתא, כולם נתבקשו, ו-14 נכשלו בטיימאאוט. אותו סימפטום, שורש אחר לגמרי. אל תחפש
את חסרי האסמכתא לפני שקראת את הדוח.

**השורש:** `chequeImages` מחזיר את כל צילומי האצווה **רק בסוף**. אצווה של 6 שחורגת
מ-300 שניות זורקת גם את מה שכבר צולם בהצלחה. 18 = שלוש אצוות שהספיקו; 14 = 6+6+2
שנתקעו. **זו בדיוק צורת הכשל של 0.58.1** („catch{} בולע וכל ששת השיקים נזרקים"),
שחזרה מכיוון אחר: אז הסיבה הייתה תקרה קצרה מדי, עכשיו דף שנתקע.

**השתנה (0.93.0):**
- `leumi-content.js` שולח `LEUMI_CHEQUE_IMAGE` **מיד עם כל צילום שנקלט**;
  הרקע שומר אותו ל-IndexedDB באותו רגע. timeout מאבד לכל היותר את השיק שבאוויר.
- `chequeCtx` מחזיק `selectionKey` ו-`savedRefs`, ולכן שיק שנשמר חי **אינו נספר
  ככישלון** גם כשהאצווה שלו לא חזרה.
- אצווה שנתקעה גוררת ניתוב מחדש (`prepareLeumiRoute`) והזדמנות שנייה;
  **שתי תקיעות רצופות עוצרות את הקציר** במקום להמתין 300 שניות לכל אצווה נוספת.

**נבדק:** תחביר `background.js` ו-`leumi-content.js` מהדיסק + `manifest.json`
כ-0.93.0. **לא נבדק:** ריצה חיה. ⚠ **הסיבה לתקיעה עצמה לא נחקרה** — 300 שניות
לאצווה של 6 הן הרבה מאוד, וייתכן `loadAllRows` או חלון צילום שלא נסגר. השינוי
הזה מציל את התוצרת, **אינו מרפא את התקיעה**. המדידה הבאה: להריץ קציר ולראות אם
`why` חוזר, ואיזה שיק בדיוק נשאר תקוע לפי `LEUMI_CHEQUE_PROGRESS` האחרון.

**לבדיקת המשתמש:** לסנכרן לאומי שוב. השיקים שכבר נשמרו לא ייסרקו מחדש
(`have.has(...)`), ולכן הריצה תתמקד ב-14 החסרים ותהיה קצרה.

## 2026-08-18 17:55 · Claude · כרטיס חדש/ריק שילם 16 שניות לכל חודש שאינו קיים — 0.92.0

**דיווח:** „כרטיס עם 0 חיובים או חדש יחסית נסרק ממש באיטיות", ומיד אחריו ההערה
שסוגרת את הלולאה: **„אנחנו רשמנו בעבר כרטיס חדש מתאריך זה וזה."**

**נמדד בקוד, ולא בהרגשה — עלות חודש שאינו קיים לאותו כרטיס:**
`clickIsracardMonth` מנסה **24 פעם × 500ms = 12 שניות**, ואחריהן רצפת הכישלון
4 שניות. **16 שניות לכל חודש ריק**, ובכרטיס חדש כפול תשעה חודשים = כשתי דקות וחצי
של המתנה טהורה. הפונקציה המוזרקת מחזירה „אינו זמין בבורר" **רק** כשהבורר כבר טעון
והחודש פשוט אינו ברשימה (`selectedIndex>=0` הוא תנאי מוקדם) — כלומר תשובה סופית
שנוסתה שוב 23 פעם.

**⚠ תיקון למה שנכתב ב-0.91.0:** הורדת רצפת ההמתנה מ-4000 ל-1500 כמעט לא חוסכת דבר.
`readIsracardCardMonth` מכיל שהיות קבועות של 1800+1800+2000ms, ולכן קריאה שמצליחה
עוברת ~5.6 שניות ממילא והרצפה כלל אינה נוגעת. **הרווח האמיתי הוא בכישלונות.**

**השתנה (0.92.0):**
- „אינו זמין בבורר" חוזר **מיד** מ-`clickIsracardMonth` במקום 24 ניסיונות.
- סימן אחד כזה **סוגר את הכרטיס** לכל החודשים הישנים יותר — הסריקה יורדת מהחדש
  לישן, ולכן חודש שאינו בבורר מבטיח שגם קודמיו אינם. עד עכשיו נדרש חודש שנטען
  קודם, ולכן כרטיס חדש או ריק שנפל כבר בחודש הראשון סרק את כל השנה.
- **`isracardActiveSince` מזריע את הריצה מראש** — זו בדיוק ההערה של טל. „פעיל מחודש X"
  כבר נשמר מריצות קודמות, אבל הריצה גילתה אותו מחדש כל פעם ושילמה שוב. עכשיו כל
  חודש שקדם למועד הידוע מדולג בלי לגעת בדפדפן, ונספר ב-`preSkipped`.
- `activeSince` לא נכתב יותר כ-`undefined` כשאין ולו חודש אחד שנטען.

**נבדק:** תחביר `background.js` מהדיסק + `manifest.json` כ-0.92.0.
**לא נבדק:** ריצה חיה.

**הצעד הבא אם עדיין איטי — וכאן זה כבר בשהיות הקבועות:** 1800ms אחרי הניווט,
1800ms אחרי ההמתנה לתוכן ו-2000ms בסוף כל קריאה. שלושתן נוספו אחרי באגים אמיתיים
(טבלה ישנה שנשמרה לחודש הלא נכון), ולכן **אין לקצץ אותן בניחוש** — למדוד כמה זמן
באמת לוקח למזהה הטבלה להתחלף, ורק אז להחליף המתנה קבועה בהמתנה עד שינוי.

## 2026-08-18 17:30 · Claude · „השלם חסרים בלבד", והרצפה של 4 שניות ירדה מההצלחות — 0.91.0

**דיווח:** „תוסיף אופציה, כי זה נורא איטי גם עם כרטיס אחד."

**הכפלת המקורות לאיטיות, בחישוב ולא בהרגשה:** לכרטיס אחד הריצה היא 12 דפים,
וכל דף מחויב ברצפת שהייה. 12 × 4 שניות = 48 שניות **לפני** זמן הטעינה עצמו,
וזה גם כשכל 12 החודשים כבר שמורים במסד ואין מה לקרוא מחדש.

**השתנה (0.91.0), שני דברים נפרדים:**
- **`onlyMissing`** — צ'קבוקס „השלם חסרים בלבד" בשורת החודשים, **מסומן כברירת מחדל**,
  שעובר ל-`loadIsracardYear` ומסנן את `todo` לפי `cardHistStats()[suffix].months`.
  כשאין חסרים בכלל הריצה נגמרת מיד עם „כל 12 החודשים כבר שמורים" ובלי לפתוח דף.
  ⚠ **זו חריגה מהחלטה מתועדת** („אין לדלג לפי קיום חודש במסד", בגלל ריצות ישנות
  שאולי שמרו את אותו דף תחת כמה חודשים). לכן היא **בחירה גלויה** ולא התנהגות
  סמויה: הסרת הסימון מחזירה רענון מלא, וזה כתוב בצ'קבוקס עצמו.
- **רצפת ההמתנה 4000ms ירדה ל-1500ms במסלול ההצלחה** (`pageOk`). הרצפה נכתבה
  במקור כדי שכישלון לא יזנק לדף הבא ויראה כאילו מדלגים על הכול — **הנימוק הזה
  שייך לכישלון בלבד**; בקריאה שהצליחה כבר המתנו לטעינה אמיתית. הכישלון נשאר על
  4 שניות. חיסכון של ~30 שניות לכרטיס ברענון מלא.

**נבדק:** תחביר שני הקבצים מהדיסק + `manifest.json` כ-0.91.0.
**לא נבדק:** ריצה חיה. ⚠ **1500ms הוא ניחוש מבוסס-נימוק, לא מדידה** — אם קריאות
יתחילו לחזור ריקות דווקא אחרי השינוי הזה, הוא החשוד הראשון, והמדידה היא רצף
כשלים ב-`statusBySource` מול הזמן.

**הצעד הבא:** 0.91.0, כרטיס בודד עם „השלם חסרים בלבד" — אמור להיגמר במיידית אם
הכול שמור, ובכ-20 שניות אם חסרים כמה חודשים.

## 2026-08-18 17:10 · Claude · שנה לכל הכרטיסים סגרה את הסשן — בורר כרטיס ועצירה בניתוק, 0.90.0

**דיווח:** „טענתי מחדש, עשיתי בורר כללי לטעינת שנה אחורה ומרוב כרטיסים הסשן של
ישראכרט נסגר. הסנכרון היה מאוד ארוך; צריך אופציה לסנכרון 12 חודשים פר כרטיס."

**נמדד באחסון:** הריצה הגיעה ל-`syncProgress` **96/96** (8 כרטיסים × 12 חודשים),
אבל המונה בטקסט נתקע על **„חודש 6/12"** — כלומר מחצית הריצה החזירה דפים ריקים
אחרי שהסשן נסגר, והגלגל המשיך להתקדם כאילו כלום. אחריה מיד
„ממתין להתחברות לישראכרט". **הגלגל החדש עבד:** „כרטיס 9809 · 09/2025".

**החשבון שמסביר את האורך:** כל דף מחויב ב-`4000ms` שהייה מינימלית (ה-`finally`
בלולאה). 96 דפים × 4 שניות = **6.4 דקות רצפה**, בפועל יותר. לכרטיס אחד: 12 דפים,
כדקה. זה כל ההבדל.

**השתנה (0.90.0):**
- **בורר כרטיס בשורת החודשים** (`#cardYearCard`) — „טעינת 12 חודשים: כל הכרטיסים /
  כרטיס 4719 / …". הכפתור שולח `suffixes:[pick]`. המסלול הזה כבר היה קיים ב-
  `LOAD_CARD_YEAR`, ובכפתור שבתוך כל כרטיס; מה שחסר היה בחירה במקום שבו טל עומד.
- אישור נפרד לכל מסלול: לכרטיס בודד „כ-12 דפים, כדקה"; לכל הכרטיסים אזהרה מפורשת
  שהסשן עלול להיסגר ושעדיף לבחור כרטיס.
- **עצירה בניתוק:** `DISCONNECT_STREAK=6` — שישה כשלים רצופים על כרטיסים שונים
  אינם תקלת כרטיס אלא ניתוק. הריצה נעצרת, מה שנשמר נשאר, וההודעה אומרת „ישראכרט
  ניתק את הסשן — נשמרו N חודשים… התחבר ולחץ שוב". במקום לטחון עוד 40 דפים ריקים.

**נבדק:** תחביר `background.js` ו-`dashboard.js` מהדיסק + `manifest.json` כ-0.90.0.
**לא נבדק:** ריצה חיה של הבורר ושל העצירה בניתוק.

**⚠ מה שלא שיניתי במכוון:** הריצה עדיין קוראת את כל 12 החודשים ואינה מדלגת על
חודש ששמור — זו החלטה מתועדת בקוד („ריצה ישנה עלולה הייתה לשמור את אותו דף תחת
חודשים שונים"). אם טל ירצה „המשך מהנקודה שנעצרה" זו הנקודה לפתוח, ובכוונה.

**הצעד הבא:** לטעון 0.90.0, לבחור כרטיס אחד בבורר, ולראות שהריצה נגמרת בדקה.
פתוח: כרטיס 2120 · תפוגת דגלי `pending` לשאר הבנקים.

## 2026-08-18 16:45 · Claude · טעינת שנה מחקה היסטוריה לפני שקראה, והגלגל קיבל חודש — 0.89.0

**דיווח:** „יפה עובד, נעלם לי האופציה לטעון שנה אחורה לכרטיס. תחזיר אותה, ושבגלגל
יופיע חודש הסנכרון ומספר הכרטיס."

**נמדד באחסון — הסנכרון הרגיל עבד, וזה סוגר שני פתוחים:**
„ישראכרט: קורא כרטיס 8 מתוך 8 · 9809" → **„הסנכרון הסתיים — נקראו 8 כרטיסים,
7 שויכו, 1 ממתין לשיוך"**. כלומר **0.86.0 ו-0.88.0 אומתו חי**, והגלגל עם ארבע
הספרות עבד. `isracardLastCards` מחזיק 4719 · 1976 · 2961 · 9238.

**⚠ „האופציה נעלמה" — הכפתור לא נעלם, הוא נלחץ ונכשל.** ברשומה הבאה באחסון:
„ישראכרט: מאתר את כרטיס 4719" ואז „ישראכרט לכרטיס 4719: הסנכרון לא התחיל —
רשימת הכרטיסים לא נטענה". `git log -S'sync-card-history'` מראה שהכפתור לא נגע
מאז ההעברה. **מה שנעלם הוא ההיסטוריה עצמה, ואיתה תווית „כבר סונכרן · N חודשים"
וטקסט הכפתור „סנכרן מחדש".**

**השורש, וזו תקלת נתונים ולא תצוגה:** `loadIsracardYear` קרא ל-
`cardHistDeleteMonths(todo,[...requested])` **לפני** הקריאה — מוחק 12 חודשים
כ„רענון מלא" — ורק אחר כך ניגש לקרוא. כשקריאת הרשימה נכשלה, ההיסטוריה כבר נמחקה
ושום דבר לא בא במקומה. **המחיקה המוקדמת מיותרת מלכתחילה:** `cardHistPut` כותב
לפי `id=suffix|month` ודורס ממילא.

**השתנה (0.89.0):**
- המחיקה עברה לתוך הלולאה — `cardHistDeleteMonths([month],…)` רץ **רק** כשיש
  `out.length`, כלומר רגע לפני שנשמר מחליף טרי. כישלון כבר לא מוחק כלום.
- רשת ביטחון: אם `isracardSummaryFromHome` נכשל, נופלים ל-`isracardLastCards`
  מהסנכרון האחרון (מסונן לסיומת שהתבקשה) במקום ליפול. הכרטיס כבר מוכר — אין סיבה
  שקריאה חוזרת של הקרוסלה תחסום אותו.
- הגלגל מציג חודש + כרטיס: בסנכרון הרגיל „כרטיס 4719 · 08/2026", ובטעינת שנה
  „כרטיס 4719 · 07/2026" לפי החודש הנקרא.

**נבדק:** תחביר `background.js` מהדיסק + `manifest.json` כ-0.89.0, דרך
`python -m http.server` ו-`no-store`. **לא נבדק:** טעינת שנה חיה אחרי השינוי.
⚠ **ההיסטוריה של 4719 שנמחקה בריצה שנכשלה אינה חוזרת מעצמה** — צריך להריץ לה
טעינת שנה מחדש.

**עדיין פתוח:** כרטיס 2120 — הוא מופיע כשורת חיוב בכרטסת פועלים אך אינו בין
שמונת הכרטיסים שנקראו מישראכרט; ייתכן שהוא כרטיס מבוטל או של מנפיק אחר. לא נחקר.
כן: תפוגת דגלי `pending` לשאר הבנקים (הנושא של החטיפה).

## 2026-08-18 16:20 · Claude · 0.87.0 שבר את הסנכרון היזום של ישראכרט — 0.88.0

**דיווח:** „כרגע מסנכרן את ישראכרט וביקשתי גלגל עם 4 ספרות הכרטיס." הגלגל לא הופיע.

**נמדד באחסון תוך כדי הריצה שלו:** `syncStatus` „ממתין להתחברות לישראכרט — חיבור 1"
→ **„זוהתה כניסה לישראכרט, אך הסנכרון האוטומטי כבוי"** → „ממתין להתחברות".
הסנכרון פשוט **מעולם לא התחיל**, ולכן גם הגלגל לא היה יכול להופיע. הגלגל עצמו
תקין ב-`runIsracard` (`syncStep('… כרטיס i מתוך N · suffix','כרטיס <suffix>')`).

**⚠ זו נסיגה שגרמתי ב-0.87.0, והרשומה של 11:20 טענה במפורש „לא נשבר: סנכרון שהמשתמש
יוזם עובד כרגיל".** הטענה נבדקה על `pendingSources` של פועלים בלבד. **ישראכרט הוא
היעד היחיד שאין לו דגל `pending` משלו** — `ISRACARD_AUTHENTICATED` (שורה 180) קרא
ישירות ל-`maybeAutoRun`, וזה חוסם כש-`autoSyncOnLogin:false`. `startIsracard` פתח
חלון כניסה וחיכה לאירוע שנבלע. כאל, MAX, יהב, מזרחי ובינלאומי לא נפגעו — לכולם
יש דגל, והוא גובר על השער.

**השתנה (0.88.0):**
- `pendingIsracard` + `pendingIsracardAt` — `startIsracard` דורך אותם; `isracardOnAuth`
  מכריע: דגל טרי → ריצה ישירה בעקיפת השער; אין דגל → `maybeAutoRun` כמקודם.
- **הדגל נצרך פעם אחת ופג אחרי 15 דקות** (`PENDING_TTL_MS`). זה מונע את הצורה
  ההפוכה של אותו באג — דגל ישן שנשאר דלוק וחוטף כניסה מאוחרת שאינה קשורה לתוסף.
- `pendingIsracard:false,pendingIsracardAt:0` נוספו ל-`freshStart`.

**נבדק:** תחביר `background.js` ו-`dashboard.js` + `manifest.json` כ-JSON עם 0.88.0,
דרך `python -m http.server` ו-`no-store`. **לא נבדק:** ריצה חיה — לא רץ סנכרון
ישראכרט מאז השינוי, ולכן 0.86.0 והשאלה על כרטיס 2120 **עדיין פתוחות**.

**⚠ לקח כללי לשאר הבנקים:** דגל `pending` הוא גם המנעול וגם החטיפה. אין לו תפוגה
בשום יעד אחר — לאומי אף נשאר „דרוך" במכוון בשגיאה רגעית (שורה 173). התפוגה שנוספה
כאן היא התקדים; להרחיב אותה לשאר הדגלים הוא הצעד הבא בנושא החטיפה.

**הצעד הבא:** טל טוען מחדש, מוודא 0.88.0, ולוחץ ישראכרט בדשבורד. הגלגל אמור להציג
„כרטיס <4 ספרות>" לכל כרטיס. מיד אחרי — לקרוא `statusBySource` ולראות אם 2120 חזר.

## 2026-08-18 15:55 · Claude · 0.87.0 אומת חי מהאחסון — הסנכרון האוטומטי נחסם בפועל

**לא היה דיווח חדש.** הרשומה הקודמת השאירה שני דברים „לא נבדק", ושניהם הוכרעו
**בלי לשאול את טל** — קריאת `000003.log` מ-`Local Extension Settings`.

**נמדד:**
- `autoSyncDefaultOffApplied:true` ו-`autoSyncOnLogin:false` שמורים באחסון —
  **המיגרציה רצה.** זו הייתה הבדיקה הראשונה שהרשומה הקודמת ביקשה.
- מיד אחריה, ושוב ושוב עד סוף הלוג: `statusBySource.business` =
  „זוהתה כניסה לפועלים עסקי, **אך הסנכרון האוטומטי כבוי**" (11:26:00 ואילך).
  כלומר כניסה אמיתית לבנק זוהתה ו**לא** נחטפה. 0.87.0 עובד.
- לפניה, ב-11:14 וב-11:21, אותו מקור עדיין החזיר „סונכרן לפני פחות מ-6 שעות" —
  כלומר ההתנהגות התחלפה בדיוק בנקודת המיגרציה.

**מלכודת חותמות זמן, שוב:** ה-mtime של `background.js` הוא 11:27 — **אחרי**
המיגרציה שנמדדה ב-11:21–11:26. אל תסיק מכך „רץ קוד ישן": הדגל
`autoSyncDefaultOffApplied` **קיים רק ב-0.87.0**, ולכן 0.87.0 הוא שרץ. ה-mtime
נדרס אחרי הטעינה. הכרע לפי תוכן האחסון, לא לפי `ls`.

**עדיין לא נבדק — וזה אותו שורש:** `statusBySource.isracard` **קפוא על 10:38:50**,
„רשימת הכרטיסים לא נטענה לאחר המתנה" — כלומר **לא רץ ולו סנכרון ישראכרט אחד
מאז 0.86.0**. התיקון ההוא עדיין לא אומת.

**היפותזה לכרטיס 2120:** בחיפוש באחסון, „2120" מופיע **רק** כשורת חיוב בכרטסת
פועלים (`"action":"2120 - ישראכרט בע\"מ"`, 10/07, 30/07, 10/08) ו**לא** ברשימת
כרטיסים כלשהי. אם רשימת הכרטיסים של ישראכרט מעולם לא נטענה — „2120 לא מופיע"
אינו באג נפרד אלא **תסמין של כשל ישראכרט**. סנכרון ישראכרט מוצלח אחד יכריע.

**הצעד הבא:** טל מתחבר לישראכרט ומריץ סנכרון ידני מהדשבורד; מיד אחריו נקרא את
`statusBySource` — הוא יאמת את 0.86.0 ויגיד אם 2120 חוזר. פעולה אחת, שני פתוחים.

## 2026-08-18 11:20 · Claude · הכניסה לבנק נחטפה — סנכרון אוטומטי כבוי בברירת מחדל, 0.87.0

**דיווח:** „אני מנסה להיכנס לבנק הפועלים לבצע פעולות שלא קשורות לתוסף והדף נחטף",
ואחריו השאלה החדה: **„למה זה נחטף אם אין בקשת סנכרון, ואם לא חלפו 6 שעות?"**

**⚠ ההסבר הראשון שלי היה חצי נכון, והשאלה הפילה אותו.** אמרתי „הצינון פג" בלי לבדוק
לאיזה פורטל. **הצינון נספר בנפרד לכל `source`**, ושני פורטלי פועלים הם שני מקורות:

| מקור | `autoSyncLast` | לפני | צינון 6ש' |
|---|---|---|---|
| `business` · biz2 | 18.08 10:51 | 0.4 שעות | פעיל |
| `private` · login.bankhapoalim | **16.08 18:22** | **40.9 שעות** | **פג** |

הסנכרון של 10:51 היה **העסקי**. המונה של **הפרטי** לא אופס יומיים, ולכן כל כניסה
לפורטל האישי — בדיוק זה שבו טל עושה דברים שאינם קשורים לתוסף — נתפסה.
**„אין בקשת סנכרון" אינו תנאי בקוד: עצם הכניסה היא הטריגר.**

**נמדד באחסון (`statusBySource` ושכניו ב-LevelDB):** `pendingSources` היה **ריק**.
כלומר החשד השני — מסלול `queueDiscover` — **נפסל לגבי האירוע הזה**. אל תאשים אותו
בלי לקרוא את הערך.

**⚠ אבל הוא מלכודת אמיתית, והתגלה תוך כדי:** `handleAuthenticatedNavigation` פונה
ל-`queueDiscover` כש-`pendingSources` אינו ריק, והמסלול הזה **אינו בודק
`autoSyncOnLogin` ואף לא צינון**. `discover` מסירה מקור מהרשימה **רק בהצלחה**,
ו-`freshStart` לא איפס אותה — כך שסנכרון שנקטע נועל את המצב **לצמיתות**, גם אחרי
הפעלה מחדש של הדפדפן. אז הצ'קבוקס לא מגן על כלום.

**החלטת טל:** „שבדיפולט הכפתור יהיה כבוי ורק על ידי לחיצה זה יחזור לעבוד."

**השתנה (0.87.0):**
- `autoSyncOnLogin` — ברירת המחדל `false` בכל ארבע קריאות ה-`get` וב-`onInstalled`.
- `applyAutoSyncDefaultOff` — מיגרציה חד-פעמית עם דגל `autoSyncDefaultOffApplied`,
  כי הערך `true` **שכבר שמור** אצל טל גובר על שינוי ברירת מחדל. בלעדיה השינוי היה
  חסר משמעות בהתקנה קיימת.
- `pendingSources:[]` נוסף ל-`freshStart` — סוגר את המלכודת לעיל.

**לא נשבר:** `discover` עדיין מגן על עצמו ב-`pendingSources.includes(source)`
(שורה 258), ולכן **סנכרון שהמשתמש יוזם מהדשבורד עובד כרגיל** — `start()` ממלא את
הרשימה רגע לפני. אימות: הביטוי קיים בקובץ אחרי העריכה.

**לא נבדק:** ⚠ לא אומת חי. רק פרסור של שלושת קובצי ה-JS ו-`manifest.json` תקין עם
`0.87.0`, דרך `python -m http.server` ו-`no-store`. **לא נראתה כניסה אמיתית לפועלים
אחרי השינוי**, ולא אומת שהמיגרציה אכן רצה — `onInstalled` בטעינה מחדש של unpacked
לא נצפה בפועל. אם אחרי הטעינה הצ'קבוקס עדיין מסומן, **המיגרציה לא רצה** — זו
הבדיקה הראשונה לעשות, לא לחפש במקום אחר.

**הצעד הבא:** ממתין לריצה חיה של 0.86.0 (ישראכרט) ושל השינוי הזה. פתוח עדיין:
„כרטיס 2120 לא מופיע ברשימה", שלא נחקר.

**לבדיקת המשתמש:** לטעון מחדש, לוודא 0.87.0, ולוודא בדשבורד ש„סנכרון אוטומטי בכל
כניסה לבנק" **אינו מסומן**. אז להיכנס לפועלים פרטי ולוודא שהדף נשאר שלך.

## 2026-08-18 11:00 · Claude · הניווט האוטומטי הרג את ישראכרט, והמחיקה לא רונדרה — 0.86.0

**דיווח:** „בכרטיסי אשראי סינכרון נכשל, ואין מחיקת כרטיס והגלגל מסתובב ללא מספר כרטיס."

**⚠ הופרך מיד: ההסבר „גרסה ישנה טעונה".** התיקייה כאן אכן הייתה על 0.51.4 ונמשכה
ל-0.85.0, אבל **הכשל נמדד ב-10:38:50 — אחרי המשיכה ב-10:35.** זו הייתה 0.85.0.
המשתמש אמר „ברור שטענתי מחדש" וצדק. אל תחזור על ההשערה הזו בלי חותם זמן.

**נמדד — `Local Extension Settings\dpedknjoddmipnhbmjplcgnekfmpmafp\000003.log`,
מפתח `statusBySource`:** ההיסטוריה נקראת ישירות מה-LevelDB (LOCK תפוס, מעתיקים
רק את ה-`.log`). ארבע רשומות: 10:37:34 ממתין להתחברות · 10:38:39 „כניסה חדשה" ·
10:38:41 „קורא את רשימת הכרטיסים" · 10:38:50 כשל. **9.2 שניות = בדיוק 12×750ms.**

**השורש — הניווט הוא שהרג, לא הצילו:** `ISRACARD_AUTHENTICATED` נשלח
מ-`isracard-content.js` **רק** מדף שכבר מציג „נותר לניצול|מסגרת", ושורה 21 שם
כבר פוסלת `/transactions`. כלומר ההערה שהצדיקה את הניווט („המשתמש היה בדף כרטיס
בודד") מגנה מפני מקרה **שהמתאם כבר מונע**. הניווט זרק דף מוכן ונתן לו
1800ms + 9 שניות לעלות קר. זה מסביר גם את „לסירוגין" שבהערה בשורה 786.

**„הגלגל בלי מספר כרטיס" אינו תקלה שלישית** — הגלגל מציג את סיומת הכרטיס *הנקרא*,
והריצה מתה לפני הכרטיס הראשון.

**„אין מחיקת כרטיס" — באג נפרד ואמיתי, ב-`dashboard.js:141`:** `cardHistoryMark`
החזיר את הכפתור רק כש-`cardHistoryStats[suffix].count>0`. כלומר **כרטיס שנטען
בטעות — המקרה היחיד שלשמו נכתב הפיצ'ר — לעולם לא קיבל כפתור**, וגם כרטיסי כאל
ו-MAX לא קיבלו, אף ש-`deleteCardEverywhere` מוחק משלוש רשימות הלא-משויכים.

**השתנה (0.86.0):**
- מסלול אוטומטי: `runIsracard(id,8)` על הדף הטעון, וניווט רק כגיבוי עם `40`.
- `runIsracard(tabId,attempts=40)` — התקציב הפך לפרמטר.
- `cardDeleteButton` חדש, מרונדר לכל כרטיס עם סיומת ובכל מנפיק, ללא תלות בהיסטוריה.
- `saveIsracardMiss` + `ISRACARD_BUTTONS_SAMPLE`: כשל עתידי שומר ל-`bankDiagnostics.isracard`
  כמה כפתורים יש, כמה עם סימן וכמה עם סיומת, ו-20 דוגמאות. **מקומי בלבד.**

**לא נבדק — וזה העיקר:** ⚠ **שום דבר מזה לא אומת חי.** רק פרסור:
`background.js` · `dashboard.js` · `isracard-content.js` עוברים `new Function`,
ו-`manifest.json` תקין עם `0.86.0` — נקרא ב-`no-store` דרך `python -m http.server`.
**אין מנוע JS על המחשב הזה** — הנתיב ל-node שב-`PROJECT-HISTORY-AND-RULES.md` §22
(`.cache\codex-runtimes\...`) **כבר אינו קיים**, ואין node/deno/bun ב-PATH.

**⚠ שתי ההשערות עדיין חיות.** לא נמדד DOM חי, ולכן לא הוכח שהכשל היה תזמון
ולא שינוי מבנה שהשבית את `cards()`. אם 0.86.0 עדיין נכשל — **אל תנחש סלקטור**:
קרא את `bankDiagnostics.isracard` מהאחסון. אם `withMarker>0` אבל `withSuffix=0`,
הרגקס של הסיומת הוא האשם; אם `buttons` נמוך, הדף פשוט לא היה טעון.

**הצעד הבא:** לחכות לריצה חיה. אחריה — הדיווח הפתוח „כרטיס 2120 לא מופיע ברשימה",
שעדיין לא נחקר.

**לבדיקת המשתמש:** לטעון מחדש ב-`chrome://extensions`, לוודא 0.86.0, להתחבר
לישראכרט ולראות אם הסנכרון עובר; ובדשבורד — לוודא ש„מחק כרטיס" מופיע עכשיו
לצד כל כרטיס, גם כזה בלי היסטוריה.

## 2026-08-18 · Claude · כרטיס מחוק חזר מהסנכרון — 0.84.0

**דיווח: „עדיין מופיע הכרטיס שנמחק", והגרסה הטעונה אומתה כ-0.83.2** — כלומר
הקוד החדש רץ, וההסבר על „לשונית ישנה" לא מספיק.

**השורש, וזה פער תפיסתי ולא באג נקודתי:** `deleteCardEverywhere` מחקה נתונים
**מקומיים** — היסטוריה, `accounts[].cards`, לא-משויכים, שיוכים. אבל **הכרטיס
עדיין קיים אצל חברת האשראי**, והסנכרון הבא מחזיר אותו. מבחינת המשתמש הכרטיס
„לא נמחק"; מבחינת הקוד המחיקה הצליחה. **שניהם צדקו.**

⚠ **הלקח:** „מחק" בממשק שמסנכרן ממקור חיצוני **חייב להיות מתמשך**. מחיקה
חד-פעמית של נתונים מקומיים אינה מחיקה — היא עיכוב עד הסנכרון הבא.

**השתנה — `hiddenCards`:**
- המחיקה מוסיפה את הסיומת לרשימת הסתרה קבועה באחסון.
- הדשבורד מסנן לפיה **בטעינה** (חשבונות + שלוש רשימות הלא-משויכים)
  **ובשורות החודש** — כלומר גם נתונים שיגיעו בסנכרון הבא לא יוצגו.
- **הפיך:** שורת „כרטיסים שהוסתרו · <סיומת> · החזר" בתחתית תצוגת הכרטיסים.
  הסתרה בטעות חייבת להיות בת-ביטול.

**אומת בהרצת הקוד מהקבצים:**

| בדיקה | תוצאה |
|---|---|
| מחיקת 2961 | 3 חודשי היסטוריה · 1 כרטיס · **נוסף ל-`hiddenCards`** לצד ערך קיים |
| מה נשאר בחשבון | `4719` בלבד ✓ |
| הסינון | תופס `2961` כאובייקט וכמחרוזת · **אינו תופס** `4719` · אינו תופס כרטיס בלי סיומת |
| שורת ההחזרה | „כרטיסים שהוסתרו: 2961 החזר" |
| בלי מוסתרים | השורה אינה מוצגת ✓ |
| נקודות החיבור | רינדור · טעינה · שורות החודש — שלושתן ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.84.0**.

**⚠ שני דיווחים מאותו סבב שלא נפתרו:**
1. **„אין גלגל"** — 0.83.2 חיבר את טעינת השנה, אך **טרם אומת חי**.
2. **„כרטיס 2120 לא מופיע ברשימה"** — לא נחקר. ייתכן שהוא נכנס כלא-משויך,
   וייתכן שהסנכרון לא שמר אותו. **דורש מדידה, לא ניחוש.**

---

## 2026-08-18 · Claude · הגלגל גם בטעינת השנה — 0.83.2

**דיווח: „אין גלגל · החיוב הקרוב לא נמחק · החיוב של 1,865 מופיע גם בקרוב וגם
בחודש הקודם."**

### 1. הגלגל — נפתר

`beginProgress` חובר ב-0.82.0 ל-`runIsracard` בלבד. **הכפתור „סנכרן שנה
מישראכרט" הוא מסלול אחר** (טעינת 12 חודשים) עם לולאה משלו, ובו לא היה מונה.

**השתנה:** `beginProgress(todo.length × active.length)` לפני לולאת החודשים,
`syncStep` עם סיומת הכרטיס בכל צעד, `endProgress` בסיום.
**שלושה מסלולים מזינים עכשיו את הגלגל:** פועלים · ישראכרט רגיל · טעינת שנה.

**אומת:** 12 חודשים × 2 כרטיסים → `4% · כרטיס 2961` → `8% · כרטיס 4719` →
`13% · כרטיס 2961`, וניקוי בסיום.

### 2. „החיוב הקרוב לא נמחק" — כנראה נבדק על גרסה ישנה

המחיקה המלאה (`deleteCardEverywhere`, שמסיר גם מ-`accounts[].cards`
ומ-`isracardUnassigned`) נכנסה ב-**0.83.1**. הכרטיס בצילום מסומן „ממתין
לשיוך", כלומר הוא ב-`isracardUnassigned` — **רשימה ש-0.83.0 לא נגעה בה בכלל.**
צריך ⟳ ומחיקה חוזרת לפני שמסיקים שזה עדיין שבור.

### 3. ⚠ 1,865.44 גם בחיוב הקרוב וגם כתנועה — **לא נחקר, ולא נוחש**

בצילום: סכום הכרטיס **1,865.44**, ובתוך התנועות שורה **10.08.26 · 1,865.44 ·
„יזמקו חברה בעמ"**. שתי אפשרויות שלא הוכרעו:
1. המחלץ קולט שורת **סיכום/חיוב חודשי** כאילו הייתה תנועה רגילה.
2. באמת יש עסקה בסכום זהה לחיוב.

**מה שיכריע:** בדף ישראכרט עצמו — האם השורה „יזמקו חברה בעמ" מופיעה ברשימת
העסקאות, או שהיא שורת חיוב. **אין טעם לגעת במחלץ לפני התשובה הזו**; זה בדיוק
סוג הניחוש שעלה היום שלושה סבבים.

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.83.2**.

**פתוח:** הנעילה שנתקעת (0.58.0) · „נטענו 12 חודשים אך נשמר אחד" · הכפילות
לעיל · אימות חי.

---

## 2026-08-18 · Claude · דף אינו סשן עד שהוא מוכיח זאת — 0.83.1

**דיווח: „לא מסנכרן" (אותה שגיאה), ו„במחק כרטיס החיוב הקרוב נשאר".**

### 1. המלכודת מדיסקונט, בפעם השלישית — והפעם אני יצרתי אותה שעה קודם

`isracardTab()` מחזיר כל לשונית על `web.isracard.co.il` **שאין בכתובתה
`login|signin`**. `StatusPage` אינו מכיל את המילים האלה — **וב-0.83.0 העברתי
את חלונית ההתחברות לפתוח בדיוק שם.**

**התוצאה:** ברגע שהחלונית נפתחת, `isracardTab()` מכריז „מחובר",
`startIsracard` מריץ סנכרון **לפני שהמשתמש הקליד**, 12 הניסיונות לא מוצאים
כרטיסים, והכל נופל אחרי ~10 שניות. **שתי ההשערות הקודמות שלי (occlusion,
מארח שגוי) לא היו הסיבה** — הן פשוט הזיזו את התסמין.

⚠ **התיקון כאן אינו סינון כתובת** — `StatusPage` **הוא** דף הסנכרון, ואי אפשר
לסנן אותו. התיקון הוא עקרוני: **דף אינו סשן עד שהוא מוכיח זאת.**
`startIsracard` בודק פעם אחת אם יש רשימת כרטיסים; אם אין — מצב
`waiting_login`, וההרצה מגיעה מ-`ISRACARD_AUTHENTICATED`, שהגייט שלו **כבר
דורש שהרשימה מרונדרת**.

**הכלל המלא, אחרי שלוש פעמים:** בדיקת „יש חיבור פעיל" לא יכולה להישען על
מארח, ולא על נתיב, אלא רק על **סימן חיובי שהנתונים קיימים בדף**.

### 2. מחיקת כרטיס הייתה חלקית

`CARD_HISTORY_DELETE_CARD` מחק **היסטוריה בלבד**, בעוד החיוב הקרוב מוצג מתוך
`accounts[].cards`. **מחיקה חלקית גרועה ממחיקה שלא קרתה — היא נראית כאילו
עבדה.** `deleteCardEverywhere` מסיר עכשיו גם את הכרטיס מכל החשבונות,
מרשימות הלא-משויכים, מהשיוכים ומ-`activeSince`.

**אומת בהרצת הקוד מהקובץ:**

| בדיקה | תוצאה |
|---|---|
| הדף עדיין לא מחובר (0 כרטיסים) | **`waiting_login`** · „התחבר בחלון שנפתח" · **בלי כישלון שקרי** ✓ |
| הדף מחובר (3 כרטיסים) | הסנכרון רץ ✓ |
| מחיקת 4719 | 7 חודשי היסטוריה · **1 כרטיס מהחשבונות** |
| מה נשאר | כרטיסים `1156`,`0937` · לא-משויכים 0 · שיוך `4719` הוסר, `1156` נשמר · `activeSince` נוקה |
| סיומת ריקה | **סירוב** — אין מחיקה גורפת ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.83.1**.

**עדיין פתוח:** הנעילה שנתקעת (0.58.0) · „נטענו 12 חודשים אך נשמר אחד" ·
אימות חי.

---

## 2026-08-18 · Claude · ישראכרט חוזר למארח שלו, ומחיקת כרטיס — 0.83.0

**דיווח: הסנכרון נכשל שוב („רשימת הכרטיסים לא נטענה"), הגלגל לא מציג מספר
כרטיס, ואין דרך למחוק כרטיס שנטען בטעות.**

### 1. הכישלון — הופעל החשוד השני שהוגדר מראש

**הגלגל לא הציג מספר כרטיס כי הסנכרון נפל *לפני* לולאת הכרטיסים.** אלה לא שתי
תקלות אלא אחת: `beginProgress` יושב אחרי קריאת רשימת הכרטיסים.

**היסט החלון (0.81.1) לא פתר** — הצליח פעם אחת וחזר להיכשל, כלומר ההשערה על
occlusion **לא אושרה**. הופעל החשוד שנרשם ב-0.81.1: ב-0.78.0 העברתי את
ההתחברות ל-`digital.isracard.co.il` בעוד הסנכרון קורא מ-`web.isracard.co.il`.
**התחברות על מארח אחד אינה מבטיחה סשן על השני**, וזה מסביר גם למה הכישלון
היה לסירוגין.

**השתנה:** שלוש נקודות הפתיחה חזרו ל-`ISRACARD_HOME` — האתר מפנה משם
להתחברות ומחזיר לאותו מארח שהזרימה עובדת מולו. **נשארו חלוניות `popup`.**
הקבוע הישן נשמר בשם `ISRACARD_LOGIN_UNUSED` עם הסבר, כדי שלא ינוסה שוב.

⚠ **הלקח:** כתובת כניסה נכונה אינה מספיקה — **היא חייבת להיות על המארח שהסנכרון
קורא ממנו.** מדדתי את הכתובת ולא בדקתי את הדבר הזה.

### 2. מחיקת כרטיס — יכולת חדשה

**„אין לי אפשרות למחוק כרטיסים במידה ונטענו בטעות."** שלוש חוליות:
- `cardHistDeleteCard(suffix)` ב-`card-history.js` — מוחק את כל חודשי הכרטיס.
- `CARD_HISTORY_DELETE_CARD` ב-`background.js`.
- כפתור **„מחק כרטיס"** ליד תג הסנכרון, עם **אישור מפורש** שכולל את מספר
  הכרטיס ומבהיר שהנתונים אצל חברת האשראי אינם נמחקים.

הכפתור מופיע **רק כשיש היסטוריה שמורה** — אין כפתור מחיקה לכרטיס ריק.

**תוקן אגב:** „1 חודשים" → **„חודש אחד"**.

**אומת בהרצת הקוד מהקבצים:**

| בדיקה | תוצאה |
|---|---|
| פתיחות ישראכרט | **3** על `ISRACARD_HOME` · אפס על `digital` ✓ |
| מחיקה מול מסד מדומה (3 רשומות, 2 מהן 4719) | **נמחקו 2** — סינון לפי סיומת ✓ |
| סיומת ריקה | **0 נמחקו** — אין מחיקה גורפת ✓ |
| דקדוק | „כבר סונכרן · **חודש אחד**" · „· **12 חודשים**" |
| כרטיס בלי היסטוריה | **בלי כפתור מחיקה** ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.83.0**.

**⚠ תיקון למדידה שלי:** בבדיקת המחיקה הדפסתי גם את רשימת המזהים שנמחקו,
והיא יצאה ריקה — **ניקיתי את המערך בשורה שאחריה ברתמה עצמה.** הספירה
(2 מתוך 3) היא הראיה התקפה; רשימת המזהים לא נבדקה.

**עדיין פתוח:** הנעילה שנתקעת (0.58.0) · „נטענו 12 חודשים אך נשמר אחד" —
טרם הוכרע, וממתין להודעת הסיום של טעינת השנה · אימות חי של 0.83.0.

---

## 2026-08-18 · Claude · הגלגל מציג את מספר הכרטיס — 0.82.0

✅ **ישראכרט סונכרן בהצלחה:** „נקראו 8 כרטיסים, 7 שויכו לחשבונות, 1 ממתינים
לשיוך." הכישלון מהסבב הקודם לא חזר אחרי 0.81.1 — **תמיכה בהשערת ה-occlusion,
לא הוכחה שלה.**

⚠ **שינוי שלא עשיתי, במכוון.** דיווח הביניים היה „הוא כן קורא אבל אומר שנכשל",
ומצאתי מועמד: הלולאה יוצאת על `summary?.cards?.length` בעוד הבדיקה שאחריה
דורשת **גם** `summary.ok`. הרמתי גבה — ואז בדקתי את `isracard-content.js`
ומצאתי שהוא מחזיר `{ok:true,cards,owner}` **תמיד**. כלומר הסתירה אינה אפשרית
דרך הנתיב הזה, והסנכרון אכן הצליח בהרצה הבאה.

**חוסר עקביות אמיתי אך לא מזיק אינו סיבה לשנות קוד בזרימה שעובדת.** אילו
שיניתי, הייתי מוסיף סיכון לתיקון של תקלה שלא קיימת. נרשם כאן כדי שלא ייחקר שוב.

### מה כן השתנה — הבקשה עצמה

**„אני רוצה שבגלגל יופיע 4 הספרות של הכרטיס שהוא קורא."**

הסטטוס **כבר** הכיל את הסיומת („קורא כרטיס 3 מתוך 8 · 4821") — אבל הגלגל קורא
מ-`syncProgress`, שנכתב עד היום **רק** במסלול פועלים. ישראכרט כתב סטטוס בלבד,
בלי התקדמות.

**השתנה ב-`runIsracard`:** `beginProgress(active.length)` לפני לולאת הכרטיסים,
`syncStep(status, card.suffix)` בכל כרטיס, ו-`endProgress()` אחריה.

**אומת בהרצת הקוד מהקובץ:**

| בדיקה | תוצאה |
|---|---|
| מיקום הקריאות | `beginProgress` לפני הלולאה · `endProgress` אחריה ✓ |
| הקריאה בלולאה | סטטוס מלא + טקסט גלגל „כרטיס ${card.suffix}" |
| רצף על 8 כרטיסים | **13% · כרטיס 4821** → **25% · כרטיס 0937** → **38% · כרטיס 1156** |
| אחרי הסיום | `syncProgress` מתנקה ✓ |
| רינדור הגלגל | מרכז הטבעת: **38%** ומתחתיו **„כרטיס 4821"** |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.82.0**.

**לכאל ול-MAX אין עדיין התקדמות בגלגל** — אותו דפוס, שלוש שורות לכל אחד,
כשיתבקש.

---

## 2026-08-18 · Claude · חלון העבודה היה מכוסה לחלוטין — 0.81.1

**דיווח: „סנכרון אוטומטי בישראכרט נכשל: רשימת הכרטיסים לא נטענה לאחר המתנה."**

**המדידה שצמצמה את החשד:** הגייט של `ISRACARD_AUTHENTICATED` ב-content script
**חזק** — הוא דורש שהטקסט יכיל „נותר לניצול" או „מסגרת", כלומר **שרשימת
הכרטיסים כבר מרונדרת**. אם ההודעה נשלחה, הדף **כן** הוצג.

**מכאן הכשל אינו בהתחברות אלא אחריה:** `waitIsracardReady` מנווטת את הלשונית
מחדש ל-`StatusPage` ומחכה — ובשלב הזה החלון כבר מאחורי הדשבורד. הניווט הזה
איבד את `active:true` ב-0.61.0.

**החשד:** מ-0.73.0 חלון העבודה יורש את מידות הדשבורד **בדיוק** — אותו מיקום
ואותו גודל. חלון כזה **מכוסה לחלוטין**, ו-Chrome מסמן חלון מכוסה כנסתר ומשהה
בו `rAF`. **זה הסיכון שרשמתי ב-0.62.0 ומעולם לא נסגר** — רק הוזז ממקום למקום.

**השתנה:** חלון העבודה מקבל היסט של 28px בכל צד — 1720×1010 הופך ל-**1664×954**
ב-(148,68). שוליים גלויים בכל צד מונעים סימון „מכוסה", והרוחב נשאר שולחני
לגמרי, בלי שינוי breakpoint.

**אומת בהרצת `returnToDashboard` מהקובץ:**

| מצב | תוצאה |
|---|---|
| חלון עבודה חדש מול דשבורד 1720×1010 | **1664×954 ב-(148,68)** — שוליים גלויים בכל צד ✓ |
| חלונית התחברות 560×780 קיימת | מוגדלת לאותה גאומטריה ✓ |
| דשבורד קטן (1000×700) | **מינימום 1100×700 נשמר** — לא מצטמצם לפריסה צרה ✓ |
| בלי מידות | בלי ערכים פסולים ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.81.1**.

**⚠ זו השערה, לא מדידה.** ההיגיון עקבי עם הראיות — הרשימה רונדרה לפני שהחלון
ירד לרקע ולא רונדרה אחרי — אבל **occlusion עצמו לא נמדד**, ואין דרך למדוד אותו
בלי דפדפן חי מול הבנק.

**אם ישראכרט עדיין נכשל — החשוד הבא מוגדר מראש:** ב-0.78.0 העברתי את ההתחברות
ל-`digital.isracard.co.il` בעוד הסנכרון חי על `web.isracard.co.il`. ייתכן
שהסשן אינו נוצר על המארח שהזרימה עובדת מולו. **החזרה לפתיחת `ISRACARD_HOME`
כחלונית היא שינוי של שורה אחת**, והיא מחזירה את הזרימה שעבדה לפני היום.

---

## 2026-08-18 · Claude · הודעת ביניים שהתיישנה הוצגה כמצב נוכחי — 0.81.0

**המשתמש שלח צילום של לוח האריחים.** 0.80.0 תיקנה את **מלל ברירת המחדל**,
אבל רוב האריחים לא הגיעו אליו בכלל — הם הציגו **הודעות ביניים ישנות**:

| אריח | הוצג בפועל | מתי זה נכתב |
|---|---|---|
| פועלים עסקי | „ממתין להתחברות אל פועלים עסקי" | סנכרון שהסתיים מזמן |
| דיסקונט פרטי | „קורא משכנתאות חשבון 1" | קפא באמצע ריצה קודמת |
| דיסקונט עסקי | „מזהה מספר חשבון 4 מתוך 4" | כנ״ל |

**השורש:** `statusBySource` (0.66.0) שומר את ההודעה האחרונה **לנצח** — בלי גיל,
ובלי הבחנה בין **הודעת ביניים** לבין **תוצאה**. „ממתין להתחברות" הוא מצב חולף;
כשהוא מוצג שעות אחר כך הוא פשוט שקר.

⚠ **פגם בתכנון שלי מ-0.66.0.** בניתי „סטטוס לכל בנק" ובדקתי שהשיוך נכון —
**ולא שאלתי מה קורה להודעה כשהיא מתיישנת.** שיוך נכון של הודעה לא הופך אותה
לנכונה בזמן.

**השתנה — שלוש דרגות במקום אחת:**

| גיל ההודעה | מוצג |
|---|---|
| < 3 דקות | ההודעה כלשונה — **סנכרון חי** |
| מעל, ו**תוצאה** (הסתיים · נכשל · דולג) | ההודעה **+ גיל**: „הסתיים ואומת · 7 חשבונות · לפני 3 שע׳" |
| מעל, ו**הודעת ביניים** | **נזרקת** — חוזרים ל„לחיצה תפתח מסך כניסה" |

**אומת בהרצת `bankLine` מהקובץ, על המקרים מהצילום עצמו:**

| מקרה | תוצאה |
|---|---|
| „ממתין להתחברות" מלפני 4 שעות | **„לחיצה תפתח מסך כניסה"** ✓ |
| „קורא משכנתאות" מלפני שעה | **„לחיצה תפתח מסך כניסה"** ✓ |
| הודעת ביניים מלפני 20 דק׳ | **„לחיצה תפתח מסך כניסה"** ✓ |
| סנכרון שרץ עכשיו | „שומר צילומי שיקים 12/24" ✓ |
| הסתיים לפני 3 שעות | „הסתיים ואומת · 7 חשבונות · **לפני 3 שע׳**" ✓ |
| נכשל אתמול | „שגיאה בטעינת הדף · **אתמול**" ✓ |
| מעולם לא סונכרן | ברירת המחדל ✓ |
| אבחון קיים | **גובר**, כמו קודם ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.81.0**.

**לא נבדק:** מראה ויזואלי. אומת בטקסט המוחזר, לא בצילום.

## 2026-08-18 · Claude · מלל כפתורי הבנקים — 0.80.0

**בקשה: „תתקן את המלל לכל כפתורי הסנכרון לבנקים."** אחרי שכל אחד־עשר היעדים
פותחים חלונית כניסה, המלל תיאר מציאות ישנה.

**שלושה תיקונים, ושניים מהם היו מטעים ולא רק מיושנים:**

| מקום | קודם | עכשיו | למה |
|---|---|---|---|
| שורת האריח | **„סנכרון פעיל"** | „לחיצה תפתח מסך כניסה" | ⚠ נקרא כאילו סנכרון **רץ ברגע זה**, בעוד שהכוונה הייתה „נתמך". כל 12 היעדים `ready:true`, ולכן זה הוצג תמיד |
| אריח לא-נתמך | „ממתין לחיבור" | „טרם נתמך" | „ממתין" רומז שמשהו עומד לקרות |
| **הכפתור הגדול** | **„התחברות וסנכרון הכול"** | „התחברות וסנכרון — פועלים עסקי / פרטי / שניהם" | ⚠ **הכי מטעה:** הוא מסנכרן **רק פועלים** — `chosenSources` מחזיר `business`/`private` בלבד. „הכול" פשוט לא נכון |
| כותרת הפאנל | „לחץ על הגוף שברצונך לחבר או לסנכרן" | מתאר את הזרימה החדשה: מסך כניסה נפתח, ואחרי ההתחברות הסנכרון רץ ברקע | — |

**התווית של הכפתור הגדול נגזרת מ-`syncScope` בזמן הרינדור**, ולכן היא משתנה
עם הבחירה במקום להישאר קבועה ולא-נכונה.

**סדר העדיפויות בשורת האריח לא שונה:** אבחון > סטטוס אחרון של אותו בנק >
מלל ברירת המחדל.

**אומת בהרצת `bankLine` ו-`syncAllLabel` מהקובץ:**

| בדיקה | תוצאה |
|---|---|
| ארבעה אריחים, ברירת מחדל | „לחיצה תפתח מסך כניסה" |
| בנק עם סטטוס אחרון | „שומר צילומי שיקים 12/24" — **הקידומת נחתכת** |
| בנק עם אבחון | האבחון גובר |
| הכפתור בשלושת ה-scope | עסקי · פרטי · עסקי ופרטי |
| „סנכרון פעיל" בקוד | נשאר **רק בהערה** שמסבירה את השינוי — לא מוצג |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.80.0**.

**לא נבדק:** מראה ויזואלי. אומת בטקסט שמוחזר, לא בצילום.

## 2026-08-18 · Claude · לאומי הכריז „מחובר" בזמן ההקלדה — 0.79.1

**דיווח: „כניסה לאומי לא עובד, המסך מיד מופרד לפני שאני מכניס סיסמה."**

**השורש — פריט שרשמתי כפתוח ב-0.58.0 ולא טיפלתי בו:**

```js
if(!reported && location.hostname==='hb2.bankleumi.co.il' && document.querySelector('main'))
```

הגייט היחיד ל-`LEUMI_AUTHENTICATED` היה **„מארח hb2 + קיים `<main>`"**.
דף ההתחברות עונה על שני התנאים.

**למה זה התפוצץ דווקא עכשיו:** כל עוד הכניסה הייתה על `www.leumi.co.il`,
שם **אין content script**, הבאג היה **רדום**. ב-0.79.0 העברתי את הכניסה
ל-`hb2` — והוא התעורר: התוסף הכריז „מחובר" בזמן שהמשתמש מקליד, `discoverLeumi`
התחיל וניווט את הדף מתחת לידיו.

⚠ **הלקח, והוא חוזר על עצמו היום בגרסה חדשה:** ב-0.75.1 בדקתי „מי עוד שואל
על אותו מארח" — ובדקתי רק את **background**. **שכחתי ש-content script הוא גם
כזה.** הוא רץ לפי `matches` במניפסט, וזו בדיוק שאלה על מארח.
**הרשימה המלאה: שאילתות לשוניות, ו-`matches` של כל content script.**

**השתנה ב-`leumi-content.js`:** `report()` יוצא מוקדם על `/H/Login.html`
ועל `/gate-keeper/`. יתר התנאים לא נגעו.

**אומת בהרצת `report` מהקובץ, עם `location` מוזרק:**

| דף | הודעות „מחובר" |
|---|---|
| `/H/Login.html` | **0** ✓ |
| אותו דף באותיות קטנות | **0** ✓ |
| `/staticcontent/gate-keeper/he/` | **0** ✓ |
| `/staticcontent/digitalfront/he/` | **1** ✓ |
| **דף השיקים המחובר** (גם תחת `staticcontent`) | **1** ✓ — הסינון לא גלש |
| מארח אחר | 0 ✓ |
| hb2 בלי `<main>` | 0 ✓ — השומר המקורי נשמר |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.79.1**.

**⚠ הפריט מ-0.58.0 סגור רק חלקית.** מה שתוקן: דפי התחברות ושער **אינם**
מוכרזים כסשן. מה שעדיין חסר: **סימן חיובי לכך שהמשתמש מחובר** — לא נמדד,
כי אין דרך למדוד את ה-DOM שאחרי התחברות בלי להתחבר.

## 2026-08-18 · Claude · כאל, מקס ולאומי — הושלם — 0.79.0

**שלושת האחרונים, ובכולם נבדקה מלכודת דיסקונט לפני הכתיבה.**

**נמדד — כל הכתובות מתוך ה-`href` באתרי הבנקים, לא הורכבו:**

| יעד | כתובת כניסה | על מארח הסשן? |
|---|---|---|
| כאל | `digital-web.cal-online.co.il/login?returnedUrl=%2Ftransactions#top` | **כן** ⚠ |
| מקס | `www.max.co.il/login` | **כן** ⚠ |
| לאומי | `hb2.bankleumi.co.il/H/Login.html` | **כן** ⚠ |

⚠ **בשלושתם המלכודת קיימת** — בניגוד לישראכרט ולבינלאומי, שבהם ההתחברות
יושבת על מארח אחר. בלי סינון, חלונית ההתחברות הייתה נחשבת סשן מחובר:
`calTab` **מעדיף** את `digital-web`, `maxTab` נופל ל-`tabs[0]`, ו-`leumiTab`
נופל ל-`tabs[0]`. **כל שלושתם היו נשברים בדיוק כמו דיסקונט ב-0.75.1.**

**השתנה:**
- `calTab` ו-`maxTab` מסננים `/login`.
- **לאומי:** `leumiSession()` מסנן `Login.html`, ו**שלוש** שאילתות הלשוניות
  עטופות בו — לא רק זו של הסנכרון. ⚠ לאומי הוא הרגיש בפרויקט (§9), ולכן
  הסינון נעשה בנקודה אחת משותפת ולא שוכפל.
- שלוש הפתיחות הוחלפו ב-`popup 560×780`.

**אומת בהרצת פונקציות הבחירה מהקובץ:**

| בדיקה | כאל | מקס | לאומי |
|---|---|---|---|
| רק חלונית התחברות פתוחה | **null** ✓ | **null** ✓ | **null** ✓ |
| התחברות + פורטל אמיתי | הפורטל ✓ | הפורטל ✓ | `digitalfront` ✓ |

שאילתות לאומי עטופות: **3** · פתיחות שנשארו לעמודי שיווק: **אין**.
16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.79.0**.

### 🏁 כל אחד־עשר היעדים פותחים עכשיו מסך כניסה

מזרחי · יהב · דיסקונט פרטי · דיסקונט עסקי · הבינלאומי · פועלים עסקי ·
פועלים פרטי · ישראכרט · כאל · מקס · לאומי.

**דפוס שחזר בכל אחד מהם, ושווה לזכור:** כתובת הכניסה נמדדת מה-`href` באתר —
ואז **בודקים מי עוד שואל שאלות על אותו מארח**. חמישה מתוך אחד־עשר היו נשברים
בלי הבדיקה הזו.

**עדיין פתוח:** הנעילה שנתקעת (0.58.0) · הפרדת פורטל עסקי/פרטי בדיסקונט ·
אימות חי של כאל, מקס ולאומי.

## 2026-08-18 · Claude · ישראכרט — חלונית כניסה — 0.78.0

✅ **0.77.0 אושרה חי: פועלים עובד.**

**נמדד:** הדפדפן הפנימי **חסם** את `web.isracard.co.il`, ולכן המדידה נעשתה
מאתר השיווק. ה-`href` של „כניסה לחשבון שלי":

```
https://digital.isracard.co.il/personalarea/Login/
```

**הועתק כלשונו.** באתר נצפו גם וריאנטים עם `returnUrl`/`ReturnUrl` בכתיבים
שונים — **לא הרכבתי אחד כזה.** הזרימה ממילא מנווטת ל-`StatusPage` בעצמה
(`tabs.update(tabId,{url:ISRACARD_HOME})`), ולכן אין צורך ואין סיבה לנחש
פרמטר.

⚠ **בדיקת מלכודת דיסקונט, שוב לפני ולא אחרי:** הכניסה על **`digital`**
בעוד זיהוי הסשן שואל על **`web`** — ולכן חלונית ההתחברות אינה יכולה להיחשב
סשן מחובר. **אין מלכודת.**

**השתנה:** **שלוש** נקודות `if(!tab){tabs.create(ISRACARD_HOME)}` — כולן
ענפי „אין סשן" — הוחלפו ב-`windows.create({url:ISRACARD_LOGIN,type:'popup',
560×780})`. הניווט הפנימי ל-`StatusPage` בזמן הסנכרון **נשמר כפי שהיה**.

**אומת בהרצת `isracardTab` מהקובץ:**

| לשוניות פתוחות | נבחר |
|---|---|
| רק חלונית ההתחברות (`digital`) | **null** → ייפתח חלון ✓ |
| התחברות + `StatusPage` | **StatusPage** ✓ |
| דף `web…/login` ישן | null ✓ — הסינון הקיים עדיין עובד |
| שלוש נקודות הפתיחה | `popup 560×780` ×3 |
| פתיחות שנשארו ל-`StatusPage` | **אין** · הניווט הפנימי **נשמר** |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.78.0**.

**מצב מסכי הכניסה:** מזרחי · יהב · דיסקונט ×2 · הבינלאומי · פועלים ×2 ·
**ישראכרט** ✔ — **נותרו: כאל · MAX · לאומי.**

**עדיין פתוח:** הנעילה שנתקעת (0.58.0) · הפרדת פורטל עסקי/פרטי בדיסקונט.

## 2026-08-18 · Claude · פועלים — חלונית כניסה — 0.77.0

✅ **0.76.0 אושרה חי: הבינלאומי עובד.**

**בפועלים הכתובת כבר הייתה נכונה** (`cfg.login` מוביל למסך הכניסה האמיתי) —
מה שחסר היה שייפתח כ**חלונית** ולא כלשונית מלאה, כמו בשאר היעדים.

**השתנה ב-`openSource`, והבחנה שנשמרה במכוון:**

| מצב | פעולה | למה |
|---|---|---|
| אין לשונית פועלים | **חלונית `popup 560×780`** | הבקשה |
| **יש לשונית מנותקת** | **מנווטים אותה במקום** | ⚠ פתיחת חלונית שנייה הייתה יוצרת שתי לשוניות על אותו מארח, ו-`syncSource` בוחר `tabs[0]` — עלול לתפוס את הישנה |
| יש לשונית מחוברת | ישר לסנכרון | ללא שינוי |

**⚠ הסיכון שנבדק במפורש:** חלונית של 560px היא **פריסה צרה** — בדיוק מה שהפיל
את יהב ב-0.73.0. אחרי ההתחברות `returnToDashboard` **מגדילה את החלונית
למידות הדשבורד**, ולכן הסנכרון אינו רץ בפריסה צרה. **נבדק, לא הונח.**

`openSource` משותף לעסקי ולפרטי — **שניהם קיבלו את השינוי.**

**אומת בהרצת `openSource` ו-`returnToDashboard` מהקובץ:**

| בדיקה | תוצאה |
|---|---|
| אין לשונית | `חלונית popup 560×780` |
| לשונית מנותקת | `tabs.update → דף כניסה` · **בלי חלון שני** |
| לשונית מחוברת | `returnToDashboard` → `queueDiscover` |
| **החלונית אחרי התחברות** | **הוגדלה ל-1720×1010** ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.77.0**.

**מצב פתיחת מסכי כניסה:** מזרחי · יהב · דיסקונט פרטי · דיסקונט עסקי ·
הבינלאומי · **פועלים עסקי ופרטי** ✔ — **נותרו: ישראכרט · כאל · MAX · לאומי.**

**עדיין פתוח:** הנעילה שנתקעת (0.58.0) · הפרדת פורטל עסקי/פרטי בדיסקונט.

## 2026-08-18 · Claude · הבינלאומי פותח את מסך הכניסה — 0.76.0

✅ **0.75.1 אושרה חי: דיסקונט עובד.**

**נמדד (18.08.2026), והפעם גם נבדק מראש מי עוד נשען על אותו מארח:**

| | |
|---|---|
| הטריגר באתר | `a.login-trigger` עם `href="#"` — **מודאל מונע-JS**, אין כתובת ב-href |
| מה נטען במודאל | `iframe#loginFrame` → `online.fibi.co.il/MatafLoginService/MatafLoginServlet?bankId=FIBIPORTAL&site=Private&KODSAFA=HE` |
| המארח | **`online.fibi.co.il`** — אותו מארח שבו מזוהה סשן מחובר |

⚠ **כאן הייתה יכולה להיות חזרה מדויקת על תקלת 0.75.1.** בדקתי לפני ולא אחרי:
זיהוי הסשן בבינלאומי דורש **`/shell/#/` בנתיב** ולא רק את המארח, וכתובת
הכניסה היא `/MatafLoginService/` — **ולכן אין מלכודת.** נרשמה הערה בקוד:
מי שיחליף אי-פעם את הזיהוי לבדיקת מארח בלבד, ישבור את זה.

**השתנה:** `startFibi` פותח את `FIBI_LOGIN` כ-`popup 560×780` במקום את עמוד
השיווק `fibi.co.il/private/`. **בלי סינון נוסף — כי הזיהוי כאן כבר מדויק.**

**אומת בהרצת `startFibi` מהקובץ (`fetch`, בלי שכתוב):**

| מצב | תוצאה |
|---|---|
| אין חיבור | `windows.create popup 560×780` על כתובת הכניסה |
| **לשונית התחברות פתוחה** | **עדיין פותח חלון** — לא בולבל כסשן ✓ (המבחן שדיסקונט נכשל בו) |
| פורטל מחובר (`/shell/#/`) | `returnToDashboard`, **בלי** חלון התחברות ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.76.0**.

**מצב פתיחת מסכי כניסה — 5 מתוך היעדים טופלו:** מזרחי (מודאל) · יהב · דיסקונט
פרטי · דיסקונט עסקי · הבינלאומי. **נותרו:** ישראכרט · כאל · MAX · לאומי ·
פועלים (שני האחרונים כבר מנווטים לדף התחברות אמיתי).

**עדיין פתוח, לא נגעתי:** הנעילה שנתקעת (מ-0.58.0), והפרדת פורטל עסקי/פרטי
בדיסקונט.

## 2026-08-18 · Claude · חלונית ההתחברות נראתה כמו סשן מחובר — 0.75.1

**דיווח: „דיסקונט עסקי לא פותח חלון" + „סינכרון תקוע."** שני סימפטומים,
**שורש אחד — ונסיגה שגרמתי אתמול ב-0.75.0.**

**השורש:** `discountTab()` מחזיר **כל** לשונית על `start.telebank.co.il`.
עד 0.75.0 מסך הכניסה ישב על `discountbank.co.il` — **מארח אחר** — ולכן מעולם
לא נתפס שם. ברגע שהעברתי את הכניסה ל-`start.telebank.co.il`, **חלונית
ההתחברות עצמה נראית כמו סשן פעיל**:

1. `startDiscountBusiness` קורא ל-`discountTab()` · 2. מקבל את **חלונית
ההתחברות** · 3. `if(tab)` → „כבר מחובר" → **לא נפתח חלון** · 4. הזיהוי רץ על
דף הכניסה ולא מוצא דבר → **תקוע**.

**הלקח:** העברת כתובת ממארח אחד לאחר אינה שינוי קוסמטי. **כל בדיקת „האם יש
לשונית מחוברת" שנשענת על מארח בלבד — נשברת ברגע שדף ההתחברות עובר לאותו
מארח.** לפני העברת URL — לבדוק מי עוד שואל שאלות על אותו מארח.

**השתנה — שורה אחת:** `discountTab()` מסנן `/login/`. **לשונית על דף
הכניסה אינה סשן.**

**אומת בהרצת `discountTab` מהקובץ:**

| לשוניות פתוחות | נבחר |
|---|---|
| רק חלונית התחברות (`?bank=d&t=s`) | **null** → הזרימה תפתח חלון ✓ |
| חלונית התחברות בכתיב `#/LOGIN_PAGE` | **null** ✓ |
| התחברות + פורטל עסקי | **הפורטל** ✓ |
| התחברות + פורטל פרטי | **הפורטל** ✓ |
| שום לשונית | null ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.75.1**.

**⚠ שני דברים שלא טופלו, במכוון:**

1. **נעילה שנתקעת.** פתוח מ-0.58.0. השחרור היחיד הוא ⟳. הסבב הזה תיקן את
   **הסיבה** לתקיעה הנוכחית ולא את המנגנון. **זה הפריט הבא שראוי לטפל בו** —
   שחרור מבוסס זמן לנעילות, בלי להסיר את השומרים עצמם (§9).
2. **הפרדת פורטל עסקי מפרטי.** שניהם על `start.telebank.co.il`;
   `/retail3/` ידוע כפרטי מקוד קיים, אך **נתיב הפורטל העסקי לא נמדד**.
   לא ניחשתי. סנכרון עסקי עלול עדיין לבחור לשונית של פרטי אם שתיהן פתוחות.

## 2026-08-17 · Claude · דיסקונט פותח את מסך הכניסה — 0.75.0

**דיווח: „לא עושה כלום בדיסקונט פרטי" + צילום של עמוד השיווק.** אותו מצב
שהיה במזרחי לפני 0.59.0: `startDiscountPrivate` פתח את
`discountbank.co.il/private/`, והמשתמש נדרש לאתר וללחוץ „כניסה לחשבון".

**נמדד חי מתוך ה-`href` באתר, לא נוחש:**

| | |
|---|---|
| הטריגר | `<a>` **רגיל** — אין מודאל ואין לחיצה נדרשת, רק לנווט |
| פרטי | `start.telebank.co.il/login/?multilang=he&bank=d&**t=p**` |
| עסקי | אותו נתיב עם **`t=s`** |
| הדף | עומד בפני עצמו · `#tzId` (מספר זהות) · `#tzPassword` · `#aidnum` (קוד מזהה) |

⚠ **העסקי הוא `t=s` ולא `t=b`.** זה בדיוק סוג הניחוש ה„סביר" שהיה שולח את
המשתמש לדף הלא נכון ועולה סבב שלם. **נמדד, לא הוסק.**

**השתנה:** שני היעדים נפתחים כ-`windows.create({type:'popup',560×780})`,
בעקבות הבקשה מ-0.74.0 שההתחברות תהיה **חלונית** ולא לשונית מלאה.
**גם העסקי טופל** — אותה תקלה בדיוק, ואין סיבה להמתין לדיווח נוסף עליה.

**אומת בהרצת הקוד מהקובץ:**

| בדיקה | תוצאה |
|---|---|
| שני היעדים | `windows.create` **popup** · לא `tabs.create` |
| פתיחות שנשארו לעמוד השיווק | **אין** ✓ |
| ⚠ הקבועים מוגדרים ב-802 ונקראים ב-**668** | נבדק TDZ בהרצה: **שתי הכתובות נפתרות נכון בזמן ריצה** |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.75.0**.

**לא נגעתי במזרחי.** שם ההתחברות עוברת דרך מודאל בעמוד הבית, היא **אומתה חיה**
ועובדת. אחידות אינה שווה שבירת זרימה עובדת; אם ירצו — יש כבר `MIZRAHI_LOGIN`
כקבוע והמעבר לחלונית הוא שורה אחת.

**לא נבדק:** התחברות חיה בדיסקונט, וההמשך אחריה.

## 2026-08-17 · Claude · חלונית כניסה, והחזרה מכל מסלול — 0.74.0

✅ **0.73.0 אושרה חי: יהב עובד.** ירושת מידות החלון הייתה הסיבה.

**שני ליקויים שדווחו, ושניהם שלי:**

### 1. „חלונית הכניסה היא על כל הדף"

טל ביקש **חלונית**; פתחתי לשונית מלאה. השתנה ל-`chrome.windows.create({type:'popup',
width:560,height:780})` — חלון קומפקטי בגודל טופס, והדפדפן הראשי נשאר גלוי מאחור.
Chrome מציג את המקור בחלון popup, ולכן עדיין אפשר לוודא שזה אתר הבנק.

### 2. „בסוף זה לא חוזר לדפדפן הראשי"

**`restoreSyncTabs` נקראה רק מ-`syncSelected`.** יהב, לאומי, מזרחי, כאל, MAX,
דיסקונט, ישראכרט והבינלאומי — **לכולם מסלול ריצה משלהם**, ואף אחד מהם לא החזיר
את הלשונית. בניתי מנגנון החזרה וחיברתי אותו לנתיב אחד מתוך תשעה.

**השתנה:** `await restoreSyncTabs()` בכל תשעת ה-`finally` של מסלולי הריצה.
נמדד אחרי השינוי: **9 מסלולים מחזירים**, והשניים שנותרו (`autoBusy`) עוטפים
את `syncSelected` שכבר מחזירה.

### שינוי שלישי, נגזר מהראשון

**המידות של חלון העבודה נלקחות עכשיו מחלון הדשבורד ולא מחלון המקור.** אחרת
החלונית הקומפקטית של ההתחברות הייתה הופכת לחלון העבודה, **ומשחזרת בדיוק את
תקלת 0.73.0** — פריסה צרה ובורר תאריכים שלא מרונדר. בנוסף: לשונית שכבר לבדה
בחלון קטן — **החלון מוגדל** לגודל שולחני במקום להישאר צר.

**אומת בהרצת הקוד מהקבצים (`fetch`, בלי שכתוב):**

| מצב | תוצאה |
|---|---|
| בנק בחלון משותף · דשבורד 1720×1010 | חלון עבודה נוצר **1720×1010** |
| בנק לבדו **בחלונית 560** | **החלון הוגדל ל-1720×1010** ✓ |
| אין מידות כלל | הועברו `tabId` ו-`focused` בלבד — **אפס ערכים פסולים** |
| מסלולים שמחזירים לשוניות | **9** · ללא החזרה: `autoBusy` בלבד, מכוסה |
| חלונית הכניסה ביהב | `popup 560×780` |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.74.0**.

**הלקח:** מנגנון שנבנה ונקשר לנתיב אחד מתוך תשעה אינו מנגנון אלא מקרה פרטי.
**אחרי בניית מנגנון רוחבי — לספור את נקודות החיבור מול רשימת המסלולים.**

## 2026-08-17 · Claude · חלון העבודה היה צר מדי — 0.73.0

**דיווח: „שגיאה ביהב: לא ניתן היה להגדיר טווח של שלושה חודשים" · „עוד פעם
חוזרים אחורה."** התסכול מוצדק — **זו נסיגה שגרמתי ב-0.69.0.**

**⚠ הפגם:** `chrome.windows.create({tabId,focused:false})` **בלי מידות.**
Chrome פותח חלון בגודל ברירת מחדל, **צר מהחלון של המשתמש**. אתרי בנק משנים
פריסה לפי רוחב, ו-`setThreeMonths` מחפש `input.date-picker-input` — רכיב
שנעלם בפריסה צרה. הוא החזיר `false`, וזה הפיל את כל הסנכרון.

**הלקח, והוא רחב מיהב:** **גודל החלון הוא חלק מהסביבה שהמתאמים נמדדו מולה.**
העברת לשונית לחלון אחר אינה פעולה ניטרלית — היא משנה breakpoint. פועלים שרד
במקרה; יהב לא. **כל חלון עבודה חייב לרשת את מידות המקור.**

**השתנה:**
1. חלון העבודה יורש `left/top/width/height` מהחלון המקורי. ערך לא-תקין מסונן,
   כדי שלא נעביר `undefined` ל-API.
2. כישלון בהגדרת הטווח **אינו קטלני עוד**: נקראת התקופה שהדף מציג, והאבחון
   „טווח 3 חודשים לא נקבע" מוצג על אריח יהב. **חשוב שיוצג** — אחרת המשתמש
   יחשוב שקיבל שלושה חודשים. אותו עיקרון כמו ביתרה חסרה (0.64.0) ובהלוואות
   פועלים (0.67.0). נוסף גם `withTimeout` של 30 שניות, שלא היה.

**אומת בהרצת הקוד מהקבצים (`fetch`, בלי שכתוב):**

| בדיקה | תוצאה |
|---|---|
| חלון מקור 1720×1010 ב-(120,40) | חלון העבודה נוצר **באותן מידות בדיוק** · `focused:false` |
| חלון מקור בלי מידות תקינות | נוצר עם `tabId`+`focused` בלבד — **בלי ערכים פסולים** |
| הטווח נקבע | לא נזרק · בלי אבחון |
| הטווח נכשל | **לא נזרק** · אבחון „טווח 3 חודשים לא נקבע — נקראה התקופה שהדף הציג" |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.73.0**.

**⚠ עדיין לא הוכח שהרוחב היה הסיבה.** ההסבר עקבי עם מה ששונה ועם אופי הרכיב,
אבל לא נמדד חי. **ההכרעה בהרצה הבאה:** אם יהב עובר — אושר. אם עדיין נופל,
הסנכרון **ימשיך** ויציג „טווח 3 חודשים לא נקבע", **ואז החשד עובר לסלקטור של
בורר התאריכים**, שיצטרך מדידה על הדף החי.

## 2026-08-17 · Claude · יהב פותח את מסך הכניסה — 0.72.0

✅ **0.71.0 אושרה חי: „יופי עובד."** פועלים פרטי קורא תנועות. ההסקה שהרשת
אינה `<table>` הייתה נכונה, וזה סוגר את הפרשה שנפתחה ב„נעצר בהלוואות".

**הבקשה:** כמו מזרחי — שהסנכרון יפתח את מסך הכניסה של יהב.

**נמדד חי (17.08.2026), ולא נוחש:**

| | |
|---|---|
| `digital.yahav.co.il` | **מפנה ל-`login.yahav.co.il/login/`** — מארח אחר |
| מסך הכניסה | **עמוד עצמאי**, בלי מודאל ובלי iframe, מרונדר מיד |
| שדות | `#username` (קוד משתמש) · `#pinno` (ת״ז, 9 ספרות) · `#password` · כפתור „כניסה" |

**שונה ממזרחי, ולכן פתרון אחר:** במזרחי היה צריך ללחוץ על טריגר כדי לפתוח
מודאל. **ביהב אין מה ללחוץ** — הכתובת עצמה היא המסך. לכן פתיחה ישירה של
`YAHAV_LOGIN` במקום כתובת האפליקציה, שהסתמכה על שרשרת הפניות.

**⚠ החלטת אבטחה מכוונת:** `login.yahav.co.il` **לא נוסף ל-`host_permissions`**.
פתיחת לשונית אינה דורשת הרשאה, ולכן לתוסף אין ולא תהיה גישה לדף שבו מוקלדת
הסיסמה. **הרשאה שלא צריך — לא מבקשים**, ובוודאי לא על דף אישורים.

**עוד בסבב:** במסלול „כבר מחובר" הוחלף `chrome.tabs.update({active:true})`
ב-`returnToDashboard(tab.id,true)` — יהב היה היעד האחרון שנשאר עם משיכה לחזית.

**אומת בהרצת `startYahav` מהקובץ (`fetch`, בלי שכתוב):**

| מסלול | פעולות |
|---|---|
| לא מחובר | סטטוס „ממתין להתחברות ליהב — הזן קוד משתמש, ת״ז וסיסמה" · `tabs.create login.yahav.co.il/login/ active=true` |
| כבר מחובר | `returnToDashboard` → `reload` → „מתחיל סנכרון" · **בלי `tabs.update active`** |
| `login.yahav` בהרשאות | **לא קיים** ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.72.0**.

**לא נבדק:** התחברות חיה ביהב, וההמשך אחרי שהמשתמש מקליד.

## 2026-08-17 · Claude · קורא שני לרשת שאינה table — 0.71.0

**המשתמש שלח צילום של דף התנועות בפועלים פרטי.** הוא שווה יותר משורת האבחון
שביקשתי, כי הוא נותן את **מבנה העמודות האמיתי**:

```
תאריך | הפעולה | חובה | זכות | יתרה בש"ח
```

חמש עמודות, והתאריך מגיע עם אות יום: „**ו׳ 14/08/26**".

**ההסקה שהכריעה:** `extractTransactions` **יודעת** לטפל בחמש עמודות (הענף
`cells.length<7`), והביטוי שלה מוצא `14/08/26` גם עם אות היום לפניו. כלומר
**אילו זו הייתה `<table>` — היא הייתה נקראת.** מכאן שהרשת בפרטי אינה `<table>`,
בדיוק כמו בלאומי.

**השתנה — `gridTransactions()`, קורא שני:**
- שורות לפי `[role="row"]`, כותרות לפי `[role="columnheader"]`.
- ⚠ **מיפוי עמודות לפי טקסט הכותרת ולא לפי היסט קבוע** — הדפוס המוכח בלאומי.
  **בדף הזה יש עמודות אייקון** (הוראת-קבע, מפתח), והיסט קבוע היה נשבר עליהן.
- התאריך נחלץ בביטוי מתוך „ו׳ 14/08/26".
- **המסלול הקיים לא נגע.** `<table>` נקראת קודם; הקורא השני נכנס רק כשאין
  טבלה **או** כשהטבלה לא הניבה ולו שורה אחת. עסקי אינו יכול להיסוג מכאן.

**אומת בהרצת המחלץ מהקובץ על ארבעה מבני DOM:**

| מבנה | תוצאה |
|---|---|
| רשת role **בדיוק כמו בצילום** | **3 שורות** · `debit:4000 · credit:null · balance:18399 · date:14/08/26` |
| אותה רשת **+ שתי עמודות אייקון** | **2 שורות, ערכים זהים** — המבחן שמוכיח שהמיפוי לפי כותרת ולא לפי היסט |
| `<table>` אמיתית (מסלול עסקי) | **1 שורה, ללא שינוי** |
| טבלה קיימת אך ריקה + רשת role לצידה | **1 שורה** — הנפילה לאחור עובדת |

שורת „משכורת" אומתה בנפרד: `credit:12000 · debit:null` — כלומר חובה וזכות
אינן מתחלפות.

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.71.0**.

**⚠ עדיין לא הוכח חי שהרשת בפרטי היא ARIA.** ההסקה חזקה אך אינה מדידה. אם
המבנה שם הוא שלישי — `div` בלי `role` כלל — שני הקוראים יחזירו ריק, **וטביעת
האצבע מ-0.70.0 תוצג על האריח ותגיד זאת** (`role=row:0`). זה התרחיש היחיד
שנותר, ויש לו כבר מכשיר מדידה.

## 2026-08-17 · Claude · פרטי מסנכרן, אבל בלי תנועות עו״ש — מדידה — 0.70.0

**דיווח: „הצליח לסנכרן, אבל לא הוריד תנועות עו״ש."**

✅ **0.69.0 אושרה בהרצה חיה.** הסנכרון הגיע לסופו במקום להיתקע בהלוואות —
כלומר **ההשערה על הלשונית המוסתרת הייתה נכונה**, וההחזרה לחלון עבודה נראה
פתרה אותה. זה סוגר את התלייה מ-0.62.0.

**החשד לתנועות — ולא תוקן על סמכו:**

```js
const table=[...document.querySelectorAll('table')].find(t=>/תאריך/.test(t.innerText)&&/(חובה|זכות)/.test(t.innerText));
if(!table)return [];
```

`extractTransactions` קוראת **רק מ-`<table>`**. אם בפורטל הפרטי הרשת בנויה
מ-`div` עם `role="row"` — הפונקציה מחזירה **רשימה ריקה בשקט**. זה בדיוק המקרה
שכבר מתועד ב-`README` לגבי לאומי („`querySelectorAll('table')` מחזיר אפס").

⚠ **הפונקציה לא נגעה היום** — הדיף מאשר. זו אינה נסיגה אלא תקלה שהייתה שם
מלכתחילה, ופרטי כנראה מעולם לא נקרא עד הסוף.

**⚠ לא שיניתי את המחלץ ולא ניחשתי סלקטור.** במקום זה נוספה **מדידה** שמגיעה
למשתמש בלי קונסול: כשחשבון חוזר בלי ולו תנועה אחת, נאספת טביעת אצבע **מבנית**
של הדף והיא מוצגת **על אריח הבנק** (`bankDiagnostics`, גובר על הסטטוס הרגיל).

**⚠ פרטיות — שיקול מכוון:** הטביעה כוללת **ספירות וכותרות עמודות בלבד**.
אין בה תאריכים, סכומים, שמות ספקים או אסמכתאות — כי המשתמש מצלם אותה ושולח.
נבדק במפורש מול DOM שמכיל סכומים ושמות: **אפס דליפה**.

**אומת בהרצת הקוד מהקבצים על שני מבני DOM:**

| מבנה | `extractTransactions` | הטביעה |
|---|---|---|
| `<table>` תקין | **2 שורות** — המחלץ עובד | — |
| רשת ARIA בלי `<table>` | **0 שורות** — הכשל משוחזר | `טבלאות:0 · grid:1 · role=row:3 · tr:0 · כותרות: תאריך \| תיאור \| אסמכתא \| חובה \| זכות \| יתרה` |
| דליפת נתונים פיננסיים | — | **אין** ✓ |
| אריח עם אבחון | — | מציג את שורת האבחון |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.70.0**.

**הצעד הבא — הקריאה מוכרעת לפי הטביעה:**
- `טבלאות:0` ו-`role=row` גדול מאפס → **רשת ARIA**, ואז לכתוב קורא לפי
  `role` בדומה ל-`gridRows()` של לאומי, **עם מיפוי עמודות לפי הכותרות שיוצגו**.
- `טבלאות` גדול מאפס → הטבלה קיימת אך התנאי או מבנה השורות שונה, ואז לתקן
  את הסינון ולא את מקור הקריאה.

## 2026-08-17 · Claude · מה שנשבר בפועלים פרטי — הלשונית הייתה מוסתרת — 0.69.0

**טל: „לא הייתה בעיה עם קריאת הפועלים פרטי, מה נשבר?"** שאלה נכונה. עשיתי
`git diff 4a600e0..HEAD` על מסלול הקריאה, ויש **שינוי מהותי אחד בלבד**:

```diff
-async function prepareRoute(...){… chrome.tabs.update(tabId,{url,active:true}) …}
+async function prepareRoute(...){… chrome.tabs.update(tabId,{url}) …}
```

כל השאר בקריאת פועלים קוסמטי (`syncStatus` → `syncStep`) או עטיפות מאוחרות
יותר. **הקוד שקורא את הנתונים לא נגע** — `poalim-content.js` השתנה בשלוש שורות
בלבד, וכולן בטיפול ביתרה חסרה.

**מה זה עשה:** יחד עם 0.62.0 — שהחזיר את הלשונית לחלון הראשי **בלי להפעיל
אותה** — לשונית פועלים היא לשונית **מוסתרת לאורך כל הסנכרון**. Chrome משהה בה
`requestAnimationFrame`, והדפים לא מצוירים.

**זה מסביר את שני הסימפטומים בסדר שבו הופיעו:**
1. „לא נמצאה יתרה לחשבון 645-301975" — **קריאה ריקה**, הכרטיס לא צויר.
2. אחרי שהפכתי את זה ללא-קטלני — „**נעצר בהלוואות**", הדף הבא שלא צויר.

⚠ **וזו בדיוק האזהרה שכתבתי ב-0.62.0**, מילה במילה: „סנכרון שנתקע או מחזיר
טבלה ריקה, בלי שרואים דפדוף." **רשמתי את הסיכון ואז לא קישרתי אליו במשך
ארבעה סבבים**, וחיפשתי במקומות אחרים. **כשמופיע סימפטום — לקרוא קודם את
האזהרות שכבר רשומות ב-HANDOFF.**

**עסקי שרד** ככל הנראה מפני שדפיו כבר היו טעונים; פרטי התחיל מאפס.

**השתנה — שתי הבקשות של טל מתקיימות שתיהן, בלי להתפשר על הרינדור:**
- **בזמן סנכרון** הלשונית יושבת בחלון עבודה נפרד עם `focused:false`. שם היא
  הפעילה בחלונה, נשארת `visible` ומרנדרת — והתוסף נשאר בחזית.
- **בסיום** `restoreSyncTabs` מחזירה אותה לחלון הראשי. נקראת מ-`finally`
  של `syncSelected`, ליד `endProgress`.

**אומת בהרצת הקוד מהקובץ (`fetch`, בלי שכתוב):**

| שלב | פעולות |
|---|---|
| בזמן סנכרון | חלון נפרד `focused=false` → הדשבורד קדימה · הלשונית **מסומנת להחזרה** |
| בסיום | `tabs.move` **חזרה לחלון הראשי** · הסימון נוקה · הדשבורד קדימה |
| החזרה שנייה | **no-op** ✓ |
| `prepareRoute` כבר לא מפעיל לשונית | ✓ — מיותר, היא ממילא היחידה בחלונה |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.69.0**.

**⚠ עדיין השערה שלא הוכחה חיה.** הקישור בין ההסתרה לבין הכישלון **הגיוני
ותואם את סדר הסימפטומים**, אבל לא נמדד — אין דרך למדוד בלי להתחבר לבנק.
**המבחן: אם פרטי מסתיים עכשיו, ההשערה אושרה. אם הוא עדיין נעצר בהלוואות —
ההסתרה לא הייתה הסיבה, והחשד עובר לנתיב `credit-and-mortgage` בפורטל הפרטי.**
`poalimSkipped` יבחין בין השניים.

## 2026-08-17 · Claude · כישלון אינו הצלחה — הפרדת הצינונים — 0.68.0

**טל, אחרי שראה „פועלים פרטי: סונכרן לאחרונה לפני פחות מ-6 שעות":**
*„נראה זה בעיה של סינכרון בפחות מ-6 שעות."* **הוא קרא נכון, וזו תקלה שאני
יצרתי ב-0.64.0.**

**⚠ נסיגה שלי:** כדי לעצור את הלולאה גרמתי ל**כישלון** לרשום את `autoSyncLast` —
אותו שדה של הצלחה. שתי תוצאות רעות:

1. התוסף הודיע „**סונכרן** לאחרונה" בדיוק כששום דבר לא נשמר. **הודעה שקרית,
   לא רק מטעה.**
2. ניסיון אוטומטי נחסם ל-**6 שעות** בגלל כישלון בן דקה.

**הלקח:** „לעצור לולאה" ו„לסמן הצלחה" הם שני דברים שונים. שימוש חוזר בשדה
קיים כדי לחסוך שדה חדש **מזייף את המשמעות שלו**.

**השתנה:** `autoSyncFailed` נפרד, עם צינון של **15 דקות** במקום 6 שעות.
הלולאה עדיין נעצרת — אבל אחרי רבע שעה יש ניסיון נוסף, ולא אחרי חצי יום.
בחלון הזה **לא נכתבת שום הודעה**: הודעת הכישלון המקורית עדיין על המסך והיא
המדויקת. סנכרון מוצלח **מנקה** את הסימון.

**אומת בהרצת `maybeAutoSync` מהקובץ (`fetch`, בלי שכתוב):**

| מצב | נכתב | צינון הצלחה | צינון כישלון |
|---|---|---|---|
| סנכרון מוצלח | „סנכרון אוטומטי 1/3…" | **נרשם** | **נוקה** |
| מוצלח אחרי כישלון בן 20 דק׳ | כנ״ל | נרשם | **נוקה** |
| סנכרון שנכשל | „נכשל ולא נשמר עדכון: נעצר…" | **—** | **נרשם** |
| 3 דק׳ אחרי כישלון | **כלום** — שקט מוחלט | — | נרשם |
| 30 דק׳ אחרי הצלחה | „סונכרן לאחרונה…" — **לגיטימי** | נרשם | — |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.68.0**.

**⚠ תיקון למדידה שלי, שוב:** בהרצה הראשונה שני מקרי ההצלחה נפלו על
`delay is not defined` — **חוסר ברתמה, לא בקוד.** הוספתי את התלות והרצתי שוב.
נפילה ברתמה אינה תוצאה, וגם אינה „עובד".

**מצב קיים אצל המשתמש:** `autoSyncLast.private` כבר מוטבע מהכישלון של 0.64.0,
ולכן ה**סנכרון האוטומטי** של פרטי חסום עוד כ-5.5 שעות. **לחיצה ידנית על האריח
עוקפת אותו** — `startChosenSync` שולח `force:true`, ו-`start()` מדלג על בדיקת
הרעננות. אין צורך למחוק אחסון.

**עדיין פתוח:** דף ההלוואות בפרטי (0.67.0) — `poalimSkipped` יגיד אם זה הנתיב
או המחלץ.

## 2026-08-17 · Claude · „נעצר בהלוואות" — sendMessage בלי תקרה — 0.67.0

**דיווח: „נעצר בהלוואות."** שלוש מילים, ואחרי שהסטטוס הפר-בנקי של 0.66.0 סוף
סוף אפשר לראות של מי ההודעה — הן הספיקו.

**שתי תקלות באותה שורה:**

```js
const loans=await chrome.tabs.sendMessage(tab.id,{type:'EXTRACT_PRODUCT_DETAILS',kind:'loans',keys});
if(!loans?.ok)throw Error(loans?.error||'קריאת ההלוואות נכשלה');
```

1. **`sendMessage` בלי `withTimeout`.** אם הסקריפט בדף לא עונה, ההמתנה
   **אינסופית** — לא שגיאה, לא ניסיון חוזר, פשוט תקוע. **זה ה„נעצר".**
   ⚠ `withTimeout` קיים בפרויקט ומשמש בלאומי — **הוא פשוט לא הוחל כאן.**
   נמדד: **אפס** קריאות `EXTRACT_*` בפועלים עטופות בו.
2. **כישלון בהלוואות הפיל את כל הסנכרון** — כולל תנועות ויתרות שכבר נאספו.

**השתנה:** שלב ההלוואות ושלב הכרטיסים עטופים ב-`withTimeout` של 45 שניות
וב-`try/catch`. כישלון מדלג על אותו מוצר בלבד, והסיבה נרשמת ב-`poalimSkipped`.

**זה אותו עיקרון שכבר יושם פעמיים:** יתרה חסרה (0.64.0) ולאומי ב-`README`
(„אם הכרטיס לא רונדר — נופלים אחורה ולא מכשילים את הסנכרון"). **מוצר משני
שנכשל אינו מוחק את הליבה.**

**אומת בהרצת `syncSource` מהקובץ (`fetch`, בלי שכתוב):**

| תרחיש | תוצאה |
|---|---|
| `withTimeout` האמיתי על המתנה אינסופית | **נעצר** ומחזיר „קריאת ההלוואות לא השיב תוך…" |
| הלוואות מחזירות `ok:false` | הסנכרון **מסתיים** · חשבון נשמר · **2 תנועות · יתרה 1234** · `poalimSkipped='הלוואות: הדף לא נטען'` |
| הלוואות זורקות | זהה, עם הסיבה „אין תשובה" |
| הכול תקין | ללא דילוג, חמשת השלבים רצים |
| תקרת 45 שנ' בקוד | ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.67.0**.

**⚠ מה שעדיין לא ידוע: *למה* דף ההלוואות בפרטי לא עונה.** שתי אפשרויות שלא
הוכרעו — הנתיב `credit-and-mortgage` שונה בפורטל הפרטי, או שהמחלץ עצמו נתקע.
**לא ניחשתי נתיב.** אחרי ההרצה הבאה `poalimSkipped` יגיד מי מהשניים:
„לא נטען בזמן" → הנתיב · „לא השיב תוך 45 שניות" → המחלץ.

**הצעד הבא:** להריץ פרטי, לקרוא את `poalimSkipped`, ורק אז לתקן את דף
ההלוואות עצמו. הסנכרון אמור להסתיים גם בלי הלוואות.

## 2026-08-17 · Claude · סטטוס נפרד לכל בנק — 0.66.0

**טל: „יכול להיות 8 בנקים פתוחים, הסנכרון צריך להיות של הבנק הנכון."**
**הוא צודק, וזה פגם עיצובי ולא תקלה נקודתית.** `syncStatus` הוא **מחרוזת גלובלית
אחת** לכל היעדים; מי שכותב אחרון מנצח. 0.65.0 חסם שתי דריסות ספציפיות, אבל
המבנה נשאר — וכל עוד הוא כזה, כל בנק שמדבר מוחק את מה שהשני אמר.

**מה שנעשה:** כל אריח בנק מציג את **ההודעה האחרונה שלו עצמו**, ב-`statusBySource`.

**למה בצד הדשבורד ולא ב-background:** ההודעות **כבר** נושאות את שם הבנק
(„פועלים פרטי: ..."), ולכן אפשר לשייך אותן בקריאה. החלופה — להעביר `source`
בכל אחת מעשרות נקודות הכתיבה שבתוך זרימות הסנכרון — הייתה נוגעת בכל קובץ
הליבה, בלי להוסיף דיוק. **הסטטוס הגלובלי נשאר כפי שהוא, ואף זרימת סנכרון
לא נגעה.**

**השיוך:** התאמה לשם הבנק, **והשם הארוך ביותר מנצח** — אחרת „פועלים עסקי"
ו„פועלים פרטי" היו מתנגשים. הודעה כללית („מחפש חיבורים פעילים") אינה משויכת
לאיש, וזה נכון. `bankLine` מסירה את הקידומת הכפולה, כי שם הבנק כבר מעל.

**אומת בהרצת הקוד מהקובץ (`fetch`, בלי שכתוב) על 13 הודעות:**

| | |
|---|---|
| כל 12 הבנקים | **שויכו נכון** — כולל שני הזוגות המתנגשים: פועלים עסקי/פרטי, דיסקונט עסקי/פרטי |
| מחרוזות המשתמש מהצילומים | „פועלים עסקי: סונכרן לאחרונה…" → `business` · „…בפועלים פרטי נכשל…" → `private` |
| הודעה כללית | **לא שויכה** ✓ |
| טקסט באריח | „פועלים פרטי: מוריד תנועות" → **„מוריד תנועות"** |
| כתיבה חוזרת על ערך זהה | **no-op** ✓ — אחרת כל כתיבה מפעילה `load` ונוצרת לולאת רינדור |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.66.0**.

**⚠ מה שהסבב הזה לא פתר, שוב: פועלים פרטי עדיין לא מסנכרן.** שלושת הסבבים
האחרונים טיפלו כולם ב**נראוּת** — לולאה, דריסה, שיוך — ולא בתקלה עצמה. הסיבה
שזה היה נכון: בלי הודעה נכונה לא ניתן לאבחן. **עכשיו יש.**

**הצעד הבא:** להריץ פועלים פרטי ולקרוא את ההודעה **מהאריח של פועלים פרטי**.
היא כבר לא תידרס בידי אף בנק אחר.

## 2026-08-17 · Claude · הודעה של בנק אחד הסתירה את הכישלון של השני — 0.65.0

**דיווח: „לא הצליח."** ביקשתי את ההודעה, והצילום הראה:

> „**פועלים עסקי**: סונכרן לאחרונה לפני פחות מ-6 שעות — הבא בעוד 5 שע' ו-33 דק'."

**המשתמש ניסה לסנכרן פועלים פרטי, והבאנר הציג הודעה על עסקי.** הגרסה אומתה
כ-0.64.0 מול המשתמש, כך שהקוד החדש אכן רץ — הבעיה אחרת.

**השורש:** ללשונית של עסקי יש ניווטים משלה; כל אחד יורה `AUTHENTICATED` →
`maybeAutoSync('business')` → הצינון תופס → **הודעת הדילוג נכתבת מעל הסטטוס של
פרטי שבאמצע עבודה.** ל-`maybeAutoSync` ול-`maybeAutoRun` **לא הייתה בדיקת
`running`** — נמדד: אפס אזכורים בשתי הפונקציות.

⚠ **זו לא תקלה קוסמטית.** היא הסתירה מהמשתמש — וממני — את סיבת הכישלון האמיתית
של פרטי, ולכן שני סבבים חיפשו במקום הלא נכון. **הודעה בעדיפות נמוכה שדורסת
הודעה בעדיפות גבוהה היא באג אבחון.**

**השתנה — שתי הגנות:**
1. `if(running)return false` בראש שתי הפונקציות. סנכרון אוטומטי אינו מדבר
   בזמן שסנכרון אחר עובד.
2. הודעת הדילוג אינה נכתבת כשהסטטוס הנוכחי הוא שגיאה או כישלון. **שגיאה לא
   נדרסת בהודעה אינפורמטיבית.**

**אומת בהרצת `maybeAutoSync` מהקובץ עצמו:**

| מצב | נכתב לסטטוס |
|---|---|
| סנכרון פרטי רץ (`running=true`) | **כלום** ✓ |
| שקט, צינון תופס | ההודעה — **לגיטימית, נשמרה** ✓ |
| הסטטוס הוא שגיאה של בנק אחר | **כלום** ✓ |
| סנכרון הסתיים לפני פחות מדקה | **כלום** ✓ (0.63.0) |

**אימות נוסף שהרתמה נאמנה:** המקרה השני הפיק **בדיוק** את מחרוזת המשתמש, כולל
„5 שע' ו-33 דק'" — כלומר הסימולציה משחזרת את מצבו ולא מצב מומצא.

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.65.0**.

**⚠ מה שהסבב הזה לא פתר:** **פועלים פרטי עדיין לא סונכרן.** תוקן רק מה שהסתיר
את הסיבה. ההרצה הבאה אמורה להציג הודעה **על פרטי** — ורק אז אפשר יהיה לאבחן.

**נמדד אגב כך:** `chosenSources` — `scope` הוא `business` כברירת מחדל, ו-
`#syncAll` מריץ את ה-scope השמור. פרטי מסונכרן בלחיצה על אריח „פועלים פרטי"
ברשימת הבנקים (`startChosenSync(bank.id)`), לא בכפתור „סנכרון הכול".

## 2026-08-17 · Claude · לולאת הסנכרון האוטומטי בפועלים פרטי — 0.64.0

**דיווח:** „נכנס ללופ, לא מצליח לסנכרן" (פועלים פרטי). **הנתון שהכריע** הגיע
בצילום מסך מהמשתמש: *„סנכרון אוטומטי בפועלים פרטי נכשל ולא נשמר עדכון: לא נמצאה
יתרה לחשבון 645-301975."* בלי המחרוזת הזו הייתי מחפש במקום הלא נכון — התחלתי
לחשוד ב-`discover` וזה לא היה שם.

### שורש הלולאה — שומר שהוסר בדיוק כשנדרש

```js
}catch(e){releaseAutoLogin(source,tabId); … 'נכשל ולא נשמר עדכון' … }
```

`autoSyncLast` נכתב **רק בהצלחה** (שורה 222). בכישלון הקוד **מחק את הדיבאונס**
של 90 השניות — `releaseAutoLogin` נקרא **פעם אחת בכל הקובץ, בדיוק שם**. התוצאה:

1. סנכרון אוטומטי נכשל · 2. הדיבאונס נמחק · 3. הצינון לא נרשם ·
4. הניווטים של הסנכרון עצמו משנים pathname ו-`reportAuthenticated` יורה ·
5. `acceptAutoLogin` עובר כי המפתח נמחק · 6. רץ שוב ← **לולאה ללא שום חסם.**

**השתנה:** `releaseAutoLogin` הוסר (הגדרה וקריאה — לא נותרו אזכורים בקוד חי),
והצינון נרשם **גם בכישלון**. ריצה אוטומטית אחת, ואז הכדור אצל המשתמש.

### שומר שני שחסר — `queueDiscover`

`.catch(()=>{})` **ריק**: `discover` מסירה את הבנק מ-`pendingSources` רק בהצלחה,
ובכישלון היא חוזרת לנצח **בלי לדווח מה נכשל**. נוספה תקרת 3 ניסיונות למפתח
`source|tabId`, שנפתחת מחדש ב-`start()`, ודיווח הסיבה בכל ניסיון.

### שורש הכישלון עצמו — יתרה חסרה הפילה הכול

`poalim-content.js:50` **זרק** כש-`extractBalance()` החזירה `null`, וזה הפיל את
הסנכרון של **כל** החשבונות בגלל אחד. `extractBalance` מחפשת אלמנט שהטקסט הישיר
שלו הוא **בדיוק** `'יתרה בחשבון'`.

⚠ **השערה שלא נמדדה:** בפורטל הפרטי (`/ng-portals/rb/`) הניסוח כנראה אחר. **לא
אימתתי ולא ניחשתי ניסוח חלופי** — אין דרך למדוד בלי להתחבר, ולא התחברתי.

**השתנה — הסרת הקטלניות, לא ניחוש הסלקטור:** המתנה של 3 שניות לרינדור, ואם עדיין
אין — החשבון נשמר עם `balance:null` ומסומן `balanceMissing`. **זה הדפוס שכבר תועד
בלאומי** ב-`README` („אם הכרטיס לא רונדר — נופלים אחורה ולא מכשילים את הסנכרון").
`syncSource` ממילא ממזג יתרות מדף ריכוז היתרות, ולכן היתרה עשויה להתמלא שם.
נוסף מפתח אבחון `poalimNoBalance`.

**אומת בהרצת הקוד מהקבצים (`fetch`, בלי שכתוב):**

| בדיקה | תוצאה |
|---|---|
| `queueDiscover` — 6 קריאות ברצף | **3 ריצות בפועל**, ואז עצירה |
| ההודעות | „ניסיון 1 מתוך 3" · „2 מתוך 3" · **„נכשל אחרי 3 ניסיונות ונעצר"** |
| `releaseAutoLogin` בקוד חי | **0 אזכורים** (נותר רק בהערה שמסבירה למה הוסר) |
| הצינון נרשם בכישלון | ✓ |
| זריקת השגיאה על יתרה חסרה | **הוסרה** · `balanceMissing` מסומן · המתנה 3 שנ׳ קיימת |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.64.0**.

**⚠ תיקון למדידה שלי:** בבדיקה הראשונה הרתמה שברה את `queueDiscover` (ניסיתי
לחווט מחדש את `discoveryChain` בהחלפת מחרוזות). תוקן בכך שהמשתנה סופק מבחוץ
והמקור הורץ **כמו שהוא**. שכתוב קוד בתוך רתמת בדיקה אינו בדיקה של אותו קוד.

**הצעד הבא — זו עדיין לא תקלה פתורה, רק לולאה שנעצרה.** אחרי הרצה חוזרת:
1. אם הסנכרון מסתיים — לבדוק אם ליתרת `645-301975` יש ערך (הגיע מדף ריכוז היתרות).
2. אם היא ריקה — `poalimNoBalance` יראה זאת, **ואז צריך למדוד את ניסוח התווית
   בפורטל הפרטי** ולהרחיב את `extractBalance`. **לא לנחש ניסוח.**

## 2026-08-17 · Claude · הפעולה בתוך הגלגל, ו„הסתיים בהצלחה" — 0.63.0

**דיווח המשתמש: „הפעם זה פעל יפה."** 0.62.0 עבד — הסנכרון רץ, התוסף נשאר בחזית,
ולא דווח על תקיעה. **כלומר אזהרת ה-rAF מ-0.62.0 לא התממשה בפועלים העסקי.**
היא נשארת רלוונטית לבנקים שטרם נבדקו.

**הבקשות:** טקסט הפעולה **בתוך** הגלגל („מוריד תנועות" · „מוריד יתרות" ·
„מוריד הלוואות" · „מוריד כרטיסי אשראי"), וניסוח סיום מפורש.

**השתנה:**
- `syncStep(status,action)` — `action` הוא טקסט קצר לגלגל, **נפרד** מהסטטוס
  המלא שנשאר ארוך ומפורט. חמשת השלבים ב-`syncSource` מסומנים.
- הגלגל גדל 56 → **118px**, והמרכז מציג אחוז ומתחתיו את הפעולה. שורת
  „שלב X מתוך Y · נותרו…" עברה מחוץ לטבעת.
- תווית הסיום: `הסתיים` → **„הסנכרון הסתיים בהצלחה"**. שאר המצבים לא נגעו.

**השלמה נדרשת שלא נתבקשה במפורש — ובלעדיה הבקשה לא מתקיימת:** הודעת „הסנכרון
דולג" (שנרשמה כפתוחה ב-0.62.0) **דרסה בדיוק את הודעת הסיום הזו**. לכן הוחל
התיקון שהוצע: הודעת הדילוג אינה נכתבת כשהצינון נובע מסנכרון שהסתיים לפני פחות
מדקה. **החלטת הדילוג עצמה לא השתנתה** — רק הכתיבה לסטטוס.

**אומת בהרצת הקוד מהקבצים (`fetch`, בלי שכתוב):**

| בדיקה | תוצאה |
|---|---|
| חמשת טקסטי הפעולה בתוך הטבעת | **כולם נכנסים** · הארוך („מוריד כרטיסי אשראי") 84px בשתי שורות מול קוטר פנימי 98px |
| `syncStep` מעביר `action` | `{done:1,total:4,action:'מוריד הלוואות'}` ✓ |
| „פועלים עסקי: הסתיים ואומת" | **„הסנכרון הסתיים בהצלחה"** · `done` ✓ |
| „מסנכרן תנועות" · הודעת דילוג · שגיאה | בתהליך · דולג · נכשל — **ללא שינוי** ✓ |
| בלימת הדריסה: הרגע הסתיים · 30 שנ׳ | הצינון תופס, **הודעת דילוג לא נכתבת** ✓ |
| 90 שנ׳ · שעתיים | ההודעה נכתבת כרגיל — נשארה מועילה ✓ |
| 7 שעות | הצינון לא חל, סנכרון אוטומטי רץ ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.63.0**.

**⚠ תיקון למדידה שלי באותו סבב:** בהרצה הראשונה של בדיקת התוויות קיבלתי „—"
בכל ארבעת המקרים. זו הייתה **תקלה ברתמה** (הבאנר לא נבנה כי `$` ו-`.summary`
לא סופקו), לא בקוד. תוקן והורץ שוב. **„—" הוא „לא נבדק", לא „עובד".**

**נמדד אגב כך, לא תוקן:** „כל החשבונות שנבחרו כבר סונכרנו היום" מקבל תווית
**„ממתין לחיבור"**. הסיבה: המסווג מחפש `סונכרן` (נו"ן סופית) והמחרוזת מכילה
`סונכרנו` (נו"ן רגילה) — שתי תווים שונים, ולכן אין התאמה. קוסמטי, מחוץ לבקשה.

**לא נבדק:** מראה ויזואלי בצילום (חלונית הדפדפן לא הוצגה — אומת ב-DOM,
בגאומטריה ובמדידת רוחב טקסט), וכל הזרימה החיה.

## 2026-08-17 · Claude · טבעת אחוזים, והלשונית חוזרת לחלון הראשי — 0.62.0

**שתי בקשות של טל, ואחת מהן הופכת החלטה קודמת.**

### 1. „תחזיר את הדף לדפדפן הראשי, אבל תמיד שהתוסף יהיה בחזית"

⚠ **נסיגה מודעת מ-0.60.0, לבקשת המשתמש.** `windows.create({tabId,focused:false})`
הוחלף ב-`chrome.tabs.move(tabId,{windowId:<של הדשבורד>})`. לשונית הבנק **אינה
מופעלת** — זה מה שמשאיר את התוסף בחזית.

**המחיר, שנאמר למשתמש מראש:** לשונית לא-פעילה היא לשונית מוסתרת, ו-Chrome משהה
בה `rAF` ומאט טיימרים. **הסימן אם זה יתממש: סנכרון שנתקע או מחזיר טבלה ריקה,
בלי שרואים דפדוף.** אם יקרה — לחזור ל-`windows.create` שהיה. ההערה בקוד מתעדת
את שתי האפשרויות כדי שהסוכן הבא לא ינחש.

**קדימה:** נמדד ש**אין בתוסף אף `tabs.remove` או `windows.remove`** — התוסף
מעולם לא סגר לשוניות. מה שנראה כמו „הדף נסגר" היה ההעברה לחלון נפרד.

### 2. טבעת התקדמות עם אחוזים והערכת זמן

`background` כותב `syncProgress={done,total,startedAt}`; הדשבורד גוזר אחוז
והערכת זמן **מקצב השלבים בפועל** — זמן שחלף חלקי שלבים שהושלמו — ולא ממספר קבוע.
`syncSource` מכריז 4 שלבים (עסקי) או 5 (פרטי, עם זיהוי בעל החשבון).
חמש כתיבות הסטטוס הוחלפו ב-`syncStep()` שמקדמת ומדווחת יחד.

**בנק שטרם חובר מקבל טבעת מסתובבת בלי אחוז** — עדיף על מספר מומצא.
הדשבורד כבר מרנדר מחדש בכל שינוי אחסון (`onChanged`, 120ms), ולכן ההתקדמות
מתעדכנת מעצמה; טיימר של שנייה מרענן רק את שורת הזמן.

**אומת בהרצת הקוד מהקבצים (`fetch`, בלי שכתוב):**

| בדיקה | תוצאה |
|---|---|
| בנק בחלון נפרד | `tabs.move` לחלון הדשבורד → הדשבורד קדימה |
| בנק כבר בחלון הראשי | בלי הזזה → הדשבורד קדימה |
| **הפעלת לשונית הבנק** | **אף פעם, בשני התרחישים** ✓ |
| רצף התקדמות (עסקי) | `1/4 → 2/4 → 3/4 → 4/4`, ו-`endProgress` מנקה |
| חשבון הזמן | 2/4 אחרי 24 שנ' → **50% · „נותרו כ-24 שנ׳"** · 3/5 אחרי 96 שנ' → **60% · „1:04 דק׳"** |
| גאומטריית הטבעת | אורך הקשת תואם לאחוז ב-0/25/50/100 · 56px · מתחילה ב-12 |
| סיום הסנכרון | הטבעת מוסרת ✓ |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.62.0**.

**לא נבדק:** מראה ויזואלי בפועל (חלונית הדפדפן לא הוצגה — אומת ב-DOM ובמספרים,
לא בצילום), וכל הזרימה החיה.

**נשאר פתוח — הוצע ולא אושר:** `maybeAutoSync` דורסת את „הסתיים" בהודעת „הסנכרון
דולג". `poalim-content` שולח `AUTHENTICATED` בכל שינוי pathname, כולל בניווטי
הסנכרון; הודעה מאוחרת מגיעה אחרי שהסנכרון נגמר, הצינון של 6 שעות נתפס (כי
**זה עתה** סונכרן) והסטטוס נדרס. **ל-`maybeAutoSync` אין בדיקת `running`.**
ההצעה: לא לכתוב את הודעת הדילוג כשהצינון נובע מסנכרון שהסתיים לפני פחות מדקה.

## 2026-08-17 · Claude · הניווטים עצמם מושכים לחזית — 0.61.0

**דיווח המשתמש:** „פועלים עסקי לא מוריד ברקע — קופץ, מדפדף בין הדפים, מסיים
וחוזר לתוסף בסוף." הדיווח מדויק וסימן היישר לשורש: „מדפדף בין הדפים" פירושו
שהלשונית נשארה **באותו חלון עם הדשבורד**.

**⚠ שתי טעויות מדידה שלי, ברצף:**

1. **0.60.0** — תליתי הכל בהודעת `AUTHENTICATED` בלי לבדוק מתי היא נשלחת.
2. **0.60.1** — חיפשתי מושכי מיקוד בתבנית `chrome.tabs.update(id,{active:true})`
   **וסגרתי את החקירה על סמך תבנית אחת.** התבנית פספסה את הכתיב המקוצר
   `{url,active:true}`, ושם ישב האשם.

**האשם, שורה 416:** `prepareRoute(tabId,url,path)` — הפונקציה שדרכה עובר **כל**
מעבר עמוד בפועלים — מנווטת עם `active:true`. חמישה מעברים בסנכרון (תנועות ·
ריכוז יתרות · הלוואות · כרטיסים · בעל חשבון), כל אחד מושך את הלשונית לחזית.
**זה בדיוק ה„מדפדף".** גם `syncSource` עצמה לא קראה מעולם ל-`returnToDashboard`.

**הלקח (חשוב מהתיקון):** חיפוש בתבנית אחת אינו סקר. **סורקים את כל המופעים של
המושג ומסווגים כל אחד**, ולא מריצים grep צר ומכריזים „מצאתי את כולם".

**השתנה — 14 עריכות, אחרי סיווג כל 36 המופעים אחד־אחד:**

| קבוצה | פעולה |
|---|---|
| `prepareRoute` (פועלים, כל מעבר עמוד) | `active:true` הוסר |
| `prepareLeumiRoute` + לולאת ה-renav + דף השיקים | הוסר · **שומרי הלולאה לא נגעו** |
| ארבעה ניווטי הבינלאומי · שני ניווטי מזרחי · שני ניווטי ישראכרט | הוסר |
| `syncSource` · `startIsracard` במסלול המחובר | נוספה `returnToDashboard(id,true)` |
| **14 כניסות התחברות** | **נשארו בחזית בכוונה** — שם מקלידים סיסמה |

נוספה ב-`returnToDashboard` הפעלת לשונית הבנק **בתוך החלון שלה** — היא חייבת
להיות פעילה כדי להישאר `visible` ולרנדר, וזה אינו מושך מיקוד לחלון.

**אומת בהרצת הקוד מהקובץ (`fetch`, בלי שכתוב):**

| בדיקה | תוצאה |
|---|---|
| סיווג **כל 18** מופעי `active:true` שנשארו | 14 כניסות התחברות · 2 שאילתות · הדשבורד · הפעלה פנימית — **אפס ניווטי סנכרון** |
| סדר הפעולות, מקרה רגיל | חלון נפרד → הפעלת הבנק → **הדשבורד אחרון ומנצח** |
| **העברת החלון נכשלת** | הדשבורד עדיין מנצח ✓ |
| בנק שכבר לבדו בחלון | בלי יצירת חלון ✓ |
| נקודות `returnToDashboard(id,true)` | **10** |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.61.0**.

**⚠ נקודת הכשל היחידה שנותרה:** עכשיו שהניווטים כבר לא מפעילים את הלשונית,
**הכל תלוי בהצלחת העברת החלון.** אם `windows.create` נכשלת והלשונית נשארת בחלון
הדשבורד, היא תהיה מוסתרת — ואז חוזר ה-rAF throttling מ-0.60.0. הסימן: סנכרון
שנתקע או מחזיר טבלה ריקה **בלי** שרואים דפדוף.

## 2026-08-17 · Claude · 0.60.0 לא כיסתה את המסלול הנפוץ — 0.60.1

**דיווח המשתמש:** „בפועלים עסקי זה ממש לא ברקע."

**⚠ נסיגה שגרמתי בעצמי ב-0.60.0:** תליתי את כל המנגנון בהודעת `AUTHENTICATED`,
בלי לבדוק **מתי היא נשלחת**.

**השורש, נמדד ב-`poalim-content.js`:** `reportAuthenticated()` שולחת רק כאשר
`location.pathname` **משתנה** לנתיב פורטל, ושומרת `lastAuthenticatedPath`. לכן
כשהמשתמש **כבר מחובר** ולוחץ סנכרון מהדשבורד — `openSource()` מוצא את הלשונית
הקיימת, מושך אותה לחזית ומתחיל לעבוד, **ואף הודעה אינה נשלחת.** המנגנון של
0.60.0 פשוט לא רץ. זה **המסלול הנפוץ ביותר**, לא מקרה קצה.

**זה מעולם לא היה ייחודי לפועלים.** נמדדו **שמונה** נקודות באותה צורה בדיוק —
`await chrome.tabs.update(id,{active:true});<עבודה>` — כולן „הלשונית כבר מחוברת,
מתחילים לסנכרן": `openSource` · הבינלאומי · לאומי · מזרחי · כאל · MAX ·
דיסקונט עסקי · דיסקונט פרטי. כולן חטפו את המיקוד.

**השתנה:** בכל שמונה, `chrome.tabs.update(id,{active:true})` הוחלפה
ב-`returnToDashboard(id,true)`. **המשיכה לחזית לא רק הוסרה — היא הוחלפה בהיפוכה.**
זו הייתה שורה שנועדה להביא את הבנק קדימה, וזה בדיוק מה שכבר לא רוצים.

`force=true` עוקף את ה-`Set`, כי כניסת סנכרון מפורשת חייבת למקד גם אם ההודעה
כבר נצרכה קודם. בלי `force` ההתנהגות לא השתנתה — לאומי עדיין לא חוטף מיקוד בכל ניווט.

**אומת בהרצת הקוד מהקובץ (`fetch`, בלי שכתוב):**

| בדיקה | תוצאה |
|---|---|
| התחברות ואז כניסת סנכרון מפורשת (`force`) | **ממקד שוב** ✓ |
| הודעת התחברות חוזרת, בלי `force` | **no-op** ✓ — הבלימה של לאומי שרדה |
| לשונית שכבר לבדה בחלון + `force` | בלי יצירת חלון, ממקד דשבורד ✓ |
| סריקת הקובץ אחרי השינוי | **8 קריאות כפויות** · המושך היחיד שנשאר הוא `dash.id` — הדשבורד עצמו |

16 קבצים עוברים פרסור · הגרסה מהדיסק ב-`no-store`: **0.60.1**.

**הלקח:** „ההודעה נשלחת בהתחברות" הייתה הנחה, לא מדידה. **לפני שתולים מנגנון
בהודעה — למדוד מתי היא נשלחת ומתי לא**, ובמיוחד מה קורה כשהמשתמש כבר מחובר.

**עדיין לא נבדק:** אימות חי, וגם שאלת ה-window occlusion מ-0.60.0.

## 2026-08-17 · Claude · אחרי התחברות — חוזרים לתוסף והסנכרון רץ ברקע — 0.60.0

**בקשת המשתמש:** „ברגע שהתחברתי לבנק או לחברת האשראי — תמיד תחזור לתוסף,
ועבודת הסנכרון תהיה ברקע." כלל רוחבי לכל היעדים, לא למזרחי בלבד.

**נמדד לפני הכתיבה:**
- 9 סוגי הודעות התחברות: `AUTHENTICATED` (הפועלים) + 8 עם קידומת בנק.
- **כל 12 קריאות `tabs.create({active:true})` הן כניסות התחברות** — עמודי בית ודפי
  לוגין. שם המיקוד נחוץ כדי שהמשתמש יקליד, **ולכן אף אחת מהן לא שונתה.**
- 36 קריאות `active:true` — רובן `tabs.update`, ש**אינה מושכת מיקוד לחלון**, אלא
  מפעילה לשונית בתוך חלונה. אחרי הפרדת החלון הן בלתי מזיקות, ולכן גם הן לא נגעו.

**⚠ ההחלטה החשובה: חלון נפרד, לא לשונית מוסתרת.** Chrome משהה `requestAnimationFrame`
לחלוטין בלשונית נסתרת ומאט בה טיימרים. אתר בנק שמרנדר טבלה ב-rAF פשוט **לא יצייר
אותה**, והסנכרון היה נשבר בשקט בדיוק במקום שהכי קשה לאבחן. לשונית **פעילה בחלון
שאינו במוקד** נשארת `visible` וממשיכה לרנדר. לכן `chrome.windows.create({tabId,
focused:false})` ולא `tabs.update({active:false})`.

**השתנה — נקודה אחת ב-`background.js`, לא תשעה מטפלים:** שורת יירוט בראש
`onMessage` עם `/^[A-Z_]*AUTHENTICATED$/` שקוראת ל-`returnToDashboard(tabId)`.
הפונקציה מעבירה את לשונית הבנק לחלון משלה (רק אם היא לא כבר לבדה), ואז ממקדת את
הדשבורד — לשונית קיימת אם יש, אחרת `openOptionsPage()`.

**פעם אחת ללשונית** (`Set` שמתנקה ב-`tabs.onRemoved`), כי `LEUMI_AUTHENTICATED`
נורה **בכל ניווט** — בלי הבלימה הזו כל ניווט פנימי היה חוטף את המיקוד מחדש.

**אומת — הקוד נשלף מהקובץ ב-`fetch` והורץ, בלי שכתוב:**

| בדיקה | תוצאה |
|---|---|
| הביטוי מול **כל 35** סוגי ההודעות בקובץ | 9/9 סוגי התחברות · **0 תפיסות שווא** מתוך 26 האחרים |
| לשונית בנק בחלון עם 3 לשוניות | `windows.create focused=false` → מיקוד הדשבורד |
| לשונית בנק שכבר לבדה בחלון | **בלי** יצירת חלון → מיקוד הדשבורד |
| אין לשונית דשבורד | יצירת חלון → `openOptionsPage()` |
| קריאה שנייה לאותה לשונית | **no-op** |

16 קבצים עוברים פרסור · `manifest.json` תקין · הגרסה מהדיסק ב-`no-store`: **0.60.0**.

**⚠ לא נבדק, וזה הסיכון היחיד שנותר:** האם Chrome ב-Windows משאיר את הלשונית
`visible` כשהחלון שלה **מכוסה לגמרי** בחלון הדשבורד. ל-Chrome יש זיהוי הסתרה
(window occlusion) שעלול לסמן חלון מכוסה לחלוטין כנסתר, וזה יחזיר בדיוק את בעיית
ה-rAF. **הסימן לכך: סנכרון שנתקע או מחזיר טבלה ריקה דווקא כשהחלון מוסתר לגמרי,
ועובד כשהוא מוצג בצד.** אם זה קורה — לפתוח את החלון בגודל/היסט שמונע כיסוי מלא.

**הצעד הבא:** אחרי אימות חי, להמשיך את דפוס פתיחת חלונית ההתחברות (0.59.0) לשאר
היעדים. **לכל אתר טריגר משלו — למדוד, לא להעתיק ממזרחי.**

## 2026-08-17 · Claude · הסנכרון פותח בעצמו את חלונית ההתחברות — מזרחי, 0.59.0

**בקשת המשתמש:** „כשמבקשים סנכרון בנק או כרטיס אשראי — שייפתח חלון להכנסת סיסמה."
המשתמש מקליד בעצמו; **התוסף רק פותח את הדלת** ואינו נוגע בשדות (§9).

**מה היה:** `startMizrahi` פתח את עמוד הבית `mizrahi-tefahot.co.il` וכתב „ממתין
להתחברות" — והמשתמש היה צריך לאתר וללחוץ „כניסה לחשבון" בעצמו.

**נמדד חי על האתר (17.08.2026), לא נוחש:**

| | |
|---|---|
| טריגר | `a#logInBtn.goToLogin[href="#logInModal"]` — **`.click()` רגיל מספיק**, בניגוד ללאומי שדורש `realClick()` |
| החלונית | `div#logInModal.modal` · `display:none` → `block` + מחלקה `show` |
| הטופס | **אינו בעמוד הבית** — `iframe#iframeLogIn` שנטען מ-`login/index.html#/login-he` עם cache-buster `?ttt=<ms>` |
| שדות | `#input_user` (משתמש) · `#input_pass` (סיסמה) · `#applogin-he` |
| דף הלוגין כעמוד עצמאי | נבדק ישירות — **עומד בפני עצמו ומרנדר את אותו טופס** |

**השתנה ב-`background.js`:** `openMizrahiLogin(tabId)` — עד 6 ניסיונות במרווח 1.2 שנ',
כל ניסיון בודק אם החלונית כבר פתוחה ואם לא — לוחץ. נכשל → **נפילה לאחור לניווט ישיר
ל-`MIZRAHI_LOGIN`**, שאינה תלויה ב-DOM של עמוד הבית. `startMizrahi` קורא לו אחרי
`waitTab`. `pendingMizrahi` והזרימה שאחרי ההתחברות לא נגעו.

**למה לחיצה ולא ניווט ישיר כברירת מחדל:** מה שקורה *אחרי* התחברות מוצלחת — המעבר
ל-`mto…/OnlineApp/` — אי אפשר למדוד בלי להתחבר, ולא התחברתי. הזרימה הטבעית של האתר
היא היחידה שידוע שהיא מסתיימת נכון. הניווט הישיר נשמר כרשת ביטחון בלבד.

**אומת בהרצת `openMizrahiLogin` מהקובץ עצמו** (נשלף מהדיסק), עם דמה ל-`chrome.scripting`:

| מסלול | תוצאה |
|---|---|
| עמוד הבית החי | `clicked` → `open` תוך **1.3 שנ'** · `display:block` · iframe נטען · **בלי נפילה לאחור** |
| עמוד בלי טריגר | 6 × `no-trigger` תוך **7.3 שנ'** → ניווט ל-`MIZRAHI_LOGIN` |

16 קבצים עוברים פרסור · `manifest.json` תקין · **הגרסה נקראה מהדיסק דרך HTTP עם
`no-store`: 0.59.0** (לא מהקומיט).

**לא נבדק:** הזרימה מקצה לקצה עם התחברות אמיתית — כלומר שאחרי הקלדת הסיסמה בחלונית
נוצר `MIZRAHI_AUTHENTICATED` והסנכרון ממשיך. זה מה שהמשתמש צריך לבדוק.

**הצעד הבא:** אותו דפוס לשאר היעדים. **לכל אתר טריגר משלו — למדוד, לא להעתיק את
מזרחי.** הבקשה המקורית הייתה „בנק או כרטיס אשראי", ומזרחי הוא הראשון בלבד.

**אגב:** תוקנה `extension/README.md` — השורה „לאומי מרשה סשן פעיל אחד" נשארה בה
אחרי שהופרכה ב-16.08 ו-`AGENTS.md` §9 כבר תיקן אותה.

## 2026-08-17 16:55 · Claude · המונה מציג את הכמות האמיתית — 0.58.4

**הערת המשתמש:** "יש 24 שיקים, לא צריך להיות התייחסות לכמות וגם למה שנמצא?"
המונה הראה `6/13` — מתוך **החסרים**, לא מתוך הסך. מספר שאי אפשר לקשר לשום
דבר שהמשתמש רואה.

**השתנה:** לפני האצוות מחושב הקשר לכל הקציר — סך השיקים, כמה כבר שמורים,
וכמה בלי אסמכתא. המונה סופר מתוך הסך, והשאריות מוצגות:

```
לאומי: שומר צילומי שיקים 12/24 · 11 כבר היו שמורים · 5 ללא אסמכתא
```

`leumiChequeReport` כולל עכשיו גם `total` · `already` · `noReference`, ולא
רק `asked`/`saved`/`failed`.

**אומת בהרצת הקוד מהקובץ** על 24 שיקים (11 שמורים · 8 חסרים · 5 ללא
אסמכתא): `total=24 · base=11 · noRef=5`, וההודעה מתקדמת 12/24 → 15/24 →
19/24. **19 הוא התקרה הנכונה** — חמשת חסרי האסמכתא לעולם לא ייסרקו.

## 2026-08-17 16:30 · Claude · מונה פר-שיק, ו-5 שיקים שלא נסרקים כלל — 0.58.3

**נתון מהמשתמש:** 24 שיקים בסך הכל · הקציר ביקש **19** · **5 נפתחים כהפניה
לבנק.** 24 − 19 = 5. **החפיפה מדויקת.**

**המסקנה:** חמשת השיקים שנופלים להפניה הם בדיוק אלה ש**מעולם לא נסרקו**.
התנאי הוא `t.cheque && t.reference`, ולכן **שיק בלי אסמכתא אינו נכנס
ל-`wanted`** — הוא לא נכשל, הוא לא נוסה.

⚠ **זו אינה תקלת תזמון כמו הקודמות** — היא מבנית: האסמכתא היא גם מפתח
השמירה (`selectionKey|reference`) וגם מה שמאתר את השורה
(`cells.includes(reference)`). **בלי אסמכתא אין דרך לזהות ואין במה לשמור.**
תיקון דורש מפתח חלופי — למשל תאריך+סכום — ונגיעה בשני הצדדים.

**טרם אומת ישירות.** הסניפט שיכריע רץ בדף התוסף:
`שיקים בתנועות` מול `עם אסמכתא`. אם 24 מול 19 — אושר.

**השתנה בסבב הזה (בקשת המשתמש): מונה שמתקדם עם הסריקה.**
- `chequeImages` מקבל `offset`/`total` ושולח `LEUMI_CHEQUE_PROGRESS` לכל שיק.
- `background` קולט וכותב `שומר צילומי שיקים מקומית X/Y`.
- הסטטוס הישן, שהתעדכן פעם באצווה של 6 וקפץ 6→12→18, הוסר.

**אומת:** ארבע נקודות החיבור קיימות · סימולציה על 19 שיקים באצוות של 6
נותנת רצף **1..19 בלי דילוגים** · 16 קבצים עוברים פרסור.

## 2026-08-17 16:00 · Claude · תווית הסטטוס בזמן עבודה — 0.58.2

**נצפה:** בזמן `לאומי: שומר צילומי שיקים מקומית 6/19` הבאנר הציג
**"ממתין לחיבור"**. הסיבה: המילה "שומר" לא הופיעה ברשימת המילים של
`state='running'`, וההודעה נפלה לברירת המחדל.

**השתנה:** נוספו `שומר · שולף · מוריד · פותח · מעדכן · ממתין להתחברות`.

**אומת בהרצת המסווג מהקובץ** על חמש הודעות אמיתיות: "שומר צילומי שיקים" →
**סנכרון בתהליך** · הצינון → **דולג** · "קורא תנועות" → בתהליך · שגיאה →
נכשל · "הסתיים ואומת" → הסתיים.

**⚠ תיקון למדידה שלי:** דיווחתי תחילה על 30 הודעות בעייתיות. הבדיקה קיצצה
כל הודעה ב-`${` הראשון ולכן ספרה שגוי. **המספר הנכון: 22 מתוך 69** נופלות
לברירת המחדל — בעיקר הודעות של ישראכרט והיסטוריית כרטיסים. קוסמטי, לא נוגע
לזרימה, ולא טופל.

**אושר מחדש בקוד (שאלת המשתמש):** הקציר סורק **רק שיקים חסרים** —
`!have.has(chequeId(a.selectionKey,t.reference))`. שיק שנשמר אינו נסרק שוב,
ולכן המונה מציג 19 ולא את כל השיקים.

## 2026-08-17 15:40 · Claude · 0.58.0 החמירה — תקרת האצווה נחצתה. 0.58.1

**⚠ נסיגה שגרמתי:** ב-0.58.0 הארכתי את ההמתנה לכל שיק מ-4 ל-12 שניות
**ולא נגעתי בתקרת הזמן של האצווה** (120 שנ' לשישה שיקים).

```
לפני:  6 × 4  = 24 שנ'  + טעינת דף  → בתוך 120 ✓
0.58.0: 6 × 12 = 72 שנ'  + טעינת דף  → חורג
```

`loadAllRows` לבדה יכולה לקחת 48 שניות. **וכשהאצווה חורגת, `withTimeout` זורק,
`catch{}` בולע, וכל ששת השיקים נזרקים — כולל אלה שכבר צולמו בהצלחה.** לכן
דווקא נוצרו הפניות שלא היו קודם.

**הלקח:** הארכת המתנה פנימית בלי לבדוק את התקרה שמעליה הופכת שיפור לנסיגה.
**כל שינוי זמן חייב חשבון מול התקרה החוסמת אותו.**

**השתנה:** המתנה לכל שיק 12 → **8** שניות · תקרת האצווה 120 → **300**.

**אומת:** החשבון במקרה הגרוע — 6 שיקים (53 שנ') + טעינת דף (63) = 116 שנ'
מול תקרה של 300, **מרווח 184 שניות** · `chequeImages` הורצה שוב מהקובץ על
ה-DOM המשחזר: **3 מתוך 3** · 16 קבצים עוברים פרסור.

**איך לדעת שזה עבד:** `leumiChequeReport` — `asked` מול `saved`.

## 2026-08-17 15:10 · Claude · שיקים: חלון הצילום חסם את הבא בתור — 0.58.0

**דיווח:** חלק מהשיקים עם צילום וחלק בלי.

**השורש:** `chequeImages` לחצה על שורה, אספה את התמונות — **ולא סגרה את
החלון**. החלון הפתוח מכסה את הטבלה וחוסם את הלחיצה הבאה, **וגם** מוסיף את
תמונותיו ל-`before`, כך ש"תמונה חדשה" אינה מזוהה. התוצאה: הראשון נקלט
והשאר לא.

**השתנה ב-`leumi-content.js`:**
- `closeViewer()` — כפתור `[role="dialog"] button[aria-label="סגירה"]`
  (**סלקטור שכבר היה מדוד ומופיע ב-`discover()`**), ונפילה לאחור ל-Escape.
  נקרא **לפני כל שיק** ואחרי הסבב.
- ההמתנה לתמונות: 4 שניות → **12**, עם יציאה מוקדמת. צילום שנטען לאט נפל.

**אומת בהרצת `chequeImages` מהקובץ עצמו**, על DOM שמשחזר את ההתנהגות
(לחיצה נחסמת כשחלון פתוח):

| | נקלטו |
|---|---|
| לפני התיקון | **1 מתוך 3** — בדיוק הסימפטום שדווח |
| אחרי התיקון | **3 מתוך 3** |

16 קבצים עוברים פרסור. גרסה אומתה מהדיסק ומה-junction.

**איך לדעת שזה עבד בפועל:** `leumiChequeReport` שנוסף ב-0.57.7 שומר
`{asked, saved, failed, why}`. להשוות `saved` מול `asked` אחרי הסנכרון הבא.

**נשאר פתוח, לא דחוף:** הנעילה `running` שנתקעת וחוסמת בשקט עד ⟳ ·
`LEUMI_AUTHENTICATED` נשלח בלי לוודא שהמשתמש מחובר.

## 2026-08-17 14:30 · Claude · קציר השיקים מדווח במקום לדלג — 0.57.7

**דיווח:** צילומי השיקים נפתחים כהפניה לבנק במקום מקומית. **עבד אתמול.**

**נמדד:** קוד הקציר עצמו **זהה לאתמול** — ההשוואה מול `8264c05` מראה רק
עטיפת `withTimeout` שנוספה. **לא שברתי את הקציר.** מה שהשתנה סביבו הוא
`prepareLeumiRoute`, שמ-0.57.1 **זורק** במקום לנווט; אם הוא זורק בקציר,
כל השלב מדולג בשקט ואין מה למצוא במסד.

**⚠ זו עדיין השערה ולא מדידה** — ולכן הסבב הזה הוא **אינסטרומנטציה בלבד**,
בלי שינוי התנהגות:
- כשל ב-`prepareLeumiRoute` בתוך הקציר נתפס ונרשם במקום להפיל את השלב בשקט.
- `catch{}` ו-`if(!r?.ok)continue` צוברים סיבה ומונה כשלים.
- `leumiChequeReport` נשמר תמיד: `{asked, saved, failed, why}`.
- אם ביקשנו צילומים ולא נשמר אף אחד — `syncStatus` אומר כמה וסיבה.

**אומת בהרצת `harvestLeumiCheques` עצמה, מהקובץ ולא מהעתק:**
- ניווט נכשל → 0 נשמרו, הסיבה בסטטוס
- הדף לא החזיר → נכשלו 1, סיבה "הדף לא נטען"
- מסלול תקין → נשמר 1, **ללא הודעת כשל**

**זו הפעם הראשונה היום שהרצתי את הפונקציה מהקובץ** ולא גרסה מקבילה. §8
החדש ב-`AGENTS.md` מחייב את זה מעכשיו.

**הצעד הבא:** סנכרון לאומי, ואז לקרוא את `leumiChequeReport` — הוא יגיד אם
הקציר רץ, כמה ביקש, וכמה נשמרו. משם התיקון ממוקד.

## 2026-08-17 13:50 · Claude · "נקה הכול" לא יכול היה לנצח — 0.57.6

**דיווח:** הכפתור מגיב, ומיד הכל מסומן מחדש.

**השורש:** ב-`load()` היה
```js
if(selectable.length && fresh.length===selectable.length){ /* סמן הכל */ }
```
`fresh` = מה שאינו מסומן. **אחרי "נקה הכול" כולם אינם מסומנים, התנאי מתקיים
תמיד, והרינדור מסמן בחזרה.** הכפתור והקוד נלחמו זה בזה.

**השתנה:** הסימון האוטומטי קורה **פעם אחת לכל קבוצת זיהוי** — נשמרת טביעת
אצבע של המפתחות ב-`autoPickedFor`. קבוצה חדשה → סימון; אותה קבוצה → לא.

**ובנוסף:** `נקה הכול` עשה `selectedKeys=[]` ובכך **מחק גם בחירות של בנקים
שאינם ברשימה המוצגת**. עכשיו הוא מסנן רק את מה שמוצג.

**אומת בסימולציית מחזור החיים:** זיהוי ראשון → סימן · רינדור נוסף → לא נגע ·
נקה הכול → 0 · **רינדור אחרי הניקוי → נשאר 0** · זיהוי חדש של 3 חשבונות →
סימן 3.

**⚠ עדיין פתוח:** המשתמש מדווח שסנכרון לאומי אינו מתקדם. **טרם אובחן** —
צריך את שורת הסטטוס. שים לב שסנכרון של שישה חשבונות לאומי כולל בחירת חשבון,
טעינת תנועות וצילומי שיקים לכל אחד, וזה איטי מטבעו.

## 2026-08-17 13:20 · Claude · ✅ השורש: menuItem השתמש ב-txt() — 0.57.5

**הודעת השגיאה של 0.57.3 היא שסגרה את זה:** *"הפריט עובר ושב שקלים לא נמצא
בתפריט. הלשונית על …/digitalfront/he"* — הלשונית נכונה ומחוברת, ה-content
script רץ, ורק חיפוש הפריט נכשל.

**השורש, שוחזר במדידה:** `menuItem` השווה `normalized(txt(el))===name`.
`txt()` נועד ל**שורות חשבון**: לכפתור בלי ספרות בתוך `<main>` הוא **מצרף את
טקסט אלמנט האב**. על כפתור התפריט הוא מחזיר **82 תווים**:

```
"עובר ושב שקלים עובר ושב שקלים עובר ושב שקלים יתרות · תנועות · 921-88154 · ₪ 172.99"
```

במקום 16. השוואה ב-`===` מול שם פריט **לא יכולה להתאים לעולם**.

**⚠ ולמה הבדיקה החיה שלי החמיצה את זה:** הרצתי גרסה משלי של `normalized`
ו-`txt` בדף, ולא את אלה שבקובץ. **הן לא זהות.** זו בדיוק אותה מלכודת של
"בדקתי יכולת ולא את המימוש" — הרצתי לוגיקה נכונה, אבל לא את הקוד שנשלח.

**השתנה:** `menuText(el)` — `innerText` מכווץ ומקוצץ, בלי `txt`/`normalized`.
`menuItem` משתמש בו.

**אומת בשחזור הכשל עצמו:** DOM עם כפתור תפריט בתוך `<main>` —
`normalized(txt(el))` **לא מתאים**, `menuText(el)` **מתאים**. 16 קבצים עוברים
פרסור.

**וגם ב-0.57.4:** הודעת הצינון הכילה "סונכרן" ולכן נצבעה ירוק כ"הסנכרון
הסתיים" בזמן ששום דבר לא רץ. נוסף מצב **"הסנכרון דולג"** לפני `done`.

## 2026-08-17 12:50 · Claude · הסיבה נבלעה — 0.57.3 מוציאה אותה החוצה

**התקלה חוזרת, וההודעה זהה — כי היא גנרית.** `goRoute` מחזיר סיבה מדויקת
(איזה פריט תפריט חסר / הניווט לא הגיע ליעד), ו-`prepareLeumiRoute` בלע אותה
ב-`catch{}` וזרק נוסח קבוע. **כל הכשלים נראו אותו דבר, וזה עלה סבב.**

**השתנה:** הסיבה נלכדת ל-`goWhy` — משדה `error` של `goRoute`, מחריגה, או
"הניווט הצליח אבל הדף לא השיב" — ונכנסת להודעה יחד עם **כתובת הלשונית**
(בלי query ו-hash, שלא לחשוף טוקן).

**אומת בהרצת תבנית ההודעה עצמה:**
- `הניווט בתפריט נכשל: הפריט אשראי לא נמצא בתפריט. הלשונית על …/digitalfront/he/`
- `… הפריט תנועות בחשבון שקלים לא נמצא בתפריט. הלשונית על …/gate-keeper/he/`
- `… Could not establish connection. הלשונית על https://www.leumi.co.il/he`

**כתובת הלשונית היא החלק החשוב:** אם היא `gate-keeper` או `www.leumi.co.il`,
התוסף עובד על לשונית לא מחוברת וזו התשובה. אם היא `/digitalfront/`, הבעיה
בהתאמת שם פריט בתפריט.

**⚠ תקלת גיט שכדאי לזכור:** 0.57.2 נדחף אבל **לא היה על הדיסק** — הקומיט
נחת על `split-background` ואני החזרתי את עץ העבודה ל-`main`. Chrome טוען
מהדיסק, ולכן המשתמש בדק 0.57.1 בזמן ש"נדחף ✓". **אחרי כל דחיפה — לאמת את
הגרסה מהקובץ, לא מהקומיט.**

## 2026-08-17 12:20 · Claude · ⟳ הורג את ה-content script — 0.57.2

**הודעת השגיאה של המשתמש היא שפתרה את זה:** *"הניווט בתפריט של לאומי נכשל.
אין לנווט לכתובת…"* — כלומר השרשרת רצה, ההגנה של 0.57.1 עבדה, ו-`LEUMI_GO`
הוא שנכשל.

**השורש:** **רענון התוסף (⟳) הורג את ה-content script בכל לשונית פתוחה.**
לשונית לאומי נשארת פתוחה ומחוברת, אבל `chrome.tabs.sendMessage` לא מגיע
לאף אחד. לפני 0.57.1 הקוד היה מנווט — ההזרקה הייתה קורית בדרך, **תוך הרס
הסשן**. ההגנה שלי הסירה את המסלול ההרסני ולא שמה דבר במקומו.

**⚠ האירוניה שכדאי לזכור: ההוראה שלי "לחץ ⟳ לפני שתנסה" היא שיצרה את הכשל
שביקשתי לבדוק.** כל סבב בדיקה התחיל ברענון, וכל סבב נכשל בגללו.

**השתנה:**
- `prepareLeumiRoute` — `LEUMI_PING` ואם אין תשובה, `chrome.scripting.executeScript`
  מזריק את `leumi-content.js` מחדש. **בלי ניווט ובלי לגעת בסשן.**
- `dashboard.js` — `startLeumi` קיבל `try/catch` כמו שאר הבנקים. בלעדיו כשל
  ב-service worker השאיר את הכפתור תקוע על "מזהה חשבונות לאומי…" בלי שום
  הודעה, וזה נראה בדיוק כמו "לא עושה כלום".

**אומת בהרצת הקטע עצמו, שני תרחישים:**
- content script מת → `LEUMI_PING → הזרקה → LEUMI_GO → LEUMI_PING` → יצא לפני
  הניווט, כלומר הצליח.
- content script חי → `LEUMI_PING → LEUMI_GO → LEUMI_PING`, **בלי הזרקה
  מיותרת**.

16 קבצים עוברים פרסור · סדר `importScripts` ✓.

**לבדיקת המשתמש:** ⟳ (עכשיו זה בטוח), התחברות ללאומי, "לאומי" בדשבורד.

## 2026-08-17 11:40 · Claude · ✅ לאומי עובד. המסירה מבוטלת.

**אומת ע"י המשתמש על 0.57.1: לאומי הוריד את הנתונים, כולל צילומי השיקים.**

**⚠ הרשומה שמתחת — "מסירת לאומי ל-Codex" — בטלה.** היא מתארת תקלה שנפתרה,
ומי שיתחיל לפיה יחפש מקור שלישי שאינו קיים. **הבעלות על `leumi-content.js`
ו-`background.js` חוזרת. אין צורך בעבודה על לאומי.**

**מה שפתר:** אחד משני התיקונים או שניהם יחד — הסרת `chrome.tabs.reload`
מ-`startLeumi` (0.57.0), ואיסור `chrome.tabs.update` על לשונית שכבר בתוך
`hb2` (0.57.1). **לא ניתן לייחס לאחד מהם בוודאות**, ואין טעם לפרק: שניהם
מבטאים אותו עיקרון ושניהם נדרשים.

**האבחנה שהובילה לשם, ושווה לזכור מעבר ללאומי:** הסשן חי בטוקן שב-`#ts=`
בכתובת. הדיווח "לא עושה כלום" והדיווח "חוטף את האתר" היו **אותה תקלה** —
הניווט הרס את הסשן, ואחריו לא היה מה לקרוא. חיפשתי אותם כשתי בעיות נפרדות
ובזבזתי על כך סבבים.

**גם הוכח בשטח:** מסלול השיקים (`שיקים › שיקים מזומנים`) שסומן "לא אומת"
ברשומה הקודמת — **עבד.** צילומי השיקים ירדו.

**מה שנשאר פתוח, ואינו חוסם:**
- **`running` נתקע וחוסם כל לחיצה בשקט**, ומשתחרר רק ב-⟳. הנעילה צריכה
  תפוגה. זה עיכב את האבחון היום יותר מכל דבר אחר.
- `LEUMI_AUTHENTICATED` נשלח בלי לוודא שהמשתמש מחובר.

## 2026-08-17 11:15 · Claude → Codex · מסירת לאומי. הבעלות עוברת אליך.

**קרא את הרשומה הזו לפני שאתה נוגע בקוד. היא חוסכת יום עבודה שלם.**

`main` עודכן ל-**0.57.1** ומכיל את כל עבודת היום. אל תעבוד מגרסה ישנה.

### התקלה, בדיוק

המשתמש לוחץ "לאומי" בדשבורד. **הלשונית נזרקת מהחשבון** אל
`www.leumi.co.il/he`, ואז הזיהוי נכשל כי אין סשן. שתי התלונות — "חוטף את
האתר" ו"לא עושה כלום" — הן אותה תקלה.

### הסיבה, שנצפתה בעיניים ולא הוסקה

**הסשן של לאומי חי בטוקן שב-`#ts=` בכתובת, לא רק בעוגייה.** כל ניווט שמאבד
אותו מוציא מהחשבון:

| פעולה | תוצאה שנמדדה |
|---|---|
| `chrome.tabs.reload` | הלשונית → `www.leumi.co.il`, מנותקת |
| `chrome.tabs.update` עם כתובת | אותו דבר |
| ניווט מהתפריט (`realClick`) | **הסשן שורד** |

מדידה משלימה: אותו דף תנועות נותן **0 בוררים ו-0 שורות** בהגעה לפי כתובת,
מול **2 ו-22** בהגעה מהתפריט.

### מה כבר הוסר — אל תחזיר

- `chrome.tabs.reload` ב-`startLeumi` (0.57.0) → הוחלף בקריאה ישירה
  ל-`discoverLeumi(tab.id)`.
- `chrome.tabs.update` ב-`prepareLeumiRoute` **כשהלשונית בתוך `hb2`** (0.57.1),
  כולל לולאת ה-renav → נזרקת שגיאה מפורשת במקום ניווט.

### מה עובד ואומת על סשן חי — אין צורך לגעת

- `discover()` → **6 חשבונות** עם שמות נכונים, בלי רענון
- `goRoute` מהתפריט → דף התנועות, 2 בוררים, 29 שורות
- `selectAccount` → מחליף חשבון תוך **0.25 שנ'**, הטבלה נטענת מחדש (22 שורות)
- `optionPool` → 7 אפשרויות מהבורר

מפת התפריט: עובר ושב שקלים › תנועות בחשבון שקלים **✔** · אשראי › הלוואות **✔** ·
שיקים › שיקים מזומנים **✖ לא אומת**.

### מה שנשאר פתוח — משם להמשיך

1. **אחרי הסרת שני מקורות ההרס, המשתמש דיווח שזה עדיין חוטף.** או שהוא לא
   רענן ל-0.57.1, או שיש **מקור שלישי שלא אותרתי**. זו השאלה הראשונה.
2. **`running` נתקע וחוסם הכל בשקט.** `startLeumi` יוצא מיד על `if(running)`
   בלי הודעה, ו-`running` נשאר `true` לנצח אם `await` לא נגמר. משתחרר רק
   ב-⟳. **צריך נעילה עם תפוגה.** אובחן מסמן חיצוני: הלשונית לא זזה כלל.
3. **`LEUMI_AUTHENTICATED` נשלח בלי לוודא שהמשתמש מחובר** — מספיק
   `hostname===hb2` ו-`<main>`. גם דף ההתחברות עומד בזה.
4. **אין יותר מסלול חלופי לניווט.** אם `LEUMI_GO` נכשל, הזיהוי נכשל. זו
   החלטה מכוונת — עדיף להיכשל מלהרוס סשן.

### שיטה, אחרי שבעה סבבים שרובם היו טעות

**לא לתקן לפני שמריצים את השרשרת המלאה על סשן חי.** תיקנתי היום שלוש
פונקציות נכונות שכלל לא נקראו, כי לא בדקתי את המסלול שמוביל אליהן. בדיקת
תחביר וסימולציה **אינן** "אומת".

⚠ **לשונית שנייה אינה יכולה להצטרף לסשן** — קיבלתי מסך כניסה בזמן שהמשתמש
היה מחובר. אין לפתוח לשונית לאומי משלך.

**בעלות:** `leumi-content.js` ו-`background.js` שלך מעכשיו. אני לא נוגע.

## 2026-08-17 10:45 · Claude · לאומי — הנפילה לאחור היא שחוטפת, 0.57.1

**התלונה חזרה אחרי 0.57.0.** נמדד: הלשונית ב-`www.leumi.co.il/he`, מנותקת.

**מקור שני, נפרד מהרענון:** `prepareLeumiRoute` מנסה `LEUMI_GO` ואם הוא נכשל
**נופל ל-`chrome.tabs.update` עם כתובת** — וזה הורג את הסשן בדיוק כמו הרענון.
הנפילה לאחור שהשארתי "ליתר ביטחון" ב-0.56.0 היא עצמה ההורסת.

וגם לולאת ה-renav שבתוכה מנווטת עד 4 פעמים נוספות, כל אחת הרסנית.

**השתנה:** ניווט לכתובת מותר **רק כשהלשונית אינה בתוך `hb2.bankleumi.co.il`**.
בתוך האפליקציה נזרקת שגיאה מפורשת שמסבירה למשתמש מה לעשות. אותו תנאי הוחל
על לולאת ה-renav.

**העיקרון:** עדיף להיכשל עם הודעה מאשר לחטוף את הדפדפן ולהרוס סשן פעיל.

**אומת בהרצת הקטע עצמו:** לשונית בתוך לאומי → **אין ניווט**, נזרקת שגיאה ·
לשונית מחוץ ללאומי → מנווט כרגיל · `renav` מוגן · אין `chrome.tabs.reload`
בקובץ · 16 קבצים עוברים פרסור.

**⚠ מה שנשאר פתוח, ובגדול:** אחרי התיקון הזה, אם `LEUMI_GO` נכשל — הזיהוי
פשוט נכשל. **אין יותר מסלול חלופי.** זה נכון (עדיף להיכשל מלהרוס), אבל זה
אומר שהצלחת לאומי תלויה כולה בניווט מהתפריט. `LEUMI_GO` אומת חי על שני
מסלולים; **שיקים לא אומת.**

**לבדיקת המשתמש:** ⟳, התחברות, "לאומי". **הלשונית לא אמורה לצאת מהחשבון.**
אם הזיהוי נכשל — הפעם תהיה הודעה מפורשת, ולא שקט.

## 2026-08-17 10:15 · Claude · לאומי — הרענון ב-startLeumi הרס את הסשן, 0.57.0

**⚠⚠ השורש האמיתי, נראה בעיניים:** לחיצה על "לאומי" הריצה
`chrome.tabs.reload` על לשונית לאומי. **נצפה חי: הלשונית נזרקה מהאפליקציה
המחוברת ל-`www.leumi.co.il/he` ויצאה מהחשבון.** הסשן של לאומי נשען על הטוקן
שב-`#ts=`, ורענון מאבד אותו.

זה מסביר את **שתי** התלונות בבת אחת: "חוטף את האתר" (הרענון מעיף אותך), ו-
"לא עושה כלום" (אחרי שהסשן מת אין מה לזהות).

**וזה מסביר גם למה כל התיקונים הקודמים לא הועילו** — הם רצו על דף מת.

**נבדק לפני הכתיבה, על סשן חי ובלי רענון:** `discover()` המלא החזיר **6
חשבונות עם שמות**, הסשן שרד, והדף נשאר מחובר. כלומר **הרענון מיותר לחלוטין.**

**השתנה:** `chrome.tabs.reload(tab.id)` הוחלף בקריאה ישירה
`discoverLeumi(tab.id)`, עם `catch` שכותב ל-`syncStatus` במקום להיכשל בשקט.

**אומת:** 16 קבצים עוברים פרסור · סדר `importScripts` ✓ · אין יותר
`chrome.tabs.reload` בקובץ · שלושת שומרי הלולאה במקומם · אפס הודעות לאומי
ללא `withTimeout`.

### ⚠ מה שלמדנו על לאומי ואסור לשכוח

1. **הסשן קשור לטוקן שב-hash, לא רק לעוגייה.** כל ניווט לכתובת נקייה — reload
   או `tabs.update` — הורג אותו. **מנווטים אך ורק מתוך התפריט** (`LEUMI_GO`).
2. **לשונית שנייה אינה יכולה להצטרף לסשן.** ניסיתי, וקיבלתי מסך כניסה בזמן
   שהמשתמש היה מחובר. הכלל שמחקתי הבוקר היה קרוב לאמת יותר משחשבתי.
3. **נעילה תקועה חוסמת הכל בשקט.** `startLeumi` יוצא מיד על `if(running)`,
   ו-`running` נשאר `true` לנצח אם `await` לא נגמר. אובחן מסמן חיצוני: הלשונית
   לא זזה כלל. **מומלץ להפוך אותה לנעילה עם תפוגה — טרם נעשה.**

**מה שנבדק חי בצד התוכן ועובד:** `discover` 6 חשבונות · `goRoute` לדף
התנועות · `selectAccount` מחליף תוך 0.25 שנ׳ · 22 שורות לחשבון החדש.

**מה שלא ניתן לאמת מכאן:** כל צד ה-service worker. אין גישה ל-
`chrome-extension://` ולא ל-`chrome.storage`. **רק המשתמש יכול לבדוק.**

**לבדיקת המשתמש:** ⟳ על התוסף (משחרר נעילה תקועה), התחברות ללאומי, ואז
"לאומי" בדשבורד. **הלשונית לא אמורה לקפוץ החוצה.**

## 2026-08-17 09:30 · Claude · לאומי "לא עושה כלום" — await בלי תקרה, 0.56.1

**תקלה שגרמתי ב-0.56.0:** הוספתי `await chrome.tabs.sendMessage(…LEUMI_GO…)`
**בלי `withTimeout`.** `goRoute` לוחץ בתפריט, ה-SPA מנווט, ה-content script מת
באמצע — והתשובה לא מגיעה לעולם. הסנכרון תלוי בלי להיכשל ובלי לדווח.

**ההערה מעל `withTimeout` מתארת בדיוק את זה,** ונכתבה אחרי שזה כבר קרה פעם:
*"לקח מלאומי: ניווט של ה-SPA הורג את ה-content script… בלי המעטפת הזו הסנכרון
נראה 'רץ' לנצח במקום להיכשל ולדווח."* קראתי את הקובץ ולא את ההערה.

**השתנה:** שתי הקריאות החדשות עטופות. **ובנוסף — שש קריאות לאומי ותיקות היו
חשופות לאותה תקיעה** ועכשיו עטופות גם הן: `LEUMI_DISCOVER` 90 שנ' ·
`LEUMI_SNAPSHOT` 20 · `LEUMI_SYNC_SELECTED` 180 · `LEUMI_LOANS_SELECTED` 120 ·
`LEUMI_CHEQUE_IMAGES` 120 · `LEUMI_PING` 8. התקרות רחבות בכוונה — הן אינן
יכולות לשבור מסלול עובד, רק להפוך שתיקה להודעה.

**אומת:** 16 קבצים עוברים פרסור · סדר `importScripts` ✓ · **אפס** קריאות
`LEUMI_*` ללא עוטף · `withTimeout` הורץ בפועל: הבטחה שאינה נסגרת נדחתה אחרי
410ms, הבטחה תקינה עברה כרגיל.

**⚠ ההערה החשובה:** `withTimeout` הוא `const` שמוגדר **אחרי**
`prepareLeumiRoute` בקובץ (58,801 מול 49,710). זה תקין רק כי הקריאה קורית בזמן
ריצה. **מי שיזיז קוד — לא להעביר קריאה ל-`withTimeout` לזמן ההערכה של המודול.**

**לא נבדק:** ריצה חיה. אם עדיין "לא עושה כלום" — עכשיו `syncStatus` יראה איזו
קריאה נתקעה, וזה בדיוק המידע שחסר היה.

**לבדיקת המשתמש:** רענון ל-0.56.1 וסנכרון לאומי. **לקרוא את שורת הסטטוס.**

## 2026-08-17 09:00 · Claude · לאומי — ניווט לפי כתובת נוחת על דף ריק, 0.56.0

**⚠⚠ הממצא המרכזי, ומסביר את כל שרשרת הכשלים:** אותו דף תנועות בדיוק, שתי
דרכי הגעה, נמדד חי באותה שנייה:

| | ניווט לפי כתובת | ניווט מהתפריט |
|---|---|---|
| `accountTabs()` | **0** | **2** |
| שורות תנועה | **0** | **22** |
| טקסט בדף | 568 תווים | 4,046 |
| `#ts=` בכתובת | נעלם | נשמר |

`prepareLeumiRoute` עשה `chrome.tabs.update` עם כתובת נקייה. הטוקן שב-hash הוא
נשא ההקשר, והוא אובד בניווט הזה.

**וזו הסיבה ש-`selectAccount` "לא בוחר את החשבון":** `syncSelected` מגן —
`if(accountTabs().length) await selectAccount(key)` — ובדף ריק אין בוררים,
ולכן **הפונקציה לא נקראת בכלל.** שני התיקונים הקודמים (0.55.0, 0.55.1) נכונים
אך לא נגעו בשורש, כי הקוד שתיקנתי לא רץ שם מלכתחילה.

**מפת התפריט, נמדדה:**
- `/nis-accounts/nis-transactions` ← עובר ושב שקלים › תנועות בחשבון שקלים ✔ אומת
- `/credits/loans` ← אשראי › הלוואות ✔ אומת
- `/checks/cleared-checks` ← שיקים › שיקים מזומנים ✖ **לא אומת** — הפריט לא נמצא
  אחרי לחיצה על "שיקים". ייפול לניווט לפי כתובת עד שיימדד.

**השתנה:** `LEUMI_GO` ב-`leumi-content.js` מנווט מתוך התפריט (הורה→בן, עם
`realClick`), ו-`prepareLeumiRoute` מנסה אותו **לפני** `tabs.update`.
**הקוד הישן נשאר כנפילה לאחור מילה במילה** — כולל `renav<4`,
`LEUMI_MAX_ATTEMPTS` ו-`LEUMI_COOLDOWN_MS`, שלא נגעתי בהם (§8).

**אומת:** 16 קבצים עוברים פרסור · שלוש כתובות התוסף מתמפות נכון למסלולי
התפריט · כתובת שאין לה מסלול נופלת לניווט הישן · שומרי הלולאה במקומם.

**לא נבדק:** ריצה חיה של הסנכרון המלא. זה הדבר לבדוק ראשון.

**לבדיקת המשתמש:** רענון ל-0.56.0, "לאומי", בחירת חשבון **שאינו הפעיל**,
וסנכרון. אם התנועות שייכות לחשבון שבחרת — השרשרת נסגרה.

## 2026-08-17 08:10 · Claude · לאומי — selectAccount לא בחר כלום, 0.55.1

**תקלה:** אחרי שהזיהוי החזיר שישה חשבונות, בחירת חשבון לא עשתה דבר.

**השורש — שתי בדיקות שנשענות על טקסט הדף כולו:**
```js
const compactPage=normalized(txt(document.body));
if(compactPage.includes(key))return;                       // יציאה מוקדמת
...
if(active?.key===key||normalized(txt(document.body)).includes(key))return  // הלולאה
```
**ברגע שהבורר פתוח, טקסט הדף מכיל את כל שישה מספרי החשבון.** נמדד חי: הבדיקה
מחזירה `true` עבור **כל אחד** מהיעדים, ולכן `selectAccount` יוצאת מיד בלי ללחוץ,
ובלולאה מסמנת הצלחה בלי שהחשבון התחלף.

**נמדד חי שיש סימן אמין:** `chooser()` — האלמנט עם `aria-selected="true"` —
**התעדכן ל-927-5736 תוך 0.5 שנייה** מהלחיצה, ו-`accountTabs` כולו התחלף.

**השתנה:** היציאה המוקדמת הוסרה; הלולאה נשענת על `chooser()` בלבד. טקסט הדף
נשאר כנפילה לאחור **רק כשאין בורר בדף כלל** — כך הכוונה המקורית נשמרת בלי
התוצאה החיובית-שגויה.

**אומת:** 16 קבצים עוברים פרסור · שלוש בדיקות מבנה על הקוד שנכתב · המדידה
החיה של `chooser()` קדמה לכתיבה.

**⚠ שווה לזכור:** "המחרוזת מופיעה בדף" אינו סימן לכך שהמצב השתנה. בדף עם בורר
פתוח כל האפשרויות נמצאות בטקסט — צריך סימן **ממוקד** (aria-selected), לא חיפוש
גלובלי. אותה תבנית הופיעה גם ב-`optionPool` (0.55.0) באותו קובץ.

**לבדיקת המשתמש:** רענון ל-0.55.1, "לאומי", בחירת חשבון שאינו הפעיל, וסנכרון.

## 2026-08-17 07:30 · Claude · לאומי — 1 חשבון → 6, נמדד חי, 0.55.0

**תקלה:** זיהוי לאומי החזיר חשבון אחד בזמן שיש שישה.

**נמדד חי מול האתר** (טל מחובר, `realClick` פתח את הבורר): `[role="listbox"]`
עם שש `[role="option"]`, כל אחת "שם + מספר חשבון", **ללא יתרה**.

**השורש — שורה אחת ב-`optionPool`:**
```js
if(!a||!/-?[\d,]+\.\d{2}/.test(own))continue;
```
דרישת סכום דחתה את **כל שש** האפשרויות. הבריכה חזרה ריקה, `options()` לא עבר
את הסף, והקוד נפל ל-`accountTabs()` — חשבון אחד.

**השתנה:** הסכום נדרש רק מאלמנט **כללי**; `[role="option"]` ואחיו מתקבלים בלי.
ובנוסף: אפשרות אמיתית גוברת על אלמנט כללי בבחירת הנציג — אחרת שורת המטבע
`921-88154/39` שבדף תמונת המצב תופסת את החשבון ונותנת לו את השם **"NIS"**.

**אומת פעמיים:** הלוגיקה נבדקה על ה-DOM החי לפני הכתיבה (1→6, שמות נכונים),
ואז `optionPool` **כפי שנכתב לקובץ** הורץ על DOM משוחזר — 6 חשבונות, כולם
`role=option`, שורת המטבע לא גברה. 16 קבצים עוברים פרסור.

**⚠ מה שנבדק ונמצא תקין ואין לשנות:** `key` הוא `סניף-מספר` בלי הסיומת.
`/59` ו-`/39` הם אותו חשבון במטבעות שונים; הוספת הסיומת הייתה מפצלת אותו.

**⚠ הופרך גם:** "ההתחברות ללאומי חסומה". טל התחבר בהצלחה, והדף שנראה שבור
היה **סשן שפג** — לאומי מנתק אחרי חוסר פעילות. **התוסף לא חסם דבר.**

**מה שנשאר לבדוק:** ⚠ `LEUMI_AUTHENTICATED` נשלח ברגע ש-hostname הוא
`hb2.bankleumi.co.il` ויש `<main>` — **בלי לוודא שהמשתמש מחובר**. אם
`pendingLeumi` דלוק בזמן שהמשתמש על מסך ההתחברות, `prepareLeumiRoute` ינווט
את הלשונית תוך כדי. לא נצפה בפועל היום, אבל המסלול קיים בקוד.

**לבדיקת המשתמש:** רענון ל-0.55.0, ואז "לאומי" בדשבורד — אמורים להופיע שישה
חשבונות לבחירה.

## 2026-08-17 00:10 · Claude · לאומי — נעצר לפי בקשה, ומה שכן נמדד

**התקלה שדווחה:** חיבור לאומי מזהה חשבון אחד בזמן שיש יותר. **לא נפתרה** —
טל ביקש לעצור ולחזור לזה בפעם אחרת.

**⚠ כלל שהופרך:** „אין לפתוח לשונית שנייה מול לאומי, האתר מרשה סשן אחד” היה
**מסקנה לא מאומתת** שחסמה עבודה על לאומי במשך שיחות. טל: *„לאומי פשוט לא
מאפשר להישאר באתר”* — זה timeout של חוסר פעילות, לא התנגשות סשנים. ההסבר
הסביר לנפילות שנצפו אז הוא **לולאת הניווט של 0.12.1**. `AGENTS.md` תוקן.

**נמדד חי (לא מאומת, סשן פג):** הדף מציג „אפשר לנסות שוב בקרוב”, 646 תווים
בלבד — בדיוק המצב ש-`ready()` נכשל בו. `accountTabs()` החזיר 0, ואפס
`dialog`/`listbox`/`menu`/`option`. **בלי סשן מאומת אין מה למדוד.**

**החוט להמשך:** בתפריט מופיע **„כל החשבונות”**. `accountTabs()` דורש תבנית
`000-000000` **בתוך אותו אלמנט**, ואם לאומי מציג שם חשבון בלי המספר — שום דבר
לא מתאים, וזה יסביר „אחד מתוך רבים” במדויק. **לבדוק את זה ראשון בפעם הבאה.**

**מסלול האבחון הקיים:** `captureOptionProbe` נשמר ל-`leumiOptionProbe` באחסון
כש-`found.length<2`, ו-`syncStatus` נוקב באסטרטגיה שנבחרה (בורר / תמונת מצב /
בורר נכשל). **קריאה מדף התוסף מספיקה ואינה נוגעת בלאומי בכלל.**

**לבדיקת המשתמש:** אין. הנושא מוקפא.

## 2026-08-16 23:40 · Claude · פועלים — יתרת התשלומים בהלוואות, 0.54.1

**תקלה:** עמודת "תשלומים שנותרו" בדשבורד הייתה ריקה בפועלים בלבד.

**נמדד חי מול האתר** (`biz2…/credit-and-mortgage/transactions`, חשבון 645-690300):

- **בטבלה אין ספירת תשלומים.** שמונה עמודות: סוג אשראי · תחילת חישוב · תאריך
  סיום · קרן מקורית · יתרה משוערכת במטבע · תשלום הבא משוער · תאריך תשלום ·
  למידע ופעולות. `extractLoanDetails` ממפה 0–6 ומכסה אותן במלואן.
- **המספר יושב בפירוט הנפתח**, בתא האחרון: `תשלום קרן:63מתוך71`, בלי רווחים.
  אותו פירוט מכיל גם `ריבית נוכחית` שהקוד כבר קורא ממנו.

**⚠ המספר הראשון הוא התשלומים שנותרו, לא ששולמו.** אומת על שתי הלוואות:
71−63=8 עבור הלוואה שהחלה 27/11/25, ו-71−66=5 עבור 11/02/26 — בדיוק מספר
החודשים שחלפו. **הדשבורד מצפה ל-`paid/total`**, ולכן מיפוי ישיר היה מציג הפוך.

**השתנה:** `poalim-content.js` — הפירוט נפתח פעם אחת (memoized) ומשמש גם
לריבית וגם לתשלומים; `installments` נשלח כ-`${total-remaining}/${total}`.

**אומת מקצה לקצה מול הטקסט האמיתי:** `63מתוך71` → `8/71` → הדשבורד מציג
"נותרו 63 מתוך 71", זהה לבנק. הלוואה בלי ספירה → שדה ריק ו-"—", בלי חריגה.

**מה שנשאר פתוח:**
- **המיפוי בפועלים הוא לפי מיקום עמודה ולא לפי כותרת.** עמודה שתתווסף באתר
  תזיז את כל השדות בשקט. לא שיניתי כדי לא לגעת בחילוץ עובד באמצע עבודה.
- הפירוט מכיל גם `יתרת קרן בלוח הסילוקין`, `ריבית שטרם חויבה` ו-
  `ריבית נוכחית 6% = פריים 1% +` — שדות שאינם נקראים היום.
- §6 במסמך הכללים דרש "מספר תשלומים" מלכתחילה; הדרישה תועדה ולא מומשה.

**לבדיקת המשתמש:** סנכרון פועלים עסקי, ואז לשונית ההלוואות — "נותרו 63 מתוך
71" ו-"נותרו 66 מתוך 71".

## 2026-08-16 23:00 · Claude · צינון 6 שעות לסנכרון האוטומטי — 0.54.0

**הבקשה (טל):** סנכרון אוטומטי לא ירוץ שוב אם רץ בשש השעות האחרונות,
**למעט בקשה מפורשת של המשתמש.**

**נמדד:** כל מסלול אוטומטי בודק קודם דגל `pendingX` ורק בהיעדרו נופל
ל-`maybeAutoRun`/`maybeAutoSync`. כלומר **בקשה מפורשת כבר עוקפת את שתי
הפונקציות האלה לחלוטין**, והשער נכנס בדיוק במקום הנכון בלי לגעת במסלול הידני.

**השתנה ב-`background.js`:**
- `AUTO_SYNC_MIN_GAP_MS = 6h`, `autoSyncTooSoon(st,source)` ו-`gapText`.
- השער בשתי נקודות הכניסה, מיד אחרי בדיקת `autoSyncOnLogin`. הסטטוס אומר
  כמה נותר ומפנה ללחיצה ידנית.
- `syncSelected` מחתים `autoSyncLast` גם בסנכרון ידני — אחרת כניסה לבנק מיד
  אחרי לחיצה ידנית הייתה מפעילה סבב שני מיותר. המקור נגזר מקידומת המפתח.

**אומת בהרצת הפונקציה עצמה:** מעולם לא סונכרן → רץ · 10 דק' → חסום, נותרו
5:50 · 5 שעות → חסום · 5:59 → חסום, נותרה דקה · 6 שעות ומעלה → רץ · מקור אחר
בלבד → רץ. הצינון **פר-מקור** ולא גלובלי.

**רקע:** התגלה כמכשול מעשי — הסנכרון האוטומטי חטף את לשונית פועלים שוב ושוב
באמצע מדידה ידנית של דף ההלוואות.

**לא נבדק:** ריצה חיה.

**לבדיקת המשתמש:** אפשר להחזיר את הסנכרון האוטומטי לפעולה. כניסה חוזרת לבנק
בתוך שש שעות תציג "סונכרן לאחרונה לפני פחות מ-6 שעות" ולא תריץ סבב.

## 2026-08-16 22:20 · Claude · כיבוי הסנכרון האוטומטי התבטל בכל רענון — 0.53.2

**תקלה:** המשתמש כיבה "סנכרון אוטומטי בכניסה לבנק", והוא חזר לפעול.

**נמדד — שני גורמים בלתי תלויים:**

1. `chrome.runtime.onInstalled` כתב `autoSyncOnLogin:true`. **בתוסף unpacked
   האירוע הזה נורה בכל לחיצה על ⟳ ב-`chrome://extensions`**, לא רק בהתקנה,
   ולכן כל רענון ביטל את הבחירה. `onStartup` דווקא לא נגע בשדה — אי-העקביות
   בין השניים היא שהסתירה את הבאג.
2. **שתי תיבות סימון נושאות את אותו `id="autoSyncOnLogin"`** — אחת בפאנל
   הבנקים ואחת ב-`tools`. ריענון המצב השתמש ב-`querySelector`, שמחזיר את
   הראשונה בלבד, ולכן השנייה הציגה מצב שגוי גם כשהאחסון היה נכון.

**השתנה:** `onInstalled` כותב את ברירת המחדל רק כש-`details.reason==='install'`;
שני מקומות הריענון ב-`dashboard.js` עברו ל-`querySelectorAll('[id="autoSyncOnLogin"]')`.

**אומת:** המטפל הורץ בסימולציה מול שני התרחישים — `reason:'update'` **אינו**
כותב את השדה, `reason:'install'` כותב `true` · 16 קבצים עוברים פרסור · לא
נותר `querySelector('#autoSyncOnLogin')` יחיד.

**ממצא ששווה לזכור:** `onInstalled` הוא לא "פעם אחת" בפיתוח unpacked. כל דבר
שנכתב שם מאפס העדפות משתמש בכל רענון. `freshStart` נשאר שם בכוונה — הוא מנקה
דגלי ניווט — אבל **העדפה של המשתמש אינה שייכת לאותה קטגוריה.**

**רקע:** התגלה בזמן מדידה בדף ההלוואות של פועלים, כשהסנכרון האוטומטי חטף את
הלשונית שוב ושוב וגרר את החשבון בחזרה ל-690309.

**לבדיקת המשתמש:** לרענן, לכבות את הסנכרון האוטומטי, לרענן שוב — הכיבוי אמור
לשרוד.

## 2026-08-16 21:10 · Claude · שיוך כרטיס משורת הכרטיס עצמה — 0.53.1

**תקלה:** כרטיס MAX 2910 נשאר "ממתין לשיוך". פאנל השיוך נתלה בתצוגת החשבונות,
בזמן שההודעה מופיעה בלשונית כרטיסי האשראי — הבורר לא היה במקום שבו רואים את
הבעיה.

**נמדד:** חשבון פועלים 690300 **אינו מחזיק אף כרטיס** (טל). `runMax` משייך רק
לפי סיומת שכבר קיימת ב-`account.cards`, ולכן אין למה להצליב — ההצלבה האוטומטית
לא יכולה להצליח שם לעולם, בלי קשר לרגקס של פועלים.

**השתנה ב-`dashboard.js`:**
- `assignSelect(c)` — בורר חשבונות, ו-`' · ממתין לשיוך לחשבון'` בשורת הכרטיס
  הוחלף בו. הבורר עכשיו בדיוק במקום שבו רואים את הבעיה.
- `assignCard(src,suffix,accountId)` — לוגיקה אחת לשני המקומות.
  **נופלת חזרה לרשימה המוצגת** כשהכרטיס אינו ברשימת הממתינים, ולכן השיוך עובד
  גם כשהסנכרון לא סימן אותו — וזה המצב בחשבון בלי כרטיסים.
- `document.addEventListener('change', …)` מואצל אחד במקום `panel.onchange`,
  כדי ששני המקומות לא יתחזקו לוגיקה נפרדת ולא יירו פעמיים.

**אומת:** 16 קבצים עוברים פרסור · `cardSrc` ממפה MAX/מקס→max, כאל→cal,
ישראכרט→isracard · הבורר נבנה עם `data-suffix=2910` ו-`data-src=max` ומכיל את
שני חשבונות הפועלים · `isracard-account` מופיע פעמיים בלבד (הסימון + המאזין),
כלומר אין מטפל כפול.

**לא נבדק:** לחיצה חיה על הבורר.

**לבדיקת המשתמש:** לשונית כרטיסי אשראי → בכרטיס 2910 יש עכשיו בורר במקום
הכיתוב. לבחור 690300. מהרגע הזה הכרטיס נכנס ל-`account.cards` והסנכרונים
הבאים מזהים אותו לבד.

## 2026-08-16 20:40 · Claude · יהב עובד — והכשל היה קוד ישן, לא באג

**אומת ע"י המשתמש:** יהב רץ תקין ב-0.53.0, מהתיקייה המפוצלת. **הפיצול אינו
שובר את יהב** — זו הבדיקה החיה שחסרה מאז הפיילוט.

**⚠ הממצא החשוב:** התקלה נעלמה **אחרי רענון התוסף**, לא אחרי תיקון קוד.
ה-`catch` של 0.51.4 לא ריפא דבר — הוא רק מונע בליעה. אף אחד משני הסוכנים
לא הצליח לשחזר את הכשל, וגם לא היה מה לשחזר: **Chrome הריץ קוד ישן.**

**המסקנה לפעם הבאה: לפני שמאבחנים באג בתוסף — לוודא שהגרסה ב-`chrome://extensions`
היא זו שבקבצים.** אחרת מנתחים קוד שאינו רץ. זה עלה כאן סבב שלם, והוא הצטרף
לאותה משפחה של תקלות מטמון: בדיקת התחביר בדפדפן רצה על עותק ישן באותו יום.

**⚠ ומלכודת בדיקה:** `-match` של PowerShell על מחרוזת עברית נתן "חסר" על
`catch` שכן היה בקובץ, ושלח אותי לכיוון שגוי. **לבדיקות תוכן בעברית להשתמש
ב-Python עם `encoding='utf-8'`, לא ב-PowerShell.**

**לא נבדק:** סנכרון MAX ב-0.53.0 (שינוי לולאת החודשים). הורדת הנתונים אומתה
ב-0.52.2 בלבד.

**הצעד הבא:** אחרי אימות MAX — מיזוג `split-background` ל-`main`.

## 2026-08-16 20:05 · Claude · MAX — החודש הבא נבדק ראשון, 0.53.0

**תקלה:** "החיוב הקרוב" הציג את אוגוסט, מחזור שכבר חויב ב-10.08.

**נמדד בקוד, לא בהנחה:** `for(let i=0;i<12;i++)` התחיל בחודש הלוח הנוכחי ורץ
אחורה, ולכן **ספטמבר לא נדרש אף פעם** — לא בסנכרון ולא בטעינת השנה. שאלת
"האם ל-MAX יש דף ספטמבר" מעולם לא נשאלה מהאתר.

**השתנה:** רשימת 13 חודשים — הבא, ואז 12 אחורה. הראשון מסומן `optional`:
- `MAX_SELECT_MONTH` **כבר מחזיר** `{ok:false}` כשהתווית אינה בבורר. זו המדידה,
  והיא הייתה בקוד מלכתחילה — **אין סלקטור חדש ואין רשימה שנגררת פנימה.**
- כישלון בחודש רשות (בורר / דף שלא התייצב / חזרה לאותו חודש) → `continue`.
- `nowLabel`/`nowKey` נגזרים מ-`firstRead` — החודש הראשון שנקרא בפועל.
  `new Date()` אינו קובע יותר מה החיוב הקרוב.

**אומת:** 16 קבצים עוברים פרסור · סדר `importScripts` ✓ · סדר הבקשות
`09.2026* → 08.2026 → … → 09.2025`, 13 ללא כפילויות · **קצוות:** 31.01 נותן
02.2026 ולא 03 (ה-`setDate(1)` מונע גלישת יום), ו-31.12 נותן 01.2027.

**⚠ ההבדל מהניסיון שנכשל ב-0.52.0:** שם קראתי רשימת חודשים עם
`li.month,[role="button"]` — סלקטור שנלקח מ-`find` (מתעלם ממה שלא מתאים) והופעל
ב-`map` (גורר הכל). חודשים מדומים נקראו ריקים והלולאה טסה דרכם. כאן שואלים
את האתר על **תווית אחת** ומקבלים תשובה חד-משמעית.

**לא נבדק:** לא רצה ריצה חיה. אם ל-MAX אין דף ספטמבר — הכל נשאר כשהיה, החיוב
הקרוב יהיה אוגוסט, וה-syncStatus יגיד `MAX: החיוב הקרוב — אוגוסט 2026`.
**הסטטוס הוא התשובה לשאלה שנשארה פתוחה שלושה סבבים.**

**לבדיקת המשתמש:** סנכרון MAX. לעקוב אחרי `syncStatus` — הוא אומר אם ספטמבר
נמצא, ומה נקבע כחיוב הקרוב.

## 2026-08-16 19:30 · Claude · שחזור MAX — עוגן הבורר הוסר, 0.52.2

**תקלה שגרמתי:** MAX הפסיק להוריד נתונים ורץ מהר מחודש לחודש. הסיבה:
`monthLabels()` שאספה תוויות מ-`li.month,[role="button"]` — סלקטור רחב שתופס
כפתורים שאינם בבורר החודשים. חודשים ללא נתונים נקראים מהר, `cards.length`
יוצא 0, ואז הלולאה חולפת דרכם בלי לשמור כלום.

**השתנה:** `runMax` ו-`max-content.js` הוחזרו למצבם ב-b7ae519. הודעת
`MAX_MONTHS` הוסרה. **פאנל השיוך הידני נשאר** — הוא בלתי תלוי ולא נגע ב-MAX.

**אומת:** 16 קבצים עוברים פרסור · הלולאה חזרה ל-12 חודשי לוח · אין שארית של
`MAX_MONTHS` באף קובץ · ה-`catch` של יהב במקומו.

**⚠ הלקח, והוא על השיטה ולא על הקוד:** ביקשתי מדידה של הבורר, לא קיבלתי אותה,
וכתבתי את הקוד על סמך הנחה שהסלקטור הקיים ב-`selectMonth` יחזיר את רשימת
החודשים. **סלקטור שעובד לאיתור פריט יחיד לפי טקסט אינו בהכרח תקף כרשימה** —
`find` מתעלם מכל מה שאינו מתאים, `map` גורר את הכל פנימה. זה §2 של `AGENTS.md`
במלוא מובנו, ואני עברתי עליו.

**מה שעדיין פתוח:** החיוב הקרוב ב-MAX ממשיך להציג את חודש הלוח הנוכחי, שאצל
MAX הוא **חודש חיוב שכבר ירד**. התקלה שדווחה לא תוקנה, והתיקון ממתין למדידה
של הבורר בדף העסקאות.

**אומת ע"י המשתמש (16.08.2026):** MAX חזר לקרוא נתונים תקין ב-0.52.2.

## 2026-08-16 19:05 · Claude · מיזוג 0.51.4 — התיקון של יהב הושתל ל-yahav.js

**תקלה:** התיקון של המחשב השני (`catch` ב-`runYahav`) נכתב על `background.js`
של 0.51.3, שבו קטע יהב עדיין ישב. בענף הזה הקטע כבר ב-`yahav.js`.

**נמדד:** git ביקש להחזיר את **כל** בלוק יהב ל-`background.js` — 11 הצהרות —
כי צד אחד מחק וצד שני ערך. מיזוג "מוצלח" היה משכפל את הקוד בשני קבצים, ושתי
הגדרות של `yahavBusy` ב-service worker אחד.

**השתנה:** `background.js` נפתר לצד הזה (יהב נשאר בחוץ), וה-`catch` הושתל
ידנית ל-`runYahav` שב-`yahav.js`. גרסה 0.52.1.

**אומת:** 11 שמות יהב מופיעים ב-`yahav.js` בלבד — ב-`background.js` נשארו רק
שתי נקודות הקריאה · 16 קבצים עוברים פרסור · סימולציית סדר `importScripts`
עולה בלי התנגשות הצהרות · ה-`catch` במקומו.

**ממצא ששווה לזכור:** קובץ שסוכן אחד מפצל וסוכן שני עורך הוא **התנגשות מובנית**,
ו-git לא תמיד יסמן אותה. כלל הבעלות ב-`AGENTS.md` §0 נועד בדיוק לזה — וכאן הוא
עבד: הפיצול היה בענף, לא ב-`main`.

**לא נבדק:** הכשל המקורי ביהב עדיין לא אובחן ולא שוחזר, בשני הצדדים. ה-`catch`
מגלה אותו בפעם הבאה במקום לבלוע.

**לבדיקת המשתמש:** סנכרון יהב מהתוסף — ואם ייכשל, `syncStatus` יראה עכשיו את
הסיבה במקום לקפוא.

## 2026-08-16 18:49 · Claude · יהב נכשל בשקט — נוסף ה-catch החסר

**תקלה:** סנכרון יהב נעצר בלי שום הודעה למשתמש. `runYahav` היה הפונקציה היחידה בקובץ
עם `try`/`finally` בלי `catch`, ושני מקומות הקריאה בשורה 60 בולעים את הדחייה
ב־`.catch(()=>{})`. כל אחד מששת ה־`throw` שבתוכה נעלם, ו־`syncStatus` קפא על ההודעה האחרונה.

**נמדד:** אחסון התוסף (`Local Extension Settings`) — הרשומה האחרונה של יהב היא
`"יהב: מגדיר טווח של 3 חודשים וקורא תנועות"` ואחריה כלום, ואף שורה עם `"source":"yahav"`
לא נכתבה שם מעולם. ללאומי, לעומת זאת, יש באותו אחסון שורת `"שגיאה בלאומי: ..."` מלאה —
כי ללאומי יש `catch`.

**השתנה:** נוסף `catch` ב־`background.js:584` באידיום של שורה 418. גרסה 0.51.4.
בנוסף `.gitignore` חוסם `*.crx` ו־`*.pem` — אריזה ב־`chrome://extensions` יצרה מפתח
חתימה פרטי בשורש הריפו, והריפו ציבורי.

**לא עובד / לא נבדק:** **הסיבה לכשל המקורי לא אובחנה מעולם.** אחרי טעינת 0.51.4 יהב
רץ בהצלחה — כלומר הכשל היה חולף ולא שוחזר. אל תחפש באג ביהב על סמך הרשומה הזו.
ה־`catch` עצמו לא נראה בפעולה, כי לא היה כשל שיפעיל אותו. `node --check` לא רץ —
node אינו מותקן במחשב של המשתמש.

**הצעד הבא:** נשאר ללא שינוי — חילוץ יהב כפיילוט בענף `split-background`.

**לבדיקת המשתמש:** אין. יהב אומת בריצה חיה על 0.51.4.

## 2026-08-16 · Claude · MAX — החיוב הקרוב היה חודש הלוח, ושיוך ידני חסר

**תקלה:** כרטיס MAX 2910 הציג כ"חיוב קרוב" את החיוב של אוגוסט, שכבר ירד ב-10.08,
וכל ציר החודשים היה מוסט בלשונית אחת.

**נמדד:** `runMax` חישב `nowLabel` מ-`new Date()` ובחר את חודש הלוח הנוכחי. אצל MAX
הלשונית היא **חודש החיוב**, ולכן "אוגוסט 2026" הוא מחזור סגור ומחויב — התנועות בו
09.07–05.08. שלוש חברות האשראי מגדירות "חודש" בשלוש דרכים: ישראכרט לפי לשונית
הבורר, MAX לפי `.combo-text.dates` (בפורמט `MM.YYYY` עם נקודה), וכאל לפי **תאריך
החיוב** שבכותרת. `+1` ללוח השנה היה נשבר בתחילת חודש, כשהחיוב עוד לא ירד.

**השתנה:**
- `max-content.js` — הודעה `MAX_MONTHS`. קוראת את תוויות הבורר, ופותחת אותו רק אם
  הרשימה אינה כבר ב-DOM (ומחזירה לסגור — `selectMonth` מניחה שהוא סגור).
- `background.js` — `runMax` מעגן את החיוב הקרוב ואת הלולאה ב**תווית העליונה
  שהאתר מציע**, ולא בלוח השנה. `new Date()` נשאר רק כנפילה לאחור, והנפילה מסומנת
  ב-syncStatus.
- `dashboard.js` — פאנל השיוך הידני הוכלל מישראכרט גם ל-MAX ולכאל. **זו הייתה הסיבה
  ש-2910 נשאר "ממתין לשיוך"**: `runMax` משייך רק לפי כרטיס שהבנק כבר דיווח עליו,
  לכאל יש מסלול שני לפי `debitAccount` מדף הבית, ול-MAX לא היה אף אחד מהשניים.
  אחרי שיוך ידני הכרטיס נכנס ל-`account.cards` והסנכרונים הבאים מזהים אותו לבד.

**אומת:** 16 קבצים עוברים פרסור · הרגקס של התוויות תופס "אוגוסט 2026" ודוחה
"כל הכרטיסים" · המיון בוחר ספטמבר 2026 כחיוב הקרוב נכון להיום. גרסה 0.52.0.

⚠ **אין מנוע JS על המחשב הזה** — `node --check` שב-`AGENTS.md` §5 לא ניתן להרצה.
במקומו: `preview_start` על `syntax-check` (python http.server על תיקיית התוסף)
ואז `new Function(src)` בדפדפן. **לבקש את הקובץ עם `?v=` ו-`cache:'no-store'`** —
בלי זה הדפדפן מגיש עותק ישן ובדיקת התחביר רצה על קוד שכבר הוחלף.

**ממצא ששווה לזכור:** בתוך מחרוזת JS `'\s'` שווה ל-`'s'`, ולכן
`new RegExp('...\s+\d{4}$')` נשבר בשקט. נתפס בבדיקה והוחלף בליטרל `/…/`.

**לא נבדק:** לא רצה ריצה חיה מול MAX. אם הבורר אינו מציע חודש עתידי, העוגן יישאר
אוגוסט וה-syncStatus יראה זאת — צריך לבדוק בסנכרון הבא.

**לבדיקת המשתמש:** סנכרון MAX, ואז שני דברים — שהחיוב הקרוב הוא ספטמבר, ושפאנל
השיוך מופיע ומאפשר לקשור את 2910 לחשבון.

## 2026-08-16 17:33 · Codex · תיקון עובדות בנוהל

**תקלה:** שני מסמכי הנוהל הציגו נתונים ישנים על גודל קובץ הרקע ועל גרסת התוסף.

**נמדד:** `background.js` — 104,257 תווים; הגרסה ב־`extension/manifest.json` היא `0.51.3`.

**השתנה:** עודכנו הנתונים ב־`AGENTS.md` וב־`PROJECT-HISTORY-AND-RULES.md` בלבד.

**לא עובד / לא נבדק:** לא בוצע שינוי בקוד ולא נדרשה בדיקת סנכרון.

**הצעד הבא:** נשאר ללא שינוי — חילוץ יהב בלבד כפיילוט בענף `split-background`.

**לבדיקת המשתמש:** לעבור על ניסוחי התיעוד ולאשר שהם משקפים את המצב הנוכחי.

## 2026-08-16 18:10 · Claude · פיילוט פיצול — יהב אומת חי

**תקלה:** `background.js` ריכז שבעה בנקים ב-104,257 תווים. כל נגיעה בבנק אחד סיכנה
כתיבה של הקובץ כולו, ושני סוכנים נאלצו לגעת באותו קובץ.

**נמדד:** יהב הוא המנותק ביותר — 10 פונקציות, 7,685 תווים, תלוי רק ב-`delay`
וב-`autoBusy`, ואפס קריאות לפונקציות מחוץ למקטע שלו.

**השתנה:** `extension/yahav.js` — העברה מילולית של 11 מקטעים, בלי שינוי תוכן, בלי
עטיפת IIFE ובלי שינוי נקודות קריאה. `importScripts` הורחב. `background.js` ירד
ל-96,583 תווים. **המצב המשותף נשאר ב-`background.js`**: `running`, `autoBusy`,
מאזין ההודעות ושומרי הלולאה.

**אומת:** 15 נקודות חיבור במספר מופעים זהה · 16 קבצים עוברים תחביר · סימולציית
טעינת service worker בסדר ה-`importScripts` עולה בלי חריגה · `delay`/`withTimeout`/
`running`/`autoBusy` מוצהרים פעם אחת בלבד. **המשתמש אישר: יהב עובד חי, וכן כל
שאר הבנקים וכרטיסי האשראי.**

**לא נבדק:** כאל ו-MAX לא אומתו בריצה חיה מול האתרים. מסגרת אשראי בלאומי עדיין `null`.

**הצעד הבא:** הבנק הבא לחילוץ — הבינלאומי (6.5KB, הקטן הבא) או מזרחי (16.5KB, החוסך
הגדול). הליבה נשארת ב-`background.js` עד שיחולצו שניים-שלושה בנקים.

**לבדיקת המשתמש:** אחרי כל חילוץ — טעינת התוסף וסנכרון חי של אותו בנק בלבד.

**ממצא:** ממפה שחותך לפי הצהרות בעמודה 0 מייחס בטעות בלוקים שאינם הצהרות (למשל
מאזין ההודעות המרכזי) למקטע שלפניהם — כך נוצרה "תלות" מדומה של מזרחי ביהב.

---

## 2026-08-16 17:45 · Claude · הקמת הנוהל

**תקלה:** שני סוכנים עבדו על אותם קבצים בלי לדעת זה על זה. התוצאה: גרסאות שנותחו
אחרי שכבר הוחלפו, אזהרה על יכולת "חסרה" שהייתה קיימת במימוש אחר, וקפיצות גרסה
בלתי מוסברות (0.31 → 0.45).

**נמדד:** `background.js` — 104,257 תווים, 736 שורות, 116 הצהרות. מיפוי לפי בעלות:
מזרחי 16.5K · ליבה 16.4K · ישראכרט 16.1K · היסטוריית כרטיסים 14.1K · דיסקונט 14.1K ·
לאומי 11.1K · **יהב 7.7K** · הבינלאומי 6.5K · הפועלים 1.3K.

**השתנה:** הפרויקט עבר מתיקיית ההורדות המתוארכת של Codex לריפו
`github.com/talratzabi/banks-extension`. נוספו `AGENTS.md` בשורש והקובץ הזה.

**לא עובד / לא נבדק:** כאל ו-MAX — `startCal`/`startMax` קיימים אך לא אומתו בריצה חיה.
מסגרת אשראי בלאומי נשמרת `null`, וכרטיסי אשראי אינם נקראים שם.

**הצעד הבא:** ענף `split-background`, וחילוץ יהב בלבד כפיילוט — 10 פונקציות +
`YAHAV_ROOT` + `yahavBusy`, העברה מילולית. המצב המשותף (`running`, `autoBusy`,
מאזין ההודעות) **נשאר ב-`background.js`**; העברתו היא החלק המסוכן ונדחית לסוף.

**לבדיקת המשתמש:** אחרי הפיילוט — לטעון את התוסף מהענף, לוודא שה-service worker
עולה בלי שגיאה, ולהריץ סנכרון יהב חי. אני יכול לאמת תחביר ומבנה, לא ריצה מול הבנק.

**ממצא ששווה לזכור:** ממפה שחותך לפי הצהרות בעמודה 0 ייחס בטעות את מאזין ההודעות
המרכזי ל-`mizrahiFrameData`, ומכאן "תלות" מדומה של מזרחי ביהב. שני הסוכנים הגיעו
לאותה מסקנה בנפרד. **יהב מנותק לחלוטין** — תלוי רק ב-`delay` וב-`autoBusy`.
