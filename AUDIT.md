# ביקורת עומק — 28.08.2026 (גרסה 1.48.1)

> **עדכון 28.08.2026, גרסה 1.49.0 — סטטוס יישום:**
>
> | סעיף | סטטוס |
> |---|---|
> | 1. גיבוי/ייצוא | ✅ שורת גיבוי בדשבורד: ייצוא, שחזור, תזכורת 30 יום |
> | 2. מרוץ כתיבה | ✅ `accountsMutex` — 10 מסלולי הכתיבה עברו דרכו |
> | 3. גרסת סכימה | ✅ `DATA_VERSION=2` + שלד מיגרציות |
> | 4. גידול | ✅ ניקוי אבחון בתחילת סנכרון · תפוגת 90 יום ל-missing · ניקוי יתומים ידני |
> | 5. בלוב אחד | ⏳ נדחה — refactor גדול; המנעול סוגר את הסיכון המיידי |
> | 6. catch ריקים | ✅ חלקי — האזנה גלובלית ל-unhandledrejection/error (`workerErrors`) |
> | 7. פריים מקודד | ✅ `btbPrime` מהאחסון + עורך בדשבורד + התרעת 60 יום |
> | 8. קנרית | ✅ שער שפיות בשמירה המרכזית: יתרה/תנועות/הלוואות שנעלמו |
> | 9. חיי worker | ✅ `syncInFlight` + chrome.alarms — ריצה שמתה מדווחת "נקטע" |
> | 10. עזרים כפולים | ⏳ נדחה — איחוד דורש עריכת 8 קבצים; סיכון גבוה מהתועלת כרגע |
> | 11. הצפנה | ⏳ החלטה של טל — עולה בנוחות |
> | 12. XSS | ✅ סריקה: 13 ביטויים, אחד תוקן (src של צילום שיק); השאר בטוחים |
> | 13. הרשאות | ⏳ נדחה — צמצום עלול לשבור התחברות; דורש בדיקה חיה פר-בנק |
> | קטנות | ✅ cardHistStats פעם אחת · ✅ tests.html — 15 בדיקות רגרסיה |


כל ממצא כאן **נמדד**, לא שוער. ליד כל אחד — הראיה והמלצת התיקון.
מדורג לפי נזק פוטנציאלי, לא לפי קלות תיקון.

---

## דרגה א' — אובדן נתונים אפשרי

### 1. אין שום גיבוי או ייצוא — הכול בפרופיל כרום אחד
**הראיה:** `grep export|backup|גיבוי` בדשבורד: **0 מופעים.** הנתונים
חיים ב-`chrome.storage.local` (3.6MB) וב-IndexedDB של השיקים (7.6MB) —
בתוך פרופיל הכרום בלבד.
**התרחיש:** הסרת התוסף בטעות, פרופיל שנפגם, "נקה נתוני אתרים", או מעבר
מחשב — **וכל היסטוריית התנועות, ההלוואות ו-133 צילומי השיקים נעלמים
בלי אזהרה.** אין מקור לשחזר ממנו: הבנקים עצמם מוגבלים אחורה (יהב ~6
חודשים, נמדד).
**המלצה (החשובה ביותר במסמך):** כפתור "ייצוא הכול" שמוריד JSON אחד +
תיקיית צילומים, וגיבוי אוטומטי שבועי דרך `chrome.downloads` לתיקייה
קבועה. שחזור = טעינת הקובץ, כמו במסלקה.

### 2. מרוץ קריאה-כתיבה על `accounts` — אין מנעול גלובלי
**הראיה:** **12 אתרים** ב-background עושים `get({accounts})` → שינוי →
`set` (שורות 159, 286, 317, 416, 864, 1668, 1769, 2140, 2210, 2245,
2277, 2358). המנעולים הם **פר-בנק** (`leumiBusy`, `btbBusy`...) —
אין מנעול על האחסון עצמו.
**התרחיש:** סנכרון אוטומטי רץ, המשתמש לוחץ על בנק אחר, ו-reconcile של
הכרטיסים מתעורר מ-`storage.onChanged` — שלושה קוראים את הרשימה, כל אחד
כותב את הגרסה שלו, **והאחרון מוחק את עבודת השניים בשקט.** זה בדיוק סוג
הבאג של "ההלוואה הועתקה" — רק שהפעם לא יהיה סימן.
**המלצה:** פונקציה אחת `mutateAccounts(fn)` עם תור (promise chain) שכל
12 האתרים עוברים דרכה. שינוי מכני, סיכון נמוך, סוגר את המשפחה כולה.

### 3. אין גרסת סכימה לנתונים
**הראיה:** `schemaVersion|dataVersion|migration`: מופע אחד (הערה ישנה).
שדות נוספו אד-הוק לאורך הדרך (`selectionKey`, `accountSuffix`,
`siteRemainingInstallments`...) וכל קורא מגן על עצמו עם fallbacks.
**התרחיש:** שינוי מבנה עתידי יקרא רשומות ישנות לא נכון **בלי שגיאה** —
כמו `remainingInstallments` שהחליף משמעות ב-1.33.2.
**המלצה:** `dataVersion` באחסון + בלוק מיגרציות מפורש בעליית ה-worker.
שדרוג שלא יודע לקרוא גרסה — עוצר ואומר, לא מנחש.

---

## דרגה ב' — שחיקה שתצטבר עד תקלה

### 4. גידול בלתי מוגבל בכל השכבות
**הראיה:**
- חנות השיקים: `chequePut/Get/Keys/Count` — **אין אף פונקציית מחיקה**
  (0 מופעי delete). 7.6MB היום, מונוטוני עולה.
- **28 מפתחות אבחון** נכתבים (`leumiDebug`, `btbProbe`,
  `leumiChequeWindows`...) ורק **קריאת remove אחת** בכל הקוד.
- `leumiChequeMissing` צובר רשומות לנצח (הדילוג פג אחרי 30 יום —
  הרשומה לא).
- `statusBySource` שומר שגיאות ישנות לנצח — ה-"message channel closed"
  של דיסקונט מ-18.08 בלבל את האבחון עשרה ימים אחר כך. קרה בפועל.
**המלצה:** (א) ניקוי מפתחות אבחון של מקור בתחילת כל סנכרון שלו;
(ב) מחיקת שיקים שגילם לפני `collectSince`; (ג) תפוגה של 90 יום
לרשומות `missing`; (ד) `statusBySource` שומר רק את ה-N האחרונים.

### 5. `accounts` הוא בלוב אחד של ~278KB שנכתב 9+ פעמים בסנכרון
**הראיה:** 1,541 תנועות בתמונה אחת של 277,850 בייטים; 9 כתיבות מלאות
ב-background. זה מקור ה-WAL של 3.6MB — **הגברת כתיבה פי ~13.**
**התרחיש:** ככל שההיסטוריה גדלה, כל עדכון קטן (שיוך כרטיס אחד) כותב
מגה-בייט, הסנכרון מאט, וחלון החשיפה לשחיתות גדל.
**המלצה:** מפתח לחשבון (`acct:leumi-921-348300`) ואינדקס קטן. לעשות
יחד עם סעיף 2 — אותו refactor בדיוק.

### 6. 68 catch ריקים ב-background (ועוד 57 בקבצי התוכן)
**הראיה:** ספירה ישירה. חלקם מכוונים ומתועדים — אבל היסטורית, **שלוש
מהתקלות הקשות של השבוע התחבאו מאחורי catch שקט** (הגשש של יהב מ-1.16.1,
תשובת ה-probe, שגיאת ה-bfcache).
**המלצה:** תבנית אחת: `catch(e){diag('מקור','שלב',e)}` שכותבת לרשומת
אבחון גם כשלא מציגים למשתמש. שקט כלפי המשתמש ≠ שקט כלפי היומן.

### 7. קבועים עסקיים מקודדים שהעולם ישנה
**הראיה:** `BTB_PRIME=0.05` (שורה 1618) — **כשבנק ישראל יזיז את הפריים,
הריבית, ה-monthsLeft והאזהרות של BTB יהיו שגויים בשקט.** גם
`CHEQUE_BUDGET_MS=90000`, `RETRY_MS=30 יום`, `EMPTY_MONTHS_STOP=2`.
**המלצה:** הפריים — שדה בהגדרות עם תאריך עדכון, והתרעה בדשבורד כשגילו
עולה על 60 יום. שאר הקבועים — ריכוז באובייקט `TUNING` אחד עם הערות.

---

## דרגה ג' — שבירות מבנית

### 8. אין "קנרית" לשינוי עיצוב של בנק
**הראיה:** כל המערכת היא קריאת DOM. כשבנק משנה מסך, שדות פשוט חוזרים
null — כמו „200 שורות, 200 עתידיות" של השבוע. יש גששים מצוינים, אבל הם
נקראים **אחרי** שמשהו נשבר.
**המלצה:** שער שפיות אחרי כל סנכרון, לכל המקורות (בלאומי כבר קיים חלקית
דרך `gapOf`): יתרה null, צניחת תנועות של >80% מול הסנכרון הקודם, או
הלוואות שנעלמו — מסומנים "חשוד, לא נשמר כאמת" במקום להישמר בשקט.

### 9. חיי ה-service worker תלויים ב-keepAlive לא מובטח
**הראיה:** `getPlatformInfo` כל 20 שניות. כרום כבר שינה בעבר את
הסובלנות ל-keepalive כזה, ומצב הריצה (`leumiBeat`, דגלי ה-busy) חי
בזיכרון ה-worker בלבד. ריצה שה-worker שלה מת נעלמת — נצפה השבוע.
**המלצה:** `chrome.alarms` כרשת שנייה; ורשומת `syncInFlight` באחסון
שמסומנת בתחילת סנכרון ונבדקת בעליית ה-worker — כדי שריצה שמתה תדווח
"נקטעה" במקום להיעלם. (ה-stash של 1.45.0 כבר מגן על הנתונים עצמם.)

### 10. עזרים משוכפלים בין קבצים
**הראיה:** `money()` מוגדר **בשמונה קבצים**; `realClick` בשניים. סטיית
עותקים כבר עלתה ביוקר (DATE עם `\b` מול DATE בלי).
**המלצה:** `shared-utils.js` שמוזרק ראשון בכל מתאם (executeScript תומך
ברשימת קבצים — כבר בשימוש). לאחד בהזדמנות, לא כפרויקט.

---

## דרגה ד' — פרטיות ואבטחה

### 11. הנתונים אינם מוצפנים, וה-WAL זוכר הכול
**הראיה:** את כל האבחונים של היומיים עשיתי **בקריאת ה-WAL של התוסף
מהדיסק** — אותה גישה פתוחה לכל תהליך אחר במחשב. צילומי השיקים, היתרות
וההלוואות קריאים לכל מי שמגיע לפרופיל.
**המלצה:** לכל הפחות — מודעות (זה מחשב אישי). אם רוצים יותר: הצפנת
`accounts` והצילומים במפתח שנגזר מסיסמת דשבורד. עולה בנוחות; החלטה שלך.

### 12. משטח XSS בדשבורד
**הראיה:** 44 שימושי innerHTML. הזרמים המרכזיים עוברים `esc()`, אבל
תנועה מהבנק היא קלט זר, ומספיק שדה אחד שנשכח. לא נמצאה פרצה בפועל —
וגם לא בוצעה בדיקת עומק מלאה.
**המלצה:** מעבר חד-פעמי על 44 המופעים עם רשימת וידוא; ולכלל קבוע —
כל ערך שמקורו בבנק עובר `esc()` גם אם "זה רק מספר".

### 13. הרשאות רחבות מהצורך בשוליים
**הראיה:** `https://www.max.co.il/*` (אתר שיווקי שלם) לצד
`online.max.co.il`; `*.leumi.co.il` לצד `*.bankleumi.co.il`.
**המלצה:** בהזדמנות — לצמצם לדומיינים שבשימוש בפועל. מקטין גם משטח
תקיפה וגם שאלות של כרום בעדכוני הרשאות.

---

## דרגה ה' — קטנות ששוות תיקון אגבי

- **`Date.parse` על `collectSince`** (6 מופעים) — פורמט תלוי-מנוע;
  עדיף הפירוק המפורש שכבר קיים (`dmy`).
- **אין בדיקות אוטומטיות** — הדפוס "הגנה כמעט בכל מקום" חזר שלוש פעמים
  השבוע. הפיקסטורות שכבר נכתבו בבדיקות הדפדפן יכולות לחיות ב-`tests.html`
  אחד שנפתח בלחיצה ומריץ את כולן. תשתית קיימת, חסר רק איסוף.
- **`fibiSetRangeMain`/`fibiPager`** — מסלול מת שמחכה למחיקה.
- **`cardHistStats` נקרא פעמיים בריצה** — בזבוז מדוד, תיקון של שורה.

---

## סדר עבודה מומלץ

| שלב | מה | למה קודם |
|---|---|---|
| 1 | ייצוא/גיבוי (סעיף 1) | הנזק הבלתי-הפיך היחיד ברשימה |
| 2 | `mutateAccounts` (סעיפים 2+5) | סוגר את מחלקת מירוצי הכתיבה |
| 3 | ניקוי גידול (סעיף 4) | זול, ועוצר שחיקה שכבר נראית |
| 4 | שער שפיות לכל המקורות (סעיף 8) | הופך שבירת-בנק-הבאה מ"נתון שגוי" ל"התרעה" |
| 5 | dataVersion (סעיף 3) | לפני השינוי המבני הבא, לא אחריו |
| השאר | לפי הזדמנות | |
