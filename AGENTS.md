# AGENTS — תוסף הבנקים

תיקיית הקוד: `extension/` (נטענת ב-`chrome://extensions` כ-unpacked).

**קרא לפני כל שינוי:** `PROJECT-HISTORY-AND-RULES.md` ו-`extension/README.md`.
מבנה אתרי הבנקים כבר נמדד ומתועד שם — אל תגלה אותו מחדש.

## 0. מסירה — לפני שאתה מתחיל ואחרי שאתה מסיים

**קרא את `HANDOFF.md` לפני כל עבודה.** הרשומה העליונה אומרת מה נעצר, מה כבר הופרך,
ומה הצעד הבא. בלעדיה תגלה מחדש תקלות שכבר נפתרו.

**הוסף רשומה בסיום.** הוספה בלבד, מעל הקודמות — אל תערוך רשומות של סוכן אחר.
פורמט: מה הייתה התקלה · מה נמדד · מה השתנה · מה עדיין לא עובד · הצעד הבא · מה המשתמש צריך לבדוק.

**קומיטים קטנים עם תיאור ברור.** אל תשאיר שינוי לא גמור בלי קומיט; אם הוא ניסיוני — סמן `WIP`.
לפני מיזוג — בדוק אם `main` השתנה מאז שהתחלת.

**בעלות על קבצים מסוכנים:** `background.js`, `dashboard.js` ו-`manifest.json` —
**בעלים אחד בלבד בכל רגע**. אל תערוך אותם אם סוכן אחר עובד עליהם.
`manifest.json` הוא המוקש הגדול ביותר: כל שינוי נוגע בשורת הגרסה. **עדכן גרסה פעם אחת
לסבב** ולא לכל תיקון, אחרת כל קומיט מתנגש.

## 1. אל תכתוב קובץ מחדש

התמחור לפי טוקנים, **ופלט יקר פי 6 מקלט ופי 60 מקלט מטמון**.
כתיבה מלאה של `background.js` (104,257 תווים במדידת 16.08.2026) יקרה — כמו משימה שלמה.

- ערוך נקודתית: החלפת מחרוזת ייחודית, לא פלט של הקובץ כולו.
- שינוי רחב — פצל לכמה עריכות קטנות.
- אל תדפיס קוד ארוך בתשובה; תאר בשורה מה שינית.
- אל תחזור על תוכן שכבר קיים בקובץ.

## 2. מדוד לפני שאתה כותב

כל שורש שנמצא בפרויקט הזה נמצא במדידה, אף אחד לא בהיגיון.
כשמשהו לא עובד באתר בנק — אל תנחש סלקטור. הרץ צילום DOM, קרא אותו, ורק אז כתוב.

**התמצאות בקוד — דרך גרף הידע, לא חיפוש עיוור.** קיים גרף ב-`graphify-out/`:
`graphify explain "<פונקציה>"` מחזיר מי קורא למי עם מספרי שורות, במקום לקרוא קובץ שלם.
אחרי שינויי קוד רענן עם `graphify update .` (מקומי, חינם, שניות).
ניחוש שגוי = סבב מלא של כתיבה, בנייה ובדיקה, וזו העלות היקרה בפרויקט.

## 3. מודל לפי המשימה

- עריכה מכנית, בדיקת תחביר, אריזה, עדכון גרסה → **Luna** (פלט 1/25 מ-Sol)
- ניתוח באג, קריאת מבנה, תכנון → **Terra**
- **Sol** רק כשנדרש · **Fast mode כבוי** אלא אם התבקש

## 4. קבצים קטנים

`background.js` נמדד ב־104,257 תווים (16.08.2026), ו־`dashboard.js` ~50KB — כל נגיעה מסכנת בכתיבה יקרה.
בנק חדש = קובץ נפרד, לא הרחבה של הקיים.

## 5. לפני שחרור גרסה

1. עדכן `version` ב-`manifest.json`.
2. בדיקת תחביר לכל קובץ JS שהשתנה. ⚠ **על המחשב של טל אין מנוע JS מותקן** —
   `node --check` אינו זמין שם. החלופה: שרת `python -m http.server` על תיקיית
   `extension`, ואז `new Function(await (await fetch(f)).text())` בדפדפן.
   **חובה `?v=<חותם>` עם `cache:'no-store'`** — אחרת הדפדפן מגיש עותק מהמטמון
   והבדיקה רצה על קוד שכבר הוחלף. זה קרה, והבדיקה עברה על קובץ ישן.
3. ודא ש-`manifest.json` תקין כ-JSON.
4. **אמת את הגרסה מתוך ה-zip ולא לפי שם הקובץ** — נארזה בעבר חבילה בשם 0.20.0 שהכילה 0.19.1.
5. אל תסמן "הסתיים" לפני שהנתונים נשמרו ואומתו.

## 6. תיאום — עובד כאן גם סוכן שני

- קרא את הקובץ מהדיסק, גם אם כתבת אותו לפני רגע.
- בדוק גרסה ותאריך שינוי בתחילת כל אבחון.
- בדוק **יכולת** בקוד ולא מחרוזת מדויקת: מימוש שקול נראה בטעות כמו "חסר".

## 7. עבודה מכמה מחשבים

GitHub מחזיק את **הקוד בלבד**, ואין שום מנגנון עדכון אוטומטי. תוסף unpacked נטען
מתיקייה מקומית על הדיסק, ולכן מחשב רואה רק את מה שנמשך אליו ידנית.

שלוש סיבות נפרדות ש"עדכון לא מופיע", ואסור לבלבל ביניהן:

1. **העבודה יושבת בענף צדדי.** מחשב טוען מ-`main`; כל עוד לא מוזג, אין מה לראות.
2. **הריפו לא נמשך, או שהתוסף לא רוענן.** `git pull` ואז **⟳ ב-`chrome://extensions`** —
   בלי הרענון Chrome ממשיך להריץ את הקוד שכבר נטען לזיכרון.
3. **נתונים לעולם אינם עוברים בין מחשבים.** `chrome.storage.local` ו-IndexedDB הם
   מקומיים לכל מחשב ולכל מזהה תוסף, ואינם ב-Chrome Sync. **זו החלטה ולא מגבלה** —
   נתוני בנק אינם נכנסים ל-GitHub. כל מחשב מסנכרן מהבנקים בעצמו.

הכללים:

- **`main` הוא הענף שממנו טוענים.** ענף צדדי הוא לעבודה בתהליך בלבד; ממזגים ל-`main`
  אחרי אימות חי על מחשב אחד. עבודה שנשארת בענף היא עבודה ששאר המחשבים לא רואים.
- **נתיב שכפול אחיד בכל מחשב:** `C:\Users\User\Documents\banks-extension`, וטעינה
  מתת-התיקייה `extension`. Chrome גוזר את מזהה התוסף מהנתיב — **נתיב שונה = מזהה
  שונה = כל הנתונים המקומיים ריקים.**
- **הגרסה ב-`manifest.json` היא הסימן היחיד שנראה בעין.** `chrome://extensions` מציג
  אותה; אם היא אינה זו שבגיט, המחשב אינו מעודכן. זו הסיבה לעדכן גרסה בכל סבב.

⚠ **חריג במחשב הנוכחי (16.08.2026):** התוסף נטען מ-
`…\Codex\2026-08-15\new-chat\outputs\poalim-sync-extension`, שהוא **junction**
לתיקיית `extension` שבריפו. נעשה כדי לשמור את מזהה התוסף הישן ואיתו את הנתונים
שנצברו. הקוד הוא של הריפו — רק הדלת ישנה.

## 8. טבלת מצב — חובה בכל סבב עבודה

**נקבע ע"י טל, 17.08.2026, אחרי יום שבו עשרה תיקונים רצופים בלאומי נראו כמו
תנועה במקום.** בלי טבלה אין דרך לדעת אם התקדמנו או חזרנו אחורה.

**בכל פעם שנוגעים בחלק של הפרויקט — להציג טבלה לפני ההסבר:**

| יכולת | קודם | עכשיו | איך נקבע |
|---|---|---|---|
| שם היכולת | עבד / לא / לא ידוע | עבד / לא / **לא נבדק** | מדידה חיה · דיווח משתמש · לא נבדק |

**שלושה כללים:**

1. **עמודת "קודם" אינה זיכרון — היא הטבלה מהסבב הקודם.** כך נסיגה נראית מיד.
2. **"לא נבדק" הוא ערך לגיטימי ואסור להסתיר אותו.** בדיקת תחביר, סימולציה או
   הרצת לוגיקה מקבילה **אינן** "עובד".
3. **מה שנשבר בסבב הזה מסומן במפורש**, גם אם התיקון של אותו סבב הצליח.

⚠ **הרצת לוגיקה מקבילה אינה בדיקה.** ב-17.08 הרצתי בדף גרסאות משלי של
`txt`/`normalized` במקום את אלה שבקובץ, הכרזתי "אומת", ואצל המשתמש זה נכשל —
כי הפונקציות אינן זהות. **או שמריצים את הקוד מהקובץ עצמו, או שכותבים
"לא נבדק".**

⚠ **גרסה נבדקת מהדיסק ולא מהקומיט.** `git checkout` משנה את עץ העבודה, ו-Chrome
טוען מהדיסק. ב-17.08 נדחפה 0.57.2 בעוד הדיסק נשאר 0.57.1, והמשתמש בדק קוד ישן.

## 9. אסור

- אין להזין סיסמאות או קודי אימות, ואין לבצע פעולות כספיות.
- **לאומי:** הכלל שהיה כאן — „האתר מרשה סשן אחד ומבטל את הקיים” — **לא אומת מעולם**
  ותוקן ב-16.08.2026. טל: *„אין לי בעיה עם ניתוק, פשוט לאומי לא מאפשר להישאר באתר”* —
  כלומר האתר מנתק מעצמו אחרי חוסר פעילות, וזו תופעה אחרת לגמרי. המסקנה המקורית
  נרשמה אחרי נפילות סשן שהיו ככל הנראה **לולאת הניווט של 0.12.1**, לא התנגשות לשוניות.
  מותר לפתוח לשונית מול לאומי. מה שכן — הסשן קצר, ולכן למדוד מהר ולא להשאיר פתוח.
- אין להסיר שומרי לולאה (נעילה, צינון, תקרת ניסיונות) — בלעדיהם נוצרה לולאת ניווט שהשביתה דפדפן.
- אין סנכרון אוטומטי בלי בחירת חשבון של המשתמש (גובר על §4 במסמך הכללים).
